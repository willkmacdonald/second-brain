---
phase: 12.2-rename-admin-errands
plan: 02
type: execute
wave: 2
depends_on: ["12.2-01"]
files_modified:
  - backend/src/second_brain/api/shopping_lists.py
  - backend/src/second_brain/api/errands.py
  - backend/src/second_brain/main.py
  - backend/src/second_brain/processing/admin_handoff.py
  - backend/tests/test_shopping_lists_api.py
  - backend/tests/test_errands_api.py
autonomous: true
requirements: []

must_haves:
  truths:
    - "GET /api/errands returns errand items grouped by destination with display names"
    - "DELETE /api/errands/{id}?destination=jewel deletes an errand item"
    - "Old /api/shopping-lists endpoints no longer exist"
    - "Admin Agent tool is wired as add_errand_items in main.py lifespan"
    - "All API tests pass with new endpoint paths and field names"
  artifacts:
    - path: "backend/src/second_brain/api/errands.py"
      provides: "Errands API endpoints (GET /api/errands, DELETE /api/errands/{id})"
      contains: "get_errands"
    - path: "backend/src/second_brain/main.py"
      provides: "Router and tool wiring for errands"
      contains: "errands_router"
    - path: "backend/tests/test_errands_api.py"
      provides: "Tests for errands API endpoints"
      contains: "test_get_errands"
  key_links:
    - from: "backend/src/second_brain/api/errands.py"
      to: "backend/src/second_brain/models/documents.py"
      via: "import KNOWN_DESTINATIONS"
      pattern: "from second_brain.models.documents import KNOWN_DESTINATIONS"
    - from: "backend/src/second_brain/main.py"
      to: "backend/src/second_brain/api/errands.py"
      via: "router import"
      pattern: "from second_brain.api.errands import router as errands_router"
    - from: "backend/src/second_brain/main.py"
      to: "backend/src/second_brain/tools/admin.py"
      via: "tool reference"
      pattern: "admin_tools.add_errand_items"
---

<objective>
Rename the API layer and wiring from shopping lists to errands: file rename, endpoint paths, response models, main.py imports, and all API tests.

Purpose: Complete the backend rename by updating the REST API endpoints and the app wiring that ties everything together.
Output: errands.py with /api/errands endpoints, updated main.py imports, renamed test file, all tests passing.
</objective>

<execution_context>
@/Users/willmacdonald/.claude/get-shit-done/workflows/execute-plan.md
@/Users/willmacdonald/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/12.2-rename-admin-errands/12.2-CONTEXT.md
@.planning/phases/12.2-rename-admin-errands/12.2-RESEARCH.md
@.planning/phases/12.2-rename-admin-errands/12.2-01-SUMMARY.md

<interfaces>
<!-- After Plan 01, these are the new names to import from the data layer -->

From backend/src/second_brain/models/documents.py (after Plan 01):
```python
KNOWN_DESTINATIONS: list[str] = ["jewel", "cvs", "pet_store", "other"]

class ErrandItem(BaseModel):
    id: str = Field(default_factory=lambda: str(uuid4()))
    destination: str  # Partition key
    name: str
```

From backend/src/second_brain/tools/admin.py (after Plan 01):
```python
class AdminTools:
    async def add_errand_items(self, items: ...) -> str:
        ...
```

From backend/src/second_brain/db/cosmos.py (after Plan 01):
```python
CONTAINER_NAMES: list[str] = ["Inbox", "People", "Projects", "Ideas", "Admin", "Errands"]
```

Current API file to rename (backend/src/second_brain/api/shopping_lists.py):
```python
router = APIRouter()
STORE_DISPLAY_NAMES: dict[str, str] = {...}
class ShoppingItemResponse(BaseModel): ...
class StoreSection(BaseModel): ...
class ShoppingListResponse(BaseModel): ...
@router.get("/api/shopping-lists", response_model=ShoppingListResponse)
async def get_shopping_lists(request: Request) -> ShoppingListResponse: ...
@router.delete("/api/shopping-lists/items/{item_id}", status_code=204)
async def delete_shopping_item(..., store: str = Query(...)): ...
```

Current main.py imports:
```python
from second_brain.api.shopping_lists import router as shopping_lists_router
app.state.admin_agent_tools = [admin_tools.add_shopping_list_items]
app.include_router(shopping_lists_router)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename shopping_lists.py to errands.py with full content rename, update main.py and admin_handoff.py</name>
  <files>
    backend/src/second_brain/api/shopping_lists.py
    backend/src/second_brain/api/errands.py
    backend/src/second_brain/main.py
    backend/src/second_brain/processing/admin_handoff.py
  </files>
  <action>
**File rename:** Delete `backend/src/second_brain/api/shopping_lists.py` and create `backend/src/second_brain/api/errands.py` with the full renamed content. Use `git mv` if possible, otherwise create new file and delete old.

**In the new `errands.py`:**
- Update module docstring: "Errands API endpoints for reading and deleting errand items." Replace all "shopping list" references with "errands" and "store" with "destination"
- Rename import: `from second_brain.models.documents import KNOWN_DESTINATIONS` (was KNOWN_STORES)
- Rename `STORE_DISPLAY_NAMES` to `DESTINATION_DISPLAY_NAMES` (same values: jewel -> "Jewel-Osco", cvs -> "CVS", pet_store -> "Pet Store", other -> "Other")
- Rename `ShoppingItemResponse` to `ErrandItemResponse` with field `destination: str` (was `store: str`). Update docstring: "Single errand item."
- Rename `StoreSection` to `DestinationSection` with fields: `destination: str` (was `store: str`), `displayName: str`, `items: list[ErrandItemResponse]`, `count: int`. Update docstring: "A group of errand items for a single destination."
- Rename `ShoppingListResponse` to `ErrandsResponse` with field `destinations: list[DestinationSection]` (was `stores: list[StoreSection]`), `totalCount: int`, `processingCount: int = 0`. Update docstring: "Full errands response with items grouped by destination."
- Rename `get_shopping_lists` to `get_errands` at path `"/api/errands"` with `response_model=ErrandsResponse`
  - Update all internal variable names: `store` loop var -> `destination`, container name `"ShoppingLists"` -> `"Errands"`
  - `STORE_DISPLAY_NAMES` -> `DESTINATION_DISPLAY_NAMES`
  - Construct `ErrandItemResponse(id=..., name=..., destination=item.get("destination", destination))`
  - Construct `DestinationSection(destination=destination, displayName=display_name, items=items, count=len(items))`
  - Return `ErrandsResponse(destinations=sections, totalCount=total_count, processingCount=processing_count)`
  - Update `sections.sort` key: `lambda s: s.count` (unchanged)
  - Update log message: "Errands: %d destinations, %d total items"
  - Update HTTP 503 detail: "Cosmos DB not configured. Errands unavailable."
- Rename `delete_shopping_item` to `delete_errand_item` at path `"/api/errands/{item_id}"` (NOTE: the path drops the `/items/` segment per CONTEXT.md: `DELETE /api/errands/{id}`)
  - Change query param from `store: str = Query(...)` to `destination: str = Query(..., description="Destination name (partition key)")`
  - Replace `KNOWN_STORES` with `KNOWN_DESTINATIONS`
  - Replace `"ShoppingLists"` with `"Errands"` container name
  - Update error messages: "Unknown destination" instead of "Unknown store", "Errand item" instead of "Shopping list item"
  - Update log message: "Deleted errand item %s from destination '%s'"

**In `main.py`:**
- Change import: `from second_brain.api.errands import router as errands_router` (was `shopping_lists`)
- Change: `app.include_router(errands_router)` (was `shopping_lists_router`)
- Change: `app.state.admin_agent_tools = [admin_tools.add_errand_items]` (was `add_shopping_list_items`)
- Update comment on AdminTools line: "errand item writes" instead of "shopping list writes"

**In `admin_handoff.py`:**
- Update module docstring: replace "shopping items to store lists" with "errand items to destination lists"
- Update comment line 26-27 area: `admin_tools.add_errand_items` (was `add_shopping_list_items`)
- Update docstring for `process_admin_capture`: replace "shopping lists via add_shopping_list_items" with "errands via add_errand_items"
- Update arg docstring: "e.g., [admin_tools.add_errand_items]" (was add_shopping_list_items)
- These are all comment/docstring changes -- no functional code changes
  </action>
  <verify>
    <automated>cd /Users/willmacdonald/Documents/Code/claude/second-brain/backend && python3 -c "from second_brain.api.errands import router; from second_brain.main import app; print('OK')" && test ! -f src/second_brain/api/shopping_lists.py && echo "Old file deleted"</automated>
  </verify>
  <done>errands.py exists with /api/errands and /api/errands/{item_id} endpoints. shopping_lists.py is deleted. main.py imports errands_router and references add_errand_items. admin_handoff.py comments updated. No references to old names remain.</done>
</task>

<task type="auto">
  <name>Task 2: Rename test_shopping_lists_api.py to test_errands_api.py with full content rename</name>
  <files>
    backend/tests/test_shopping_lists_api.py
    backend/tests/test_errands_api.py
  </files>
  <action>
**File rename:** Delete `backend/tests/test_shopping_lists_api.py` and create `backend/tests/test_errands_api.py`. Use `git mv` if possible.

**In the new `test_errands_api.py`:**
- Update module docstring: "Tests for GET /api/errands and DELETE /api/errands/{id}."
- Update import: `from second_brain.api.errands import router as errands_router` (was `shopping_lists`)
- Rename sample item dicts: `"store"` keys become `"destination"` keys (e.g., `{"id": "j1", "name": "2 lbs ground beef", "destination": "jewel"}`)
- Rename fixture `shopping_app` to `errands_app` and update `app.include_router(errands_router)` (was `shopping_lists_router`)
- Update `_setup_store_items` helper: rename to `_setup_destination_items`, change container from `"ShoppingLists"` to `"Errands"`, rename `store_items` param to `destination_items`
- Rename all test functions:
  - `test_get_shopping_lists_returns_grouped_items` -> `test_get_errands_returns_grouped_items`
  - `test_get_shopping_lists_empty` -> `test_get_errands_empty`
  - `test_get_shopping_lists_excludes_empty_stores` -> `test_get_errands_excludes_empty_destinations`
  - `test_delete_shopping_item_success` -> `test_delete_errand_item_success`
  - `test_delete_shopping_item_not_found` -> `test_delete_errand_item_not_found`
  - `test_delete_shopping_item_unknown_store` -> `test_delete_errand_item_unknown_destination`
  - `test_get_shopping_lists_triggers_processing` -> `test_get_errands_triggers_processing`
  - `test_get_shopping_lists_triggers_for_pending_items` -> `test_get_errands_triggers_for_pending_items`
  - `test_get_shopping_lists_no_trigger_when_no_admin_client` -> `test_get_errands_no_trigger_when_no_admin_client`
- Update all endpoint URLs in requests: `/api/shopping-lists` -> `/api/errands`, `/api/shopping-lists/items/{id}?store=` -> `/api/errands/{id}?destination=`
- Update response assertions: `data["stores"]` -> `data["destinations"]`, `["store"]` -> `["destination"]`
- Update container references: `"ShoppingLists"` -> `"Errands"`
- Update assertion strings: `"Unknown store"` -> `"Unknown destination"`, `"not found"` check stays the same
- Update `patch("second_brain.api.shopping_lists.asyncio.create_task")` -> `patch("second_brain.api.errands.asyncio.create_task")`
- Rename `shopping_app` parameter in all test function signatures to `errands_app`
  </action>
  <verify>
    <automated>cd /Users/willmacdonald/Documents/Code/claude/second-brain/backend && python3 -m pytest tests/test_errands_api.py -x -q 2>&1 | tail -5</automated>
  </verify>
  <done>test_errands_api.py exists with all 9 tests renamed and passing. test_shopping_lists_api.py is deleted. All endpoint paths, container references, and field names use errands/destination terminology.</done>
</task>

</tasks>

<verification>
1. `python3 -m pytest tests/test_errands_api.py tests/test_admin_tools.py tests/test_admin_handoff.py -x` all pass
2. `grep -r "shopping_lists\|ShoppingList\|KNOWN_STORES\|STORE_DISPLAY" backend/src/ backend/tests/` returns no matches
3. `test ! -f backend/src/second_brain/api/shopping_lists.py` confirms old file deleted
4. `test ! -f backend/tests/test_shopping_lists_api.py` confirms old test file deleted
</verification>

<success_criteria>
- errands.py serves GET /api/errands and DELETE /api/errands/{id}
- shopping_lists.py no longer exists
- main.py wires errands_router and add_errand_items
- admin_handoff.py comments reference add_errand_items
- test_errands_api.py has 9 passing tests
- test_shopping_lists_api.py no longer exists
- Zero remaining references to old shopping list names in backend/
</success_criteria>

<output>
After completion, create `.planning/phases/12.2-rename-admin-errands/12.2-02-SUMMARY.md`
</output>
