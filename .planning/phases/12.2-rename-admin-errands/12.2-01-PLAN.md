---
phase: 12.2-rename-admin-errands
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/second_brain/models/documents.py
  - backend/src/second_brain/tools/admin.py
  - backend/src/second_brain/db/cosmos.py
  - backend/tests/test_admin_tools.py
  - backend/tests/test_admin_integration.py
  - backend/tests/test_admin_handoff.py
autonomous: true
requirements: []

must_haves:
  truths:
    - "ErrandItem model exists with 'destination' field instead of ShoppingListItem with 'store' field"
    - "KNOWN_DESTINATIONS constant replaces KNOWN_STORES with the same values"
    - "add_errand_items tool replaces add_shopping_list_items with updated LLM-facing description"
    - "Cosmos CONTAINER_NAMES includes 'Errands' instead of 'ShoppingLists'"
    - "All existing tests pass with updated references"
  artifacts:
    - path: "backend/src/second_brain/models/documents.py"
      provides: "ErrandItem model, KNOWN_DESTINATIONS constant"
      contains: "KNOWN_DESTINATIONS"
    - path: "backend/src/second_brain/tools/admin.py"
      provides: "add_errand_items tool method"
      contains: "add_errand_items"
    - path: "backend/src/second_brain/db/cosmos.py"
      provides: "Errands container in CONTAINER_NAMES"
      contains: '"Errands"'
  key_links:
    - from: "backend/src/second_brain/tools/admin.py"
      to: "backend/src/second_brain/models/documents.py"
      via: "import ErrandItem, KNOWN_DESTINATIONS"
      pattern: "from second_brain.models.documents import KNOWN_DESTINATIONS, ErrandItem"
---

<objective>
Rename the data layer from shopping list terminology to errands terminology: model, constants, tool function, and Cosmos container name.

Purpose: Foundation rename that all other plans depend on -- model names, constant names, and tool function names must change before API and mobile layers can update their references.
Output: Renamed ErrandItem model, KNOWN_DESTINATIONS constant, add_errand_items tool, and Errands container name. All existing unit tests updated and passing.
</objective>

<execution_context>
@/Users/willmacdonald/.claude/get-shit-done/workflows/execute-plan.md
@/Users/willmacdonald/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/12.2-rename-admin-errands/12.2-CONTEXT.md
@.planning/phases/12.2-rename-admin-errands/12.2-RESEARCH.md

<interfaces>
<!-- Current interfaces being renamed. Executor should use these as reference for what exists today. -->

From backend/src/second_brain/models/documents.py:
```python
KNOWN_STORES: list[str] = ["jewel", "cvs", "pet_store", "other"]

class ShoppingListItem(BaseModel):
    id: str = Field(default_factory=lambda: str(uuid4()))
    store: str  # Partition key: "jewel", "cvs", "pet_store", "other"
    name: str   # Full natural language: "2 lbs ground beef", "cat litter"
```

From backend/src/second_brain/tools/admin.py:
```python
from second_brain.models.documents import KNOWN_STORES, ShoppingListItem

class AdminTools:
    @tool(approval_mode="never_require")
    async def add_shopping_list_items(self, items: Annotated[list[dict], Field(...)]) -> str:
        container = self._manager.get_container("ShoppingLists")
        # Uses KNOWN_STORES, ShoppingListItem, store_counts
```

From backend/src/second_brain/db/cosmos.py:
```python
CONTAINER_NAMES: list[str] = ["Inbox", "People", "Projects", "Ideas", "Admin", "ShoppingLists"]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename data model and constants in documents.py, cosmos.py, and admin.py</name>
  <files>
    backend/src/second_brain/models/documents.py
    backend/src/second_brain/tools/admin.py
    backend/src/second_brain/db/cosmos.py
  </files>
  <action>
In `backend/src/second_brain/models/documents.py`:
- Rename `KNOWN_STORES` to `KNOWN_DESTINATIONS` (same values: `["jewel", "cvs", "pet_store", "other"]`)
- Rename `ShoppingListItem` class to `ErrandItem`
- Update docstring: "Individual errand item in the Errands Cosmos container." and "Partition key is /destination"
- Rename field `store: str` to `destination: str` with updated comment: `# Partition key: "jewel", "cvs", "pet_store", "other"`

In `backend/src/second_brain/db/cosmos.py`:
- Change `"ShoppingLists"` to `"Errands"` in `CONTAINER_NAMES` list

In `backend/src/second_brain/tools/admin.py`:
- Update module docstring: replace "shopping list management" with "errand item management", "add_shopping_list_items" with "add_errand_items", "ShoppingLists" with "Errands"
- Update import: `from second_brain.models.documents import KNOWN_DESTINATIONS, ErrandItem` (was KNOWN_STORES, ShoppingListItem)
- Update class docstring and usage example: `agent_tools = [tools.add_errand_items]`
- Rename method `add_shopping_list_items` to `add_errand_items`
- Update `Field(description=...)` to: `"List of errand items to add. Each dict must have 'name' (str, lowercase, natural language like '2 lbs ground beef') and 'destination' (str: jewel, cvs, pet_store, or other)"`
- Update method docstring: "Add items to errands, grouped by destination." and "Each item is written as an individual document to the Errands container. Unknown destination names silently fall back to 'other'."
- Change `self._manager.get_container("ShoppingLists")` to `self._manager.get_container("Errands")`
- Rename local variable `store_counts` to `destination_counts`
- Rename all local variable references: `store` to `destination` in the item processing loop (the variable name, not the dict key access -- `item_data.get("store", ...)` becomes `item_data.get("destination", ...)`)
- Update log messages: "Unknown store" -> "Unknown destination", "store" -> "destination"
- Update `KNOWN_STORES` reference to `KNOWN_DESTINATIONS`
- Construct `ErrandItem(destination=destination, name=name)` instead of `ShoppingListItem(store=store, name=name)`
- Update confirmation breakdown string: `f"{count} to {destination}"` instead of `f"{count} to {store}"`
  </action>
  <verify>
    <automated>cd /Users/willmacdonald/Documents/Code/claude/second-brain/backend && python3 -c "from second_brain.models.documents import ErrandItem, KNOWN_DESTINATIONS; from second_brain.tools.admin import AdminTools; from second_brain.db.cosmos import CONTAINER_NAMES; assert 'Errands' in CONTAINER_NAMES; assert 'ShoppingLists' not in CONTAINER_NAMES; assert hasattr(AdminTools, 'add_errand_items'); print('OK')"</automated>
  </verify>
  <done>ErrandItem model, KNOWN_DESTINATIONS constant, add_errand_items tool method, and "Errands" container name all exist. No references to old ShoppingListItem, KNOWN_STORES, add_shopping_list_items, or "ShoppingLists" remain in these three files.</done>
</task>

<task type="auto">
  <name>Task 2: Update all backend test files referencing renamed data layer</name>
  <files>
    backend/tests/test_admin_tools.py
    backend/tests/test_admin_integration.py
    backend/tests/test_admin_handoff.py
  </files>
  <action>
In `backend/tests/test_admin_tools.py`:
- Update module docstring: "Unit tests for AdminTools (add_errand_items)."
- Replace all `add_shopping_list_items` method calls with `add_errand_items`
- Replace all `"ShoppingLists"` container references with `"Errands"`
- Replace all `"store"` dict keys in test item dicts with `"destination"` (e.g., `{"name": "milk", "destination": "jewel"}`)
- Replace assertions checking `b["store"]` with `b["destination"]`
- Update test names if they reference "shopping" (optional -- Claude's discretion)

In `backend/tests/test_admin_integration.py`:
- Update docstring: "AdminTools.add_errand_items writes items to real Cosmos DB."
- Replace `add_shopping_list_items` call with `add_errand_items`
- Replace `"store"` dict keys with `"destination"` in test data
- Replace `"ShoppingLists"` container references with `"Errands"`
- Replace `partition_key="jewel"` / `partition_key="cvs"` (these stay the same -- destination values are unchanged)

In `backend/tests/test_admin_handoff.py`:
- Update comments referencing `add_shopping_list_items` to `add_errand_items` (line 285 area: "no tool call (agent responds without invoking add_shopping_list_items)" -> "add_errand_items")
- No functional code changes needed in this file -- it mocks tools generically
  </action>
  <verify>
    <automated>cd /Users/willmacdonald/Documents/Code/claude/second-brain/backend && python3 -m pytest tests/test_admin_tools.py tests/test_admin_handoff.py -x -q 2>&1 | tail -5</automated>
  </verify>
  <done>All test_admin_tools.py tests pass with add_errand_items and "Errands" container. All test_admin_handoff.py tests pass. No references to add_shopping_list_items or "ShoppingLists" remain in test files.</done>
</task>

</tasks>

<verification>
1. `python3 -c "from second_brain.models.documents import ErrandItem, KNOWN_DESTINATIONS"` succeeds
2. `python3 -c "from second_brain.tools.admin import AdminTools; assert hasattr(AdminTools, 'add_errand_items')"` succeeds
3. `grep -r "ShoppingListItem\|KNOWN_STORES\|add_shopping_list_items\|ShoppingLists" backend/src/second_brain/models/ backend/src/second_brain/tools/ backend/src/second_brain/db/` returns no matches
4. `pytest tests/test_admin_tools.py tests/test_admin_handoff.py -x` passes
</verification>

<success_criteria>
- ErrandItem model with destination field exists in documents.py
- KNOWN_DESTINATIONS constant exists in documents.py
- add_errand_items tool method exists in admin.py with updated LLM-facing description
- "Errands" in CONTAINER_NAMES in cosmos.py
- All unit tests in test_admin_tools.py and test_admin_handoff.py pass
- Zero remaining references to old names in modified files
</success_criteria>

<output>
After completion, create `.planning/phases/12.2-rename-admin-errands/12.2-01-SUMMARY.md`
</output>
