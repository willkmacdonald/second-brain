---
phase: 20-feedback-collection
plan: 04
type: execute
wave: 3
depends_on: [20-01, 20-02, 20-03]
files_modified: []
autonomous: false
requirements: [FEED-01, FEED-02, FEED-03, FEED-04]
must_haves:
  truths:
    - "All four FEED requirements verified end-to-end against deployed system"
  artifacts: []
  key_links: []
---

<objective>
Human-verify checkpoint: confirm all feedback collection features work end-to-end after deployment. Covers implicit signals (recategorize, HITL, errand re-route), explicit thumbs up/down UI, investigation agent feedback query, and golden dataset promotion.

Purpose: Ensures the deployed system correctly captures quality signals and the investigation agent can query and promote them.
Output: Verified feedback collection system ready for Phase 21 (Eval Framework).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/20-feedback-collection/20-CONTEXT.md
@.planning/phases/20-feedback-collection/20-01-SUMMARY.md
@.planning/phases/20-feedback-collection/20-02-SUMMARY.md
@.planning/phases/20-feedback-collection/20-03-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: End-to-end verification of all feedback collection features</name>
  <files></files>
  <action>
Present the verification checklist to the user. This is a human-verify checkpoint -- no automated work to perform. The user will verify the deployed system manually.

Pre-requisite: Merge to main, wait for CI/CD deploy. Then paste updated `docs/foundry/investigation-agent-instructions.md` into the Foundry portal for the Investigation Agent (asst_5feSWWTMA8rBSUyQo6aSCsEJ).
  </action>
  <what-built>
Feedback collection infrastructure across backend and mobile:
1. POST /api/feedback endpoint for explicit thumbs up/down
2. Inline FeedbackDocument writes in recategorize handler (recategorize + HITL signals)
3. Inline FeedbackDocument write in errand re-route handler
4. Two new Investigation Agent tools: query_feedback_signals and promote_to_golden_dataset
5. Thumbs up/down buttons in inbox detail modal
6. Updated Investigation Agent portal instructions

NOTE: After merging to main and CI/CD deploying to Azure Container Apps, the Investigation Agent portal instructions must be manually pasted into the Foundry portal. The canonical doc is at `docs/foundry/investigation-agent-instructions.md`.
  </what-built>
  <how-to-verify>
**Step 1 -- FEED-02 Explicit feedback (mobile)**
1. Open the Second Brain app on your phone
2. Go to the Inbox tab
3. Tap any inbox item to open the detail modal
4. Verify you see "FEEDBACK" label with thumbs-down and thumbs-up buttons between Timestamp and bucket sections
5. Tap thumbs-up -- verify it highlights with green border
6. Verify "Feedback recorded" toast appears and auto-dismisses
7. Tap thumbs-up again -- verify it deselects (toggle)
8. Tap thumbs-down -- verify it highlights with red border

**Step 2 -- FEED-01 Implicit signal (recategorize)**
1. In the Inbox tab, open any classified item
2. Tap a different bucket to recategorize it
3. (Signal is written silently in the background -- no visible confirmation needed for this step)

**Step 3 -- FEED-04 Investigation agent query**
1. Go to Status tab and tap the investigate icon (or open Investigation chat)
2. Ask: "what are the most common misclassifications?"
3. Verify the agent calls query_feedback_signals and returns a response with signal data
4. If no signals yet (fresh deploy), ask: "show me recent feedback signals" -- agent should report "no signals found" or similar

**Step 4 -- FEED-03 Golden dataset promotion (conversational)**
1. In the investigation chat, if recategorize signals exist from Step 2:
   Ask: "promote signal [ID from Step 3 response]"
2. Verify the agent shows a preview of the signal (capture text, buckets)
3. Confirm with "yes"
4. Verify the agent reports successful promotion to golden dataset

Expected: All four steps complete. If Step 3/4 show "Feedback data unavailable" the Investigation Agent may need a Container App restart to pick up the new cosmos_manager parameter.
  </how-to-verify>
  <verify>Human verification -- user confirms all four steps pass</verify>
  <done>All FEED-01 through FEED-04 requirements verified working on deployed system</done>
  <resume-signal>Type "approved" if all four steps pass, or describe which steps failed</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries in this checkpoint plan -- verification only.

## STRIDE Threat Register

No threats applicable to a verification-only checkpoint.
</threat_model>

<verification>
Human verification -- no automated checks in this plan.
</verification>

<success_criteria>
- Thumbs up/down buttons visible and functional in inbox detail modal
- Recategorize silently emits feedback signal (verifiable via investigation agent query)
- Investigation agent answers misclassification queries using feedback data
- Investigation agent can preview and promote signals to golden dataset after confirmation
</success_criteria>

<output>
After completion, create `.planning/phases/20-feedback-collection/20-04-SUMMARY.md`
</output>
