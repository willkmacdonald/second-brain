---
phase: 24-foundry-ga-migration
plan: 17
type: execute
wave: 4
depends_on: [16]
files_modified:
  - backend/src/second_brain/models/documents.py
  - backend/src/second_brain/eval/dry_run_tools.py
autonomous: true
requirements: [F-08, F-13, D-07b, P0-1, P0-2]

must_haves:
  truths:
    - "InboxDocument ADDS `conversationHistory: list[ConversationTurn] | None = None` field (P0-1 OUTCOME Option A)"
    - "ConversationTurn is a Pydantic model `{role: Literal['user', 'assistant'], content: str}` re-exported (or defined) alongside InboxDocument so models/documents.py is the canonical type source"
    - "`foundryThreadId: str | None = None` field is KEPT (rollback safety — RC code can still read it during the migration window; cleanup happens in 24-24 post-UAT)"
    - "NO `sessionId` field is added — P0-1 OUTCOME proved sessionId rehydration doesn't work cross-process on GA SDK 1.3.0"
    - "eval/dry_run_tools.py: @tool decorators stripped from EvalClassifierTools.file_capture and DryRunAdminTools.* methods"
    - "API/capture.py reads/writes conversationHistory via the adapter (already wired in 24-16); InboxDocument accepts the new field"
    - "P0-1 OUTCOME fixture session_rehydration_fresh_process.json (recalled_pineapple=false) referenced in this plan's pre-execution verification"
    - "NO backfill script exists for conversationHistory — legacy RC docs cannot be reconstructed because the client never had the message list; graceful continuity loss is the accepted Option A trade-off (per 24-15 helper warning path)"
  artifacts:
    - path: "backend/src/second_brain/models/documents.py"
      provides: "Inbox model with conversationHistory field ADDED alongside foundryThreadId (P0-1 OUTCOME Option A — additive, not rename)"
    - path: "backend/src/second_brain/eval/dry_run_tools.py"
      provides: "Eval-side tool stubs (decorator-stripped)"
  key_links:
    - from: "models/documents.py"
      to: "api/capture.py inbox round-trip via resolve_inbox_conversation_history"
      via: "conversationHistory field on InboxDocument"
      pattern: "conversationHistory"
---

<objective>
Two scope items under P0-1 OUTCOME Option A (NO field renaming or deletion in this plan; NO backfill script):

1. **InboxDocument.conversationHistory ADDITION (F-13, D-07b probe finding, P0-1 OUTCOME, P0-2 amendment):** Per the live 24-06.5 probe (`session_rehydration_fresh_process.json`, `recalled_pineapple=false`), cross-process `AgentSession.session_id` rehydration FAILS on GA Foundry SDK 1.3.0. The InboxDocument gets a NEW optional list field `conversationHistory: list[ConversationTurn] | None = None`. The legacy `foundryThreadId` is KEPT for rollback safety.

   **The original plan 24-17 RENAMED foundryThreadId → sessionId in place; that design is dead. The first amendment ADDED sessionId alongside; that design is ALSO dead.** Under P0-1 OUTCOME Option A: no `sessionId` field is added — only `conversationHistory` (the explicit message history). Both `conversationHistory` AND `foundryThreadId` coexist during the migration window. Plan 24-24 deletes `foundryThreadId` after UAT confirms GA stable.

2. **eval/dry_run_tools.py decorator strip (F-08):** Strip `@tool(approval_mode="never_require")` decorators from EvalClassifierTools and DryRunAdminTools methods. Same mechanical pattern as 24-15.

3. **Backfill: NOT NEEDED.** Legacy RC docs have `foundryThreadId` (server-side opaque pointer) but the client never had the message list, so we cannot reconstruct conversationHistory from RC data. Old RC follow-ups will gracefully lose continuity per Option A's accepted trade-off (logged warning via 24-15 helper, classifier treats as new conversation). The empty `conversationHistory` field defaults to None on read. The original `backend/scripts/backfill_inbox_session_id.py` (renamed/scoped under earlier P0-2 amendment) is DELETED from this plan's scope — there is no backfill to run.

The remaining classifier scope items (deleting old agents/middleware.py, warmup migration, RC dep removal, final config cleanup, cumulative audit, deploy, UAT, post-UAT cleanup) live in plans 24-18 through 24-24.

**P0-1 OUTCOME confirmed in fixture session_rehydration_fresh_process.json (recalled_pineapple=false). This plan implements Option A — explicit conversationHistory persistence — as the locked fallback design.**
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-foundry-ga-migration/24-CONTEXT.md
@.planning/phases/24-foundry-ga-migration/24-PATTERNS.md
@.planning/phases/24-foundry-ga-migration/24-PLAN-DEFECTS.md
@.planning/phases/24-foundry-ga-migration/FRAMEWORK-FIDELITY-calibration.md
@.planning/phases/23-foundry-ga-prep/FOUNDRY-PROBE-FINDINGS.md
@backend/src/second_brain/models/documents.py
@backend/src/second_brain/api/capture.py
@backend/src/second_brain/cosmos/inbox_conversation_history.py
@backend/src/second_brain/eval/dry_run_tools.py
@backend/tests/fixtures/foundry-probe/session_rehydration_fresh_process.json

<interfaces>
Pydantic field ADDITION (per F-13 + probe 4 + P0-1 OUTCOME + P0-2):

Original plan (REJECTED per P0-2):
```python
class InboxDocument(BaseModel):
    foundryThreadId: str | None = None    # OLD — would be RENAMED
```
First amendment (REJECTED per P0-1 OUTCOME):
```python
class InboxDocument(BaseModel):
    foundryThreadId: str | None = None    # KEPT
    sessionId: str | None = None          # ADDED — but doesn't work cross-process
```

Final amended plan (P0-1 OUTCOME Option A — additive conversationHistory):
```python
from second_brain.cosmos.inbox_conversation_history import ConversationTurn

class InboxDocument(BaseModel):
    # ... other fields ...
    foundryThreadId: str | None = None              # KEPT during migration window; deleted in 24-24 post-UAT
    conversationHistory: list[ConversationTurn] | None = None
    # ^ NEW (Phase 24 P0-1 OUTCOME Option A — added alongside foundryThreadId, not a rename.
    # Holds explicit conversation history reconstructed into Message[] on each follow-up.)
```

Both fields are Optional. Capture handler reads/writes conversationHistory via the adapter (already wired in 24-16); the adapter loads history via `resolve_inbox_conversation_history()` from 24-15 and writes the updated list back to Cosmos after each stream completes.

The Pydantic camelCase exemption is configured in pyproject.toml line 63 — KEEP using camelCase for compat with Cosmos's existing field naming.

NOTE on ConversationTurn placement: the `ConversationTurn` model is defined in `backend/src/second_brain/cosmos/inbox_conversation_history.py` (24-15). models/documents.py imports it. If circular-import issues arise (unlikely — inbox_conversation_history.py only imports stdlib + pydantic), the executor may relocate ConversationTurn into models/documents.py and re-export it from inbox_conversation_history.py. Either placement is acceptable; what matters is that there is ONE canonical definition.

Why no backfill (P0-1 OUTCOME):
The original 24-17 had a backfill script that copied `foundryThreadId → sessionId` to seed the new field. Under P0-1 OUTCOME, the new field is `conversationHistory: list[ConversationTurn]` — a structured list of message turns. RC docs never persisted the message list client-side (the RC server held the messages opaquely behind `foundryThreadId`). There is no client-side data to backfill from. The helper from 24-15 logs a warning when it encounters a legacy doc and returns an empty list; the classifier treats those follow-ups as new conversations. This is the accepted Option A trade-off.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ADD conversationHistory field to InboxDocument; KEEP foundryThreadId (P0-1 OUTCOME Option A)</name>
  <files>backend/src/second_brain/models/documents.py</files>
  <read_first>
    - backend/src/second_brain/models/documents.py (current InboxDocument class, lines 40-53)
    - backend/src/second_brain/cosmos/inbox_conversation_history.py (ConversationTurn definition from 24-15)
    - backend/tests/fixtures/foundry-probe/session_rehydration_fresh_process.json (P0-1 OUTCOME — `recalled_pineapple=false`)
    - .planning/phases/23-foundry-ga-prep/FOUNDRY-PROBE-FINDINGS.md (Probe 4 — session_id)
    - .planning/phases/24-foundry-ga-migration/24-PLAN-DEFECTS.md (P0-1 OUTCOME + P0-2)
  </read_first>
  <action>
    **Pre-flight check:** read `backend/tests/fixtures/foundry-probe/session_rehydration_fresh_process.json`. Confirm `recalled_pineapple == false` (P0-1 OUTCOME — this is the design driver for the conversationHistory field; without this counter-example the field would not exist).

    Edit `backend/src/second_brain/models/documents.py`:

    1. Locate `class InboxDocument(BaseModel):` (around line 40-53).
    2. Find the field `foundryThreadId: str | None = None` (around line 52).
    3. **DO NOT REMOVE OR RENAME `foundryThreadId`.** Per P0-2, it stays during the migration window. Plan 24-24 deletes it after UAT.
    4. **DO NOT ADD `sessionId`.** P0-1 OUTCOME proved cross-process sessionId rehydration fails; the field is permanently absent from the model.
    5. Add the import at the top of the file:
       ```python
       from second_brain.cosmos.inbox_conversation_history import ConversationTurn
       ```
       (If circular-import problems arise, relocate `ConversationTurn` to models/documents.py and update 24-15's helper module to import it from here instead. Either layout is acceptable; pick the one that import-cycles cleanly.)
    6. ADD a NEW field `conversationHistory: list[ConversationTurn] | None = None` directly below `foundryThreadId`.
    7. Add a docstring above the new field:
       ```
       # Per CONTEXT D-07b + probe 4 (session_rehydration.json) + P0-1 OUTCOME (Option A):
       # Holds the explicit conversation history reconstructed into Message[] on
       # each follow-up. Cross-process AgentSession.session_id rehydration FAILS on
       # GA Foundry SDK 1.3.0 (proven by session_rehydration_fresh_process.json,
       # recalled_pineapple=false). Operator locked Option A — persist full message
       # history client-side and pass it to agent.run(messages=[...]) on each turn.
       # ADDED alongside foundryThreadId (NOT a rename) for rollback safety during
       # the deploy window. Both fields coexist until plan 24-24 (post-UAT)
       # deletes foundryThreadId.
       ```
    8. Verify Pydantic still accepts the various shapes:
       ```
       cd backend && uv run python -c "
       from second_brain.models.documents import InboxDocument
       from second_brain.cosmos.inbox_conversation_history import ConversationTurn

       m1 = InboxDocument(id='a', userId='u', foundryThreadId='legacy')
       m2 = InboxDocument(id='b', userId='u', conversationHistory=[ConversationTurn(role='user', content='hi')])
       m3 = InboxDocument(id='c', userId='u', foundryThreadId='legacy', conversationHistory=[ConversationTurn(role='user', content='hi')])
       m4 = InboxDocument(id='d', userId='u')  # neither field

       assert m1.foundryThreadId == 'legacy'
       assert m1.conversationHistory is None
       assert m2.conversationHistory[0].content == 'hi'
       assert m3.foundryThreadId == 'legacy' and len(m3.conversationHistory) == 1
       assert m4.foundryThreadId is None and m4.conversationHistory is None
       print('OK')
       "
       ```
       All four shapes must succeed.
  </action>
  <verify>
    <automated>grep -q "foundryThreadId" backend/src/second_brain/models/documents.py && grep -q "conversationHistory" backend/src/second_brain/models/documents.py && ! grep -q "sessionId" backend/src/second_brain/models/documents.py && cd backend && uv run python -c "from second_brain.models.documents import InboxDocument; from second_brain.cosmos.inbox_conversation_history import ConversationTurn; m1 = InboxDocument(id='a', userId='u', foundryThreadId='legacy'); m2 = InboxDocument(id='b', userId='u', conversationHistory=[ConversationTurn(role='user', content='hi')]); m3 = InboxDocument(id='c', userId='u', foundryThreadId='legacy', conversationHistory=[ConversationTurn(role='user', content='hi')]); assert m1.foundryThreadId == 'legacy' and m1.conversationHistory is None; assert m2.conversationHistory[0].content == 'hi'; assert m3.foundryThreadId == 'legacy' and len(m3.conversationHistory) == 1"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "conversationHistory" backend/src/second_brain/models/documents.py` returns 0 (P0-1 OUTCOME — added)
    - `grep -q "class ConversationTurn" backend/src/second_brain/models/documents.py` returns 0 OR `grep -q "from second_brain.cosmos.inbox_conversation_history import ConversationTurn" backend/src/second_brain/models/documents.py` returns 0 (ConversationTurn definition reachable via documents.py)
    - `! grep -q "sessionId" backend/src/second_brain/models/documents.py` returns 0 (NEVER added per P0-1 OUTCOME)
    - `grep -q "foundryThreadId" backend/src/second_brain/models/documents.py` returns 0 (RETAINED for rollback)
    - `grep -q "P0-1 OUTCOME\|Option A" backend/src/second_brain/models/documents.py` returns 0 (doc comment present)
    - InboxDocument accepts: foundryThreadId-only, conversationHistory-only, both-fields, neither-field
    - `cd backend && uv run python -c "from second_brain.models.documents import InboxDocument"` exits 0
  </acceptance_criteria>
  <done>InboxDocument has conversationHistory ADDED alongside foundryThreadId (P0-1 OUTCOME Option A). No sessionId field. Capture handler from 24-16 reads/writes conversationHistory via the adapter.</done>
</task>

<task type="auto">
  <name>Task 2: Strip @tool decorators from eval/dry_run_tools.py (F-08)</name>
  <files>backend/src/second_brain/eval/dry_run_tools.py</files>
  <read_first>
    - backend/src/second_brain/eval/dry_run_tools.py (EvalClassifierTools, DryRunAdminTools)
    - .planning/phases/24-foundry-ga-migration/FRAMEWORK-FIDELITY-calibration.md (F-08)
  </read_first>
  <action>
    1. Grep for `@tool(approval_mode=` in `backend/src/second_brain/eval/dry_run_tools.py` and record line numbers.
    2. For each occurrence: delete the decorator line, leaving the `async def` signature intact.
    3. Keep the `Annotated[..., Field(description=...)]` parameter shapes — these are still tools (called by the eval framework via the agent).
    4. Remove unused `tool` import in same edit (auto-format trap).
    5. Run any eval-related unit tests: `cd backend && uv run pytest tests/test_eval.py -x` (if it exists).
  </action>
  <verify>
    <automated>! grep -q "@tool(approval_mode=" backend/src/second_brain/eval/dry_run_tools.py && cd backend && uv run python -c "from second_brain.eval.dry_run_tools import EvalClassifierTools, DryRunAdminTools"</automated>
  </verify>
  <acceptance_criteria>
    - `! grep -q "@tool(approval_mode=" backend/src/second_brain/eval/dry_run_tools.py` returns 0
    - `grep -q "class EvalClassifierTools" backend/src/second_brain/eval/dry_run_tools.py` returns 0
    - `grep -q "class DryRunAdminTools" backend/src/second_brain/eval/dry_run_tools.py` returns 0
    - `cd backend && uv run python -c "from second_brain.eval.dry_run_tools import EvalClassifierTools, DryRunAdminTools"` exits 0
  </acceptance_criteria>
  <done>Eval-side tool stubs are GA-shaped.</done>
</task>

</tasks>

<verification>
- InboxDocument.conversationHistory field ADDED alongside foundryThreadId (P0-1 OUTCOME Option A — both kept until 24-24)
- NO sessionId field (P0-1 OUTCOME — proven not to work)
- NO backfill script (P0-1 OUTCOME — RC docs lack the client-side data to backfill from; graceful continuity loss is the accepted trade-off)
- All Python source under backend/src/ uses resolve_inbox_conversation_history() helper for reads (already wired in 24-15/24-16)
- New captures write conversationHistory via the adapter (already wired in 24-16)
- eval/dry_run_tools.py decorator-stripped
- P0-1 OUTCOME fixture (`recalled_pineapple=false`) verified before edits
</verification>

<success_criteria>
Plan 24-18 (delete agents/middleware.py + RCEvalAgentInvoker + classifier eval call switches to GA invoker implementation) is unblocked. The Inbox schema supports explicit conversation-history persistence per Option A. Rollback safety preserved by retaining foundryThreadId until 24-24.
</success_criteria>

<output>
After completion, create `.planning/phases/24-foundry-ga-migration/24-17-SUMMARY.md` documenting:
- F-13 partially cleared (conversationHistory added; foundryThreadId KEPT per rollback safety)
- F-08 cleared on dry_run_tools
- NO backfill script (P0-1 OUTCOME — RC docs lack the data; graceful continuity loss is the locked Option A trade-off)
- foundryThreadId deletion deferred to 24-24 (post-UAT)
- P0-1 OUTCOME fixture verified pre-execution (`recalled_pineapple=false`)
</output>
</content>
</invoke>