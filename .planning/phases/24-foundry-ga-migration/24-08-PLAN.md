---
phase: 24-foundry-ga-migration
plan: 08
type: execute
wave: 2
depends_on: [07]
files_modified:
  - .planning/phases/24-foundry-ga-migration/FIDELITY-23.1.patch
  - .planning/phases/24-foundry-ga-migration/FRAMEWORK-FIDELITY-23.1.md
autonomous: false
requirements: [D-07, D-13]

must_haves:
  truths:
    - "git diff for task group 23.1 (plans 24-01..24-07) captured to FIDELITY-23.1.patch"
    - "framework-fidelity auditor produced FRAMEWORK-FIDELITY-23.1.md report"
    - "Report has zero ❌ for 23.1 scope; outstanding ❌ for 23.2/23.3 scope is acceptable"
    - "If 23.1-scoped ❌ exist, this plan blocks until fixed in a follow-up plan or rebutted with explicit-justification"
  artifacts:
    - path: ".planning/phases/24-foundry-ga-migration/FIDELITY-23.1.patch"
      provides: "Captured cumulative diff for task group 23.1"
    - path: ".planning/phases/24-foundry-ga-migration/FRAMEWORK-FIDELITY-23.1.md"
      provides: "Auditor report — Pass / Warnings / Failures"
      contains: "Pass, Failures"
  key_links:
    - from: "FIDELITY-23.1.patch"
      to: "FRAMEWORK-FIDELITY-23.1.md"
      via: "auditor consumed the patch as input"
      pattern: "audit subject"
---

<objective>
Run the framework-fidelity auditor at the end of task group 23.1 commit cluster (per AUDITOR-VERIFICATION invocation contract step 1-5). The auditor consumes the cumulative diff for plans 24-01 through 24-07 and produces a Pass/Warnings/Failures report scoped to that diff.

Per CONTEXT D-13 invocation timing: "every task group's final commit includes a `gsd-framework-fidelity-auditor` invocation."

This plan is a CHECKPOINT-style gate — if the auditor produces ❌ findings on 23.1-scoped surfaces (Investigation: agents/investigation.py, streaming/investigation_adapter.py, agents/middleware/capture_trace.py, KQL AGENT_RUNS, tools/investigation.py decorator strip, lifespan Investigation slice), THIS PLAN BLOCKS task group 23.2 from starting. Either:
1. Fix the ❌ in a follow-up plan (24-08.5 or amended back into 24-01..24-07 via a new commit), OR
2. Rebut with the D-07 explicit-justification template recorded inline.

Outstanding ❌ for surfaces NOT yet migrated (Admin: F-03/F-16, Classifier: F-04/F-09/F-10/F-11/F-13/F-14, eval: F-06/F-07, warmup: F-02, main.py Admin/Classifier slices: F-01, F-12, instructions admin.md/classifier.md: F-19) is EXPECTED and acceptable at this gate. The auditor must scope its strict checks to the 23.1 diff only — out-of-scope findings get logged but do not block.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-foundry-ga-migration/24-CONTEXT.md
@.planning/phases/23-foundry-ga-prep/AUDITOR-VERIFICATION.md
@.planning/phases/24-foundry-ga-migration/FRAMEWORK-FIDELITY-calibration.md
@docs/superpowers/specs/2026-05-05-foundry-ga-migration-design.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture cumulative 23.1 diff</name>
  <files>.planning/phases/24-foundry-ga-migration/FIDELITY-23.1.patch</files>
  <read_first>
    - .planning/phases/23-foundry-ga-prep/AUDITOR-VERIFICATION.md (Phase 24 invocation contract step 1)
  </read_first>
  <action>
    1. Determine the start SHA for task group 23.1: this is the commit IMMEDIATELY BEFORE plan 24-01's first commit (the push guard install). The Phase 24 task-group-start-sha is the HEAD of `main` at the moment Phase 23 ended (which should match the most recent commit on `main` not authored by Phase 24).
    
    2. Find the start SHA: `git log --oneline -20` and locate the commit corresponding to plan 23-05 completion or any commit message starting with "docs(23-" or before the first "docs(24-" / "feat(24-" commit. Save this SHA as `TASK_GROUP_START_SHA`.
    
    3. Capture the cumulative diff for plans 24-01..24-07:
       ```
       git diff $TASK_GROUP_START_SHA..HEAD -- \
         backend/ \
         .planning/phases/24-foundry-ga-migration/PUSH-GUARD-ACTIVE \
         > .planning/phases/24-foundry-ga-migration/FIDELITY-23.1.patch
       ```
    
    4. Verify the patch is non-empty and includes diffs for: pyproject.toml/uv.lock, config.py, agents/instructions/investigation.md (added), agents/middleware/__init__.py + capture_trace.py (added), agents/investigation.py, main.py, tools/investigation.py, observability/kql_templates.py, observability/queries.py, observability/span_processor.py, streaming/investigation_adapter.py, api/investigate.py.
       ```
       grep -c "^diff --git" .planning/phases/24-foundry-ga-migration/FIDELITY-23.1.patch
       # MUST return at least 12 (one per file modified or added in 23.1)
       ```
  </action>
  <verify>
    <automated>test -f .planning/phases/24-foundry-ga-migration/FIDELITY-23.1.patch && test -s .planning/phases/24-foundry-ga-migration/FIDELITY-23.1.patch && [ "$(grep -c "^diff --git" .planning/phases/24-foundry-ga-migration/FIDELITY-23.1.patch)" -ge 10 ]</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/phases/24-foundry-ga-migration/FIDELITY-23.1.patch` returns 0
    - `test -s .planning/phases/24-foundry-ga-migration/FIDELITY-23.1.patch` returns 0 (non-empty)
    - `grep -c "^diff --git" .planning/phases/24-foundry-ga-migration/FIDELITY-23.1.patch` returns at least 10
    - The patch references `backend/src/second_brain/agents/middleware/capture_trace.py` (verify with `grep -q "agents/middleware/capture_trace.py" .planning/phases/24-foundry-ga-migration/FIDELITY-23.1.patch`)
    - The patch references `backend/src/second_brain/streaming/investigation_adapter.py`
    - The patch references `backend/src/second_brain/agents/investigation.py`
  </acceptance_criteria>
  <done>Cumulative 23.1 diff captured. Ready to spawn the auditor with the patch as input.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 24 task group 23.1 commit cluster complete (plans 24-01 through 24-07). Investigation agent migrated to GA `Agent` + `FoundryChatClient`; instructions promoted; tools decorator-stripped; capture-trace middleware in place; custom `investigate` span deleted; KQL AGENT_RUNS template updated. Local main is buildable for the Investigation surface only — Admin and Classifier slices still reference RC types (expected; 23.2 and 23.3 fix them).
  </what-built>
  <how-to-verify>
    1. Spawn the framework-fidelity auditor subagent at `~/.claude/agents/gsd-framework-fidelity-auditor.md` with these inputs (use the Task tool with `subagent_type` matching the auditor):
       - **Task group ID:** "23.1"
       - **Scope:** Investigation surface — files listed in 24-PATTERNS.md "Modified files (Task group 23.1 - Investigation)" + the new files (instructions/investigation.md, middleware/__init__.py, middleware/capture_trace.py)
       - **Diff patch path:** `.planning/phases/24-foundry-ga-migration/FIDELITY-23.1.patch`
       - **Calibration baseline:** `.planning/phases/24-foundry-ga-migration/FRAMEWORK-FIDELITY-calibration.md` (the 19 ❌ findings F-01..F-19 are the target list)
       - **Output path:** `.planning/phases/24-foundry-ga-migration/FRAMEWORK-FIDELITY-23.1.md`
       - **Expected closures for 23.1:** F-01 partial (Investigation slice of main.py), F-05 (streaming/investigation_adapter.py), F-08 partial (tools/investigation.py 9 decorators), F-13 partial (Investigation conversation_id replaced with AgentSession), F-14 partial (deletion limited to Investigation surface), F-15 (the only F-15 location), F-17 (capture_trace middleware replaces it), F-19 partial (investigation.md instruction file present), W-01 (narrowed-scope comment). Outstanding ❌ for Admin/Classifier surfaces is EXPECTED.
    
    2. Wait for the auditor to produce `.planning/phases/24-foundry-ga-migration/FRAMEWORK-FIDELITY-23.1.md` with sections **Pass** / **Warnings** / **Failures**.
    
    3. Read the report. Confirm:
       - Zero ❌ on the 23.1 scope (anything within agents/investigation.py, agents/middleware/, streaming/investigation_adapter.py, tools/investigation.py decorator-strip, KQL AGENT_RUNS update, instructions/investigation.md, the Investigation slice of main.py)
       - The W-01 warning's narrowed-scope-doc-comment was added to span_processor.py
       - Outstanding ❌ for non-23.1 surfaces is logged but does NOT count against this gate
    
    4. If any 23.1-scoped ❌ remains:
       - Read the auditor's specific complaint
       - Either fix the offending code (creating a small follow-up commit) and re-run the auditor, OR
       - Inline the D-07 explicit-justification template in the failing source file as a comment, and re-run the auditor
       - Repeat until 23.1-scoped ❌ count is zero
    
    5. When zero 23.1 ❌, type "approved" to proceed to task group 23.2.
  </how-to-verify>
  <resume-signal>
    Type "approved" if FRAMEWORK-FIDELITY-23.1.md shows zero 23.1-scoped ❌ findings.
    Type "block: <issue>" if a 23.1-scoped ❌ requires a fix or justification before continuing.
  </resume-signal>
</task>

</tasks>

<verification>
- FIDELITY-23.1.patch exists, non-empty, contains diffs for at least 10 files
- FRAMEWORK-FIDELITY-23.1.md exists with explicit Pass/Warnings/Failures sections
- Zero ❌ on 23.1-scoped surfaces; outstanding ❌ for 23.2/23.3 surfaces is logged
- W-01 narrowed-scope comment confirmed in span_processor.py
- F-15 (the only investigate-span deletion) cleared
- F-17 (middleware re-implementation) cleared
</verification>

<success_criteria>
Auditor zero ❌ on 23.1 scope. Plan 24-09 (Admin migration kickoff) unblocked.
</success_criteria>

<output>
After completion, create `.planning/phases/24-foundry-ga-migration/24-08-SUMMARY.md` documenting:
- Auditor report path
- Closures: F-05, F-15, F-17, partial F-01/F-08/F-13/F-14/F-19, W-01
- Outstanding (acceptable at this gate): F-01 (Admin/Classifier slices of main.py), F-02 (warmup), F-03 (admin_handoff), F-04 (classifier streaming), F-06/F-07 (eval), F-08 (admin/classifier/recipe/transcription tool decorators), F-09/F-10/F-11/F-13 (classifier surface), F-12 (Admin/Classifier agent_id pinning), F-14/F-16 (admin/classifier custom spans), F-19 (admin.md/classifier.md instructions)
- 23.2 unblocked
</output>
