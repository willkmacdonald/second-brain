---
phase: 24-foundry-ga-migration
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - backend/pyproject.toml
  - backend/uv.lock
  - backend/src/second_brain/config.py
autonomous: true
requirements: [F-01, D-12, P1-4]

must_haves:
  truths:
    - "backend/pyproject.toml ADDS agent-framework + agent-framework-foundry; agent-framework-azure-ai==1.0.0rc2 STILL PRESENT (P1-4 amendment — RC dep removed in plan 24-19.5 only after source imports gone)"
    - "backend/uv.lock matches the new pyproject.toml; both GA tree AND RC tree resolvable in venv"
    - "Settings.foundry_model exists with default 'gpt-4o'"
    - "azure_ai_*_agent_id settings still present (NOT YET removed; 24-21 cleanup removes them)"
    - "Mid-migration commits between 24-04 and 24-19 remain individually runnable (uv sync produces a working venv) per CONTEXT D-13"
  artifacts:
    - path: "backend/pyproject.toml"
      provides: "GA dep set ADDITIVE — both GA + RC packages installed"
      contains: "agent-framework-foundry"
    - path: "backend/uv.lock"
      provides: "Locked dep tree containing BOTH GA and RC trees"
    - path: "backend/src/second_brain/config.py"
      provides: "foundry_model setting"
      contains: "foundry_model"
  key_links:
    - from: "backend/pyproject.toml"
      to: "agent-framework, agent-framework-foundry, agent-framework-azure-ai"
      via: "[project] dependencies pin (all three present mid-migration)"
      pattern: "agent-framework-foundry"
---

<objective>
Apply the GA dependency set ADDITIVELY (per defect P1-4) and add the `foundry_model` Pydantic Settings field. This is the foundational config commit for task group 23.1: every subsequent commit assumes GA imports are resolvable in the local venv.

This plan is the FIRST commit in the 23.1 commit cluster. Per CONTEXT D-12, all subsequent commits build on this one in strict sequence. Per CONTEXT D-13 (commits stay individually runnable), the RC dep STAYS pinned until plan 24-19.5 removes it AFTER plan 24-19 has cleared the last RC import from source.

**P1-4 amendment (relative to original plan 24-02):** the original plan removed `agent-framework-azure-ai==1.0.0rc2` in this commit. That created an ImportError window between 24-04 (some Admin/Classifier code still imports `AzureAIAgentClient`) and 24-19 (last RC import removed) where intermediate commits would not pass `uv pip sync` cleanly. The amended approach: ADD GA deps without removing the RC dep. Plan 24-19.5 removes the RC dep + regenerates the lock when no source imports it.

Per CONFIG-DELTAS Step A: keep the three `azure_ai_*_agent_id` settings present in this commit. They are removed in plan 24-21 (final cleanup) so the deployed RC image is never starved of those env vars before the GA image deploys.

Output: pyproject.toml + uv.lock pin BOTH GA packages AND the RC package; config.py adds `foundry_model: str = "gpt-4o"` after `azure_ai_project_endpoint`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-foundry-ga-migration/24-CONTEXT.md
@.planning/phases/24-foundry-ga-migration/24-PATTERNS.md
@.planning/phases/24-foundry-ga-migration/24-PLAN-DEFECTS.md
@.planning/phases/23-foundry-ga-prep/CONFIG-DELTAS.md
@.planning/phases/23-foundry-ga-prep/CANDIDATE-pyproject.toml
@.planning/phases/23-foundry-ga-prep/CANDIDATE-uv.lock
@backend/pyproject.toml
@backend/src/second_brain/config.py

<interfaces>
The candidate files at `.planning/phases/23-foundry-ga-prep/CANDIDATE-pyproject.toml` and `CANDIDATE-uv.lock` were drafted by Phase 23 plan 23-01 to be drop-in REPLACEMENTS that fully removed the RC dep. Per P1-4 amendment, those drop-ins are NOT used directly anymore. Instead:

1. Read the current `backend/pyproject.toml` and inspect which dep entries are RC vs GA.
2. ADD the GA-only entries from CANDIDATE-pyproject.toml (`agent-framework`, `agent-framework-foundry`, transitive bumps like `azure-ai-projects>=2.1.0`) to current pyproject.toml.
3. KEEP the existing `agent-framework-azure-ai==1.0.0rc2` line.
4. Run `uv lock` to regenerate the lock with BOTH trees resolvable.
5. Verify both GA imports AND RC imports work in the venv.

Net dep state after this plan:
- `agent-framework-azure-ai==1.0.0rc2` (RC; removed in 24-19.5)
- `agent-framework` (GA, NEW)
- `agent-framework-foundry` (GA, NEW)
- `azure-ai-projects>=2.1.0` (transitive bump, NEW)

Settings shape (existing class in `backend/src/second_brain/config.py:8-20`, KEEP all current fields):
```python
class Settings(BaseSettings):
    azure_ai_project_endpoint: str = ""
    azure_ai_classifier_agent_id: str = ""    # KEEP — deleted in 24-21
    azure_ai_admin_agent_id: str = ""          # KEEP — deleted in 24-21
    azure_ai_investigation_agent_id: str = ""  # KEEP — deleted in 24-21
    # ADD here: foundry_model: str = "gpt-4o"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Additively add GA deps to pyproject.toml; KEEP RC dep; regenerate uv.lock</name>
  <files>backend/pyproject.toml, backend/uv.lock</files>
  <read_first>
    - backend/pyproject.toml (current — confirm agent-framework-azure-ai==1.0.0rc2 line and any other RC pins)
    - .planning/phases/23-foundry-ga-prep/CANDIDATE-pyproject.toml (target GA dep set; useful as REFERENCE for which lines to add, even though we don't drop in wholesale per P1-4)
    - .planning/phases/24-foundry-ga-migration/24-PLAN-DEFECTS.md (P1-4 specifies additive-only)
  </read_first>
  <action>
    1. Read `backend/pyproject.toml`. Locate the `[project] dependencies` section. Confirm `agent-framework-azure-ai==1.0.0rc2` (or whatever the current pin is) is present.

    2. Read `.planning/phases/23-foundry-ga-prep/CANDIDATE-pyproject.toml` and identify the lines that ADD GA support: `agent-framework`, `agent-framework-foundry`, and any transitive pin bumps (e.g. `azure-ai-projects>=2.1.0`).

    3. Edit `backend/pyproject.toml`:
       - ADD `"agent-framework"` (or specific version pin matching candidate) to dependencies.
       - ADD `"agent-framework-foundry"` (or matching pin) to dependencies.
       - If candidate bumps any transitives (e.g. `azure-ai-projects` from `==2.0.0b3` to `>=2.1.0`), apply that bump too — it's required for GA compat.
       - DO NOT remove `agent-framework-azure-ai==1.0.0rc2`. Plan 24-19.5 removes it.
       - DO NOT remove any other existing dep.

    4. Regenerate the lock with both trees resolvable:
       ```
       cd backend && uv lock
       ```
       Expected: lock resolves with both `agent-framework-azure-ai==1.0.0rc2` AND the GA tree present. If `uv lock` fails with a conflict (e.g. transitive dep version pin clash), inspect the error. The candidate dep set was designed to coexist; if a conflict surfaces, escalate to operator before improvising.

    5. Sync the venv:
       ```
       cd backend && uv sync
       ```
       Should exit 0.

    6. Verify GA imports work:
       ```
       cd backend && uv run python -c "from agent_framework import Agent, ChatOptions, AgentSession; from agent_framework_foundry import FoundryChatClient; print('GA imports OK')"
       ```
       Output: `GA imports OK`.

    7. Verify RC imports STILL work (P1-4 invariant — until 24-19.5):
       ```
       cd backend && uv run python -c "from agent_framework.azure import AzureAIAgentClient; print('RC imports OK')"
       ```
       Output: `RC imports OK`. This MUST succeed; the original plan's verify check `import agent_framework.azure 2>&1 | grep ModuleNotFoundError` is INVERTED in the amended plan.

    Per the auto-format trap warning in MEMORY: do steps 3 and 4 in a single coherent edit pass. If you split, the lock regeneration may run against a half-edited pyproject.toml.
  </action>
  <verify>
    <automated>grep -q "agent-framework-foundry" backend/pyproject.toml && grep -q "agent-framework-azure-ai" backend/pyproject.toml && cd backend && uv run python -c "from agent_framework import Agent, ChatOptions, AgentSession; from agent_framework_foundry import FoundryChatClient" && cd backend && uv run python -c "from agent_framework.azure import AzureAIAgentClient"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "agent-framework-foundry" backend/pyproject.toml` returns 0 (GA Foundry pin added)
    - `grep -qE "\"agent-framework\"|\"agent-framework[~>=]" backend/pyproject.toml` returns 0 (GA core pin added)
    - `grep -q "agent-framework-azure-ai" backend/pyproject.toml` returns 0 (RC pin RETAINED — P1-4 amendment)
    - `cd backend && uv sync` exits 0
    - `cd backend && uv run python -c "from agent_framework import Agent, ChatOptions, AgentSession; from agent_framework_foundry import FoundryChatClient"` exits 0 (GA importable)
    - `cd backend && uv run python -c "from agent_framework.azure import AzureAIAgentClient"` exits 0 (RC importable — INVERTED from original plan; per P1-4 the RC namespace MUST stay importable until 24-19.5)
    - `grep -q "agent-framework-azure-ai" backend/uv.lock` returns 0 (RC tree present in lock)
    - `grep -q "agent-framework-foundry\|agent_framework_foundry" backend/uv.lock` returns 0 (GA tree present in lock)
  </acceptance_criteria>
  <done>Both GA and RC dep trees installed in local venv. Mid-migration commits remain individually runnable. RC removal deferred to 24-19.5.</done>
</task>

<task type="auto">
  <name>Task 2: Add foundry_model setting to config.py</name>
  <files>backend/src/second_brain/config.py</files>
  <read_first>
    - backend/src/second_brain/config.py (current Settings class)
    - .planning/phases/23-foundry-ga-prep/CONFIG-DELTAS.md (foundry_model addition spec, lines 7-25)
    - .planning/phases/24-foundry-ga-migration/24-PATTERNS.md (S-6 — additions-only convention)
  </read_first>
  <action>
    Edit `backend/src/second_brain/config.py`. Locate the Settings class (currently around lines 8-20). Add a new field `foundry_model: str = "gpt-4o"` immediately AFTER `azure_ai_project_endpoint: str = ""` and BEFORE `azure_ai_classifier_agent_id: str = ""`.

    The diff target shape:
    ```python
    class Settings(BaseSettings):
        # ... existing fields above ...
        azure_ai_project_endpoint: str = ""
        foundry_model: str = "gpt-4o"              # NEW (Phase 24 task group 23.1)
        azure_ai_classifier_agent_id: str = ""     # KEEP — deleted in plan 24-21
        azure_ai_admin_agent_id: str = ""          # KEEP — deleted in plan 24-21
        azure_ai_investigation_agent_id: str = ""  # KEEP — deleted in plan 24-21
        # ... existing fields below ...
    ```

    Do NOT remove any existing field. Do NOT change `model_config`. Do NOT add `FOUNDRY_MODEL` env var read explicitly — pydantic-settings reads `FOUNDRY_MODEL` automatically from the case-insensitive convention already configured.

    Verify the import works: `cd backend && uv run python -c "from second_brain.config import settings; print(settings.foundry_model)"` should print `gpt-4o`.
  </action>
  <verify>
    <automated>grep -q "foundry_model: str = \"gpt-4o\"" backend/src/second_brain/config.py && grep -q "azure_ai_classifier_agent_id" backend/src/second_brain/config.py && cd backend && uv run python -c "from second_brain.config import settings; assert settings.foundry_model == 'gpt-4o'"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "foundry_model: str = \"gpt-4o\"" backend/src/second_brain/config.py` returns 0
    - `grep -q "azure_ai_classifier_agent_id" backend/src/second_brain/config.py` returns 0 (RC field still present)
    - `grep -q "azure_ai_admin_agent_id" backend/src/second_brain/config.py` returns 0
    - `grep -q "azure_ai_investigation_agent_id" backend/src/second_brain/config.py` returns 0
    - `grep -q "azure_ai_project_endpoint" backend/src/second_brain/config.py` returns 0
    - `cd backend && uv run python -c "from second_brain.config import settings; assert settings.foundry_model == 'gpt-4o'"` exits 0
  </acceptance_criteria>
  <done>foundry_model added to Settings with default 'gpt-4o'. All four `azure_ai_*` settings still present per CONFIG-DELTAS deferred-deletion plan.</done>
</task>

</tasks>

<verification>
- pyproject.toml has BOTH agent-framework-azure-ai (RC) AND agent-framework + agent-framework-foundry (GA) entries
- `from agent_framework_foundry import FoundryChatClient` works
- `from agent_framework.azure import AzureAIAgentClient` STILL works (P1-4 invariant — RC dep retained until 24-19.5)
- `settings.foundry_model == "gpt-4o"` after instantiation
- All three `azure_ai_*_agent_id` settings still present (deferred-deletion intent honored)
- Lock file resolves both trees without conflict
</verification>

<success_criteria>
The local venv runs on BOTH GA and RC packages during the migration window. Subsequent plans (24-03 through 24-19) can `import` from agent_framework_foundry without resolution errors AND from agent_framework.azure (until those imports are removed). `settings.foundry_model` is available everywhere the lifespan needs it. Backward-compat fields stay so the still-deployed RC image continues to read its own env vars without disruption. `git bisect` across the migration window remains usable per CONTEXT D-13.
</success_criteria>

<output>
After completion, create `.planning/phases/24-foundry-ga-migration/24-02-SUMMARY.md` documenting:
- Diff vs. previous pyproject.toml (additive only — no removals)
- Output of `uv lock` + `uv sync` (clean, both trees resolved)
- Confirmed GA imports work; RC imports STILL work (P1-4 invariant)
- Settings.foundry_model default = "gpt-4o"
- Reminder: agent_id settings retained per CONFIG-DELTAS (delete in 24-21); RC dep retained per P1-4 (delete in 24-19.5)
</output>
</content>
</invoke>
