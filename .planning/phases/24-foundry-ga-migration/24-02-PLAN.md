---
phase: 24-foundry-ga-migration
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - backend/pyproject.toml
  - backend/uv.lock
  - backend/src/second_brain/config.py
autonomous: true
requirements: [F-01, D-12]

must_haves:
  truths:
    - "backend/pyproject.toml pins agent-framework + agent-framework-foundry, removes agent-framework-azure-ai"
    - "backend/uv.lock matches the new pyproject.toml"
    - "Settings.foundry_model exists with default 'gpt-4o'"
    - "azure_ai_*_agent_id settings still present (NOT YET removed; 24-22 cleanup removes them)"
  artifacts:
    - path: "backend/pyproject.toml"
      provides: "GA agent-framework dep set"
      contains: "agent-framework-foundry"
    - path: "backend/uv.lock"
      provides: "Locked GA dep tree"
    - path: "backend/src/second_brain/config.py"
      provides: "foundry_model setting"
      contains: "foundry_model"
  key_links:
    - from: "backend/pyproject.toml"
      to: "agent-framework, agent-framework-foundry"
      via: "[project] dependencies pin"
      pattern: "agent-framework-foundry"
---

<objective>
Apply the GA dependency set and add the `foundry_model` Pydantic Settings field. This is the foundational config commit for task group 23.1: every subsequent commit assumes GA imports are resolvable in the local venv.

This plan is the FIRST commit in the 23.1 commit cluster. Per CONTEXT D-12, all subsequent commits build on this one in strict sequence.

Per CONFIG-DELTAS Step A: keep the three `azure_ai_*_agent_id` settings present in this commit. They are removed in plan 24-22 (final cleanup) so the deployed RC image is never starved of those env vars before the GA image deploys.

Output: pyproject.toml + uv.lock pin GA packages; config.py adds `foundry_model: str = "gpt-4o"` after `azure_ai_project_endpoint`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-foundry-ga-migration/24-CONTEXT.md
@.planning/phases/24-foundry-ga-migration/24-PATTERNS.md
@.planning/phases/23-foundry-ga-prep/CONFIG-DELTAS.md
@.planning/phases/23-foundry-ga-prep/CANDIDATE-pyproject.toml
@.planning/phases/23-foundry-ga-prep/CANDIDATE-uv.lock
@backend/pyproject.toml
@backend/src/second_brain/config.py

<interfaces>
The candidate files at `.planning/phases/23-foundry-ga-prep/CANDIDATE-pyproject.toml` and `CANDIDATE-uv.lock` were drafted by Phase 23 plan 23-01 and verified to resolve cleanly in a throwaway local venv. They are drop-in replacements — no edits should be needed.

Key dep changes (verify after copy):
- `agent-framework-azure-ai==1.0.0rc2` → REMOVED
- `agent-framework` → ADDED (1.3.0+)
- `agent-framework-foundry` → ADDED
- `azure-ai-projects` pin: `==2.0.0b3` → `>=2.1.0` (transitive)

Settings shape (existing class in `backend/src/second_brain/config.py:8-20`, KEEP all current fields):
```python
class Settings(BaseSettings):
    azure_ai_project_endpoint: str = ""
    azure_ai_classifier_agent_id: str = ""    # KEEP — deleted in 24-22
    azure_ai_admin_agent_id: str = ""          # KEEP — deleted in 24-22
    azure_ai_investigation_agent_id: str = ""  # KEEP — deleted in 24-22
    # ADD here: foundry_model: str = "gpt-4o"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply CANDIDATE-pyproject.toml + CANDIDATE-uv.lock</name>
  <files>backend/pyproject.toml, backend/uv.lock</files>
  <read_first>
    - .planning/phases/23-foundry-ga-prep/CANDIDATE-pyproject.toml (drop-in replacement)
    - .planning/phases/23-foundry-ga-prep/CANDIDATE-uv.lock (drop-in replacement)
    - backend/pyproject.toml (current state — to confirm what is being replaced)
  </read_first>
  <action>
    1. Copy `.planning/phases/23-foundry-ga-prep/CANDIDATE-pyproject.toml` to `backend/pyproject.toml` (overwrite).
    2. Copy `.planning/phases/23-foundry-ga-prep/CANDIDATE-uv.lock` to `backend/uv.lock` (overwrite).
    3. Run `cd backend && uv sync` to verify the lock resolves and installs cleanly.
    4. After sync, run `cd backend && uv run python -c "from agent_framework import Agent, ChatOptions; from agent_framework_foundry import FoundryChatClient; print('GA imports OK')"` and confirm output is `GA imports OK`.
    5. Confirm RC import is gone: `cd backend && uv run python -c "import agent_framework.azure" 2>&1 | grep -q "ModuleNotFoundError" && echo "RC removed OK"`.

    Do NOT edit either candidate file in this commit — they are the audited drop-ins. If `uv sync` fails, escalate to the user; do not improvise the lock file.
  </action>
  <verify>
    <automated>cd backend && grep -q "agent-framework-foundry" pyproject.toml && ! grep -q "agent-framework-azure-ai==1.0.0rc2" pyproject.toml && uv run python -c "from agent_framework import Agent, ChatOptions; from agent_framework_foundry import FoundryChatClient" && uv run python -c "import agent_framework.azure" 2>&1 | grep -q ModuleNotFoundError</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "agent-framework-foundry" backend/pyproject.toml` returns 0
    - `! grep -q "agent-framework-azure-ai==1.0.0rc2" backend/pyproject.toml` returns 0 (RC pin gone)
    - `cd backend && uv sync` exits 0 (lock resolves cleanly)
    - `cd backend && uv run python -c "from agent_framework import Agent, ChatOptions; from agent_framework_foundry import FoundryChatClient"` exits 0
    - `cd backend && uv run python -c "import agent_framework.azure"` exits non-zero with ModuleNotFoundError
    - `diff backend/pyproject.toml .planning/phases/23-foundry-ga-prep/CANDIDATE-pyproject.toml` returns no diff
    - `diff backend/uv.lock .planning/phases/23-foundry-ga-prep/CANDIDATE-uv.lock` returns no diff
  </acceptance_criteria>
  <done>GA dep set in place; RC packages uninstalled from local venv; agent_framework_foundry.FoundryChatClient importable.</done>
</task>

<task type="auto">
  <name>Task 2: Add foundry_model setting to config.py</name>
  <files>backend/src/second_brain/config.py</files>
  <read_first>
    - backend/src/second_brain/config.py (current Settings class)
    - .planning/phases/23-foundry-ga-prep/CONFIG-DELTAS.md (foundry_model addition spec, lines 7-25)
    - .planning/phases/24-foundry-ga-migration/24-PATTERNS.md (S-6 — additions-only convention)
  </read_first>
  <action>
    Edit `backend/src/second_brain/config.py`. Locate the Settings class (currently around lines 8-20). Add a new field `foundry_model: str = "gpt-4o"` immediately AFTER `azure_ai_project_endpoint: str = ""` and BEFORE `azure_ai_classifier_agent_id: str = ""`.

    The diff target shape:
    ```python
    class Settings(BaseSettings):
        # ... existing fields above ...
        azure_ai_project_endpoint: str = ""
        foundry_model: str = "gpt-4o"              # NEW (Phase 24 task group 23.1)
        azure_ai_classifier_agent_id: str = ""     # KEEP — deleted in plan 24-22
        azure_ai_admin_agent_id: str = ""          # KEEP — deleted in plan 24-22
        azure_ai_investigation_agent_id: str = ""  # KEEP — deleted in plan 24-22
        # ... existing fields below ...
    ```

    Do NOT remove any existing field. Do NOT change `model_config`. Do NOT add `FOUNDRY_MODEL` env var read explicitly — pydantic-settings reads `FOUNDRY_MODEL` automatically from the case-insensitive convention already configured.

    Verify the import works: `cd backend && uv run python -c "from second_brain.config import settings; print(settings.foundry_model)"` should print `gpt-4o`.
  </action>
  <verify>
    <automated>grep -q "foundry_model: str = \"gpt-4o\"" backend/src/second_brain/config.py && grep -q "azure_ai_classifier_agent_id" backend/src/second_brain/config.py && cd backend && uv run python -c "from second_brain.config import settings; assert settings.foundry_model == 'gpt-4o'"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "foundry_model: str = \"gpt-4o\"" backend/src/second_brain/config.py` returns 0
    - `grep -q "azure_ai_classifier_agent_id" backend/src/second_brain/config.py` returns 0 (RC field still present)
    - `grep -q "azure_ai_admin_agent_id" backend/src/second_brain/config.py` returns 0
    - `grep -q "azure_ai_investigation_agent_id" backend/src/second_brain/config.py` returns 0
    - `grep -q "azure_ai_project_endpoint" backend/src/second_brain/config.py` returns 0
    - `cd backend && uv run python -c "from second_brain.config import settings; assert settings.foundry_model == 'gpt-4o'"` exits 0
  </acceptance_criteria>
  <done>foundry_model added to Settings with default 'gpt-4o'. All four `azure_ai_*` settings still present per CONFIG-DELTAS deferred-deletion plan.</done>
</task>

</tasks>

<verification>
- pyproject.toml + uv.lock match the audited candidate files exactly
- `from agent_framework_foundry import FoundryChatClient` works
- `import agent_framework.azure` raises ModuleNotFoundError
- `settings.foundry_model == "gpt-4o"` after instantiation
- All three `azure_ai_*_agent_id` settings still present (deferred-deletion intent honored)
</verification>

<success_criteria>
The local venv runs on GA packages; subsequent plans (24-03 through 24-22) can `import` from agent_framework_foundry without resolution errors. `settings.foundry_model` is available everywhere the lifespan needs it. Backward-compat fields stay so the still-deployed RC image continues to read its own env vars without disruption.
</success_criteria>

<output>
After completion, create `.planning/phases/24-foundry-ga-migration/24-02-SUMMARY.md` documenting:
- Diff vs. candidate files (should be zero)
- Output of `uv sync` (clean, no warnings about RC pins)
- Confirmed GA imports work; RC imports raise ModuleNotFoundError
- Settings.foundry_model default = "gpt-4o"
- Reminder: agent_id settings retained per CONFIG-DELTAS (delete in 24-22)
</output>
