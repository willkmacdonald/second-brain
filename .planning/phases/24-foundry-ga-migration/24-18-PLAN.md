---
phase: 24-foundry-ga-migration
plan: 18
type: execute
wave: 4
depends_on: [17]
files_modified:
  - backend/src/second_brain/agents/middleware.py
  - backend/src/second_brain/eval/invoker.py
  - backend/src/second_brain/eval/runner.py
  - backend/src/second_brain/observability/kql_templates.py
  - backend/tests/test_legacy_middleware_imports_survive.py
autonomous: true
requirements: [F-17, F-06, D-04, D-12, P1-3]

must_haves:
  truths:
    - "backend/src/second_brain/agents/middleware.py is DELETED (F-17 — replaced by agents/agent_middleware/capture_trace.py)"
    - "RCEvalAgentInvoker class deleted from eval/invoker.py"
    - "All callers of EvalAgentInvoker now use GAEvalAgentInvoker (no hybrid composition needed)"
    - "eval/runner.py classifier call site uses GA invoker (no longer routes to RC)"
    - "FORCED_TOOL_FAILURE KQL template added (or RECENT_FAILURES_FILTERED extended) for the new SSE sub-code"
    - "P1-3 red test test_legacy_middleware_imports_survive.py UPDATED — the legacy-importable assertion is REMOVED or inverted (legacy module no longer exists). The new GA path assertion remains."
    - "Optional P1-3 polish: agents/agent_middleware/ may be renamed to agents/middleware/ now that the legacy file is gone (no functional change). If renamed, update all import sites."
  artifacts:
    - path: "backend/src/second_brain/agents/middleware.py"
      provides: "DELETED"
    - path: "backend/src/second_brain/eval/invoker.py"
      provides: "GA-only invoker (RC class deleted, optional rename to single class)"
    - path: "backend/src/second_brain/observability/kql_templates.py"
      provides: "Forced_tool_failure tracking query"
      contains: "forced_tool_failure"
    - path: "backend/tests/test_legacy_middleware_imports_survive.py"
      provides: "Updated regression guard — legacy importable assertion retired"
  key_links:
    - from: "eval/runner.py"
      to: "GAEvalAgentInvoker"
      via: "all eval calls now GA"
      pattern: "GAEvalAgentInvoker"
---

<objective>
End-of-23.3 file changes (cleanup + KQL):

1. **Delete agents/middleware.py (F-17):** Old `AuditAgentMiddleware` + `ToolTimingMiddleware` are no longer imported by main.py (24-04 / 24-09 / 24-14 wired in CaptureTrace* via `agents/agent_middleware/`). Verify with grep first, then delete the file.

2. **Update P1-3 red test (`test_legacy_middleware_imports_survive.py`):** the test from 24-03 had three sub-tests: (a) legacy importable, (b) new GA importable, (c) no shadowing. After deleting `middleware.py`, sub-test (a) will fail. This plan UPDATES the test: drop sub-test (a) — the legacy module no longer exists; the regression guard for non-shadowing migrates to a "no agents/middleware.py file" check. Keep sub-test (b) and a renamed (c). Sub-test (c) becomes "no `agents/middleware.py` file or directory exists" (post-deletion invariant).

3. **Delete RCEvalAgentInvoker (EVAL-INVENTORY deletion trigger):** Per CONTEXT D-04 + EVAL-INVENTORY: "deleted at end of 23.3 final cleanup commit, when no caller uses the RC implementation." After 24-14..24-17 the classifier is on GA. Delete `RCEvalAgentInvoker` class from `eval/invoker.py`. Update `eval/runner.py` to use only `GAEvalAgentInvoker`. Optionally rename `GAEvalAgentInvoker` to `EvalAgentInvoker` (single concrete class, drop Protocol).

4. **Add KQL template for forced_tool_failure events:** Per SPAN-NAME-MAPPING: 23.3 introduces the new sub-code. Add a new template `FORCED_TOOL_FAILURE_COUNT` filtering on AppExceptions or AppTraces where the SSE error has `sub_code == "forced_tool_failure"`.

5. **OPTIONAL P1-3 polish — rename `agents/agent_middleware/` → `agents/middleware/`:** the agent_middleware/ name was chosen in 24-03 to avoid shadowing the legacy module. Now that the legacy module is deleted, the name can be tightened back to `middleware/`. If executed, update all import sites in main.py + tests. **The operator may choose to skip this polish step entirely** — the P1-3-correct name `agent_middleware/` is acceptable as a permanent location. Pick: skip the rename in this plan; document the option in a follow-up. Keeps the diff small.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-foundry-ga-migration/24-CONTEXT.md
@.planning/phases/24-foundry-ga-migration/24-PATTERNS.md
@.planning/phases/24-foundry-ga-migration/24-PLAN-DEFECTS.md
@.planning/phases/24-foundry-ga-migration/FRAMEWORK-FIDELITY-calibration.md
@.planning/phases/23-foundry-ga-prep/EVAL-INVENTORY.md
@.planning/phases/23-foundry-ga-prep/SPAN-NAME-MAPPING.md
@backend/src/second_brain/agents/middleware.py
@backend/src/second_brain/eval/invoker.py
@backend/src/second_brain/eval/runner.py
@backend/src/second_brain/observability/kql_templates.py
@backend/tests/test_legacy_middleware_imports_survive.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete legacy agents/middleware.py</name>
  <files>backend/src/second_brain/agents/middleware.py</files>
  <read_first>
    - backend/src/second_brain/agents/middleware.py (file being deleted)
    - .planning/phases/24-foundry-ga-migration/FRAMEWORK-FIDELITY-calibration.md (F-17)
  </read_first>
  <action>
    1. First confirm zero references in source: `grep -rn "from second_brain.agents.middleware import\\|from .middleware import" backend/src/second_brain/`. Should return zero. If anything still imports from the old file, fix the caller first to import from `agents.agent_middleware.capture_trace` instead.
    2. Confirm callers of `AuditAgentMiddleware` and `ToolTimingMiddleware` are zero: `grep -rn "AuditAgentMiddleware\\|ToolTimingMiddleware" backend/`. If any test file references these classes, update the test to use `CaptureTraceAgentMiddleware` / `CaptureTraceFunctionMiddleware`.
    3. Delete the file: `git rm backend/src/second_brain/agents/middleware.py` (or filesystem `rm` then `git add`).
    4. Verify the package layout is now clean: `cd backend && uv run python -c "from second_brain.agents.agent_middleware.capture_trace import CaptureTraceAgentMiddleware"` should still work; `cd backend && uv run python -c "from second_brain.agents.middleware import AuditAgentMiddleware"` should raise ImportError (the old class is gone).
    5. Verify no `agents/middleware/` package or `agents/middleware.py` module exists: `! test -e backend/src/second_brain/agents/middleware.py && ! test -d backend/src/second_brain/agents/middleware`.
  </action>
  <verify>
    <automated>! test -f backend/src/second_brain/agents/middleware.py && ! test -d backend/src/second_brain/agents/middleware && cd backend && uv run python -c "from second_brain.agents.agent_middleware.capture_trace import CaptureTraceAgentMiddleware"</automated>
  </verify>
  <acceptance_criteria>
    - `! test -f backend/src/second_brain/agents/middleware.py` returns 0 (file deleted)
    - `! test -d backend/src/second_brain/agents/middleware` returns 0 (no shadowing package created)
    - `! grep -rqE "AuditAgentMiddleware|ToolTimingMiddleware" backend/src/` returns 0
    - `cd backend && uv run python -c "from second_brain.agents.agent_middleware.capture_trace import CaptureTraceAgentMiddleware"` exits 0
    - `cd backend && uv run python -c "from second_brain.agents.middleware import AuditAgentMiddleware"` exits non-zero (ImportError)
  </acceptance_criteria>
  <done>F-17 cleared. Old middleware module gone. agent_middleware/ package remains as the GA path.</done>
</task>

<task type="auto">
  <name>Task 2: Update P1-3 red test — drop legacy-importable assertion (file deleted in Task 1)</name>
  <files>backend/tests/test_legacy_middleware_imports_survive.py</files>
  <read_first>
    - backend/tests/test_legacy_middleware_imports_survive.py (current 3 sub-tests from 24-03)
    - .planning/phases/24-foundry-ga-migration/24-PLAN-DEFECTS.md (P1-3 — test retirement is part of the original spec)
  </read_first>
  <action>
    Edit `backend/tests/test_legacy_middleware_imports_survive.py`:

    1. DELETE the function `test_legacy_agents_middleware_module_still_importable` — the legacy module no longer exists, so this test would always fail. Per the original test docstring: "After 24-18 deletes the legacy module, this test will fail — that's the trigger to retire the test."

    2. KEEP `test_new_ga_middleware_imports_at_distinct_path` — still useful as a regression guard for the agent_middleware/ package path.

    3. RENAME `test_no_package_shadowing` → `test_legacy_middleware_module_is_gone`. Update the assertion. The new check: NEITHER a file at `agents/middleware.py` NOR a directory at `agents/middleware/` exists. After 24-18 deletion the file is gone; the directory was never created.

       New body:
       ```python
       def test_legacy_middleware_module_is_gone() -> None:
           """Post-24-18: confirm the legacy agents/middleware.py is deleted AND
           no replacement package exists at the same path. The new GA middleware
           lives at agents/agent_middleware/.
           """
           from pathlib import Path

           project_root = Path(__file__).resolve().parents[1]
           legacy_module = project_root / "src" / "second_brain" / "agents" / "middleware.py"
           legacy_package = project_root / "src" / "second_brain" / "agents" / "middleware"
           assert not legacy_module.exists(), (
               f"Legacy module still present at {legacy_module}. "
               "Plan 24-18 should have deleted it."
           )
           assert not legacy_package.is_dir(), (
               f"P1-3 invariant: no package at {legacy_package} (would shadow "
               "if the legacy module were ever resurrected). The GA path is "
               "agents/agent_middleware/."
           )
       ```

    4. Update the file's module docstring to reflect the post-24-18 state:
       ```
       """Phase 24 P1-3 regression guard (post-24-18 state).
       
       After plan 24-18 deletes backend/src/second_brain/agents/middleware.py,
       this test asserts:
       - The new GA middleware imports at second_brain.agents.agent_middleware.capture_trace.
       - No file or directory exists at second_brain/agents/middleware{,.py}.
       
       The legacy-importable sub-test from 24-03 is retired here per P1-3 spec
       (the legacy module is gone — its import would now correctly raise ImportError).
       """
       ```

    5. Run the updated test: `cd backend && uv run pytest tests/test_legacy_middleware_imports_survive.py -x`. Both remaining sub-tests should PASS.
  </action>
  <verify>
    <automated>! grep -q "test_legacy_agents_middleware_module_still_importable" backend/tests/test_legacy_middleware_imports_survive.py && grep -q "test_new_ga_middleware_imports_at_distinct_path" backend/tests/test_legacy_middleware_imports_survive.py && grep -q "test_legacy_middleware_module_is_gone" backend/tests/test_legacy_middleware_imports_survive.py && cd backend && uv run pytest tests/test_legacy_middleware_imports_survive.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `! grep -q "test_legacy_agents_middleware_module_still_importable" backend/tests/test_legacy_middleware_imports_survive.py` returns 0 (retired sub-test gone)
    - `grep -q "test_new_ga_middleware_imports_at_distinct_path" backend/tests/test_legacy_middleware_imports_survive.py` returns 0
    - `grep -q "test_legacy_middleware_module_is_gone" backend/tests/test_legacy_middleware_imports_survive.py` returns 0
    - `cd backend && uv run pytest tests/test_legacy_middleware_imports_survive.py -x` exits 0
  </acceptance_criteria>
  <done>Test updated to post-24-18 invariants. Both remaining sub-tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Delete RCEvalAgentInvoker; runner uses GA only</name>
  <files>backend/src/second_brain/eval/invoker.py, backend/src/second_brain/eval/runner.py</files>
  <read_first>
    - backend/src/second_brain/eval/invoker.py (current state)
    - backend/src/second_brain/eval/runner.py (current invoker wiring)
    - .planning/phases/23-foundry-ga-prep/EVAL-INVENTORY.md (deletion trigger)
  </read_first>
  <action>
    Edit `backend/src/second_brain/eval/invoker.py`:

    1. Delete the entire `class RCEvalAgentInvoker:` block.
    2. Delete the conditional `from agent_framework.azure import AzureAIAgentClient` import in the TYPE_CHECKING block (it was only there for RCEvalAgentInvoker's type hint).
    3. KEEP `class EvalAgentInvoker(Protocol):` and `class GAEvalAgentInvoker:`. The Protocol is still useful for the runner's signature (and tests). Optional: rename `GAEvalAgentInvoker` -> `DefaultEvalAgentInvoker` to drop the GA prefix; or leave it as is. Pick: leave as `GAEvalAgentInvoker` for now to keep the diff small; subsequent cleanup can rename.
    4. ALSO delete the hybrid composition class if added in 24-12 (`_MigrationHybridInvoker` or similar) — no longer needed.

    Edit `backend/src/second_brain/eval/runner.py`:

    5. Update the constructor or factory function that wired the hybrid invoker to use `GAEvalAgentInvoker` directly. Pass both classifier_agent and admin_agent (both GA Agent objects from main.py app.state).
    6. The runner's signature stays `invoker: EvalAgentInvoker` — no API change.
    7. Verify: `cd backend && uv run python -c "from second_brain.eval.invoker import EvalAgentInvoker, GAEvalAgentInvoker; assert not hasattr(__import__('second_brain.eval.invoker', fromlist=['x']), 'RCEvalAgentInvoker')"`.
  </action>
  <verify>
    <automated>! grep -q "class RCEvalAgentInvoker" backend/src/second_brain/eval/invoker.py && ! grep -q "RCEvalAgentInvoker" backend/src/second_brain/eval/runner.py && grep -q "GAEvalAgentInvoker" backend/src/second_brain/eval/runner.py && cd backend && uv run python -c "from second_brain.eval.invoker import EvalAgentInvoker, GAEvalAgentInvoker"</automated>
  </verify>
  <acceptance_criteria>
    - `! grep -q "class RCEvalAgentInvoker" backend/src/second_brain/eval/invoker.py` returns 0
    - `! grep -rq "RCEvalAgentInvoker" backend/src/` returns 0 (zero references anywhere)
    - `grep -q "class GAEvalAgentInvoker" backend/src/second_brain/eval/invoker.py` returns 0
    - `cd backend && uv run python -c "from second_brain.eval.invoker import EvalAgentInvoker, GAEvalAgentInvoker"` exits 0
    - `! grep -q "AzureAIAgentClient" backend/src/second_brain/eval/invoker.py` returns 0 (RC type hint gone)
  </acceptance_criteria>
  <done>RCEvalAgentInvoker deleted. GA-only invoker in place. EVAL-INVENTORY deletion trigger discharged.</done>
</task>

<task type="auto">
  <name>Task 4: Add FORCED_TOOL_FAILURE KQL template</name>
  <files>backend/src/second_brain/observability/kql_templates.py</files>
  <read_first>
    - backend/src/second_brain/observability/kql_templates.py (template file structure)
    - backend/src/second_brain/streaming/adapter.py (post-24-16 — to see how forced_tool_failure is logged; verify there's a logger.error with structured extra= containing sub_code or sse_sub_code)
    - .planning/phases/23-foundry-ga-prep/SPAN-NAME-MAPPING.md (forced_tool_failure tracking guidance)
  </read_first>
  <action>
    1. Open `backend/src/second_brain/observability/kql_templates.py`.
    2. Determine how the streaming adapter (24-16) logs forced_tool_failure events. Likely:
       ```python
       logger.error("Classifier forced_tool_failure: %s", exc, extra={"sub_code": "forced_tool_failure", ...})
       ```
       App Insights captures these as AppExceptions or AppTraces with `Properties.sub_code == "forced_tool_failure"`.
    3. Add a new template constant near the existing failure-tracking templates:
       ```python
       FORCED_TOOL_FAILURE_COUNT = """\
       AppTraces
       | where TimeGenerated > ago({lookback}h)
       | where SeverityLevel >= 3
       | where tostring(Properties.sub_code) == "forced_tool_failure"
       | summarize count = count() by bin(TimeGenerated, 1h)
       | order by TimeGenerated asc
       """
       ```
       (Adjust to AppExceptions if the adapter logs as exception. Read the adapter to confirm.)
    4. Verify the constant is parseable: `cd backend && uv run python -c "from second_brain.observability.kql_templates import FORCED_TOOL_FAILURE_COUNT; assert 'forced_tool_failure' in FORCED_TOOL_FAILURE_COUNT"`.
    5. Do NOT add a corresponding `query_*` function in queries.py for now — the template is the gate; the query function can be added post-deploy when monitoring needs are clearer (or as a follow-up plan).
  </action>
  <verify>
    <automated>grep -q "FORCED_TOOL_FAILURE_COUNT" backend/src/second_brain/observability/kql_templates.py && grep -q "forced_tool_failure" backend/src/second_brain/observability/kql_templates.py && cd backend && uv run python -c "from second_brain.observability.kql_templates import FORCED_TOOL_FAILURE_COUNT"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "FORCED_TOOL_FAILURE_COUNT" backend/src/second_brain/observability/kql_templates.py` returns 0
    - `grep -q "tostring(Properties.sub_code) == \"forced_tool_failure\"" backend/src/second_brain/observability/kql_templates.py` returns 0
    - `cd backend && uv run python -c "from second_brain.observability.kql_templates import FORCED_TOOL_FAILURE_COUNT"` exits 0
  </acceptance_criteria>
  <done>KQL template for tracking forced_tool_failure events added. Post-deploy 7-day gate (per CONTEXT) can use this query.</done>
</task>

</tasks>

<verification>
- agents/middleware.py deleted; no replacement package at the same path
- P1-3 red test updated post-deletion (legacy-importable sub-test retired; new GA path + no-shadowing-file sub-tests retained)
- RCEvalAgentInvoker class deleted; eval flows GA-only
- FORCED_TOOL_FAILURE_COUNT KQL template added
- All RC patterns cleared from backend/src/second_brain/
- Optional P1-3 polish (rename agent_middleware/ → middleware/) DEFERRED — keeping the agent_middleware/ name. Acceptable as a permanent location.
</verification>

<success_criteria>
Plan 24-19 (warmup migration) — and then 24-19.5 (RC dep removal), 24-20 (cumulative audit + pre-deploy gates), 24-21 (config orphan cleanup), 24-22 (deploy), 24-23 (UAT), 24-24 (post-UAT cleanup) — unblocked. The cumulative diff for 24-09..24-18 should produce zero ❌ at the auditor.
</success_criteria>

<output>
After completion, create `.planning/phases/24-foundry-ga-migration/24-18-SUMMARY.md` documenting:
- F-17 cleared (middleware.py deleted)
- P1-3 red test updated (legacy-importable sub-test retired)
- RCEvalAgentInvoker deleted (EVAL-INVENTORY trigger fired)
- FORCED_TOOL_FAILURE_COUNT KQL template added
- Local main fully GA-clean except for warmup.py (24-19) and config orphan agent_id fields (24-21)
- Optional rename agent_middleware/ → middleware/ deferred
</output>
</content>
</invoke>
