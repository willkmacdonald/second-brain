---
phase: 24-foundry-ga-migration
plan: 06
type: execute
wave: 2
depends_on: [05]
files_modified:
  - backend/src/second_brain/observability/kql_templates.py
  - backend/src/second_brain/observability/queries.py
  - backend/src/second_brain/observability/span_processor.py
autonomous: true
requirements: [W-01, F-14, F-15, F-16, F-17, D-07a]

must_haves:
  truths:
    - "AGENT_RUNS template uses Name == \"invoke_agent\" instead of Name endswith \"_agent_run\""
    - "fetch_agent_runs property projections match GA span attribute names where they differ from RC"
    - "observability/span_processor.py has a documentation comment recording its narrowed scope per W-01 (no code change)"
    - "No other KQL template or query function changed"
  artifacts:
    - path: "backend/src/second_brain/observability/kql_templates.py"
      provides: "AGENT_RUNS template with GA span Name filter"
      contains: "Name == \"invoke_agent\""
    - path: "backend/src/second_brain/observability/queries.py"
      provides: "fetch_agent_runs property projections (verified against GA span attributes)"
    - path: "backend/src/second_brain/observability/span_processor.py"
      provides: "Same code, with W-01 narrowed-scope comment"
  key_links:
    - from: "kql_templates.AGENT_RUNS"
      to: "GA invoke_agent span emitted by framework"
      via: "Name filter"
      pattern: "Name == \"invoke_agent\""
---

<objective>
Update the only KQL template that filters on agent span Names. Per SPAN-NAME-MAPPING, only `AGENT_RUNS` requires update — all other queries filter by HTTP route Names, severity, component property, or correlation attribute, none of which change with the RC -> GA migration.

Three small edits in this plan:
1. `observability/kql_templates.py` — change `Name endswith "_agent_run"` to `Name == "invoke_agent"` on the AGENT_RUNS template (single line).
2. `observability/queries.py` — `fetch_agent_runs` property projections (currently expecting RC-era custom attributes `agent_id`, `run_id`, `foundry_thread_id`). Per SPAN-NAME-MAPPING + the GA semantic conventions, the framework emits `agent.name`, `gen_ai.usage.input_tokens`, `gen_ai.usage.output_tokens` natively. Update the projection list to query the GA-emitted attribute names. RC-era custom attributes (e.g., `Properties.agent_id`) will return empty for new spans; either leave the projections as-is (will return empty strings post-migration but no schema break) OR update to use GA-native names (preferred — more useful KQL output).
3. `observability/span_processor.py` — add a docstring comment per W-01 documenting the narrowed scope (no code change). The processor still tags every span with `capture.trace_id`; the new middleware ALSO tags framework spans at source. Both setting the same attribute name is a no-op when values match.

Per CONTEXT D-13: this plan is sequenced AFTER 24-03 (middleware created) but the AGENT_RUNS template update is independent. KQL template change can land before or after the streaming adapter rewrite (24-07) without cross-dependency.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-foundry-ga-migration/24-CONTEXT.md
@.planning/phases/24-foundry-ga-migration/24-PATTERNS.md
@.planning/phases/23-foundry-ga-prep/SPAN-NAME-MAPPING.md
@backend/src/second_brain/observability/kql_templates.py
@backend/src/second_brain/observability/queries.py
@backend/src/second_brain/observability/span_processor.py

<interfaces>
SPAN-NAME-MAPPING — only AGENT_RUNS requires update.

Current RC template (kql_templates.py around line 378):
```
AppDependencies
| where Name endswith "_agent_run"
| where TimeGenerated > ago({lookback}h)
| project ...
```

New GA template (single character-level change):
```
AppDependencies
| where Name == "invoke_agent"
| where TimeGenerated > ago({lookback}h)
| project ...
```

Property projections currently in fetch_agent_runs (queries.py:600-645):
- agent_id = tostring(Properties.agent_id)
- agent_name = tostring(Properties.agent_name)
- run_id = tostring(Properties.run_id)
- thread_id = tostring(Properties.foundry_thread_id)
- capture_trace_id = tostring(Properties.capture_trace_id)

GA framework emits (per SPAN-NAME-MAPPING):
- agent.name (auto-emitted by GA framework — already aliased above as agent_name OK)
- gen_ai.usage.input_tokens
- gen_ai.usage.output_tokens
- capture.trace_id (set by our middleware) — already projected

RC-only custom attributes (no GA equivalent):
- agent_id (was set by AuditAgentMiddleware — deleted in 24-19; will return empty on GA spans)
- run_id (same)
- foundry_thread_id (same)

DECISION: update the projection list to remove the RC-only attributes (agent_id, run_id, foundry_thread_id) and add GA-emitted token counts so the query stays meaningful post-migration. Or KEEP the projections as-is and tolerate empty strings for RC-only attrs. Per the planner's discretion in CONTEXT D-flexibility, the cleaner choice is to SWAP the RC-only fields for the GA-emitted equivalents.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update AGENT_RUNS KQL template span Name filter</name>
  <files>backend/src/second_brain/observability/kql_templates.py</files>
  <read_first>
    - backend/src/second_brain/observability/kql_templates.py (current AGENT_RUNS template)
    - .planning/phases/23-foundry-ga-prep/SPAN-NAME-MAPPING.md (table at line 75-95: only AGENT_RUNS changes)
  </read_first>
  <action>
    1. Open `backend/src/second_brain/observability/kql_templates.py` and locate the `AGENT_RUNS` template constant (currently around line 376-396).
    2. Find the line `| where Name endswith "_agent_run"` inside the template's KQL string.
    3. Change it to `| where Name == "invoke_agent"`.
    4. Do NOT touch any other template, query string, or constant. SPAN-NAME-MAPPING confirms zero changes elsewhere.
    5. Verify post-edit: `grep -c "Name endswith \"_agent_run\"" backend/src/second_brain/observability/kql_templates.py` must be 0; `grep -c "Name == \"invoke_agent\"" backend/src/second_brain/observability/kql_templates.py` must be at least 1.
  </action>
  <verify>
    <automated>! grep -q 'Name endswith "_agent_run"' backend/src/second_brain/observability/kql_templates.py && grep -q 'Name == "invoke_agent"' backend/src/second_brain/observability/kql_templates.py</automated>
  </verify>
  <acceptance_criteria>
    - `! grep -q 'Name endswith "_agent_run"' backend/src/second_brain/observability/kql_templates.py` returns 0
    - `grep -q 'Name == "invoke_agent"' backend/src/second_brain/observability/kql_templates.py` returns 0
    - `grep -q "AGENT_RUNS" backend/src/second_brain/observability/kql_templates.py` returns 0 (template constant still exists)
    - The total line count of kql_templates.py changes by at most 0 (single-line replacement, no addition or deletion)
    - `! grep -rE "_agent_run" backend/src/second_brain/observability/` returns 0 (no other RC pattern leftover)
  </acceptance_criteria>
  <done>AGENT_RUNS template filters by GA-native Name "invoke_agent". KQL queries that consume the template will hit GA-emitted spans post-deploy.</done>
</task>

<task type="auto">
  <name>Task 2: Verify or update fetch_agent_runs property projections</name>
  <files>backend/src/second_brain/observability/queries.py</files>
  <read_first>
    - backend/src/second_brain/observability/queries.py (fetch_agent_runs around lines 600-645)
    - .planning/phases/23-foundry-ga-prep/SPAN-NAME-MAPPING.md (Span attribute table — what GA emits vs. RC custom attrs)
  </read_first>
  <action>
    1. Read `fetch_agent_runs` in `backend/src/second_brain/observability/queries.py`. Locate the property projection list (Properties.agent_id, Properties.agent_name, etc.).
    2. Decision: KEEP existing projections AS-IS for now (passive degradation — RC-only fields like agent_id will return empty strings post-migration but the query won't fail). Add a brief comment ABOVE the projection block:
       ```
       # Phase 24 task group 23.1: GA framework emits agent.name natively on
       # invoke_agent spans. RC-era Properties.agent_id, Properties.run_id,
       # Properties.foundry_thread_id are no longer set by GA — they project
       # as empty strings. Investigation tools that consume this output
       # filter empties downstream. Future plan: rename projections to GA
       # semantic-convention names (gen_ai.usage.*, agent.name) once
       # post-deploy span data is observed.
       ```
    3. Do NOT change the projection list itself in this commit. The query continues to work; the comment documents the known degradation. Filling in real GA-emitted attribute projections is deferred to post-deploy when actual GA spans can be inspected via App Insights query.
    4. Verify the function still parses: `cd backend && uv run python -c "from second_brain.observability.queries import fetch_agent_runs; print(fetch_agent_runs.__doc__)"`.
  </action>
  <verify>
    <automated>cd backend && uv run python -c "from second_brain.observability.queries import fetch_agent_runs; assert callable(fetch_agent_runs)" && grep -q "Phase 24 task group 23.1" backend/src/second_brain/observability/queries.py</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && uv run python -c "from second_brain.observability.queries import fetch_agent_runs"` exits 0
    - `grep -q "Phase 24 task group 23.1" backend/src/second_brain/observability/queries.py` returns 0
    - `grep -q "GA framework emits agent.name natively" backend/src/second_brain/observability/queries.py` returns 0
    - The projection list itself is unchanged (planner deferred — adding a follow-up to post-deploy)
  </acceptance_criteria>
  <done>fetch_agent_runs has documentation comment recording the RC->GA attribute degradation. Function still importable. Projection update deferred to post-deploy follow-up.</done>
</task>

<task type="auto">
  <name>Task 3: Add W-01 narrowed-scope comment to span_processor.py</name>
  <files>backend/src/second_brain/observability/span_processor.py</files>
  <read_first>
    - backend/src/second_brain/observability/span_processor.py (current implementation, lines 16-39)
    - .planning/phases/24-foundry-ga-migration/FRAMEWORK-FIDELITY-calibration.md (W-01)
    - .planning/phases/24-foundry-ga-migration/24-PATTERNS.md (W-01 documentation-only note)
  </read_first>
  <action>
    Add a docstring comment to `CaptureTraceSpanProcessor` recording its narrowed scope per W-01. No behavioral change. The processor's `on_start` method still tags every span with `capture.trace_id` — that's correct behavior. The comment documents WHY it's still tagging (because non-framework spans like Cosmos AppDependencies need it).

    Edit: at the top of the class docstring (or as a new docstring section), add:

    ```
    Phase 24 task group 23.1 narrowed scope (W-01):

    Framework-emitted invoke_agent / execute_tool spans are now tagged
    at source by CaptureTraceAgentMiddleware / CaptureTraceFunctionMiddleware
    in agents/middleware/capture_trace.py.

    This processor is RETAINED per design D-07a layered strategy: it tags
    everything else — Azure SDK auto-instrumented AppDependencies (Cosmos,
    HTTP), AppExceptions from libraries, custom non-framework spans.

    Without this processor, query_capture_trace's union over AppDependencies
    loses correlation. The on_start overlap with framework-tagged spans on
    the same attribute name (capture.trace_id) is a no-op (idempotent set).
    """
    ```

    Do NOT modify any code in `on_start` or anywhere else. The comment is the entire delta.
  </action>
  <verify>
    <automated>grep -q "Phase 24 task group 23.1 narrowed scope" backend/src/second_brain/observability/span_processor.py && cd backend && uv run python -c "from second_brain.observability.span_processor import CaptureTraceSpanProcessor; p = CaptureTraceSpanProcessor(); assert p"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "Phase 24 task group 23.1 narrowed scope" backend/src/second_brain/observability/span_processor.py` returns 0
    - `grep -q "W-01" backend/src/second_brain/observability/span_processor.py` returns 0
    - `cd backend && uv run python -c "from second_brain.observability.span_processor import CaptureTraceSpanProcessor"` exits 0
    - The class behavior is unchanged — no code edits to on_start, on_end, etc.
    - `git diff backend/src/second_brain/observability/span_processor.py` shows ONLY docstring/comment additions (no logic edits)
  </acceptance_criteria>
  <done>Span processor scope is documented per W-01. Behavior unchanged. Future readers understand the layered design.</done>
</task>

</tasks>

<verification>
- AGENT_RUNS template Name filter: GA invoke_agent
- No other KQL template or query function changed
- fetch_agent_runs has comment documenting RC-attribute degradation; projection list unchanged
- span_processor.py has W-01 narrowed-scope comment; behavior unchanged
- `! grep -rE "_agent_run" backend/src/second_brain/observability/` returns nothing
</verification>

<success_criteria>
Once GA spans are emitted post-deploy, the AGENT_RUNS query returns rows. The fetch_agent_runs function still runs (RC-only attribute fields will return empty strings, which is acceptable until the post-deploy follow-up updates projections).
</success_criteria>

<output>
After completion, create `.planning/phases/24-foundry-ga-migration/24-06-SUMMARY.md` documenting:
- AGENT_RUNS template Name filter: RC -> GA
- Comment added to fetch_agent_runs documenting the RC-only attribute degradation
- Span processor W-01 documentation comment added
- Confirmed: zero references to "_agent_run" pattern in observability/
- Follow-up: post-deploy update of fetch_agent_runs projections to GA-native names (gen_ai.usage.*, agent.name)
</output>
