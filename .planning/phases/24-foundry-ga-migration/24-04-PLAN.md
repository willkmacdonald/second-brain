---
phase: 24-foundry-ga-migration
plan: 04
type: execute
wave: 2
depends_on: [03]
files_modified:
  - backend/src/second_brain/agents/investigation.py
  - backend/src/second_brain/main.py
autonomous: true
requirements: [F-01, F-12, F-19, D-05]

must_haves:
  truths:
    - "agents/investigation.py exposes build_investigation_agent(chat_client, tools, middleware) -> Agent"
    - "main.py lifespan constructs ONE FoundryChatClient with credential=ManagedIdentityCredential, project_endpoint, model"
    - "main.py lifespan calls build_investigation_agent and stores Agent on app.state.investigation_agent"
    - "Investigation agent is constructed with instructions=load_instructions('investigation') and middleware=[CaptureTraceAgentMiddleware(), CaptureTraceFunctionMiddleware()]"
    - "RC AzureAIAgentClient is removed from agents/investigation.py and from the Investigation lifespan slice in main.py"
  artifacts:
    - path: "backend/src/second_brain/agents/investigation.py"
      provides: "build_investigation_agent factory + load_instructions helper"
      contains: "FoundryChatClient, Agent, load_instructions"
    - path: "backend/src/second_brain/main.py"
      provides: "Lifespan-singleton FoundryChatClient + Agent for Investigation"
      contains: "FoundryChatClient, build_investigation_agent"
  key_links:
    - from: "main.py lifespan"
      to: "agents/investigation.build_investigation_agent"
      via: "factory call"
      pattern: "build_investigation_agent"
    - from: "main.py lifespan"
      to: "agent_framework_foundry.FoundryChatClient"
      via: "client construction"
      pattern: "FoundryChatClient"
---

<objective>
Replace the Investigation agent's RC implementation with a GA Agent constructed via FoundryChatClient. This is the first agent migration; patterns set here transfer to Admin (24-09) and Classifier (24-13).

Two file scope:
1. `backend/src/second_brain/agents/investigation.py` — replace `ensure_investigation_agent(...)` body with `build_investigation_agent(chat_client, tools, middleware)` returning a constructed Agent. Drop the portal-shell creation path (F-19).
2. `backend/src/second_brain/main.py` — replace the existing Investigation lifespan slice (lines 484-510 Foundry probe + 685-723 Investigation client construction) with a single shared FoundryChatClient + a call to `build_investigation_agent`. Wire the new middleware classes from 24-03. Keep the existing non-fatal try/except around the Investigation block.

Per CONFIG-DELTAS Probe 5: `FoundryChatClient(project_endpoint=..., model=..., credential=ManagedIdentityCredential())`. Production env uses managed identity, NOT AzureCliCredential.

Per CONTEXT D-05/D-06: tools list is bound methods. The existing InvestigationTools class instance + Cosmos DI shape is unchanged.

The Admin and Classifier lifespan slices in main.py still reference AzureAIAgentClient after this commit. Plan 24-09 and 24-13 handle those slices. Local main is intentionally not buildable for those agents until their respective plans land — the push guard prevents push.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-foundry-ga-migration/24-CONTEXT.md
@.planning/phases/24-foundry-ga-migration/24-PATTERNS.md
@.planning/phases/24-foundry-ga-migration/FRAMEWORK-FIDELITY-calibration.md
@.planning/phases/23-foundry-ga-prep/FOUNDRY-PROBE-FINDINGS.md
@.planning/phases/23-foundry-ga-prep/CONFIG-DELTAS.md
@backend/src/second_brain/agents/investigation.py
@backend/src/second_brain/main.py
@backend/src/second_brain/agents/middleware/capture_trace.py
@backend/tests/fixtures/foundry-probe/auth_probe.json

<interfaces>
GA Agent + FoundryChatClient construction (per probe 5 + CONFIG-DELTAS):

```
from agent_framework import Agent
from agent_framework_foundry import FoundryChatClient
from azure.identity.aio import ManagedIdentityCredential

chat_client = FoundryChatClient(
    project_endpoint=settings.azure_ai_project_endpoint,
    model=settings.foundry_model,
    credential=ManagedIdentityCredential(),
)

investigation_agent = Agent(
    client=chat_client,
    instructions=load_instructions("investigation"),
    tools=[investigation_tools.trace_lifecycle, ...],
    middleware=[
        CaptureTraceAgentMiddleware(),
        CaptureTraceFunctionMiddleware(),
    ],
)
```

InvestigationTools tool method names (preserve all 9 from existing main.py:702-723):
- trace_lifecycle
- recent_errors
- system_health
- usage_patterns
- query_feedback_signals
- promote_to_golden_dataset
- run_classifier_eval
- run_admin_eval
- get_eval_results

NOTE: tools are still decorated with @tool(approval_mode='never_require') at this point. They get fixed in 24-06 (decorator strip). The GA Agent(tools=[...]) accepts the bound methods regardless because the decorator wraps without unwrapping; the strip in 24-06 is for fidelity-auditor F-08 cleanliness, not functional necessity here.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite agents/investigation.py with build_investigation_agent + load_instructions</name>
  <files>backend/src/second_brain/agents/investigation.py</files>
  <read_first>
    - backend/src/second_brain/agents/investigation.py (current ensure_investigation_agent)
    - backend/src/second_brain/agents/middleware/capture_trace.py (created in 24-03)
    - .planning/phases/24-foundry-ga-migration/24-PATTERNS.md (Group B — agents/investigation.py pattern)
    - backend/src/second_brain/agents/instructions/investigation.md (created in 24-03)
  </read_first>
  <behavior>
    - Test 1: load_instructions('investigation') returns the contents of agents/instructions/investigation.md as a string with length over 1000 chars.
    - Test 2: load_instructions('nonexistent') raises FileNotFoundError.
    - Test 3: build_investigation_agent(client_stub, [], []) returns an Agent instance.
    - Test 4: build_investigation_agent accepts a non-empty tools list and a non-empty middleware list.
  </behavior>
  <action>
    Replace the entire current body of `backend/src/second_brain/agents/investigation.py` with the following module text (preserving the "from __future__ import annotations" header):

    File body to write:

    ```
    """Investigation agent factory.

    GA pattern (Phase 24 task group 23.1): instructions live in repo (D-02),
    Agent constructed at lifespan startup with bound tool methods and capture-trace
    middleware. Replaces the RC ensure_investigation_agent portal-shell creation
    pattern (F-19).
    """

    from __future__ import annotations

    import logging
    from collections.abc import Sequence
    from pathlib import Path
    from typing import Any

    from agent_framework import Agent
    from agent_framework_foundry import FoundryChatClient

    logger = logging.getLogger(__name__)

    INSTRUCTIONS_DIR = Path(__file__).parent / "instructions"


    def load_instructions(name: str) -> str:
        """Read agents/instructions/{name}.md and return its contents.

        Raises FileNotFoundError if the file is missing — fail fast at startup.
        """
        return (INSTRUCTIONS_DIR / f"{name}.md").read_text(encoding="utf-8")


    def build_investigation_agent(
        chat_client: FoundryChatClient,
        tools: Sequence[Any],
        middleware: Sequence[Any],
    ) -> Agent:
        """Construct the Investigation Agent."""
        instructions = load_instructions("investigation")
        return Agent(
            client=chat_client,
            instructions=instructions,
            tools=list(tools),
            middleware=list(middleware),
        )
    ```

    Delete the old `ensure_investigation_agent` function entirely. No portal-shell creation in this file anymore.

    Verify imports: `cd backend && uv run python -c "from second_brain.agents.investigation import build_investigation_agent, load_instructions; print('OK')"`
  </action>
  <verify>
    <automated>cd backend && uv run python -c "from second_brain.agents.investigation import build_investigation_agent, load_instructions; assert callable(build_investigation_agent); content = load_instructions('investigation'); assert len(content) > 1000" && ! grep -q "ensure_investigation_agent" backend/src/second_brain/agents/investigation.py && ! grep -q "AzureAIAgentClient" backend/src/second_brain/agents/investigation.py && ! grep -q "create_agent" backend/src/second_brain/agents/investigation.py</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && uv run python -c "from second_brain.agents.investigation import build_investigation_agent, load_instructions"` exits 0
    - `! grep -q "ensure_investigation_agent" backend/src/second_brain/agents/investigation.py` returns 0
    - `! grep -q "AzureAIAgentClient" backend/src/second_brain/agents/investigation.py` returns 0
    - `! grep -q "create_agent" backend/src/second_brain/agents/investigation.py` returns 0
    - `! grep -q "SET INSTRUCTIONS IN AI FOUNDRY PORTAL" backend/src/second_brain/agents/investigation.py` returns 0
    - `grep -q "from agent_framework_foundry import FoundryChatClient" backend/src/second_brain/agents/investigation.py` returns 0
    - `grep -q "def build_investigation_agent" backend/src/second_brain/agents/investigation.py` returns 0
    - `grep -q "def load_instructions" backend/src/second_brain/agents/investigation.py` returns 0
  </acceptance_criteria>
  <done>agents/investigation.py is GA-shaped. Provides build_investigation_agent factory + load_instructions helper. RC portal-shell pattern is gone.</done>
</task>

<task type="auto">
  <name>Task 2: Wire FoundryChatClient + Investigation Agent in main.py lifespan</name>
  <files>backend/src/second_brain/main.py</files>
  <read_first>
    - backend/src/second_brain/main.py (current lifespan, focus lines 484-510 + 685-723)
    - backend/src/second_brain/agents/investigation.py (factory created in Task 1)
    - backend/src/second_brain/agents/middleware/capture_trace.py (created in 24-03)
    - .planning/phases/23-foundry-ga-prep/CONFIG-DELTAS.md (FoundryChatClient construction shape)
    - .planning/phases/24-foundry-ga-migration/24-PATTERNS.md (Group B — main.py Investigation lifespan slice)
    - backend/tests/fixtures/foundry-probe/auth_probe.json (credential class shape)
  </read_first>
  <action>
    Edit `backend/src/second_brain/main.py` in three parts. The Admin and Classifier slices STAY intact (they still use AzureAIAgentClient — fixed in 24-09 / 24-13).

    **Part A — Imports (top of file):**
    Find the existing `from agent_framework.azure import AzureAIAgentClient` and KEEP it (other slices still need it). ADD these new imports beside it:
    ```
    from agent_framework_foundry import FoundryChatClient
    from azure.identity.aio import ManagedIdentityCredential as AsyncManagedIdentityCredential
    from second_brain.agents.investigation import build_investigation_agent
    from second_brain.agents.middleware.capture_trace import (
        CaptureTraceAgentMiddleware,
        CaptureTraceFunctionMiddleware,
    )
    ```

    Note the existing module already imports `AzureAIAgentClient`, `AuditAgentMiddleware`, `ToolTimingMiddleware`, etc. KEEP those for now — they're used by Admin and Classifier slices that haven't migrated yet.

    **Part B — Replace the Foundry probe block (currently lines 484-510):**
    Today's code wraps `AzureAIAgentClient(...)` + `agents_client.list_agents(limit=1)` in try/except as a fail-fast probe. Replace this entire block with:
    ```
    # Phase 24 task group 23.1: GA FoundryChatClient is the single chat client
    # shared across Investigation, Admin, and Classifier agents.
    # Probe 5 (auth_probe) confirmed credential class shape.
    try:
        chat_client = FoundryChatClient(
            project_endpoint=settings.azure_ai_project_endpoint,
            model=settings.foundry_model,
            credential=AsyncManagedIdentityCredential(),
        )
        app.state.foundry_chat_client = chat_client
        logger.info(
            "FoundryChatClient initialized: endpoint=%s model=%s",
            settings.azure_ai_project_endpoint,
            settings.foundry_model,
        )
    except Exception:
        logger.error("FATAL: Could not initialize FoundryChatClient", exc_info=True)
        raise
    ```

    Connectivity probing via `agents_client.list_agents` is removed — the agent runs themselves are the smoke test, exercised by warmup.py (24-15) and the lifespan's existing warmup loop start.

    **Part C — Replace the Investigation construction block (currently around lines 685-723):**
    Today's code constructs `investigation_client = AzureAIAgentClient(agent_id=investigation_agent_id, ...)` plus middleware list, then assembles `app.state.investigation_tools = [...]` (9 tool method references).

    KEEP the surrounding non-fatal try/except (Investigation is non-fatal at startup).

    KEEP the construction of `investigation_tools` (the InvestigationTools class instance) and the rate limiter at `app.state.investigation_rate_limiter` — those are not part of this rewrite.

    REPLACE the `investigation_client = AzureAIAgentClient(...)` block + the `app.state.investigation_tools = [...]` list with:
    ```
    investigation_agent = build_investigation_agent(
        chat_client=chat_client,
        tools=[
            investigation_tools.trace_lifecycle,
            investigation_tools.recent_errors,
            investigation_tools.system_health,
            investigation_tools.usage_patterns,
            investigation_tools.query_feedback_signals,
            investigation_tools.promote_to_golden_dataset,
            investigation_tools.run_classifier_eval,
            investigation_tools.run_admin_eval,
            investigation_tools.get_eval_results,
        ],
        middleware=[
            CaptureTraceAgentMiddleware(),
            CaptureTraceFunctionMiddleware(),
        ],
    )
    app.state.investigation_agent = investigation_agent
    ```

    DELETE: any reference to `app.state.investigation_client`. DELETE: the `ensure_investigation_agent(...)` call (function no longer exists). DELETE: any read of `settings.azure_ai_investigation_agent_id` (it's still in config but no longer used by Investigation; Admin/Classifier still read theirs).

    Do NOT touch the `_make_investigation_client(...)` factory at lines around 779-823 IF it exists for warmup self-heal — that's plan 24-15's scope (warmup migration). Leave the factory body alone in this commit; main.py just won't reference it for Investigation anymore.

    **Auto-format trap warning (per MEMORY):** The `AsyncManagedIdentityCredential` import will be auto-stripped by ruff if it's added in a separate edit before its use. Make Part A and Part B in a single coherent Edit pass — the import and its usage should be in the same diff.
  </action>
  <verify>
    <automated>grep -q "from agent_framework_foundry import FoundryChatClient" backend/src/second_brain/main.py && grep -q "build_investigation_agent" backend/src/second_brain/main.py && grep -q "CaptureTraceAgentMiddleware" backend/src/second_brain/main.py && grep -q "CaptureTraceFunctionMiddleware" backend/src/second_brain/main.py && grep -q "app.state.foundry_chat_client" backend/src/second_brain/main.py && grep -q "app.state.investigation_agent" backend/src/second_brain/main.py && ! grep -q "ensure_investigation_agent" backend/src/second_brain/main.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "from agent_framework_foundry import FoundryChatClient" backend/src/second_brain/main.py` returns 0
    - `grep -q "build_investigation_agent" backend/src/second_brain/main.py` returns 0
    - `grep -q "CaptureTraceAgentMiddleware" backend/src/second_brain/main.py` returns 0
    - `grep -q "CaptureTraceFunctionMiddleware" backend/src/second_brain/main.py` returns 0
    - `grep -q "app.state.foundry_chat_client = chat_client" backend/src/second_brain/main.py` returns 0
    - `grep -q "app.state.investigation_agent = investigation_agent" backend/src/second_brain/main.py` returns 0
    - `! grep -q "ensure_investigation_agent" backend/src/second_brain/main.py` returns 0 (function no longer exists)
    - `! grep -q "investigation_client = AzureAIAgentClient" backend/src/second_brain/main.py` returns 0
    - `grep -q "AzureAIAgentClient" backend/src/second_brain/main.py` returns 0 (STILL present — Admin/Classifier slices haven't migrated yet)
    - `grep -q "FOUNDRY_MODEL\\|foundry_model" backend/src/second_brain/main.py` returns 0 (settings.foundry_model is read)
    - `grep -q "AsyncManagedIdentityCredential\\|ManagedIdentityCredential" backend/src/second_brain/main.py` returns 0
  </acceptance_criteria>
  <done>main.py constructs a single FoundryChatClient and uses build_investigation_agent to create the Investigation Agent. The Admin and Classifier slices still reference AzureAIAgentClient — that's expected; they migrate in 24-09 and 24-13.</done>
</task>

</tasks>

<verification>
- agents/investigation.py exposes build_investigation_agent and load_instructions; ensure_investigation_agent is gone
- main.py imports FoundryChatClient and the new middleware classes
- main.py lifespan creates a single shared chat_client at app.state.foundry_chat_client
- main.py creates an investigation_agent via build_investigation_agent and stores at app.state.investigation_agent
- AzureAIAgentClient still imported and referenced — used by Admin/Classifier slices not yet migrated (NOT a defect at this point)
</verification>

<success_criteria>
Plan 24-05 (streaming/investigation_adapter.py rewrite) can read app.state.investigation_agent and call agent.run(stream=True) on it. The capture-trace middleware tags framework spans at source — the `investigate` custom span deletion in 24-05 is safe.
</success_criteria>

<output>
After completion, create `.planning/phases/24-foundry-ga-migration/24-04-SUMMARY.md` documenting:
- agents/investigation.py: ensure_investigation_agent gone; build_investigation_agent + load_instructions present
- main.py imports added (FoundryChatClient, ManagedIdentityCredential, build_investigation_agent, CaptureTrace*)
- Foundry probe block replaced with FoundryChatClient construction
- Investigation lifespan slice constructs Agent with 9 tools + 2 middlewares
- Confirmed: AzureAIAgentClient still in main.py (Admin/Classifier slices); local main is mid-transition
</output>
