---
phase: 24-foundry-ga-migration
plan: 10
type: execute
wave: 3
depends_on: [09]
files_modified:
  - backend/src/second_brain/tools/admin.py
  - backend/src/second_brain/tools/recipe.py
autonomous: true
requirements: [F-08, D-05, D-06]

must_haves:
  truths:
    - "All 6 @tool(approval_mode='never_require') decorators removed from tools/admin.py"
    - "1 @tool(approval_mode='never_require') decorator removed from tools/recipe.py"
    - "Annotated[..., Field(description=...)] parameter shape preserved on every tool method"
    - "AdminTools and RecipeTools class shapes unchanged"
  artifacts:
    - path: "backend/src/second_brain/tools/admin.py"
      provides: "6 GA-shaped tool methods (decorator-free)"
      contains: "class AdminTools"
    - path: "backend/src/second_brain/tools/recipe.py"
      provides: "fetch_recipe_url GA-shaped (decorator-free)"
      contains: "class RecipeTools"
  key_links:
    - from: "tools/admin.py"
      to: "main.py admin_agent tools list"
      via: "bound method registration"
      pattern: "admin_tools\\."
    - from: "tools/recipe.py"
      to: "main.py admin_agent tools list"
      via: "bound method registration"
      pattern: "recipe_tools\\."
---

<objective>
Mechanical decorator strip across 7 tool methods (6 in AdminTools, 1 in RecipeTools). Same pattern as 24-05 (InvestigationTools). Class shapes, __init__ signatures, Annotated params, and docstrings all preserved.

Per CONTEXT D-05/D-06: tools registered as bound methods on the existing classes. The decorator was RC-only.

Calibration F-08 cites these specific line numbers (may have shifted): tools/admin.py:121, 191, 235, 249, 370, 537 (6 methods); tools/recipe.py:102 (1 method).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-foundry-ga-migration/24-CONTEXT.md
@.planning/phases/24-foundry-ga-migration/24-PATTERNS.md
@.planning/phases/24-foundry-ga-migration/FRAMEWORK-FIDELITY-calibration.md
@backend/src/second_brain/tools/admin.py
@backend/src/second_brain/tools/recipe.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Strip @tool(approval_mode=...) from AdminTools (6 methods)</name>
  <files>backend/src/second_brain/tools/admin.py</files>
  <read_first>
    - backend/src/second_brain/tools/admin.py
    - .planning/phases/24-foundry-ga-migration/FRAMEWORK-FIDELITY-calibration.md (F-08)
  </read_first>
  <action>
    1. Grep for `grep -n "@tool(approval_mode=" backend/src/second_brain/tools/admin.py` and record the 6 line numbers.
    2. Delete each `@tool(approval_mode="never_require")` line, leaving the `async def {tool_name}(...)` signature intact.
    3. Keep all `Annotated[..., Field(description=...)]` parameter shapes, docstrings, return types, and class structure unchanged.
    4. If `from agent_framework import tool` becomes unused (no other `@tool` references in the file), remove it in the SAME edit per MEMORY auto-format trap.
    5. Run admin tool unit tests: `cd backend && uv run pytest tests/test_admin_tools.py -x` (or whichever test file covers admin tools).
  </action>
  <verify>
    <automated>! grep -q "@tool(approval_mode=" backend/src/second_brain/tools/admin.py && grep -q "class AdminTools" backend/src/second_brain/tools/admin.py && cd backend && uv run python -c "from second_brain.tools.admin import AdminTools; assert AdminTools.__init__"</automated>
  </verify>
  <acceptance_criteria>
    - `! grep -q "@tool(approval_mode=" backend/src/second_brain/tools/admin.py` returns 0
    - `grep -q "class AdminTools" backend/src/second_brain/tools/admin.py` returns 0
    - `grep -q "Annotated\\[" backend/src/second_brain/tools/admin.py` returns 0
    - `grep -q "Field(description=" backend/src/second_brain/tools/admin.py` returns 0
    - `cd backend && uv run python -c "from second_brain.tools.admin import AdminTools; t = AdminTools.__init__"` exits 0
    - All existing admin tool unit tests still pass
  </acceptance_criteria>
  <done>6 decorators stripped from AdminTools. Class methods are plain async coroutines.</done>
</task>

<task type="auto">
  <name>Task 2: Strip @tool(approval_mode=...) from RecipeTools.fetch_recipe_url</name>
  <files>backend/src/second_brain/tools/recipe.py</files>
  <read_first>
    - backend/src/second_brain/tools/recipe.py
    - .planning/phases/24-foundry-ga-migration/FRAMEWORK-FIDELITY-calibration.md (F-08)
  </read_first>
  <action>
    1. Grep `grep -n "@tool(approval_mode=" backend/src/second_brain/tools/recipe.py` — should return 1 line (around line 102).
    2. Delete that decorator line.
    3. Keep everything else unchanged.
    4. Remove unused `tool` import if applicable (same edit, per MEMORY).
  </action>
  <verify>
    <automated>! grep -q "@tool(approval_mode=" backend/src/second_brain/tools/recipe.py && cd backend && uv run python -c "from second_brain.tools.recipe import RecipeTools"</automated>
  </verify>
  <acceptance_criteria>
    - `! grep -q "@tool(approval_mode=" backend/src/second_brain/tools/recipe.py` returns 0
    - `grep -q "class RecipeTools" backend/src/second_brain/tools/recipe.py` returns 0
    - `grep -q "async def fetch_recipe_url" backend/src/second_brain/tools/recipe.py` returns 0
    - `cd backend && uv run python -c "from second_brain.tools.recipe import RecipeTools"` exits 0
  </acceptance_criteria>
  <done>RecipeTools.fetch_recipe_url is GA-shaped (no decorator).</done>
</task>

</tasks>

<verification>
- Zero @tool(approval_mode=...) anywhere in tools/admin.py + tools/recipe.py
- Class shapes preserved
- Existing tests still pass
</verification>

<success_criteria>
Admin Agent constructed in 24-09 receives bound methods that are now GA-clean. Plan 24-11 (admin_handoff rewrite) can call agent.run() with these tools.
</success_criteria>

<output>
After completion, create `.planning/phases/24-foundry-ga-migration/24-10-SUMMARY.md` documenting:
- 6 strips in admin.py + 1 strip in recipe.py
- Test pass confirmation
- Annotated[...] parameter shapes preserved
</output>
