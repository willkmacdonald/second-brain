---
phase: 24-foundry-ga-migration
plan: 19
type: execute
wave: 4
depends_on: [18]
files_modified:
  - backend/src/second_brain/warmup.py
  - backend/src/second_brain/main.py
autonomous: true
requirements: [F-02]

must_haves:
  truths:
    - "warmup.py imports Agent (not AzureAIAgentClient)"
    - "agent_warmup_loop signature accepts list[tuple[str, Agent]]"
    - "Warmup ping uses agent.run('ping') (not client.get_response with Message)"
    - "main.py warmup factories build Agent instances (not AzureAIAgentClient)"
    - "main.py top-level RC import is removed (last RC reference disappears here)"
  artifacts:
    - path: "backend/src/second_brain/warmup.py"
      provides: "GA-shaped warmup loop"
      contains: "Agent, agent.run"
    - path: "backend/src/second_brain/main.py"
      provides: "Lifespan with no RC imports"
      contains: "AzureAIAgentClient (NOT — should be ABSENT)"
  key_links:
    - from: "warmup.py"
      to: "main.py warmup factory"
      via: "agent: Agent param"
      pattern: "Agent"
---

<objective>
Migrate the warmup loop from RC to GA. This is the last RC import site in `backend/src/second_brain/`. After this plan, `! grep -rE "AzureAIAgentClient|agent_framework\\.azure" backend/src/second_brain/` returns zero.

Two file edits:

1. **warmup.py rewrite (F-02):** Change function signature from `clients: list[tuple[str, AzureAIAgentClient]]` to `agents: list[tuple[str, Agent]]`. Replace `await client.get_response(messages=[Message(role="user", text="ping")])` with `await agent.run("ping")`. The self-heal recreate logic (lines 53-76) still works in shape — it recreates Agent objects via the factory dict.

2. **main.py warmup factory + warmup loop wiring:** The `_make_classifier_client(...)`, `_make_admin_client(...)`, `_make_investigation_client(...)` factories at lines 779-823 currently build RC clients. Rewrite each to build GA Agent instances. The factories now take `chat_client: FoundryChatClient` (the shared one from app.state) and return an Agent. The warmup loop start in lifespan switches to passing `agents=[("classifier", classifier_agent), ...]`.

After this plan, RC imports in main.py are deleted. The codebase is GA-clean.

Plan 24-20 runs cumulative audit + pre-deploy gates (tests, golden-trace replay, eval suite, auth_probe). Plan 24-21 final config cleanup (remove `azure_ai_*_agent_id` fields). Plan 24-22 unguard and deploy.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-foundry-ga-migration/24-CONTEXT.md
@.planning/phases/24-foundry-ga-migration/24-PATTERNS.md
@.planning/phases/24-foundry-ga-migration/FRAMEWORK-FIDELITY-calibration.md
@backend/src/second_brain/warmup.py
@backend/src/second_brain/main.py

<interfaces>
warmup.py change shape (per F-02 + 24-PATTERNS Group D):

```
# Before (RC):
from agent_framework import Message
from agent_framework.azure import AzureAIAgentClient

async def agent_warmup_loop(clients: list[tuple[str, AzureAIAgentClient]], ...):
    for name, client in clients:
        messages = [Message(role="user", text="ping")]
        await client.get_response(messages=messages)

# After (GA):
from agent_framework import Agent

async def agent_warmup_loop(agents: list[tuple[str, Agent]], factories: dict, ...):
    for name, agent in agents:
        await agent.run("ping")
```

main.py warmup factory rewrite:

```
# Before (RC):
def _make_classifier_client(...):
    return AzureAIAgentClient(agent_id=settings.azure_ai_classifier_agent_id, ...)

# After (GA):
def _make_classifier_agent(chat_client, ...):
    return build_classifier_agent(
        chat_client=chat_client,
        tools=[classifier_tools.file_capture],
        middleware=[CaptureTraceAgentMiddleware(), CaptureTraceFunctionMiddleware()],
    )
```

Same shape for admin and investigation factories.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite warmup.py against GA Agent</name>
  <files>backend/src/second_brain/warmup.py</files>
  <read_first>
    - backend/src/second_brain/warmup.py (current implementation, agent_warmup_loop lines 15-77)
    - .planning/phases/24-foundry-ga-migration/24-PATTERNS.md (Group D — warmup.py)
  </read_first>
  <action>
    Edit `backend/src/second_brain/warmup.py`:

    **Part A — Imports:**
    Replace:
    ```
    from agent_framework import Message
    from agent_framework.azure import AzureAIAgentClient
    ```
    With:
    ```
    from agent_framework import Agent
    ```

    **Part B — Function signature:**
    Change `agent_warmup_loop(clients: list[tuple[str, AzureAIAgentClient]], ...)` to `agent_warmup_loop(agents: list[tuple[str, Agent]], ...)`. Update all internal `client` references to `agent`.

    **Part C — Ping body:**
    Replace:
    ```
    messages = [Message(role="user", text="ping")]
    await client.get_response(messages=messages)
    ```
    With:
    ```
    await agent.run("ping")
    ```

    **Part D — Self-healing recreate logic (lines 53-76):**
    The self-heal currently calls factories[name]() to recreate a client. The factories should now produce Agent objects. The internal logic stays — just the type changes. If the factory dict signature changed (e.g., factories now need `chat_client` arg), update the call site:
    ```
    new_agent = factories[name](chat_client)
    ```

    Plan 24-19 Task 2 updates the factories to accept chat_client as their only arg.
  </action>
  <verify>
    <automated>! grep -q "from agent_framework.azure import AzureAIAgentClient" backend/src/second_brain/warmup.py && ! grep -q "Message" backend/src/second_brain/warmup.py && grep -q "agent.run(\"ping\")" backend/src/second_brain/warmup.py && grep -q "agents: list\\[tuple\\[str, Agent\\]\\]\\|agents: list\\[" backend/src/second_brain/warmup.py && cd backend && uv run python -c "from second_brain.warmup import agent_warmup_loop"</automated>
  </verify>
  <acceptance_criteria>
    - `! grep -q "from agent_framework.azure" backend/src/second_brain/warmup.py` returns 0
    - `! grep -q "Message" backend/src/second_brain/warmup.py` returns 0
    - `grep -q "from agent_framework import Agent" backend/src/second_brain/warmup.py` returns 0
    - `grep -q "agent.run(\"ping\")" backend/src/second_brain/warmup.py` returns 0
    - `grep -q "agents:" backend/src/second_brain/warmup.py` returns 0 (param renamed from clients)
    - `cd backend && uv run python -c "from second_brain.warmup import agent_warmup_loop"` exits 0
    - `! grep -q "client.get_response" backend/src/second_brain/warmup.py` returns 0
  </acceptance_criteria>
  <done>warmup.py is GA-shaped. F-02 cleared.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite main.py warmup factories + warmup loop start</name>
  <files>backend/src/second_brain/main.py</files>
  <read_first>
    - backend/src/second_brain/main.py (current factories around lines 779-823 + warmup loop start)
    - backend/src/second_brain/warmup.py (after Task 1)
    - backend/src/second_brain/agents/investigation.py (build_investigation_agent)
    - backend/src/second_brain/agents/admin.py (build_admin_agent)
    - backend/src/second_brain/agents/classifier.py (build_classifier_agent)
  </read_first>
  <action>
    Edit `backend/src/second_brain/main.py`:

    **Part A — Top-level RC import deletion:**
    Find: `from agent_framework.azure import AzureAIAgentClient`. DELETE it.

    Also delete unused RC type-related imports if any (e.g., `from agent_framework import Message` if it appears at top level — it shouldn't, but grep to confirm).

    **Part B — Rewrite each factory:**
    For each of `_make_classifier_client`, `_make_admin_client`, `_make_investigation_client` (around lines 779-823):
    
    1. Rename the function from `_make_*_client` to `_make_*_agent`.
    2. Change the body to build an Agent via the corresponding `build_*_agent` factory.
    3. The factory takes `chat_client: FoundryChatClient` as input. Each factory closes over the relevant tool instance + middleware list (already constructed at lifespan start).

    Example:
    ```
    def _make_classifier_agent(chat_client: FoundryChatClient) -> Agent:
        return build_classifier_agent(
            chat_client=chat_client,
            tools=[classifier_tools.file_capture],
            middleware=[
                CaptureTraceAgentMiddleware(),
                CaptureTraceFunctionMiddleware(),
            ],
        )
    ```

    NOTE: the factories MUST close over the tool instances by reference (not value) to access the live cosmos_manager DI. The factories are only called by warmup self-heal — at that point, the lifespan-singleton tool instances exist. Confirm `classifier_tools`, `admin_tools`, `recipe_tools`, `investigation_tools` are available in scope for the factory definitions (they should be — they're constructed at lifespan startup).

    **Part C — Warmup loop start:**
    Find the existing `asyncio.create_task(agent_warmup_loop(clients=[...], factories=..., ...))` call at the bottom of lifespan. Change `clients=[...]` to `agents=[...]` passing tuples like `("classifier", classifier_agent)`. Update the `factories=` dict to use the renamed `_make_*_agent` factories.

    **Part D — Verify zero RC imports remain:**
    Grep: `! grep -q "AzureAIAgentClient\\|agent_framework\\.azure" backend/src/second_brain/main.py` MUST return 0.
  </action>
  <verify>
    <automated>! grep -q "AzureAIAgentClient" backend/src/second_brain/main.py && ! grep -q "agent_framework\\.azure" backend/src/second_brain/main.py && grep -q "_make_classifier_agent\\|_make_admin_agent\\|_make_investigation_agent" backend/src/second_brain/main.py && cd backend && uv run python -c "import second_brain.main"</automated>
  </verify>
  <acceptance_criteria>
    - `! grep -q "AzureAIAgentClient" backend/src/second_brain/main.py` returns 0 (last RC reference gone)
    - `! grep -q "agent_framework\\.azure" backend/src/second_brain/main.py` returns 0
    - `grep -q "_make_classifier_agent" backend/src/second_brain/main.py` returns 0
    - `grep -q "_make_admin_agent" backend/src/second_brain/main.py` returns 0
    - `grep -q "_make_investigation_agent" backend/src/second_brain/main.py` returns 0
    - `! grep -rE "AzureAIAgentClient|agent_framework\\.azure" backend/src/second_brain/` returns 0 (zero RC patterns anywhere)
    - `cd backend && uv run python -c "import second_brain.main"` exits 0
  </acceptance_criteria>
  <done>main.py has no RC imports. Warmup factories produce Agent objects. The codebase under backend/src/second_brain/ is RC-free.</done>
</task>

</tasks>

<verification>
- F-02 cleared (warmup.py)
- F-01 fully cleared (main.py — last RC import gone)
- Warmup factories return Agent
- Codebase grep for RC patterns returns zero
</verification>

<success_criteria>
Plan 24-20 (cumulative audit + pre-deploy gates) is unblocked. The codebase is in its final pre-deploy state except for the orphan `azure_ai_*_agent_id` settings (cleanup in 24-21).
</success_criteria>

<output>
After completion, create `.planning/phases/24-foundry-ga-migration/24-19-SUMMARY.md` documenting:
- F-02 cleared
- F-01 fully cleared
- Codebase RC-free (verified via grep)
- Warmup factories use build_*_agent helpers
</output>
