---
phase: 24-foundry-ga-migration
plan: 14
type: execute
wave: 4
depends_on: [13.5]
files_modified:
  - backend/src/second_brain/agents/instructions/classifier.md
  - backend/src/second_brain/agents/classifier.py
  - backend/src/second_brain/main.py
autonomous: true
requirements: [F-01, F-11, F-12, F-19, D-01, D-04, D-05, P1-3, P1-5]

must_haves:
  truths:
    - "agents/instructions/classifier.md exists with content from CANDIDATE-instructions/classifier.md"
    - "agents/classifier.py exposes build_classifier_agent(chat_client, tools, middleware) -> Agent"
    - "main.py Classifier lifespan slice constructs Agent with tools=[classifier_tools.file_capture] ONLY (NOT transcribe_audio)"
    - "transcription_tools instance still constructed at lifespan; its transcribe_audio method is no longer registered as a tool"
    - "RC AzureAIAgentClient is removed from agents/classifier.py and from the Classifier lifespan slice"
    - "Middleware imports come from second_brain.agents.agent_middleware.capture_trace (P1-3)"
    - "Sync ManagedIdentityCredential invariant preserved (P1-5)"
    - "F-11 voice path split prepared at lifespan level; voice direct call lands in 24-15"
  artifacts:
    - path: "backend/src/second_brain/agents/instructions/classifier.md"
      provides: "Classifier agent instructions (D-02 source of truth)"
    - path: "backend/src/second_brain/agents/classifier.py"
      provides: "build_classifier_agent factory"
    - path: "backend/src/second_brain/main.py"
      provides: "Lifespan-singleton Classifier Agent on app.state.classifier_agent with file_capture-only tools"
  key_links:
    - from: "main.py classifier construction"
      to: "tools=[classifier_tools.file_capture]"
      via: "single-tool registration"
      pattern: "tools=\\[classifier_tools.file_capture\\]"
---

<objective>
First commit in task group 23.3 commit cluster. The Classifier migration is the most complex because of:
- Voice path split (D-01..D-04, F-11): classifier registers ONLY `file_capture`; transcription becomes a direct call from `api/capture.py` (lands in plan 24-15).
- `tool_choice='required'` enforcement (D-02): unambiguous because only one tool registered.
- Safety net deletion (D-03): lands in plan 24-16 (streaming/adapter.py rewrite).
- `forced_tool_failure` SSE sub-code (D-04): emitted from streaming adapter (plan 24-16).
- Per-call session rehydration (probe 4 + P0-1 fresh-process probe outcome from 24-06.5): replaces `foundryThreadId` round-trip (plan 24-17 via dual-read per P0-2).
- Inbox sessionId field rename (F-13, plan 24-17 — additive copy preserving foundryThreadId per P0-2).

This plan's scope is narrow: instructions promotion + agent factory + lifespan wiring with file_capture-only registration. Voice direct call, safety net deletion, session rehydration land in subsequent plans.

**P1-3 amendment:** middleware imports come from `second_brain.agents.agent_middleware.capture_trace` (the new package path). The import line already exists in main.py from 24-04 — this plan reuses it without re-importing.

**P1-5 invariant:** main.py already uses sync `ManagedIdentityCredential` from 24-04. This plan does not change credential class.

Per CONFIG-DELTAS Step A: keep `azure_ai_classifier_agent_id` setting present in config.py and any reads in non-Classifier slices. Cleanup at plan 24-21 (final config cleanup).

Three file edits:
1. Promote `.planning/phases/23-foundry-ga-prep/CANDIDATE-instructions/classifier.md` to `backend/src/second_brain/agents/instructions/classifier.md`.
2. Rewrite `backend/src/second_brain/agents/classifier.py` with `build_classifier_agent`.
3. Edit `backend/src/second_brain/main.py` Classifier lifespan slice (around lines 511-587 + 779-793). Replace `classifier_client = AzureAIAgentClient(...)` + the F-11 anti-pattern at lines 578-581 (which conditionally appends `transcribe_audio` to the tools list) with `build_classifier_agent(...)` taking ONLY `[classifier_tools.file_capture]`.

After this plan, local main is buildable for Investigation, Admin, and (partially) Classifier — Classifier agent constructed correctly, BUT streaming/adapter.py still uses RC `client.get_response(...)` and `app.state.classifier_client` (until plan 24-16 rewrites it). The remaining gap is closed in 24-15..24-17.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-foundry-ga-migration/24-CONTEXT.md
@.planning/phases/24-foundry-ga-migration/24-PATTERNS.md
@.planning/phases/24-foundry-ga-migration/24-PLAN-DEFECTS.md
@.planning/phases/24-foundry-ga-migration/FRAMEWORK-FIDELITY-calibration.md
@.planning/phases/23-foundry-ga-prep/CANDIDATE-instructions/classifier.md
@backend/src/second_brain/agents/classifier.py
@backend/src/second_brain/agents/admin.py
@backend/src/second_brain/main.py
@backend/src/second_brain/agents/agent_middleware/capture_trace.py

<interfaces>
Same factory pattern as 24-04 (Investigation) and 24-09 (Admin). Reuse `load_instructions` from `agents/investigation`.

```python
def build_classifier_agent(chat_client, tools, middleware) -> Agent:
    return Agent(
        client=chat_client,
        instructions=load_instructions("classifier"),
        tools=list(tools),
        middleware=list(middleware),
    )
```

P1-3 import path (already in main.py from 24-04 — verify present):
```python
from second_brain.agents.agent_middleware.capture_trace import (
    CaptureTraceAgentMiddleware,
    CaptureTraceFunctionMiddleware,
)
```

CRITICAL F-11 fix in main.py:
```python
# DELETE this anti-pattern (current main.py:578-581):
agent_tools = [classifier_tools.file_capture]
if app.state.transcription_tools:
    agent_tools.append(app.state.transcription_tools.transcribe_audio)
app.state.classifier_agent_tools = agent_tools

# REPLACE with:
classifier_agent = build_classifier_agent(
    chat_client=chat_client,
    tools=[classifier_tools.file_capture],   # ONLY file_capture (D-04)
    middleware=[
        CaptureTraceAgentMiddleware(),
        CaptureTraceFunctionMiddleware(),
    ],
)
app.state.classifier_agent = classifier_agent
```

The `app.state.transcription_tools` instance remains — its `transcribe_audio` method is no longer a registered tool but is still callable directly (decorator gets stripped in 24-15). Plan 24-15 wires the direct call from api/capture.py.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Promote classifier.md instructions</name>
  <files>backend/src/second_brain/agents/instructions/classifier.md</files>
  <read_first>
    - .planning/phases/23-foundry-ga-prep/CANDIDATE-instructions/classifier.md (source content)
  </read_first>
  <action>
    Copy `.planning/phases/23-foundry-ga-prep/CANDIDATE-instructions/classifier.md` to `backend/src/second_brain/agents/instructions/classifier.md` (verbatim).

    Verify identical: `diff .planning/phases/23-foundry-ga-prep/CANDIDATE-instructions/classifier.md backend/src/second_brain/agents/instructions/classifier.md`.
  </action>
  <verify>
    <automated>diff .planning/phases/23-foundry-ga-prep/CANDIDATE-instructions/classifier.md backend/src/second_brain/agents/instructions/classifier.md && test -s backend/src/second_brain/agents/instructions/classifier.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f backend/src/second_brain/agents/instructions/classifier.md` returns 0
    - `diff .planning/phases/23-foundry-ga-prep/CANDIDATE-instructions/classifier.md backend/src/second_brain/agents/instructions/classifier.md` returns no diff
    - File size > 500 bytes
  </acceptance_criteria>
  <done>Classifier instructions in repo. All 3 instruction files now present.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite agents/classifier.py with build_classifier_agent</name>
  <files>backend/src/second_brain/agents/classifier.py</files>
  <read_first>
    - backend/src/second_brain/agents/classifier.py (current ensure_classifier_agent body)
    - backend/src/second_brain/agents/admin.py (build_admin_agent — same pattern)
    - backend/src/second_brain/agents/instructions/classifier.md (created Task 1)
    - .planning/phases/24-foundry-ga-migration/24-PATTERNS.md (Group D — agents/classifier.py)
  </read_first>
  <action>
    Replace the entire current body of `backend/src/second_brain/agents/classifier.py` with:

    ```python
    """Classifier agent factory.

    GA pattern (Phase 24 task group 23.3): mirrors agents/investigation.py and
    agents/admin.py. Per D-04 (voice path split): classifier registers ONLY
    file_capture. Transcription is a direct helper called from api/capture.py
    when audio is on the request — no longer a registered tool.

    Replaces RC ensure_classifier_agent portal-shell creation pattern (F-19).
    """

    from __future__ import annotations

    import logging
    from collections.abc import Sequence
    from typing import Any

    from agent_framework import Agent
    from agent_framework_foundry import FoundryChatClient

    from second_brain.agents.investigation import load_instructions

    logger = logging.getLogger(__name__)


    def build_classifier_agent(
        chat_client: FoundryChatClient,
        tools: Sequence[Any],
        middleware: Sequence[Any],
    ) -> Agent:
        """Construct the Classifier Agent (streaming, single-tool: file_capture).

        Per CONTEXT D-04: tools list MUST contain ONLY file_capture. With one
        tool registered, tool_choice='required' (set on agent.run() options) is
        unambiguous — the model must call file_capture. The Python safety net
        is deleted (D-03); failures route to forced_tool_failure SSE sub-code.
        """
        instructions = load_instructions("classifier")
        return Agent(
            client=chat_client,
            instructions=instructions,
            tools=list(tools),
            middleware=list(middleware),
        )
    ```
  </action>
  <verify>
    <automated>cd backend && uv run python -c "from second_brain.agents.classifier import build_classifier_agent; from second_brain.agents.investigation import load_instructions; assert callable(build_classifier_agent); assert len(load_instructions('classifier')) > 500" && ! grep -q "ensure_classifier_agent" backend/src/second_brain/agents/classifier.py && ! grep -q "AzureAIAgentClient" backend/src/second_brain/agents/classifier.py && ! grep -q "create_agent" backend/src/second_brain/agents/classifier.py</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && uv run python -c "from second_brain.agents.classifier import build_classifier_agent"` exits 0
    - `! grep -q "ensure_classifier_agent" backend/src/second_brain/agents/classifier.py` returns 0
    - `! grep -q "AzureAIAgentClient" backend/src/second_brain/agents/classifier.py` returns 0
    - `! grep -q "create_agent" backend/src/second_brain/agents/classifier.py` returns 0
    - `grep -q "from second_brain.agents.investigation import load_instructions" backend/src/second_brain/agents/classifier.py` returns 0
    - `grep -q "def build_classifier_agent" backend/src/second_brain/agents/classifier.py` returns 0
  </acceptance_criteria>
  <done>agents/classifier.py rewritten. Same shape as Investigation and Admin factories.</done>
</task>

<task type="auto">
  <name>Task 3: Wire Classifier Agent in main.py with file_capture-ONLY tools (F-11) — verify P1-3 + P1-5 invariants</name>
  <files>backend/src/second_brain/main.py</files>
  <read_first>
    - backend/src/second_brain/main.py (Classifier lifespan slice lines 511-587, F-11 violation at 578-581)
    - backend/src/second_brain/agents/classifier.py (factory created Task 2)
    - .planning/phases/24-foundry-ga-migration/24-PATTERNS.md (Group D — main.py Classifier slice)
    - .planning/phases/24-foundry-ga-migration/24-PLAN-DEFECTS.md (P1-3 agent_middleware/ + P1-5 sync credential invariants)
  </read_first>
  <action>
    Edit `backend/src/second_brain/main.py`:

    **Part A — Imports:**
    Add `from second_brain.agents.classifier import build_classifier_agent` (alongside existing `build_investigation_agent`, `build_admin_agent`).

    Verify P1-3 invariant — the existing CaptureTrace middleware import (added by 24-04) uses the agent_middleware/ package:
    ```python
    from second_brain.agents.agent_middleware.capture_trace import (
        CaptureTraceAgentMiddleware,
        CaptureTraceFunctionMiddleware,
    )
    ```
    If this line is wrong (`agents.middleware.capture_trace`), correct it. The Classifier slice reuses these classes.

    Verify P1-5 invariant — the credential class import is sync:
    ```python
    from azure.identity import ManagedIdentityCredential   # sync, NOT azure.identity.aio
    ```
    Should already be the case from 24-04 — verify, do not duplicate.

    **Part B — Replace the Classifier lifespan slice (currently around lines 511-587):**
    Today the code constructs `classifier_client = AzureAIAgentClient(agent_id=classifier_agent_id, ...)`, builds the F-11 anti-pattern tool list at 578-581, and assigns to `app.state.classifier_client`.

    KEEP: the construction of `app.state.transcription_tools` (around lines 542-559). Its `transcribe_audio` method is still callable but no longer a registered tool. Plan 24-15 wires the direct call from api/capture.py.

    KEEP: any rate limiter or per-classifier state setup that's not part of the agent construction.

    REPLACE the AzureAIAgentClient block + F-11 anti-pattern with:
    ```python
    classifier_agent = build_classifier_agent(
        chat_client=chat_client,
        tools=[classifier_tools.file_capture],   # ONLY file_capture (CONTEXT D-04)
        middleware=[
            CaptureTraceAgentMiddleware(),
            CaptureTraceFunctionMiddleware(),
        ],
    )
    app.state.classifier_agent = classifier_agent
    ```

    DELETE: any reference to `app.state.classifier_client`. DELETE: the `ensure_classifier_agent(...)` call. DELETE: the `app.state.classifier_agent_tools = agent_tools` assignment. DELETE: the conditional `if app.state.transcription_tools: agent_tools.append(...)` block (this is the F-11 fix).

    DO NOT delete: `azure_ai_classifier_agent_id` reads (cleanup is in plan 24-21). Actually — if the read was happening immediately before constructing the classifier_client (e.g., `classifier_agent_id = settings.azure_ai_classifier_agent_id`), DELETE that local variable assignment because no caller in the new path uses it. The setting itself stays in config.py.

    **Part C — Warmup factory at lines 779-793:**
    Plan 24-19 (warmup migration) handles. Leave the `_make_classifier_client(...)` factory body unchanged in this plan.

    KEEP the existing fail-fast try/except around the Classifier construction (Classifier is fatal at startup).

    **Part D — Verification:**
    `! grep -q "agent_tools.append" backend/src/second_brain/main.py` should now succeed (F-11 anti-pattern removed).

    Re-run the P1-5 red test: `cd backend && uv run pytest tests/test_foundry_credential_shape.py -x` should still pass.

    Re-run the P1-3 red test: `cd backend && uv run pytest tests/test_legacy_middleware_imports_survive.py -x` should still pass.
  </action>
  <verify>
    <automated>grep -q "build_classifier_agent" backend/src/second_brain/main.py && grep -q "tools=\[classifier_tools.file_capture\]" backend/src/second_brain/main.py && ! grep -q "ensure_classifier_agent" backend/src/second_brain/main.py && ! grep -q "agent_tools.append(app.state.transcription_tools.transcribe_audio)" backend/src/second_brain/main.py && ! grep -q "classifier_client = AzureAIAgentClient" backend/src/second_brain/main.py && grep -q "from second_brain.agents.agent_middleware.capture_trace" backend/src/second_brain/main.py && ! grep -q "from azure.identity.aio import ManagedIdentityCredential" backend/src/second_brain/main.py && cd backend && uv run pytest tests/test_foundry_credential_shape.py tests/test_legacy_middleware_imports_survive.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "from second_brain.agents.classifier import build_classifier_agent" backend/src/second_brain/main.py` returns 0
    - `grep -q "build_classifier_agent(" backend/src/second_brain/main.py` returns 0
    - `grep -q "tools=\\[classifier_tools.file_capture\\]" backend/src/second_brain/main.py` returns 0
    - `! grep -q "ensure_classifier_agent" backend/src/second_brain/main.py` returns 0
    - `! grep -q "agent_tools.append" backend/src/second_brain/main.py` returns 0 (F-11 anti-pattern absent)
    - `! grep -q "classifier_client = AzureAIAgentClient" backend/src/second_brain/main.py` returns 0
    - `grep -q "app.state.classifier_agent = classifier_agent" backend/src/second_brain/main.py` returns 0
    - `grep -q "app.state.transcription_tools" backend/src/second_brain/main.py` returns 0 (still constructed for direct call in 24-15)
    - `grep -q "AzureAIAgentClient" backend/src/second_brain/main.py` returns 0 (still present — referenced by warmup factory; clean up in 24-19)
    - `grep -q "from second_brain.agents.agent_middleware.capture_trace" backend/src/second_brain/main.py` returns 0 (P1-3 path)
    - `! grep -q "from azure.identity.aio import ManagedIdentityCredential" backend/src/second_brain/main.py` returns 0 (P1-5 sync invariant)
    - `cd backend && uv run pytest tests/test_foundry_credential_shape.py -x` exits 0 (P1-5 still green)
    - `cd backend && uv run pytest tests/test_legacy_middleware_imports_survive.py -x` exits 0 (P1-3 still green)
  </acceptance_criteria>
  <done>Classifier Agent constructed via GA factory with file_capture-ONLY registration. F-11 voice path split prepared at lifespan level. P1-3 + P1-5 invariants preserved.</done>
</task>

</tasks>

<verification>
- 3 instructions files now in repo (investigation, admin, classifier)
- agents/classifier.py uses GA factory pattern (mirrors admin.py)
- main.py constructs classifier_agent with EXACTLY one tool registered (file_capture)
- transcription_tools still constructed for direct-call use in 24-15
- F-11 voice anti-pattern removed
- P1-3 + P1-5 red tests still green
</verification>

<success_criteria>
Plan 24-15 (api/capture.py voice direct call) can read app.state.classifier_agent + app.state.transcription_tools. Plan 24-16 (streaming/adapter.py rewrite) can call agent.run(stream=True) with tool_choice='required' on the single-tool agent.
</success_criteria>

<output>
After completion, create `.planning/phases/24-foundry-ga-migration/24-14-SUMMARY.md` documenting:
- All 3 instruction files in repo
- agents/classifier.py rewritten
- main.py Classifier slice has Agent with file_capture-ONLY tools (P1-3 + P1-5 invariants preserved)
- F-11 anti-pattern absent
- Transcription tools still constructed for direct call (24-15)
</output>
</content>
</invoke>
