---
phase: 24-foundry-ga-migration
plan: 12
type: execute
wave: 3
depends_on: [11]
files_modified:
  - backend/src/second_brain/eval/invoker.py
  - backend/src/second_brain/eval/runner.py
  - backend/src/second_brain/eval/foundry.py
autonomous: true
requirements: [F-06, F-07, D-04, D-07]

must_haves:
  truths:
    - "eval/invoker.py exists with EvalAgentInvoker Protocol + RCEvalAgentInvoker + GAEvalAgentInvoker"
    - "eval/runner.py admin call site (lines 278-294) replaced with await invoker.invoke_admin(...)"
    - "eval/foundry.py admin call site replaced similarly"
    - "eval/runner.py classifier call site (lines 133-149) STILL uses RCEvalAgentInvoker (deleted in 24-18 when classifier migrates)"
    - "RCEvalAgentInvoker exists temporarily; deletion triggered in 24-18"
  artifacts:
    - path: "backend/src/second_brain/eval/invoker.py"
      provides: "EvalAgentInvoker facade hiding RC vs. GA call shape during migration"
      contains: "class EvalAgentInvoker, class RCEvalAgentInvoker, class GAEvalAgentInvoker"
    - path: "backend/src/second_brain/eval/runner.py"
      provides: "Eval orchestration calling invoker (not direct client.get_response)"
    - path: "backend/src/second_brain/eval/foundry.py"
      provides: "Dataset gen calling invoker (not direct client.get_response)"
  key_links:
    - from: "eval/runner.py admin call"
      to: "eval/invoker.GAEvalAgentInvoker.invoke_admin"
      via: "await invoker.invoke_admin"
      pattern: "invoker.invoke_admin"
    - from: "eval/runner.py classifier call"
      to: "eval/invoker.RCEvalAgentInvoker.invoke_classifier"
      via: "RC implementation kept until 24-18"
      pattern: "invoker.invoke_classifier"
---

<objective>
Introduce the `EvalAgentInvoker` Protocol facade with two implementations during the migration window. Per CONTEXT D-04 + design D-07 explicit-justification template (recorded in EVAL-INVENTORY): the facade is a temporary seam, deleted at end of 23.3.

After this plan:
- Admin eval call site uses `GAEvalAgentInvoker.invoke_admin(...)` (Admin agent migrated in 24-09 → 24-11).
- Classifier eval call site STILL uses `RCEvalAgentInvoker.invoke_classifier(...)` (Classifier still RC at this point — migrates in 24-13 → 24-17).
- 24-18 removes RCEvalAgentInvoker after classifier flips to GA.

Three file edits:
1. Create `backend/src/second_brain/eval/invoker.py` (NEW) — Protocol + RC impl + GA impl.
2. Edit `backend/src/second_brain/eval/runner.py` — replace lines 278-294 (admin call) with `await invoker.invoke_admin(...)`. KEEP lines 133-149 (classifier call) routed through `invoker.invoke_classifier(...)` but with the RC implementation selected.
3. Edit `backend/src/second_brain/eval/foundry.py` — replace `client.get_response(...)` calls in `generate_app_mediated_dataset` (around lines 856-942) with the same invoker shape.

Per F-06 + F-07 calibration findings + EVAL-INVENTORY round-15 P-01: per-destination flat-accuracy contract STAYS unchanged. Do NOT widen `compute_admin_metrics` to per-destination precision/recall. Per CONTEXT-deferred: that's a separate `eval/metrics.py` follow-up, not a facade change.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-foundry-ga-migration/24-CONTEXT.md
@.planning/phases/24-foundry-ga-migration/24-PATTERNS.md
@.planning/phases/24-foundry-ga-migration/FRAMEWORK-FIDELITY-calibration.md
@.planning/phases/23-foundry-ga-prep/EVAL-INVENTORY.md
@.planning/phases/23-foundry-ga-prep/FOUNDRY-PROBE-FINDINGS.md
@backend/src/second_brain/eval/runner.py
@backend/src/second_brain/eval/foundry.py
@backend/src/second_brain/eval/dry_run_tools.py

<interfaces>
Per EVAL-INVENTORY:

```
class EvalAgentInvoker(Protocol):
    async def invoke_classifier(
        self, input_text: str, tools_instance: "EvalClassifierTools"
    ) -> None: ...

    async def invoke_admin(
        self, input_text: str, tools_instance: "DryRunAdminTools",
        routing_context: str,
    ) -> None: ...
```

GAEvalAgentInvoker uses `agent.run(input_text, tools=[tools_instance.method, ...], options=ChatOptions(tool_choice="required"))`. Tools passed PER-CALL because eval uses per-case stub tool instances (not the lifespan-singleton agent's pre-registered tools).

NOTE on tools=per-call: Agent.run accepts `tools=` kwarg per probe + GA semantic conventions. The eval path needs per-case tools_instance whose state captures eval results — those are case-scoped, not lifespan-scoped. The GAEvalAgentInvoker passes `tools=[...]` to override the agent's default tools for the duration of that single run() call.

If GA Agent.run() does NOT accept tools= kwarg (i.e., tools must be set at Agent construction), the GAEvalAgentInvoker constructs a fresh per-case Agent in invoke_admin / invoke_classifier. The probe streaming_shape.json was a 1-tool stub agent constructed for the probe — that establishes that constructing a fresh Agent per case is feasible. The planner's preference is per-call tools= if the SDK supports it; otherwise per-case Agent construction.

Side-effect contract preserved (per EVAL-INVENTORY):
- Classifier: tools_instance.last_bucket / tools_instance.last_confidence are populated inside file_capture stub when the model calls it.
- Admin: dry_run_tools.captured_destinations / dry_run_tools.captured_tasks are populated inside add_errand_items / add_task_items stubs.

The runner reads side-effects, not the response.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create eval/invoker.py with Protocol + RC + GA + Hybrid implementations</name>
  <files>backend/src/second_brain/eval/invoker.py</files>
  <read_first>
    - backend/src/second_brain/eval/runner.py (lines 133-149 classifier call, 278-294 admin call)
    - backend/src/second_brain/eval/dry_run_tools.py (EvalClassifierTools, DryRunAdminTools)
    - .planning/phases/23-foundry-ga-prep/EVAL-INVENTORY.md (facade interface spec)
    - backend/tests/fixtures/foundry-probe/tool_call_extraction.json (probe 2 — for response.messages walking inside GA impl)
    - .planning/phases/24-foundry-ga-migration/24-PATTERNS.md (Group C — eval/invoker.py synthesised structure)
  </read_first>
  <action>
    Create `backend/src/second_brain/eval/invoker.py` with:

    ```
    """EvalAgentInvoker — temporary facade hiding RC/GA call shape during migration.

    Introduced in Phase 24 task group 23.2 when Admin migrates to GA. The RC
    implementation is deleted at end of 23.3 (plan 24-18) when no caller remains.

    D-07 EXPLICIT JUSTIFICATION (per EVAL-INVENTORY):
    1. Framework primitive considered: Agent.run(messages) direct.
    2. What custom code provides: translation between eval cases (input + expected
       label) and agent.run() call shape, AND adapting response back to eval
       runner's existing per-case dict format.
    3. Why not middleware/context provider/tool/configuration: it CAN be solved
       by either, but during the migration window we have BOTH RC and GA call
       shapes alive (classifier on RC until 23.3, admin on GA after 23.2). The
       facade hides that split for one migration window.
    4. Permanent or temporary: temporary. Deletion trigger: end of plan 24-18,
       when no RCEvalAgentInvoker caller remains.
    """

    from __future__ import annotations

    from typing import TYPE_CHECKING, Protocol

    from agent_framework import ChatOptions

    if TYPE_CHECKING:
        from agent_framework import Agent
        from agent_framework.azure import AzureAIAgentClient
        from second_brain.eval.dry_run_tools import (
            EvalClassifierTools,
            DryRunAdminTools,
        )


    class EvalAgentInvoker(Protocol):
        async def invoke_classifier(
            self,
            input_text: str,
            tools_instance: "EvalClassifierTools",
        ) -> None: ...

        async def invoke_admin(
            self,
            input_text: str,
            tools_instance: "DryRunAdminTools",
            routing_context: str,
        ) -> None: ...


    class GAEvalAgentInvoker:
        """GA implementation — calls Agent.run(...).

        Per probe tool_call_extraction.json: tools registered via `tools=` kwarg
        on agent.run(). Side effects already captured on tools_instance attributes
        when the model invokes the stub tools.
        """

        def __init__(self, classifier_agent: "Agent", admin_agent: "Agent") -> None:
            self._classifier = classifier_agent
            self._admin = admin_agent

        async def invoke_classifier(
            self,
            input_text: str,
            tools_instance: "EvalClassifierTools",
        ) -> None:
            await self._classifier.run(
                input_text,
                tools=[tools_instance.file_capture],
                options=ChatOptions(tool_choice="required"),
            )

        async def invoke_admin(
            self,
            input_text: str,
            tools_instance: "DryRunAdminTools",
            routing_context: str,
        ) -> None:
            prompt = f"{routing_context}\n\n---\n{input_text}"
            await self._admin.run(
                prompt,
                tools=[
                    tools_instance.add_errand_items,
                    tools_instance.add_task_items,
                    tools_instance.get_routing_context,
                ],
                # tool_choice not set — admin eval contract uses default (auto)
            )


    class RCEvalAgentInvoker:
        """RC implementation — temporary, deleted in plan 24-18.

        Body lifted from eval/runner.py:133-149 + 278-294 verbatim. Used while
        the corresponding agent has not yet migrated to GA. After 24-18, the
        classifier is GA and this class has no callers.
        """

        def __init__(
            self,
            classifier_client: "AzureAIAgentClient",
            admin_client: "AzureAIAgentClient",
        ) -> None:
            self._classifier_client = classifier_client
            self._admin_client = admin_client

        async def invoke_classifier(
            self,
            input_text: str,
            tools_instance: "EvalClassifierTools",
        ) -> None:
            from agent_framework import ChatOptions as RCChatOptions, Message

            messages = [Message(role="user", text=input_text)]
            options = RCChatOptions(
                tools=[tools_instance.file_capture],
                tool_choice={
                    "mode": "required",
                    "required_function_name": "file_capture",
                },
            )
            await self._classifier_client.get_response(
                messages=messages, options=options
            )

        async def invoke_admin(
            self,
            input_text: str,
            tools_instance: "DryRunAdminTools",
            routing_context: str,
        ) -> None:
            from agent_framework import ChatOptions as RCChatOptions, Message

            prompt = f"{routing_context}\n\n---\n{input_text}"
            messages = [Message(role="user", text=prompt)]
            options = RCChatOptions(
                tools=[
                    tools_instance.add_errand_items,
                    tools_instance.add_task_items,
                    tools_instance.get_routing_context,
                ],
            )
            await self._admin_client.get_response(messages=messages, options=options)


    class _MigrationHybridInvoker:
        """Hybrid composition for the 23.2-23.3 migration window.

        Routes invoke_classifier -> RC implementation (classifier still RC until 24-13..24-17),
        invoke_admin -> GA implementation (admin migrates in 24-09..24-11).

        Deletion trigger: end of plan 24-18 (cleanup commit), once both agents are GA
        and the classifier_invoker can be a single GAEvalAgentInvoker. The leading
        underscore signals deletion-trigger-aligned private API, matching the RC/GA
        siblings.
        """

        def __init__(
            self,
            rc_invoker: "RCEvalAgentInvoker",
            ga_invoker: "GAEvalAgentInvoker",
        ) -> None:
            self._rc = rc_invoker
            self._ga = ga_invoker

        async def invoke_classifier(
            self,
            input_text: str,
            tools_instance: "EvalClassifierTools",
        ) -> None:
            await self._rc.invoke_classifier(input_text, tools_instance)

        async def invoke_admin(
            self,
            input_text: str,
            tools_instance: "DryRunAdminTools",
            routing_context: str,
        ) -> None:
            await self._ga.invoke_admin(input_text, tools_instance, routing_context)
    ```

    Verify imports work: `cd backend && uv run python -c "from second_brain.eval.invoker import EvalAgentInvoker, RCEvalAgentInvoker, GAEvalAgentInvoker, _MigrationHybridInvoker"`.
  </action>
  <verify>
    <automated>cd backend && uv run python -c "from second_brain.eval.invoker import EvalAgentInvoker, RCEvalAgentInvoker, GAEvalAgentInvoker, _MigrationHybridInvoker" && grep -q "class EvalAgentInvoker(Protocol)" backend/src/second_brain/eval/invoker.py && grep -q "class GAEvalAgentInvoker" backend/src/second_brain/eval/invoker.py && grep -q "class RCEvalAgentInvoker" backend/src/second_brain/eval/invoker.py && grep -q "class _MigrationHybridInvoker" backend/src/second_brain/eval/invoker.py && grep -q "D-07 EXPLICIT JUSTIFICATION" backend/src/second_brain/eval/invoker.py</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && uv run python -c "from second_brain.eval.invoker import EvalAgentInvoker, RCEvalAgentInvoker, GAEvalAgentInvoker, _MigrationHybridInvoker"` exits 0
    - `grep -q "class EvalAgentInvoker(Protocol)" backend/src/second_brain/eval/invoker.py` returns 0
    - `grep -q "class GAEvalAgentInvoker" backend/src/second_brain/eval/invoker.py` returns 0
    - `grep -q "class RCEvalAgentInvoker" backend/src/second_brain/eval/invoker.py` returns 0
    - `grep -q "class _MigrationHybridInvoker" backend/src/second_brain/eval/invoker.py` returns 0 (hybrid lives here per N-5 lock-in, NOT in runner.py)
    - `! grep -q "_MigrationHybridInvoker" backend/src/second_brain/eval/runner.py` returns 0 (hybrid must NOT be defined in runner.py)
    - `grep -q "D-07 EXPLICIT JUSTIFICATION" backend/src/second_brain/eval/invoker.py` returns 0
    - `grep -q "Deletion trigger: end of plan 24-18" backend/src/second_brain/eval/invoker.py` returns 0
    - `grep -q "tool_choice=\"required\"" backend/src/second_brain/eval/invoker.py` returns 0 (GA classifier path)
  </acceptance_criteria>
  <done>Facade in place with Protocol + RC impl + GA impl + _MigrationHybridInvoker (hybrid lives in eval/invoker.py per N-5 lock-in).</done>
</task>

<task type="auto">
  <name>Task 2: Wire eval/runner.py to call invoker (mixed RC/GA dispatch)</name>
  <files>backend/src/second_brain/eval/runner.py</files>
  <read_first>
    - backend/src/second_brain/eval/runner.py (lines 21 RC import, 133-149 classifier call, 278-294 admin call)
    - backend/src/second_brain/eval/invoker.py (created Task 1 — contains EvalAgentInvoker Protocol, RCEvalAgentInvoker, GAEvalAgentInvoker, AND _MigrationHybridInvoker per N-5 lock-in)
  </read_first>
  <action>
    Edit `backend/src/second_brain/eval/runner.py`:

    **Part A — Imports:**
    KEEP `from agent_framework import ChatOptions, Message` for now — RCEvalAgentInvoker still needs them via `eval/invoker.py`'s local import. Actually RC classes import these inside their methods (per Task 1 design), so the runner module can DROP the top-level RC imports if it has no other RC usage. Grep first to confirm no other usage in runner.py.

    Add: `from second_brain.eval.invoker import EvalAgentInvoker`.

    **Part B — Constructor / orchestration update:**
    The runner currently takes `classifier_client` and/or `admin_client` arguments and calls `client.get_response(...)` directly. Identify the constructor or factory function that wires the runner.

    Add an `invoker: EvalAgentInvoker` parameter alongside or replacing the client args. The caller (api/eval.py and any orchestrator) chooses which implementation to inject:
    - For Admin: GAEvalAgentInvoker (admin migrated in 24-09 → 24-11)
    - For Classifier: RCEvalAgentInvoker (classifier still on RC until 24-13 → 24-17)

    Plan 24-12 may need the runner to accept BOTH (one for classifier, one for admin) since they're different invocations. Simpler: the runner takes ONE invoker, and the eval entrypoint constructs an invoker that routes classifier→RC and admin→GA via composition. Or: the runner takes two invokers (`classifier_invoker`, `admin_invoker`). Pick the cleanest split that avoids leaking RC types into runner.py.

    Locked approach (per N-5 review): runner takes a single `invoker: EvalAgentInvoker` parameter. The caller (api/eval.py or wherever the eval entrypoint constructs the runner) composes a `_MigrationHybridInvoker(rc_invoker=RCEvalAgentInvoker(...), ga_invoker=GAEvalAgentInvoker(...))` and passes that single hybrid as the `invoker` argument. The hybrid class is defined in `eval/invoker.py` (Task 1), NOT in `eval/runner.py`. Phase 24-13 onwards switches the implementation; plan 24-18 deletes the hybrid + RC class together.

    The hybrid composition pattern is implemented in Task 1 as `_MigrationHybridInvoker` inside `eval/invoker.py`. Task 2 ONLY imports + uses it; do NOT redefine it in runner.py.

    The runner.py code does NOT need to import `_MigrationHybridInvoker` directly — it only types the parameter as `EvalAgentInvoker` (the Protocol). The hybrid composition happens at the call-site (api/eval.py or the eval entrypoint), which DOES import and construct `_MigrationHybridInvoker(rc_invoker=..., ga_invoker=...)`. Update the call-site accordingly.

    **Part C — Replace classifier call site (lines 133-149):**
    The current code:
    ```
    messages = [Message(role="user", text=case["inputText"])]
    options = ChatOptions(...)
    await _call_with_retry(
        lambda m=messages, o=options: classifier_client.get_response(messages=m, options=o),
        run_id=run_id, case_index=i, runs_dict=runs_dict,
    )
    ```
    
    Replace with:
    ```
    await _call_with_retry(
        lambda: invoker.invoke_classifier(case["inputText"], eval_tools),
        run_id=run_id, case_index=i, runs_dict=runs_dict,
    )
    ```

    **Part D — Replace admin call site (lines 278-294):**
    The current code:
    ```
    messages = [Message(role="user", text=case["inputText"])]
    options = ChatOptions(tools=[...])
    await _call_with_retry(
        lambda m=messages, o=options: admin_client.get_response(messages=m, options=o),
        ...
    )
    ```
    
    Replace with:
    ```
    await _call_with_retry(
        lambda: invoker.invoke_admin(case["inputText"], dry_run_tools, routing_context),
        run_id=run_id, case_index=i, runs_dict=runs_dict,
    )
    ```

    Update the runner's signature so the test fixtures and callers compile against the new contract.

    Verify the runner module still imports cleanly: `cd backend && uv run python -c "from second_brain.eval.runner import run_classifier_eval"` (or whichever entrypoint exists).
  </action>
  <verify>
    <automated>grep -q "from second_brain.eval.invoker import EvalAgentInvoker" backend/src/second_brain/eval/runner.py && grep -q "invoker.invoke_classifier" backend/src/second_brain/eval/runner.py && grep -q "invoker.invoke_admin" backend/src/second_brain/eval/runner.py && ! grep -q "classifier_client.get_response" backend/src/second_brain/eval/runner.py && ! grep -q "admin_client.get_response" backend/src/second_brain/eval/runner.py && cd backend && uv run python -c "import second_brain.eval.runner"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "from second_brain.eval.invoker import EvalAgentInvoker" backend/src/second_brain/eval/runner.py` returns 0
    - `grep -q "invoker.invoke_classifier" backend/src/second_brain/eval/runner.py` returns 0
    - `grep -q "invoker.invoke_admin" backend/src/second_brain/eval/runner.py` returns 0
    - `! grep -q "classifier_client.get_response" backend/src/second_brain/eval/runner.py` returns 0
    - `! grep -q "admin_client.get_response" backend/src/second_brain/eval/runner.py` returns 0
    - `cd backend && uv run python -c "import second_brain.eval.runner"` exits 0
    - `! grep -q "class _MigrationHybridInvoker" backend/src/second_brain/eval/runner.py` returns 0 (hybrid stays in invoker.py per N-5 lock-in)
    - The hybrid composition strategy (single invoker arg, hybrid constructed at call-site from `eval/invoker.py`) is documented in a docstring/comment near the runner's signature
  </acceptance_criteria>
  <done>runner.py routes both classifier and admin through invoker; no direct client.get_response calls remain.</done>
</task>

<task type="auto">
  <name>Task 3: Wire eval/foundry.py admin call site to invoker</name>
  <files>backend/src/second_brain/eval/foundry.py</files>
  <read_first>
    - backend/src/second_brain/eval/foundry.py (lines 856-942 generate_app_mediated_dataset)
    - backend/src/second_brain/eval/invoker.py (created Task 1)
  </read_first>
  <action>
    Edit `backend/src/second_brain/eval/foundry.py`:

    1. Locate `generate_app_mediated_dataset` around lines 856-942.
    2. Find the `client.get_response(messages=..., options=...)` call (around line 892).
    3. Find the duck-typed extraction `if hasattr(response, "messages"):` (around lines 884-896).
    4. Replace the call with `await invoker.invoke_classifier(item["inputText"], classifier_tools)` (this dataset-gen path uses the classifier; preserve the existing classifier_tools instance and the side-effect read pattern).
    5. Update the function signature to accept `invoker: EvalAgentInvoker` (or reuse an existing parameter pattern matching runner.py).
    6. Update callers of `generate_app_mediated_dataset` to pass the invoker.
    7. The `hasattr(msg, "tool_calls")` duck-typed extraction is no longer needed because the eval results are captured by the side-effect on `classifier_tools.last_bucket` etc. (per EVAL-INVENTORY behavior contract). Delete the duck-typed extraction block.
    8. Verify: `cd backend && uv run python -c "from second_brain.eval.foundry import generate_app_mediated_dataset"`.
  </action>
  <verify>
    <automated>! grep -q "client.get_response" backend/src/second_brain/eval/foundry.py && ! grep -q 'hasattr(msg, "tool_calls")' backend/src/second_brain/eval/foundry.py && grep -q "invoker.invoke_classifier\\|invoker.invoke_admin" backend/src/second_brain/eval/foundry.py && cd backend && uv run python -c "import second_brain.eval.foundry"</automated>
  </verify>
  <acceptance_criteria>
    - `! grep -q "client.get_response" backend/src/second_brain/eval/foundry.py` returns 0
    - `! grep -q "hasattr(msg, \"tool_calls\")" backend/src/second_brain/eval/foundry.py` returns 0
    - `grep -q "invoker.invoke_classifier" backend/src/second_brain/eval/foundry.py` OR `grep -q "invoker.invoke_admin" backend/src/second_brain/eval/foundry.py` returns 0
    - `cd backend && uv run python -c "import second_brain.eval.foundry"` exits 0
    - `! grep -q "from agent_framework import ChatOptions, Message" backend/src/second_brain/eval/foundry.py` returns 0 (top-level RC imports gone — they live inside RCEvalAgentInvoker now)
  </acceptance_criteria>
  <done>generate_app_mediated_dataset routes through invoker. Duck-typed extraction replaced by side-effect read.</done>
</task>

</tasks>

<verification>
- eval/invoker.py exists with all three classes (Protocol + RC + GA)
- eval/runner.py classifier and admin call sites both routed through invoker
- eval/foundry.py admin call site routed through invoker
- All RC `client.get_response(...)` calls in eval/ are now confined to RCEvalAgentInvoker (with delayed local imports)
- The hybrid composition pattern (RC for classifier, GA for admin) documented; deletion trigger noted
</verification>

<success_criteria>
Plan 24-13 (Classifier kickoff) is unblocked. The eval gate suite can run with admin on GA + classifier on RC during the 23.2 window. Plan 24-18 (final cleanup) removes RCEvalAgentInvoker after classifier flips to GA in 24-13..24-17.
</success_criteria>

<output>
After completion, create `.planning/phases/24-foundry-ga-migration/24-12-SUMMARY.md` documenting:
- F-06 cleared (eval/runner.py)
- F-07 cleared (eval/foundry.py)
- EvalAgentInvoker facade live with both implementations
- Hybrid composition pattern (classifier→RC, admin→GA) documented
- D-07 explicit-justification block in invoker.py
- Per-destination flat-accuracy contract preserved (NOT widened to precision/recall)
- Deletion trigger: 24-18
</output>
