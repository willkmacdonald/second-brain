---
phase: 24-foundry-ga-migration
plan: 21
type: execute
wave: 5
depends_on: [20]
files_modified:
  - backend/src/second_brain/config.py
  - backend/src/second_brain/main.py
autonomous: true
requirements: [F-19, D-02, D-12]

must_haves:
  truths:
    - "azure_ai_classifier_agent_id removed from config.py Settings class"
    - "azure_ai_admin_agent_id removed from config.py Settings class"
    - "azure_ai_investigation_agent_id removed from config.py Settings class"
    - "All reads of these settings removed from main.py"
    - "azure_ai_project_endpoint and foundry_model REMAIN in config.py"
    - "Container App env vars are NOT touched in this plan (Step C runs in 24-23)"
  artifacts:
    - path: "backend/src/second_brain/config.py"
      provides: "Settings class with only GA-relevant fields"
    - path: "backend/src/second_brain/main.py"
      provides: "Lifespan with no orphan agent_id reads"
  key_links:
    - from: "config.py"
      to: "FoundryChatClient at lifespan"
      via: "azure_ai_project_endpoint + foundry_model"
      pattern: "settings.azure_ai_project_endpoint, settings.foundry_model"
---

<objective>
Final source-code cleanup commit. Per CONFIG-DELTAS Step C: this plan removes the three `azure_ai_*_agent_id` settings from config.py + any remaining reads in main.py. The Container App env vars (`AZURE_AI_*_AGENT_ID`) are NOT removed in this plan — that's a SEPARATE post-deploy step in plan 24-23, AFTER UAT confirms the GA revision is healthy.

The reason for the asymmetry: per CONFIG-DELTAS NEGATIVE assertion, removing the env vars BEFORE the GA image deploys would cause the still-running RC image to lose `settings.azure_ai_*_agent_id` reads (which fall through to creating BLANK portal agents — live agent drift). The env-var removal MUST wait until after the GA revision is healthy.

Code-side, the orphans are gone. Env-side, the GA image will see harmless leftover env vars from the RC era — they're just unread.

Two file edits:

1. `backend/src/second_brain/config.py` — delete the 3 fields.
2. `backend/src/second_brain/main.py` — grep for any remaining `settings.azure_ai_classifier_agent_id` / `_admin_agent_id` / `_investigation_agent_id` reads and delete them. Most should already be gone (24-04, 24-09, 24-14 removed lifespan-slice reads); this plan catches any leftover (e.g., dead code, unused local variable assignments).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-foundry-ga-migration/24-CONTEXT.md
@.planning/phases/23-foundry-ga-prep/CONFIG-DELTAS.md
@backend/src/second_brain/config.py
@backend/src/second_brain/main.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete agent_id orphan fields from config.py</name>
  <files>backend/src/second_brain/config.py</files>
  <read_first>
    - backend/src/second_brain/config.py (current Settings class)
    - .planning/phases/23-foundry-ga-prep/CONFIG-DELTAS.md (Step C — orphan cleanup)
  </read_first>
  <action>
    Edit `backend/src/second_brain/config.py`:

    1. Locate the Settings class.
    2. Delete these three lines:
       ```
       azure_ai_classifier_agent_id: str = ""
       azure_ai_admin_agent_id: str = ""
       azure_ai_investigation_agent_id: str = ""
       ```
    3. KEEP `azure_ai_project_endpoint: str = ""` (used by FoundryChatClient construction).
    4. KEEP `foundry_model: str = "gpt-4o"` (added in 24-02; used by FoundryChatClient model param).

    Verify Settings still imports cleanly: `cd backend && uv run python -c "from second_brain.config import settings; assert settings.azure_ai_project_endpoint is not None; assert settings.foundry_model == 'gpt-4o'; assert not hasattr(settings, 'azure_ai_classifier_agent_id')"`.
  </action>
  <verify>
    <automated>! grep -q "azure_ai_classifier_agent_id" backend/src/second_brain/config.py && ! grep -q "azure_ai_admin_agent_id" backend/src/second_brain/config.py && ! grep -q "azure_ai_investigation_agent_id" backend/src/second_brain/config.py && grep -q "azure_ai_project_endpoint" backend/src/second_brain/config.py && grep -q "foundry_model" backend/src/second_brain/config.py && cd backend && uv run python -c "from second_brain.config import settings; assert settings.foundry_model == 'gpt-4o'"</automated>
  </verify>
  <acceptance_criteria>
    - `! grep -q "azure_ai_classifier_agent_id" backend/src/second_brain/config.py` returns 0
    - `! grep -q "azure_ai_admin_agent_id" backend/src/second_brain/config.py` returns 0
    - `! grep -q "azure_ai_investigation_agent_id" backend/src/second_brain/config.py` returns 0
    - `grep -q "azure_ai_project_endpoint" backend/src/second_brain/config.py` returns 0 (preserved)
    - `grep -q "foundry_model" backend/src/second_brain/config.py` returns 0 (preserved)
    - `cd backend && uv run python -c "from second_brain.config import settings; assert settings.foundry_model == 'gpt-4o'"` exits 0
    - `cd backend && uv run python -c "from second_brain.config import settings; assert not hasattr(settings, 'azure_ai_classifier_agent_id')"` exits 0
  </acceptance_criteria>
  <done>Three orphan fields gone from config.py. GA-only settings remain.</done>
</task>

<task type="auto">
  <name>Task 2: Strip any remaining agent_id reads from main.py</name>
  <files>backend/src/second_brain/main.py</files>
  <read_first>
    - backend/src/second_brain/main.py
  </read_first>
  <action>
    1. Grep for remaining references: `grep -rn "azure_ai_classifier_agent_id\\|azure_ai_admin_agent_id\\|azure_ai_investigation_agent_id" backend/src/second_brain/`. Should be zero or near-zero (the lifespan-slice reads were removed in 24-04 / 24-09 / 24-14, but a dead local variable assignment may remain).
    2. For each reference: delete the line. If the variable is referenced elsewhere in the same function, also delete the dependent code.
    3. Verify main.py still imports: `cd backend && uv run python -c "import second_brain.main"`.
    4. Verify zero references in source: `! grep -rq "azure_ai_classifier_agent_id\\|azure_ai_admin_agent_id\\|azure_ai_investigation_agent_id" backend/src/second_brain/`.
  </action>
  <verify>
    <automated>! grep -rq "azure_ai_classifier_agent_id\\|azure_ai_admin_agent_id\\|azure_ai_investigation_agent_id" backend/src/second_brain/ && cd backend && uv run python -c "import second_brain.main"</automated>
  </verify>
  <acceptance_criteria>
    - `! grep -rq "azure_ai_classifier_agent_id" backend/src/second_brain/` returns 0
    - `! grep -rq "azure_ai_admin_agent_id" backend/src/second_brain/` returns 0
    - `! grep -rq "azure_ai_investigation_agent_id" backend/src/second_brain/` returns 0
    - `cd backend && uv run python -c "import second_brain.main"` exits 0
    - All 5 must_haves grep tests pass (one `-e` per pattern, plain string match — fixed N-8 regex escaping):
        - `! grep -r -e "AzureAIAgentClient" backend/src/second_brain/` returns 0
        - `! grep -r -e "agent_framework.azure" backend/src/second_brain/` returns 0
        - `! grep -r -e "@tool(approval_mode" backend/src/second_brain/` returns 0
        - `! grep -r -e "_agent_run" backend/src/second_brain/` returns 0
        - `! grep -r -e "foundryThreadId" backend/src/second_brain/` returns 0
  </acceptance_criteria>
  <done>main.py has no orphan agent_id reads. Codebase is fully GA-clean.</done>
</task>

</tasks>

<verification>
- 3 fields deleted from config.py
- Zero references to agent_id settings anywhere in backend/src/
- All previous calibration grep guards pass
- Container App env vars NOT touched (deferred to 24-23 Step C per CONFIG-DELTAS)
</verification>

<success_criteria>
Plan 24-22 (deploy) is unblocked. The next commit is the deploy push.
</success_criteria>

<output>
After completion, create `.planning/phases/24-foundry-ga-migration/24-21-SUMMARY.md` documenting:
- 3 config fields removed
- Zero orphan reads in main.py
- All grep guards pass (cumulative GA cleanliness check)
- Container App env-var removal deferred to 24-23 per NON-NEGOTIABLE assertion in CONFIG-DELTAS
</output>
