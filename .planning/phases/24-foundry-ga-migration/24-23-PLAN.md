---
phase: 24-foundry-ga-migration
plan: 23
type: execute
wave: 7
depends_on: [22]
files_modified:
  - .planning/phases/24-foundry-ga-migration/UAT-RESULTS.md
  - .planning/phases/24-foundry-ga-migration/24-FINAL-SUMMARY.md
autonomous: false
requirements: [D-12]

must_haves:
  truths:
    - "Day-after UAT executed against deployed GA build"
    - "10+ representative captures verified end-to-end (text + voice + low-confidence + admin routing + investigation chat)"
    - "Container App managed identity confirmed authenticating to Foundry (the only auth-related unknown that pre-deploy can't verify)"
    - "Step C executed: AZURE_AI_*_AGENT_ID env vars removed from Container App"
    - "7-day forced_tool_failure rate < 1% of captures and < 5/day (tracked via FORCED_TOOL_FAILURE_COUNT KQL)"
    - "Phase 24 final summary document captured"
  artifacts:
    - path: ".planning/phases/24-foundry-ga-migration/UAT-RESULTS.md"
      provides: "Day-after UAT outcomes (10+ captures + observability checks)"
    - path: ".planning/phases/24-foundry-ga-migration/24-FINAL-SUMMARY.md"
      provides: "Phase 24 closeout document"
  key_links:
    - from: "Step C az command"
      to: "Container App managed identity"
      via: "orphan env-var removal"
      pattern: "az containerapp update --remove-env-vars"
---

<objective>
Phase 24 closeout. Three sub-tasks across an extended timeframe:

1. **Day-after UAT (T+24h after 24-22 push):** Run 10+ representative captures (per CONTEXT post-deploy validation list). Confirm Container App managed identity authenticates to Foundry. Confirm logs show clean GA spans (`invoke_agent`, `execute_tool`). Document outcomes in UAT-RESULTS.md.

2. **Step C — Orphan env-var removal:** Per CONFIG-DELTAS Step C: AFTER UAT confirms GA revision stable, run `az containerapp update --remove-env-vars AZURE_AI_CLASSIFIER_AGENT_ID AZURE_AI_ADMIN_AGENT_ID AZURE_AI_INVESTIGATION_AGENT_ID`. This causes a new revision to start with the orphan vars absent. Safe because GA code does not reference them.

3. **7-day forced_tool_failure tracking:** Track via the FORCED_TOOL_FAILURE_COUNT KQL template (added in 24-18). Per CONTEXT post-deploy validation: rate must be < 1% of captures and < 5/day. NOT gate-blocking but tracked. If the rate exceeds threshold, file a follow-up to investigate.

This plan is checkpoint-style with TWO checkpoints (one for day-after UAT, one for Step C after UAT passes).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-foundry-ga-migration/24-CONTEXT.md
@.planning/phases/23-foundry-ga-prep/CONFIG-DELTAS.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    GA revision is live in production (since 24-22 push). 24-hour soak period before formal UAT. The day-after UAT is the human-verify checkpoint that confirms the system actually works end-to-end against real captures.
  </what-built>
  <how-to-verify>
    Wait at least 24 hours after the 24-22 deploy push. Then execute UAT.

    Document each step's result in `.planning/phases/24-foundry-ga-migration/UAT-RESULTS.md` with the format:
    ```
    | UAT step | Status | Evidence |
    |----------|--------|----------|
    | text capture (admin) | PASS | trace_id abc123 |
    | voice capture | PASS | trace_id def456, transcription completed |
    ...
    ```

    **UAT step 1 — Text captures (4 captures):**
    From mobile app or curl, send 4 text captures covering:
    - Admin (errand): "Pick up dry cleaning tomorrow"
    - Admin (recipe): "Make pasta tonight https://example.com/recipe"
    - Project: "Add OAuth login to second brain"
    - Personal note: "Sarah's birthday is March 12"
    
    For each: confirm SSE stream emits `CLASSIFIED` event with the right bucket. Confirm Inbox row created with `sessionId` field set (not `foundryThreadId`).

    **UAT step 2 — Voice captures (2 captures):**
    From mobile, send 2 voice captures (audio blob URL provided by mobile):
    - Voice admin: "Schedule a dentist appointment"
    - Voice misunderstood (deliberate gibberish or empty audio)
    
    For each: confirm transcription succeeded (or transcription_failed sub_code emitted). Confirm classifier received text-only input. Confirm SSE event sequence completes.

    **UAT step 3 — Low-confidence + follow-up (2 captures):**
    Send 2 ambiguous captures designed to trigger `LOW_CONFIDENCE`:
    - "Maybe think about that thing"
    - "TBD task"
    
    For each: confirm SSE emits `LOW_CONFIDENCE`. Trigger the follow-up flow (mobile prompts user; respond with clarification). Confirm `agent.run(session=...)` rehydrates via `sessionId`.

    **UAT step 4 — Admin routing (2 captures via errands screen):**
    Open the errands screen (triggers admin processing). Confirm 2+ inbox items are routed correctly:
    - Errand items land in errand destination
    - Task items land in task destination
    
    Confirm `_output_tool_called` correctly identifies the tool fired (post-hoc message inspection).

    **UAT step 5 — Investigation chat (2 questions):**
    Open mobile investigation chat (or use `/investigate` slash command). Ask:
    - "What's the system health?"
    - "Show me the last 5 errors"
    
    Confirm SSE streams text deltas; tool calls (`recent_errors`, `system_health`) execute; final answer renders. `sessionId` rehydration on follow-up.

    **UAT step 6 — Observability spot checks:**
    
    Query App Insights for the GA-emitted span Names:
    ```
    az monitor app-insights query --app second-brain-insights --analytics-query \
      "AppDependencies | where TimeGenerated > ago(2h) | where Name == 'invoke_agent' or Name == 'execute_tool' | take 20" \
      --resource-group shared-services-rg -o table
    ```
    Confirm both `invoke_agent` and `execute_tool` rows appear (i.e., GA framework spans flowing).

    Query for `capture.trace_id` propagation:
    ```
    az monitor app-insights query --app second-brain-insights --analytics-query \
      "AppDependencies | where TimeGenerated > ago(2h) | where Name == 'invoke_agent' | extend trace_id=tostring(Properties['capture.trace_id']) | where isnotempty(trace_id) | take 10"
    ```
    Confirm capture.trace_id is set on framework spans (i.e., CaptureTraceAgentMiddleware works).

    **UAT step 7 — Managed identity auth verification:**
    
    The Container App managed identity is the production auth path that the laptop's auth_probe (5) couldn't simulate. Confirm logs show no auth-related errors:
    ```
    az containerapp logs show -n second-brain-api -g shared-services-rg --tail 200 | grep -i "credential\|token\|auth\|forbidden\|unauthorized"
    ```
    No FATAL / ERROR auth-related lines should appear.

    **All UAT steps PASS:**
    Type "approved" to proceed to Step C (orphan env-var removal).

    **Any UAT step FAIL:**
    Type "block: <reason>". Decide whether to roll back (revision-promote-back to RC + git revert) or fix-forward (file follow-up plan to address the specific issue).
  </how-to-verify>
  <resume-signal>
    Type "approved" if all 7 UAT steps PASS.
    Type "block: <reason>" or "rollback" if a UAT step fails irrecoverably.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 2: Step C — Remove orphan AZURE_AI_*_AGENT_ID env vars</name>
  <files>(no source files; az CLI invocation)</files>
  <read_first>
    - .planning/phases/23-foundry-ga-prep/CONFIG-DELTAS.md (Step C exact command)
    - .planning/phases/24-foundry-ga-migration/UAT-RESULTS.md (UAT must show all PASS)
  </read_first>
  <action>
    Execute the Step C command (verbatim from CONFIG-DELTAS):
    ```
    az containerapp update \
      --name second-brain-api \
      --resource-group shared-services-rg \
      --remove-env-vars \
        AZURE_AI_CLASSIFIER_AGENT_ID \
        AZURE_AI_ADMIN_AGENT_ID \
        AZURE_AI_INVESTIGATION_AGENT_ID
    ```

    This causes a new revision to start with the orphan vars absent. The GA code (already deployed) does not reference them, so this is safe.

    Verify the env vars are gone:
    ```
    az containerapp show -n second-brain-api -g shared-services-rg \
      --query "properties.template.containers[0].env[?name=='AZURE_AI_CLASSIFIER_AGENT_ID' || name=='AZURE_AI_ADMIN_AGENT_ID' || name=='AZURE_AI_INVESTIGATION_AGENT_ID']" -o json
    ```
    Output must be `[]` (empty list).

    Confirm new revision is healthy:
    ```
    az containerapp revision list -n second-brain-api -g shared-services-rg \
      --query "[?properties.active].{name:name, healthState:properties.healthState}" -o table
    ```
    Plus `curl -s https://brain.willmacdonald.com/api/health` returns 200.

    Note in UAT-RESULTS.md: "Step C executed at <timestamp>; orphan env vars removed; revision <name> active and healthy."

    The portal-side agent records (`asst_Fnjkq5RVrvdFIOSqbreAwxuq` Classifier, the Admin agent, `asst_5feSWWTMA8rBSUyQo6aSCsEJ` Investigation) are now fully unreferenced. Per CONFIG-DELTAS: leave them as orphaned-but-harmless OR delete manually via Foundry portal. Document choice in 24-FINAL-SUMMARY.md.
  </action>
  <verify>
    <automated>[ "$(az containerapp show -n second-brain-api -g shared-services-rg --query "properties.template.containers[0].env[?name=='AZURE_AI_CLASSIFIER_AGENT_ID']" -o json)" = "[]" ]</automated>
  </verify>
  <acceptance_criteria>
    - `AZURE_AI_CLASSIFIER_AGENT_ID` absent from Container App env
    - `AZURE_AI_ADMIN_AGENT_ID` absent
    - `AZURE_AI_INVESTIGATION_AGENT_ID` absent
    - New revision healthState = "Healthy"
    - `/api/health` returns 200
    - UAT-RESULTS.md updated with Step C row
  </acceptance_criteria>
  <done>Orphan env vars gone. Container App matches the GA code's expected env shape exactly.</done>
</task>

<task type="auto">
  <name>Task 3: Final summary + 7-day forced_tool_failure tracking setup</name>
  <files>.planning/phases/24-foundry-ga-migration/24-FINAL-SUMMARY.md</files>
  <read_first>
    - .planning/phases/24-foundry-ga-migration/UAT-RESULTS.md
    - .planning/phases/24-foundry-ga-migration/FRAMEWORK-FIDELITY-cumulative.md (from 24-20)
    - .planning/phases/24-foundry-ga-migration/PRE-DEPLOY-GATE-RESULTS.md (from 24-20)
  </read_first>
  <action>
    Create `.planning/phases/24-foundry-ga-migration/24-FINAL-SUMMARY.md` documenting:

    ```
    # Phase 24 Foundry GA Migration — Final Summary

    **Deployed:** <date of 24-22 push>
    **UAT completed:** <date of 24-23 UAT approval>
    **Step C (orphan removal):** <date of 24-23 Step C completion>

    ## What shipped
    - All 3 agents (Investigation, Admin, Classifier) on GA agent-framework + agent-framework-foundry
    - Voice path split: classifier registers ONLY file_capture; transcription is direct call
    - tool_choice='required' enforced; Python safety net deleted
    - forced_tool_failure SSE sub-code introduced
    - Capture-trace middleware tags framework invoke_agent / execute_tool spans
    - InboxDocument.foundryThreadId renamed to sessionId
    - EvalAgentInvoker facade introduced and removed within Phase 24
    - 19 calibration F-## findings discharged; W-01 narrowed
    - Container App env vars updated (Step A→B→C three-step deploy)

    ## Pre-deploy gates summary
    (Reference PRE-DEPLOY-GATE-RESULTS.md)

    ## UAT outcomes
    (Reference UAT-RESULTS.md)

    ## Forced tool failure rate (7-day window)

    Add a TODO row that tracks the 7-day forced_tool_failure rate:

    Run daily for 7 days post-deploy:
    ```
    az monitor app-insights query --app second-brain-insights --analytics-query \
      "AppTraces | where TimeGenerated > ago(24h) | where SeverityLevel >= 3 | where tostring(Properties.sub_code) == 'forced_tool_failure' | summarize count = count()" \
      --resource-group shared-services-rg
    ```
    Total over 7 days < 5/day on average AND < 1% of total captures.

    | Day | Date | Forced fails | Total captures | Rate |
    |-----|------|--------------|----------------|------|
    | 1   | <date> | <count>    | <total>        | <%>  |
    | 2   |        |             |                 |       |
    ...

    ## Follow-ups generated
    - fetch_agent_runs property projection update (queries.py: rename agent_id/run_id/foundry_thread_id projections to GA semantic-convention names like agent.name, gen_ai.usage.*) — post-deploy follow-up
    - Optional: rename `GAEvalAgentInvoker` -> `EvalAgentInvoker` (drop GA prefix; collapse Protocol)
    - Per-destination precision/recall metric expansion in eval/metrics.py — separate phase
    - `'mode'` dict schema spike for tool_choice provider-dict pinning — separate phase
    - Delete backend/scripts/foundry_probe.py — defer 7 days post-deploy
    - Foundry-native eval cutover (Phase 21.1 follow-up phase)

    ## Portal-side cleanup
    - Old portal agents: asst_Fnjkq5RVrvdFIOSqbreAwxuq (Classifier), <admin agent ID>, asst_5feSWWTMA8rBSUyQo6aSCsEJ (Investigation) — orphaned-but-harmless. Decision: <leave / delete manually>.
    ```

    Verify the file is created.
  </action>
  <verify>
    <automated>test -f .planning/phases/24-foundry-ga-migration/24-FINAL-SUMMARY.md && grep -q "Phase 24 Foundry GA Migration" .planning/phases/24-foundry-ga-migration/24-FINAL-SUMMARY.md && grep -q "Forced tool failure rate" .planning/phases/24-foundry-ga-migration/24-FINAL-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/phases/24-foundry-ga-migration/24-FINAL-SUMMARY.md` returns 0
    - `grep -q "Phase 24 Foundry GA Migration" .planning/phases/24-foundry-ga-migration/24-FINAL-SUMMARY.md` returns 0
    - `grep -q "tool_choice='required'" .planning/phases/24-foundry-ga-migration/24-FINAL-SUMMARY.md` returns 0
    - `grep -q "Forced tool failure rate" .planning/phases/24-foundry-ga-migration/24-FINAL-SUMMARY.md` returns 0
    - `grep -q "Follow-ups generated" .planning/phases/24-foundry-ga-migration/24-FINAL-SUMMARY.md` returns 0
    - The Forced tool failure rate table has a header and at least Day 1 row
    - Portal-side cleanup decision is documented
  </acceptance_criteria>
  <done>Phase 24 closeout document captured. 7-day forced_tool_failure tracking schedule documented.</done>
</task>

</tasks>

<verification>
- UAT-RESULTS.md shows 7/7 UAT steps PASS
- Step C executed; orphan env vars gone
- New revision healthy
- 24-FINAL-SUMMARY.md captures the outcome + follow-ups + 7-day tracking schedule
</verification>

<success_criteria>
Phase 24 complete. Production runs cleanly on GA. Orphan env vars removed. 7-day soak monitoring in place.
</success_criteria>

<output>
After completion, create `.planning/phases/24-foundry-ga-migration/24-23-SUMMARY.md` documenting:
- UAT outcome
- Step C completion
- Final summary written
- Follow-ups filed
- Phase 24 SHIPPED
</output>
