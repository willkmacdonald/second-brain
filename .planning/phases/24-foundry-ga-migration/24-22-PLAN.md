---
phase: 24-foundry-ga-migration
plan: 22
type: execute
wave: 6
depends_on: [21]
files_modified:
  - .planning/phases/24-foundry-ga-migration/PUSH-GUARD-ACTIVE
autonomous: false
requirements: [D-12]

must_haves:
  truths:
    - "Container App env vars FOUNDRY_MODEL, ENABLE_INSTRUMENTATION, ENABLE_SENSITIVE_DATA set on RC revision (Step A)"
    - "RC revision verified healthy AFTER Step A and BEFORE git push"
    - "AZURE_AI_*_AGENT_ID env vars STILL present (NOT REMOVED at this step — Step C in 24-23)"
    - "PUSH-GUARD-ACTIVE sentinel deleted"
    - "git push origin main executed (Step B); GitHub Actions OIDC builds GA image; Container Apps creates GA revision"
    - "GA revision verified healthy after push"
  artifacts:
    - path: ".planning/phases/24-foundry-ga-migration/PUSH-GUARD-ACTIVE"
      provides: "DELETED — push guard removed"
  key_links:
    - from: "git push origin main"
      to: "GitHub Actions CI/CD -> ACR -> Container Apps GA revision"
      via: "OIDC workload identity"
      pattern: "GA revision created"
---

<objective>
The deploy event. Three sequenced sub-steps:

**Step A — Set GA env vars on RC revision (BEFORE push):**
Per CONFIG-DELTAS: `az containerapp update --set-env-vars FOUNDRY_MODEL=gpt-4o ENABLE_INSTRUMENTATION=true ENABLE_SENSITIVE_DATA=false`. The RC code ignores these (they're not referenced in RC main.py). Verify the RC revision still healthy after the update with the OLD env vars (AZURE_AI_*_AGENT_ID) STILL PRESENT — this is the NON-NEGOTIABLE per CONFIG-DELTAS NEGATIVE assertion.

**Unguard step:**
Delete `.planning/phases/24-foundry-ga-migration/PUSH-GUARD-ACTIVE` so the pre-push hook stops blocking. This deletion can be either a separate commit OR included in the next commit before push — but it MUST be committed (the worktree state with sentinel still present would block push).

**Step B — git push origin main:**
Execute the push. This triggers GitHub Actions OIDC workflow, which builds the GA image with the updated `pyproject.toml` + `uv.lock` from 24-02, pushes to ACR, and Container Apps creates a new GA revision. The GA revision starts up reading FOUNDRY_MODEL / ENABLE_INSTRUMENTATION / ENABLE_SENSITIVE_DATA env vars (already set via Step A) plus the harmless leftover AZURE_AI_*_AGENT_ID env vars (which the GA code does not reference).

Verify the GA revision is healthy after push:
- `az containerapp revision list -n second-brain-api -g shared-services-rg --query "[?properties.active]"` shows the new revision active.
- `curl -s -H "X-API-Key: $API_KEY" https://brain.willmacdonald.com/api/health` returns 200.
- `az containerapp logs show -n second-brain-api -g shared-services-rg --tail 50` shows clean startup (no FATAL errors, FoundryChatClient initialized).

This plan is checkpoint-style — the human operator confirms each step's success before moving on. Step A failure triggers rollback (revert env-var update). Step B failure (post-push) triggers the rollback paths from CONTEXT (revision-promote-back primary, git revert durable).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-foundry-ga-migration/24-CONTEXT.md
@.planning/phases/23-foundry-ga-prep/CONFIG-DELTAS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Step A — Set GA env vars on RC revision</name>
  <files>(no source files; az CLI invocation)</files>
  <read_first>
    - .planning/phases/23-foundry-ga-prep/CONFIG-DELTAS.md (Step A exact command)
  </read_first>
  <action>
    Execute the Step A command (verbatim from CONFIG-DELTAS):
    ```
    az containerapp update \
      --name second-brain-api \
      --resource-group shared-services-rg \
      --set-env-vars \
        FOUNDRY_MODEL=gpt-4o \
        ENABLE_INSTRUMENTATION=true \
        ENABLE_SENSITIVE_DATA=false
    ```

    NOTE: do NOT remove AZURE_AI_*_AGENT_ID at this step (NEGATIVE assertion).

    Verify the new env vars are present alongside the old:
    ```
    az containerapp show -n second-brain-api -g shared-services-rg \
      --query "properties.template.containers[0].env[?name=='AZURE_AI_CLASSIFIER_AGENT_ID' || name=='FOUNDRY_MODEL' || name=='ENABLE_INSTRUMENTATION' || name=='ENABLE_SENSITIVE_DATA']" \
      -o table
    ```
    Output must show all 4 vars (3 new + AZURE_AI_CLASSIFIER_AGENT_ID still present).

    Verify the RC revision is healthy:
    ```
    az containerapp revision list -n second-brain-api -g shared-services-rg \
      --query "[?properties.active].{name:name, healthState:properties.healthState}" -o table
    ```
    The active revision should be the RC revision in `Healthy` state.

    `curl -s https://brain.willmacdonald.com/api/health` returns 200.
  </action>
  <verify>
    <automated>az containerapp show -n second-brain-api -g shared-services-rg --query "properties.template.containers[0].env[?name=='FOUNDRY_MODEL'].value" -o tsv | grep -q "gpt-4o" && az containerapp show -n second-brain-api -g shared-services-rg --query "properties.template.containers[0].env[?name=='AZURE_AI_CLASSIFIER_AGENT_ID']" -o tsv | grep -q "."</automated>
  </verify>
  <acceptance_criteria>
    - `FOUNDRY_MODEL=gpt-4o` set on Container App
    - `ENABLE_INSTRUMENTATION=true` set
    - `ENABLE_SENSITIVE_DATA=false` set
    - `AZURE_AI_CLASSIFIER_AGENT_ID` STILL present (NOT removed)
    - `AZURE_AI_ADMIN_AGENT_ID` STILL present
    - `AZURE_AI_INVESTIGATION_AGENT_ID` STILL present
    - RC revision healthState = "Healthy"
    - `/api/health` returns 200
  </acceptance_criteria>
  <done>Step A complete. GA env vars in place; RC revision healthy and serving traffic.</done>
</task>

<task type="auto">
  <name>Task 2: Unguard — delete PUSH-GUARD-ACTIVE sentinel</name>
  <files>.planning/phases/24-foundry-ga-migration/PUSH-GUARD-ACTIVE</files>
  <read_first>
    - .planning/phases/24-foundry-ga-migration/PUSH-GUARD-ACTIVE (sentinel about to be deleted)
    - .git/hooks/pre-push (hook that tests for the sentinel)
  </read_first>
  <action>
    1. Confirm Step A succeeded (Task 1 verify passed).
    2. Delete the sentinel: `git rm .planning/phases/24-foundry-ga-migration/PUSH-GUARD-ACTIVE`.
    3. Commit the deletion:
       ```
       git commit -m "docs(24-22): remove push guard — pre-deploy gates passed, ready for GA push"
       ```
    4. Verify the hook now allows push (run a dry-run): `git push --dry-run origin main 2>&1 | tail -5`. The hook should exit 0 (no "Push BLOCKED" message).
  </action>
  <verify>
    <automated>! test -f .planning/phases/24-foundry-ga-migration/PUSH-GUARD-ACTIVE && bash .git/hooks/pre-push 2>&1 | grep -qv "Push BLOCKED"</automated>
  </verify>
  <acceptance_criteria>
    - `! test -f .planning/phases/24-foundry-ga-migration/PUSH-GUARD-ACTIVE` returns 0
    - `bash .git/hooks/pre-push` exits 0 (no BLOCKED message)
    - The deletion commit is committed to local main
    - `git status` shows clean working tree (no uncommitted changes)
  </acceptance_criteria>
  <done>Push guard removed. Local main is push-ready.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    All Phase 24 commits are on local main. Step A env vars in place on RC revision. Push guard removed. The next action is `git push origin main` — the deploy event.
  </what-built>
  <how-to-verify>
    1. Confirm one final time:
       - `git log --oneline -25` shows all Phase 24 commits in order
       - `git status` is clean
       - `! test -f .planning/phases/24-foundry-ga-migration/PUSH-GUARD-ACTIVE` returns 0
       - Step A env vars confirmed via `az containerapp show ...`

    2. Execute the push:
       ```
       git push origin main
       ```

    3. Watch GitHub Actions OIDC workflow:
       ```
       gh run list --branch main --limit 1
       gh run watch
       ```
       The workflow:
       - Resolves OIDC credentials via Workload Identity Federation
       - Builds GA image (uses pyproject.toml + uv.lock from 24-02)
       - Pushes to ACR
       - Triggers Container Apps revision

    4. Wait for the GA revision to become active:
       ```
       az containerapp revision list -n second-brain-api -g shared-services-rg \
         --query "[?properties.active].{name:name, healthState:properties.healthState, traffic:properties.trafficWeight}" -o table
       ```
       The new revision should be active and `Healthy`.

    5. Verify GA revision serves cleanly:
       ```
       curl -s -H "X-API-Key: $API_KEY" https://brain.willmacdonald.com/api/health | jq '.'
       az containerapp logs show -n second-brain-api -g shared-services-rg --tail 100
       ```
       Logs should show "FoundryChatClient initialized: endpoint=... model=gpt-4o" — NO ImportError, NO traceback.

    6. (Optional) Test a real capture from the laptop or mobile to confirm end-to-end:
       ```
       curl -s -H "X-API-Key: $API_KEY" -H "Content-Type: application/json" \
         -d '{"text": "Phase 24 deploy smoke test"}' https://brain.willmacdonald.com/api/capture
       ```
       Should return SSE events including a `CLASSIFIED` event.

    **All steps PASS:**
    Type "approved" to mark deploy success and proceed to plan 24-23 (post-deploy UAT + Step C orphan removal).

    **Any step FAIL:**
    Type "rollback: <reason>". Execute the rollback path from CONTEXT:
    - Fast: `az containerapp ingress traffic --name second-brain-api -g shared-services-rg --revision-weight {prev_rc_revision}=100 {new_ga_revision}=0`
    - Durable: `git revert HEAD~N..HEAD; git push origin main` (count of Phase 24 commits)
  </how-to-verify>
  <resume-signal>
    Type "approved" if GA revision is healthy and a smoke-test capture works.
    Type "rollback: <reason>" if any verification step failed.
  </resume-signal>
</task>

</tasks>

<verification>
- Step A env vars in place (3 new + 3 old still present)
- Push guard sentinel deleted
- git push origin main executed; CI/CD created GA revision
- GA revision active and Healthy
- /api/health 200; logs clean
- Smoke-test capture exercises classifier successfully
</verification>

<success_criteria>
Production runs on GA SDK. Plan 24-23 (post-deploy UAT + Step C orphan removal) is unblocked.
</success_criteria>

<output>
After completion, create `.planning/phases/24-foundry-ga-migration/24-22-SUMMARY.md` documenting:
- Step A env vars set
- Push guard removed
- git push success; GA revision active
- Smoke-test capture result
- Path forward: 24-23 day-after UAT + Step C
</output>
