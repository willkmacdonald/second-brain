---
phase: 24-foundry-ga-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .git/hooks/pre-push
  - .planning/phases/24-foundry-ga-migration/PUSH-GUARD-ACTIVE
autonomous: true
requirements: [D-12]

must_haves:
  truths:
    - "Push to origin is blocked while sentinel file exists"
    - "Sentinel file is committed under .planning/phases/24-foundry-ga-migration/"
    - "Pre-push hook exits 1 with clear error when sentinel present"
  artifacts:
    - path: ".git/hooks/pre-push"
      provides: "Push guard mechanism (executable shell script)"
      contains: "PUSH-GUARD-ACTIVE"
    - path: ".planning/phases/24-foundry-ga-migration/PUSH-GUARD-ACTIVE"
      provides: "Sentinel file presence triggers push block"
  key_links:
    - from: ".git/hooks/pre-push"
      to: ".planning/phases/24-foundry-ga-migration/PUSH-GUARD-ACTIVE"
      via: "filesystem test -f"
      pattern: "if \\[ -f \"\\$SENTINEL\" \\]"
---

<objective>
Install the Phase 24 push guard before any agent migration commits land. This prevents accidental `git push origin main` during the broken intermediate window when local main is mid-migration (after task group 23.1 commits but before 23.3 final cleanup makes main buildable again). Pushing during this window would trigger CI/CD and deploy a broken image to production.

Purpose: Per CONTEXT D-12, all 23.1 → 23.2 → 23.3 commits accumulate sequentially on local main. The repo is intentionally not buildable for a window of ~15-20 commits. The push guard is the safety net.

Output: Executable `.git/hooks/pre-push` shell script + sentinel file `.planning/phases/24-foundry-ga-migration/PUSH-GUARD-ACTIVE`. The sentinel is removed at the very end of Phase 24 (final unguard plan, plan 24-23) immediately before the deploy push.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-foundry-ga-migration/24-CONTEXT.md
@.planning/phases/24-foundry-ga-migration/24-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create PUSH-GUARD-ACTIVE sentinel file</name>
  <files>.planning/phases/24-foundry-ga-migration/PUSH-GUARD-ACTIVE</files>
  <read_first>
    - .planning/phases/24-foundry-ga-migration/24-CONTEXT.md (D-12 sequential commits decision)
    - .planning/phases/24-foundry-ga-migration/24-PATTERNS.md (S-7 Push guard sentinel pattern)
  </read_first>
  <action>
    Create the sentinel file at exactly `.planning/phases/24-foundry-ga-migration/PUSH-GUARD-ACTIVE` with the following body content:

    ```
    Phase 24 Foundry GA Migration in progress.

    This sentinel blocks `git push origin main` via .git/hooks/pre-push.

    Removal trigger: end of Phase 24 task group 23.3 final unguard plan (24-23-PLAN.md),
    AFTER all pre-deploy gates pass and immediately BEFORE the deploy push.

    Removal command: `rm .planning/phases/24-foundry-ga-migration/PUSH-GUARD-ACTIVE`

    Per CONTEXT D-12: 23.1 (Investigation) -> 23.2 (Admin) -> 23.3 (Classifier) commits
    accumulate sequentially on local `main`. Local `main` is intentionally not buildable
    for the duration of those task groups. Pushing during that window would trigger
    CI/CD and deploy a broken image to production.
    ```
  </action>
  <verify>
    <automated>test -f .planning/phases/24-foundry-ga-migration/PUSH-GUARD-ACTIVE && grep -q "Phase 24 Foundry GA Migration in progress" .planning/phases/24-foundry-ga-migration/PUSH-GUARD-ACTIVE</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/phases/24-foundry-ga-migration/PUSH-GUARD-ACTIVE` returns 0
    - `grep -q "Phase 24 Foundry GA Migration in progress" .planning/phases/24-foundry-ga-migration/PUSH-GUARD-ACTIVE` returns 0
    - `grep -q "rm .planning/phases/24-foundry-ga-migration/PUSH-GUARD-ACTIVE" .planning/phases/24-foundry-ga-migration/PUSH-GUARD-ACTIVE` returns 0
  </acceptance_criteria>
  <done>Sentinel file created with the required body content; ready for the hook to test against.</done>
</task>

<task type="auto">
  <name>Task 2: Install pre-push hook that blocks while sentinel exists</name>
  <files>.git/hooks/pre-push</files>
  <read_first>
    - .planning/phases/24-foundry-ga-migration/24-PATTERNS.md (S-7 — exact shell-script template)
    - .planning/phases/24-foundry-ga-migration/PUSH-GUARD-ACTIVE (created in Task 1)
  </read_first>
  <action>
    Create `.git/hooks/pre-push` with the following exact body (no trailing whitespace differences):

    ```bash
    #!/usr/bin/env bash
    # Phase 24 Foundry GA Migration push guard.
    # Removed by 24-23-PLAN.md final unguard step.
    SENTINEL=".planning/phases/24-foundry-ga-migration/PUSH-GUARD-ACTIVE"
    if [ -f "$SENTINEL" ]; then
        echo "" >&2
        echo "Push BLOCKED: PUSH-GUARD-ACTIVE sentinel present at $SENTINEL." >&2
        echo "" >&2
        echo "Phase 24 Foundry GA Migration is in progress." >&2
        echo "Local 'main' is intentionally not buildable until task group 23.3 completes." >&2
        echo "Pushing now would deploy a broken image to production via CI/CD." >&2
        echo "" >&2
        echo "To unguard (only at end of Phase 24, after pre-deploy gates pass):" >&2
        echo "  rm $SENTINEL" >&2
        echo "" >&2
        exit 1
    fi
    exit 0
    ```

    Make the hook executable: `chmod +x .git/hooks/pre-push`

    Verify it blocks by running a dry-run push attempt:
    `git push --dry-run origin main 2>&1 | grep -q "Push BLOCKED"`
    (this runs the hook locally; the dry-run should still trigger the pre-push hook execution)
  </action>
  <verify>
    <automated>test -x .git/hooks/pre-push && grep -q "PUSH-GUARD-ACTIVE" .git/hooks/pre-push && bash .git/hooks/pre-push 2>&1 | grep -q "Push BLOCKED" && echo "GUARD ACTIVE"</automated>
  </verify>
  <acceptance_criteria>
    - `test -x .git/hooks/pre-push` returns 0 (file exists and is executable)
    - `grep -q "PUSH-GUARD-ACTIVE" .git/hooks/pre-push` returns 0
    - `grep -q "exit 1" .git/hooks/pre-push` returns 0
    - Running `bash .git/hooks/pre-push` while sentinel exists writes "Push BLOCKED" to stderr and exits with code 1
    - The hook references the EXACT sentinel path `.planning/phases/24-foundry-ga-migration/PUSH-GUARD-ACTIVE` (not `.planning/phases/24/...`)
  </acceptance_criteria>
  <done>Pre-push hook installed, executable, and verified to block push while sentinel exists. Phase 24 commits can now safely accumulate on local main.</done>
</task>

</tasks>

<verification>
- The sentinel file exists at the exact path
- The pre-push hook is executable and contains the sentinel-test logic
- A direct invocation of the hook script writes "Push BLOCKED" and exits 1
- The hook would NOT block if the sentinel were removed (verifiable by manually moving the sentinel and re-running, then restoring)
</verification>

<success_criteria>
Push guard active. Any subsequent `git push origin main` attempt fails with the "Push BLOCKED" message. Phase 24 task groups 23.1 / 23.2 / 23.3 can now commit freely on local main without risk of accidental deploy.
</success_criteria>

<output>
After completion, create `.planning/phases/24-foundry-ga-migration/24-01-SUMMARY.md` documenting:
- Sentinel file path and contents
- Hook script path and exact content
- Verified behavior (BLOCKED message + exit 1)
- Removal trigger (24-23-PLAN.md, AFTER all pre-deploy gates pass)
</output>
