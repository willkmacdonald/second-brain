---
phase: 24-foundry-ga-migration
plan: 20
type: execute
wave: 5
depends_on: [19]
files_modified:
  - .planning/phases/24-foundry-ga-migration/FIDELITY-23.3.patch
  - .planning/phases/24-foundry-ga-migration/FRAMEWORK-FIDELITY-23.3.md
  - .planning/phases/24-foundry-ga-migration/FIDELITY-cumulative.patch
  - .planning/phases/24-foundry-ga-migration/FRAMEWORK-FIDELITY-cumulative.md
  - .planning/phases/24-foundry-ga-migration/PRE-DEPLOY-GATE-RESULTS.md
autonomous: false
requirements: [D-06, D-07, D-12]

must_haves:
  truths:
    - "23.3 task-group cumulative diff captured"
    - "23.3 framework-fidelity audit produced; zero ❌ on 23.3 scope"
    - "Phase 24 cumulative audit (24-01..24-19) produced; zero ❌ overall"
    - "All unit tests pass (cd backend && uv run pytest -x)"
    - "All 18 golden-trace fixture replays pass"
    - "All 5 probe-fixture replays pass against local GA build"
    - "Custom admin + classifier eval suites within ±2pp of pre-migration baseline"
    - "auth_probe re-run from laptop succeeds"
    - "Container App managed identity verified to have Azure AI User role"
    - "Cosmos backfill script executed successfully (sessionId migration)"
  artifacts:
    - path: ".planning/phases/24-foundry-ga-migration/FIDELITY-23.3.patch"
      provides: "Captured cumulative diff for task group 23.3 (24-14..24-19)"
    - path: ".planning/phases/24-foundry-ga-migration/FRAMEWORK-FIDELITY-23.3.md"
      provides: "23.3 auditor report"
    - path: ".planning/phases/24-foundry-ga-migration/FIDELITY-cumulative.patch"
      provides: "Cumulative diff for entire Phase 24 (24-01..24-19)"
    - path: ".planning/phases/24-foundry-ga-migration/FRAMEWORK-FIDELITY-cumulative.md"
      provides: "Cumulative auditor report — must show zero ❌ overall"
    - path: ".planning/phases/24-foundry-ga-migration/PRE-DEPLOY-GATE-RESULTS.md"
      provides: "Tabulated pre-deploy gate outcomes"
---

<objective>
The pre-deploy gate runner. This plan blocks the deploy push (24-22) until ALL of the following pass:

1. **Task group 23.3 framework-fidelity audit** (zero ❌ on 23.3 scope).
2. **Cumulative framework-fidelity audit** for the entire Phase 24 diff (zero ❌ overall).
3. **Unit tests:** `cd backend && uv run pytest -x` exits 0.
4. **18 golden-trace fixture replays:** for each fixture in `backend/tests/fixtures/{investigation,admin,classifier}/*`, run a replay test and assert SSE event sequence + span tree match the documented expected-deltas.md.
5. **5 probe-fixture replays:** re-run the 5 probes against the real Foundry endpoint and diff the captured output against the committed fixture. Behavior matches.
6. **Eval gate:** Custom admin + classifier eval suites run end-to-end against the local GA build; scores within ±2pp of `tests/fixtures/eval-baseline-pre-migration.json`. No class-specific drop > 5pp.
7. **auth_probe re-run** from laptop succeeds (cheap, fail-loud per CONTEXT discretion).
8. **Container App managed identity RBAC** verified to have `Azure AI User` role; assigned via `az role assignment create` if missing.
9. **Cosmos backfill executed:** run `backend/scripts/backfill_inbox_session_id.py` once; confirm rows updated (or 0 rows if no in-flight follow-ups exist).

Output: `.planning/phases/24-foundry-ga-migration/PRE-DEPLOY-GATE-RESULTS.md` with each gate's pass/fail status and supporting artifacts.

This plan is checkpoint-style — the human operator confirms all gates pass before approving 24-22 (the deploy push).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-foundry-ga-migration/24-CONTEXT.md
@.planning/phases/23-foundry-ga-prep/AUDITOR-VERIFICATION.md
@.planning/phases/23-foundry-ga-prep/CONFIG-DELTAS.md
@.planning/phases/23-foundry-ga-prep/FOUNDRY-PROBE-FINDINGS.md
@.planning/phases/24-foundry-ga-migration/FRAMEWORK-FIDELITY-23.1.md
@.planning/phases/24-foundry-ga-migration/FRAMEWORK-FIDELITY-23.2.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture task group 23.3 + cumulative diffs</name>
  <files>.planning/phases/24-foundry-ga-migration/FIDELITY-23.3.patch, .planning/phases/24-foundry-ga-migration/FIDELITY-cumulative.patch</files>
  <read_first>
    - .planning/phases/23-foundry-ga-prep/AUDITOR-VERIFICATION.md
  </read_first>
  <action>
    1. Identify start SHA for 23.3: HEAD at end of 24-13 (the 23.2 audit completion).
    2. Identify start SHA for Phase 24: HEAD just before 24-01 (push guard install).
    3. Capture diffs:
       ```
       git diff $TG_23_3_START..HEAD -- backend/ \
         > .planning/phases/24-foundry-ga-migration/FIDELITY-23.3.patch
       git diff $PHASE_24_START..HEAD -- backend/ .planning/phases/24-foundry-ga-migration/ \
         > .planning/phases/24-foundry-ga-migration/FIDELITY-cumulative.patch
       ```
    4. Verify both patches non-empty.
  </action>
  <verify>
    <automated>test -s .planning/phases/24-foundry-ga-migration/FIDELITY-23.3.patch && test -s .planning/phases/24-foundry-ga-migration/FIDELITY-cumulative.patch</automated>
  </verify>
  <acceptance_criteria>
    - Both patches exist and are non-empty
    - FIDELITY-23.3.patch references classifier-surface files
    - FIDELITY-cumulative.patch references all 3 agent surfaces + middleware + eval/invoker.py
  </acceptance_criteria>
  <done>Diffs ready for auditor consumption.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 24 task groups 23.1, 23.2, 23.3 complete on local main. Codebase is GA-clean (zero RC imports under backend/src/second_brain/). Push guard still active. Pre-deploy gates need to run before deploy push.
  </what-built>
  <how-to-verify>
    Run each gate sequentially. Tabulate results in `.planning/phases/24-foundry-ga-migration/PRE-DEPLOY-GATE-RESULTS.md` with format:
    ```
    | Gate | Status | Evidence |
    |------|--------|----------|
    | 23.3 audit zero ❌ | PASS | path/to/FRAMEWORK-FIDELITY-23.3.md |
    | Cumulative audit zero ❌ | PASS | path/to/FRAMEWORK-FIDELITY-cumulative.md |
    | Unit tests | PASS | pytest output |
    ...
    ```

    **Gate 1 — 23.3 framework-fidelity audit:**
    Spawn `gsd-framework-fidelity-auditor` with task group ID "23.3", diff `.planning/phases/24-foundry-ga-migration/FIDELITY-23.3.patch`, output `.planning/phases/24-foundry-ga-migration/FRAMEWORK-FIDELITY-23.3.md`. Confirm zero ❌ on 23.3 scope. Expected closures: F-04, F-08 partial (classification + transcription + dry_run_tools), F-09, F-10, F-11, F-12 (Classifier slice), F-13, F-14, F-19 (classifier.md), F-02 (warmup), F-01 final (last main.py RC import gone), F-17 (middleware.py deleted), W-01 narrowed.

    **Gate 2 — Cumulative framework-fidelity audit:**
    Spawn auditor with cumulative scope, diff `.planning/phases/24-foundry-ga-migration/FIDELITY-cumulative.patch`, output `.planning/phases/24-foundry-ga-migration/FRAMEWORK-FIDELITY-cumulative.md`. Confirm zero ❌ across the entire Phase 24 diff (all 19 calibration F-## findings closed; W-01 narrowed). The cumulative report is the FINAL gate before push.

    **Gate 3 — Unit tests:**
    `cd backend && uv run pytest -x 2>&1 | tee .planning/phases/24-foundry-ga-migration/pytest-output.txt`. Confirm exit 0.

    **Gate 4 — 18 golden-trace fixture replays:**
    Each fixture under `backend/tests/fixtures/{investigation,admin,classifier}/` has `*.input.json`, `*.sse.jsonl`, `*.spans.json`, `*.expected-deltas.md`. For each: run a replay test that exercises the corresponding agent path and asserts the SSE event sequence + span tree match (within documented allowed deltas). If the replay test infrastructure already exists in `backend/tests/`, run it: `cd backend && uv run pytest tests/test_golden_traces.py -x`. If not, the fixture replay infrastructure must be built — that's a substantial sub-task; if missing, declare a partial fail and require a follow-up plan to build the infra.

    **Gate 5 — 5 probe-fixture replays:**
    Re-run each probe against the real Foundry endpoint:
    ```
    cd backend && for probe in streaming_shape tool_call_extraction tool_choice_required session_rehydration auth_probe; do
        uv run python -m scripts.foundry_probe $probe > /tmp/$probe.replay.json
        diff <(jq -S . backend/tests/fixtures/foundry-probe/$probe.json) <(jq -S . /tmp/$probe.replay.json)
    done
    ```
    Each diff should be empty (fixture matches replay). Save the results.

    **Gate 6 — Eval suite within ±2pp:**
    Run admin + classifier eval against the local GA build. Compare scores against `backend/tests/fixtures/eval-baseline-pre-migration.json`. Per-class drops > 5pp fail the gate. Per CONTEXT D-04 + EVAL-INVENTORY: per-destination flat-accuracy contract.

    **Gate 7 — auth_probe re-run:**
    Per CONTEXT discretion: cheap, fail-loud. Already covered by Gate 5 (auth_probe is one of the 5).

    **Gate 8 — Container App managed identity RBAC:**
    ```
    az role assignment list \
      --assignee $(az containerapp show -n second-brain-api -g shared-services-rg --query identity.principalId -o tsv) \
      --scope /subscriptions/$(az account show --query id -o tsv) \
      -o table | grep "Azure AI User"
    ```
    If missing, assign:
    ```
    az role assignment create \
      --assignee $(az containerapp show -n second-brain-api -g shared-services-rg --query identity.principalId -o tsv) \
      --role "Azure AI User" \
      --scope /subscriptions/$(az account show --query id -o tsv)
    ```

    **Gate 9 — Cosmos backfill:**
    ```
    cd backend && COSMOS_ENDPOINT=https://shared-services-cosmosdb.documents.azure.com:443/ \
      uv run python -m scripts.backfill_inbox_session_id
    ```
    Capture row count from the script output. 0 rows is OK (no in-flight migrations needed). Any error fails the gate.

    **All 9 gates PASS:**
    Type "approved" to unblock plan 24-21 (config orphan cleanup) and 24-22 (deploy).

    **Any gate FAIL:**
    Type "block: gate-N <reason>". Fix the issue (often via a small follow-up plan that amends back into 24-NN's work) and re-run the gate.
  </how-to-verify>
  <resume-signal>
    Type "approved" if all 9 gates PASS and PRE-DEPLOY-GATE-RESULTS.md is filled in.
    Type "block: <gate>" if any gate fails.
  </resume-signal>
</task>

</tasks>

<verification>
- All 9 gates documented in PRE-DEPLOY-GATE-RESULTS.md
- All gates PASS
- Cumulative auditor report at zero ❌
- Unit tests + golden replays + probe replays + eval suite + auth_probe + RBAC + backfill all green
</verification>

<success_criteria>
Pre-deploy gate runner approves the deploy. Plan 24-21 (config orphan cleanup) is unblocked.
</success_criteria>

<output>
After completion, create `.planning/phases/24-foundry-ga-migration/24-20-SUMMARY.md` documenting:
- Task group 23.3 audit closures
- Cumulative audit closures (all 19 F-## findings discharged)
- All 9 pre-deploy gates passed
- Backfill row count
- RBAC verification result
</output>
