---
phase: 24-foundry-ga-migration
plan: 05
type: execute
wave: 2
depends_on: [04]
files_modified:
  - backend/src/second_brain/tools/investigation.py
autonomous: true
requirements: [F-08, D-05, D-06]

must_haves:
  truths:
    - "All 9 @tool(approval_mode='never_require') decorators removed from tools/investigation.py"
    - "Annotated[..., Field(description=...)] parameter shape preserved on every tool method"
    - "Docstrings preserved on every tool method"
    - "InvestigationTools class still has __init__(cosmos_manager, ...) Cosmos DI"
    - "All 9 tool methods are still bound methods on InvestigationTools"
  artifacts:
    - path: "backend/src/second_brain/tools/investigation.py"
      provides: "9 GA-shaped tool methods (decorator-free, Annotated params)"
      contains: "class InvestigationTools"
  key_links:
    - from: "tools/investigation.py"
      to: "main.py investigation_agent tools list"
      via: "bound method registration"
      pattern: "investigation_tools.{tool_name}"
---

<objective>
Strip the RC `@tool(approval_mode="never_require")` decorator from all 9 InvestigationTools methods. The decorator's `approval_mode` kwarg is RC-only — GA passes the bound method directly to `Agent(tools=[...])`. The Annotated[...] + Field(description=...) parameter shape and the docstrings stay (those are the GA-recommended way to describe tool inputs and the tool itself).

Per CONTEXT D-05/D-06: the InvestigationTools class shape is unchanged. The `__init__` signature for Cosmos DI stays. Tests that mock `cosmos_manager` continue to work without modification.

Per the calibration F-08 finding, this strip applies to 9 method decorations at lines 112, 179, 250, 297, 372, 485, 594, 663, 743 (per the calibration line numbers). The exact line numbers may have shifted since calibration; the executor should grep for `@tool(approval_mode=` and remove every match in this file.

Output: tools/investigation.py with zero `@tool(approval_mode=` patterns. Tests still pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-foundry-ga-migration/24-CONTEXT.md
@.planning/phases/24-foundry-ga-migration/24-PATTERNS.md
@.planning/phases/24-foundry-ga-migration/FRAMEWORK-FIDELITY-calibration.md
@backend/src/second_brain/tools/investigation.py

<interfaces>
Pattern (RC -> GA mechanical change):

Before (RC):
```
@tool(approval_mode="never_require")
async def trace_lifecycle(
    self,
    capture_trace_id: Annotated[str, Field(description="The capture trace ID...")],
) -> dict:
    """Doc string here describes the tool to the agent."""
    ...
```

After (GA):
```
async def trace_lifecycle(
    self,
    capture_trace_id: Annotated[str, Field(description="The capture trace ID...")],
) -> dict:
    """Doc string here describes the tool to the agent."""
    ...
```

KEEP:
- The `async def` signature
- All `Annotated[..., Field(description=...)]` parameter annotations
- The docstring (becomes tool description in GA)
- The `__init__(cosmos_manager, ...)` shape

REMOVE:
- The `@tool(approval_mode=...)` line ONLY

If the file imports `tool` from `agent_framework`, that import becomes unused. Either remove it OR leave it (the auto-format hook will strip unused imports — but per MEMORY, auto-format strips immediately, so the import will go on save).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Strip @tool(approval_mode=...) from all 9 InvestigationTools methods</name>
  <files>backend/src/second_brain/tools/investigation.py</files>
  <read_first>
    - backend/src/second_brain/tools/investigation.py (current decorator usage)
    - .planning/phases/24-foundry-ga-migration/FRAMEWORK-FIDELITY-calibration.md (F-08 finding)
    - .planning/phases/24-foundry-ga-migration/24-PATTERNS.md (Group A — F-08 mechanical pattern)
  </read_first>
  <action>
    1. Grep for the pattern: `grep -n "@tool(approval_mode=" backend/src/second_brain/tools/investigation.py`. Record all line numbers found.
    2. For each occurrence: delete the entire decorator line (the `@tool(approval_mode="never_require")` line) leaving the `async def {tool_name}(...)` signature exactly as it is.
    3. Do NOT modify any parameter annotations, return types, or docstrings.
    4. Do NOT modify `__init__` or any other method.
    5. After all 9 deletions, verify by grep:
       - `grep -c "@tool(approval_mode=" backend/src/second_brain/tools/investigation.py` returns 0
       - `grep -c "async def" backend/src/second_brain/tools/investigation.py` returns at least 9 (the methods are still defined)
    6. If `from agent_framework import tool` is now unused (no other `@tool` references in file), remove that specific name from the import. The auto-format hook (per MEMORY) may strip it on save anyway — this is acceptable. Do this in the SAME edit where the decorators are removed (per MEMORY auto-format trap: imports stripped before usage edits get a chance).
    7. Run the existing unit tests for InvestigationTools: `cd backend && uv run pytest tests/test_investigation.py -x` — they must still pass.
  </action>
  <verify>
    <automated>! grep -q "@tool(approval_mode=" backend/src/second_brain/tools/investigation.py && cd backend && uv run pytest tests/test_investigation.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `! grep -q "@tool(approval_mode=" backend/src/second_brain/tools/investigation.py` returns 0
    - `grep -c "@tool(approval_mode" backend/src/second_brain/tools/investigation.py` returns 0
    - `grep -c "async def trace_lifecycle\\|async def recent_errors\\|async def system_health\\|async def usage_patterns\\|async def query_feedback_signals\\|async def promote_to_golden_dataset\\|async def run_classifier_eval\\|async def run_admin_eval\\|async def get_eval_results" backend/src/second_brain/tools/investigation.py` returns 9 (or more if some method names recur in the file)
    - `grep -q "Annotated\\[" backend/src/second_brain/tools/investigation.py` returns 0 (Annotated parameter shape preserved)
    - `grep -q "Field(description=" backend/src/second_brain/tools/investigation.py` returns 0 (Field descriptions preserved)
    - `grep -q "class InvestigationTools" backend/src/second_brain/tools/investigation.py` returns 0
    - `grep -q "def __init__" backend/src/second_brain/tools/investigation.py` returns 0
    - `cd backend && uv run pytest tests/test_investigation.py -x` exits 0
    - `cd backend && uv run python -c "from second_brain.tools.investigation import InvestigationTools; assert hasattr(InvestigationTools, 'trace_lifecycle'); assert hasattr(InvestigationTools, 'recent_errors')"` exits 0
  </acceptance_criteria>
  <done>All 9 @tool(approval_mode=...) decorators removed. Tool methods are plain async coroutines with Annotated params + docstrings. InvestigationTools instance is GA-ready for `Agent(tools=[instance.method, ...])` registration.</done>
</task>

</tasks>

<verification>
- 9 decorator strips, no other code change in tools/investigation.py
- Unit tests still pass (existing mocks against cosmos_manager are unchanged)
- The unused `tool` import (if applicable) is removed in same edit to avoid auto-format trap
</verification>

<success_criteria>
The Investigation Agent constructed in 24-04 receives bound methods that are now GA-clean: no decorator wrapping, framework's @tool semantics absent. Plan 24-06 (KQL update) is independent and unaffected. Plan 24-07 (streaming/investigation_adapter.py rewrite) consumes the agent built from these tools.
</success_criteria>

<output>
After completion, create `.planning/phases/24-foundry-ga-migration/24-05-SUMMARY.md` documenting:
- 9 decorator strips, line numbers before/after
- Confirmation: pytest tests/test_investigation.py passes
- Confirmation: Annotated[...] + Field(description=...) parameter shapes preserved
- Confirmation: __init__(cosmos_manager, ...) shape unchanged
</output>
