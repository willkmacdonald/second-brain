---
phase: 16.1-improve-deployment-process
plan: 01
subsystem: infra
tags: [github-actions, uv, azure-container-apps, ci-cd]

# Dependency graph
requires:
  - phase: 16-query-foundation
    provides: "Deployed backend with uv-managed dependencies and Dockerfile pinning uv 0.5.4"
provides:
  - "Pre-build uv lockfile validation (uv lock --check) preventing dependency drift"
  - "Commit-correlated revision naming (sha-{7chars}) for deploy traceability"
  - "PIPELINE_START, IMAGE_TAG, REVISION_SUFFIX, REVISION_NAME in GITHUB_ENV for Plan 02"
affects: [16.1-02-PLAN.md]

# Tech tracking
tech-stack:
  added: [astral-sh/setup-uv@v5]
  patterns: [pre-build-dependency-gate, commit-correlated-revision-naming]

key-files:
  created: []
  modified: [".github/workflows/deploy-backend.yml"]

key-decisions:
  - "uv version pinned to 0.5.4 matching Dockerfile COPY --from=ghcr.io/astral-sh/uv:0.5.4"
  - "Revision suffix uses sha- prefix so Azure never rejects names starting with digits"
  - "IMAGE_TAG exported to GITHUB_ENV for reuse in deploy step instead of re-computing"

patterns-established:
  - "Pre-build gate: uv lock --check runs before Docker build to catch lockfile drift"
  - "Revision naming: sha-{7chars} suffix enables git log --oneline correlation"

requirements-completed: []

# Metrics
duration: 1min
completed: 2026-04-06
---

# Phase 16.1 Plan 01: Pre-build Dependency Validation and Revision Naming Summary

**uv lockfile validation gate and sha-prefixed revision naming added to deploy-backend workflow**

## Performance

- **Duration:** 1 min
- **Started:** 2026-04-06T01:12:52Z
- **Completed:** 2026-04-06T01:14:10Z
- **Tasks:** 2
- **Files modified:** 1

## Accomplishments
- uv lockfile validation runs before Docker build, preventing the pyproject.toml/uv.lock drift that caused Phase 16's crash-loop
- Every deployed revision name now contains the git SHA (format: second-brain-api--sha-{7chars}) for instant commit correlation
- Pipeline timing and image/revision metadata exported to GITHUB_ENV for Plan 02's verification and summary steps

## Task Commits

Each task was committed atomically:

1. **Task 1: Add uv lockfile validation as pre-build gate** - `e2a4a38` (feat)
2. **Task 2: Add commit-correlated revision naming to deploy step** - `e1bcd30` (feat)

## Files Created/Modified
- `.github/workflows/deploy-backend.yml` - Added pipeline timing, uv install + lockfile check, IMAGE_TAG export, revision-suffix with sha- prefix, REVISION_SUFFIX/REVISION_NAME exports

## Decisions Made
- uv version pinned to 0.5.4 matching Dockerfile -- prevents lockfile format mismatches between CI and Docker build
- Revision suffix uses sha- prefix (not bare SHA) so Azure naming rules are satisfied even when git SHA starts with a digit
- IMAGE_TAG exported to GITHUB_ENV and referenced in deploy step via `${{ env.IMAGE_TAG }}` instead of re-computing the string

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- PIPELINE_START, IMAGE_TAG, REVISION_SUFFIX, and REVISION_NAME are all available in GITHUB_ENV for Plan 02 to use in post-deploy verification, revision cleanup, and deploy summary steps
- The workflow is structurally ready for Plan 02 to append health check, revision deactivation, and summary steps after the deploy step

## Self-Check: PASSED

- FOUND: 16.1-01-SUMMARY.md
- FOUND: e2a4a38 (Task 1 commit)
- FOUND: e1bcd30 (Task 2 commit)
- FOUND: deploy-backend.yml

---
*Phase: 16.1-improve-deployment-process*
*Completed: 2026-04-06*
