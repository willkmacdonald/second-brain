---
phase: 19.2-transaction-first-spine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/19.2-transaction-first-spine/SPIKE-MEMO.md
autonomous: false
requirements: []
must_haves:
  truths:
    - "Operator has evidence (not speculation) about why 8 of 9 segments show zero workload events"
    - "Each of the 9 segments is categorized as broken_emitter, pull_by_design, or correlation_lost"
    - "Mobile-segment push-path question has a yes/no answer with rationale"
    - "Plan 02 scope is defined by memo findings, not pre-planned speculation"
  artifacts:
    - path: ".planning/phases/19.2-transaction-first-spine/SPIKE-MEMO.md"
      provides: "Per-segment categorization table + file/line findings + mobile push decision"
      contains: "Per-segment audit"
  key_links:
    - from: "SPIKE-MEMO.md"
      to: "Plan 02 (emitter fixes)"
      via: "memo.broken_emitters list drives plan 02 scope"
      pattern: "category.*broken_emitter|category.*correlation_lost"
---

<objective>
Reproduce one real capture end-to-end against the deployed backend, query `spine_events` / `spine_correlation` / App Insights for ground truth, and produce a memo categorizing each of the 9 segments into exactly one of: `broken_emitter`, `pull_by_design`, or `correlation_lost`. Use a caller-supplied trace id as the audit key when triggering a fresh capture; do not rely on the capture route to return one. The memo is the ONLY input that drives Plan 02's (emitter fixes) scope — do not plan emitter tasks before the memo arrives.

Purpose: CONTEXT.md makes the spike a non-negotiable first step. The researcher's RESEARCH.md documents three nearly-identical failure modes (no emit, emit-but-no-correlation, emit-and-correlation-but-no-native-tag) that all look alike from the status board. The memo forces the planner (and later the executor of Plan 02) to distinguish them and fix the right thing.

Output:
- `.planning/phases/19.2-transaction-first-spine/SPIKE-MEMO.md` with:
  - Per-segment table (9 rows): `emit_site_exists`, `events_landed_in_spine_events`, `correlation_tag_present`, `native_record_tagged`, `category`
  - Per `broken_emitter` row: file path + line number of the failing or missing emit site
  - Per `correlation_lost` row: where the tag is dropped (middleware? OTel context? header propagation? direct `_WorkloadEvent` bypassing `emit_agent_workload`?)
  - Explicit yes/no on "should normal mobile captures become a push path?" with rationale
- A human checkpoint confirming Plan 02's scope before it executes
</objective>

<execution_context>
@/Users/willmacdonald/.claude/get-shit-done/workflows/execute-plan.md
@/Users/willmacdonald/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19.2-transaction-first-spine/19.2-CONTEXT.md
@.planning/phases/19.2-transaction-first-spine/19.2-RESEARCH.md

<!-- Tools the spike MUST use (don't hand-roll these) -->
<!-- audit_correlation MCP tool: runs spine ↔ native three-way join; produced the 2026-04-18 finding that motivated this phase -->
<!-- /investigate slash command: ad-hoc trace lookups against deployed App Insights -->
<!-- Direct Cosmos query: `az cosmosdb sql query` against `spine_events` and `spine_correlation` containers -->

<!-- Known emit sites (grep-verified from research) to test against deployed behavior: -->
<!-- backend_api:           backend/src/second_brain/spine/middleware.py (every request) -->
<!-- classifier:            backend/src/second_brain/spine/stream_wrapper.py wrapping api/capture.py:238,315,384,502 -->
<!-- admin:                 backend/src/second_brain/processing/admin_handoff.py (finally block) -->
<!-- investigation:         backend/src/second_brain/api/investigate.py:84 -->
<!-- external_services:     backend/src/second_brain/tools/recipe.py:175-185 (direct _WorkloadEvent, NO correlation_kind/id) -->
<!-- mobile_ui/capture:     backend/src/second_brain/api/telemetry.py:86-110 (crud_failure only, by design) -->
<!-- cosmos/container_app:  NO direct emit — pulled by adapters; "pull_by_design" expected -->

<!-- Known cosmos counts as of 2026-04-18 from direct query (CONTEXT §specifics): -->
<!-- backend_api 25,374 (mostly GET /health) · classifier 0 · admin 0 · investigation 0 · cosmos 0 · external_services 0 · mobile_ui 0 · mobile_capture 0 · container_app 0 -->
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reproduce one real capture and collect ground-truth data</name>
  <files>
    .planning/phases/19.2-transaction-first-spine/SPIKE-DATA.md (scratch pad for raw findings, committed alongside memo)
  </files>
  <action>
Deploy-only workflow. Do NOT run the backend locally (per user preference).

1. **Trigger one real capture against the deployed backend.** Options in order of preference:
   a. Use `/investigate` slash command with a known recent capture trace_id OR
   b. Send a fresh capture via `curl -N -X POST https://brain.willmacdonald.com/api/capture ...` with a body that will be classified to Admin (so the admin segment's processing-on-errands-screen-open side-effect fires when you open the Status screen in mobile), and set a known header such as `X-Trace-Id: spike-<timestamp>`. The live route is `/api/capture` and it streams SSE; it does NOT return a JSON `capture_trace_id`.
   c. Record the caller-supplied `X-Trace-Id` in `SPIKE-DATA.md` and use that value as the capture correlation id for all downstream audit steps.

2. **Run `audit_correlation` MCP tool** for `correlation_kind="capture"` with the new `correlation_id=<trace>`. This gives the spine-↔-App-Insights-↔-Cosmos three-way join in one call. Save the full report.

3. **Direct Cosmos queries** (use `az cosmosdb sql query` against `shared-services-cosmosdb`):
   - `spine_events` container: `SELECT c.segment_id, c.event_type, c.payload.correlation_kind, c.payload.correlation_id, c.payload.operation, c.payload.outcome, c.timestamp FROM c WHERE c.timestamp >= <1h_ago_iso> ORDER BY c.timestamp DESC` — confirms which segments emit ANY event at all, and which carry correlation tags
   - `spine_correlation` container: `SELECT * FROM c WHERE c.correlation_kind = 'capture' AND c.correlation_id = '<trace>'` — confirms the JOIN-able ledger row per segment
   - For comparison, repeat `spine_events` query for `correlation_kind="thread"` (to exercise investigation) by sending one `/api/investigate` query

4. **App Insights cross-check** via MCP `trace_lifecycle` tool with the trace_id: confirms which segments' native records carry `capture_trace_id` / `operation_Id` tagging. This is the key evidence for `correlation_lost` classification.

5. **Run `audit_correlation`** in sample mode (no correlation_id, `sample_size=5`, `time_range_seconds=3600`) to get the `instrumentation_warning` across recent captures. This is the same path that produced the 2026-04-18 backend_api warning (see `project_followup_audit_first_findings.md`).

Dump raw findings (table rows, query outputs, link to App Insights trace) into `.planning/phases/19.2-transaction-first-spine/SPIKE-DATA.md`. No analysis yet — just raw evidence.
  </action>
  <verify>
    <automated>test -f .planning/phases/19.2-transaction-first-spine/SPIKE-DATA.md &amp;&amp; grep -qE "X-Trace-Id|trace_id" .planning/phases/19.2-transaction-first-spine/SPIKE-DATA.md &amp;&amp; grep -q "spine_events" .planning/phases/19.2-transaction-first-spine/SPIKE-DATA.md &amp;&amp; grep -q "spine_correlation" .planning/phases/19.2-transaction-first-spine/SPIKE-DATA.md</automated>
  </verify>
  <done>SPIKE-DATA.md exists with raw query output for ≥1 real capture trace keyed by a known caller-supplied trace id, the `audit_correlation` MCP report for that trace, and a sample-mode audit report on recent captures.</done>
</task>

<task type="auto">
  <name>Task 2: Categorize each segment and write the memo</name>
  <files>
    .planning/phases/19.2-transaction-first-spine/SPIKE-MEMO.md
  </files>
  <action>
Using the raw data from Task 1, produce a structured memo with exactly these sections:

1. **Summary** (3 lines): one-sentence findings for `capture` chain, `thread` chain, and mobile-emit-decision.

2. **Per-segment table** (9 rows, one per segment in `EXPECTED_CHAINS` ∪ {container_app}):
   | segment | emit_site_exists | events_in_spine_events | correlation_tag_present | native_record_tagged | category |
   Where `category` is exactly one of: `broken_emitter` | `pull_by_design` | `correlation_lost` | `working_correctly`

3. **Findings per category:**
   - **Category `broken_emitter`**: for each row, list file path + line number of the failing emit site OR "NO EMIT SITE — must be added at <suggested location>". Example format: `classifier: backend/src/second_brain/spine/stream_wrapper.py:41 — emit site exists and is called, but capture_trace_id is None on the deployed path because <root cause>`.
   - **Category `correlation_lost`**: for each row, identify the drop point. Is it direct `_WorkloadEvent` construction bypassing `emit_agent_workload` (see `tools/recipe.py:175`)? Missing `capture_trace_id` threading in the caller? Missing OTel context propagation? Missing Cosmos `x-ms-client-request-id` header? Cite file + line.
   - **Category `pull_by_design`**: for each row, one sentence stating the native source (e.g. "cosmos: pulled by CosmosAdapter from AzureDiagnostics — workload events are not expected, ever").
   - **Category `working_correctly`**: confirm with the correlation row count from Task 1.

4. **Mobile push-path decision:** Answer: should `mobile_capture` and `mobile_ui` emit normal-capture workload events to spine today, not just `crud_failure`? One of:
   - **YES** — add new push path. Scope: <describe>. Justification: operator ledger UX demands "mobile_capture seen, classifier missing" semantics per CONTEXT §decisions.
   - **NO** — UI must render mobile segments as "native-only by design; see Sentry for native telemetry." Justification: <why the ledger UX works without mobile emit>.
   This answer DRIVES whether Plan 02 and Plan 04 include mobile-specific tasks.

5. **Recommendation for Plan 02:** a numbered list of concrete fixes, each referencing a specific file + line from section 3. The list becomes Plan 02's task scope. If the fix list is empty (everything is `pull_by_design` or `working_correctly` except native-tagging gaps), say so explicitly and recommend that Plan 02 instead scope correlation-tagging fixes only.

6. **Open questions for the human checkpoint:** any ambiguity where you want Will's call before Plan 02 executes.

Anti-patterns to avoid:
- Do NOT recommend fixes for segments categorized `pull_by_design` — that would regress the intentional architecture.
- Do NOT classify a segment `broken_emitter` if the emit site exists and runs, but `capture_trace_id` is None. That's `correlation_lost`, and the fix is different (upstream threading, not a new emit site).
- Do NOT propose a new push path for a `correlation_lost` segment — fix the tagging, not the emit.
  </action>
  <verify>
    <automated>test -f .planning/phases/19.2-transaction-first-spine/SPIKE-MEMO.md &amp;&amp; grep -qE "category" .planning/phases/19.2-transaction-first-spine/SPIKE-MEMO.md &amp;&amp; grep -qE "mobile.*push.*decision|push path" .planning/phases/19.2-transaction-first-spine/SPIKE-MEMO.md &amp;&amp; grep -qE "Recommendation for Plan 02" .planning/phases/19.2-transaction-first-spine/SPIKE-MEMO.md</automated>
  </verify>
  <done>SPIKE-MEMO.md exists, contains a 9-row categorization table, file/line citations for every non-`pull_by_design` row, an explicit mobile push-path yes/no decision with rationale, and a numbered scope recommendation for Plan 02.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking" status="complete" completed="2026-04-18" approved-commit="febcca0">
  <name>Task 3: Human review of spike memo (gate for Plan 02 scope) — APPROVED</name>
  <files>.planning/phases/19.2-transaction-first-spine/SPIKE-MEMO.md</files>
  <action>Read SPIKE-MEMO.md end to end. For each row in the categorization table, confirm the category matches your mental model. Explicitly approve or correct the mobile push-path decision. Confirm the Plan 02 scope recommendation is the scope you want Plan 02 to execute. See <how-to-verify> below for the full verification checklist.</action>
  <verify>
    <automated>User types "memo approved" or provides corrections; orchestrator pauses until then.</automated>
  </verify>
  <done>User has explicitly approved the memo, OR the planner has revised it per user feedback and re-submitted for approval.</done>
  <approval-record>
    Approved after round 3 corrections on 2026-04-18. Operator quote: "Everything else checks out. After that edit: memo approved." Round 3 commit febcca0 closed the last gap (terminal-path coverage for mobile emit). Prior rounds: round 1 (6f27ecb + 66b1484) initial memo; round 2 (c66fc48) §4/§5 rewrite for mobile Option B scope.
  </approval-record>
  <what-built>
Spike memo with per-segment categorization, root-cause citations, mobile push-path decision, and concrete Plan 02 scope recommendation.
  </what-built>
  <how-to-verify>
1. Read `.planning/phases/19.2-transaction-first-spine/SPIKE-MEMO.md` end to end.
2. For each row in the categorization table, confirm the `category` matches your mental model (or flag for re-investigation).
3. For the mobile push-path decision: confirm the yes/no answer is the one you want to ship in this phase. This is the single biggest scope decision the memo makes — if you disagree, Plan 02 and Plan 04 will diverge significantly.
4. Review the "Recommendation for Plan 02" numbered list. This becomes Plan 02's task scope verbatim. Confirm it's correct and complete.
5. Review any "Open questions" at the bottom of the memo.
  </how-to-verify>
  <resume-signal>Type "memo approved" to unblock Plan 02 execution, or describe corrections and the planner will revise the memo before Plan 02 starts.</resume-signal>
</task>

</tasks>

<verification>
- SPIKE-DATA.md contains raw query output (not just prose)
- SPIKE-MEMO.md contains a 9-row categorization table
- Every `broken_emitter` / `correlation_lost` row has a file + line citation
- Mobile push-path decision is a binary yes/no with rationale
- Plan 02 scope recommendation is a numbered list mapped to files
- Human checkpoint signals approval before downstream plans proceed
</verification>

<success_criteria>
- Memo categorizes all 9 segments (or declares one `working_correctly` with evidence)
- Memo cites specific files/lines for every non-pull-by-design finding
- Memo answers the mobile push-path question definitively (YES or NO, not "TBD")
- Human approves memo before Plan 02 executes
- Categorization is defensible against the `audit_correlation` MCP tool's report (no contradictions)
</success_criteria>

<output>
After completion, create `.planning/phases/19.2-transaction-first-spine/19.2-01-SUMMARY.md` with:
- Summary (1-2 sentences)
- Per-category segment count (e.g. "3 broken_emitter, 2 correlation_lost, 3 pull_by_design, 1 working_correctly")
- Mobile push-path decision (verbatim from memo)
- Link to `SPIKE-MEMO.md`
- Explicit scope callout for Plan 02: "Plan 02 addresses segments X, Y, Z with fixes at <file:line>, <file:line>"
</output>
