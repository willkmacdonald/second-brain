---
phase: 19.2-transaction-first-spine
verified: 2026-04-19T21:21:52Z
status: passed
score: 26/26 must-haves verified (plus 5/5 roadmap acceptance criteria, pending one mobile_capture EAS rebuild captured as a todo)
---

# Phase 19.2: Transaction-First Spine Verification Report

**Phase Goal:** Change the primary operator drill-down from native-infrastructure-first to transaction-first — (1) fix broken emitters so workload events land in `spine_events`, (2) expose per-segment and cross-segment transaction read paths with explicit gap callouts, (3) rewire the web UI so transactional evidence renders FIRST and native telemetry SECOND.

**Verified:** 2026-04-19T21:21:52Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (aggregated across all 5 plans)

| #   | Truth                                                                                                                                                                 | Status     | Evidence                                                                                                                                                                               |
| --- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | Plan 01: Operator has evidence (not speculation) about why 8/9 segments show zero workload events                                                                     | VERIFIED   | `SPIKE-MEMO.md` §2 contains 9-row categorization; §3 cites file:line for every non-pull-by-design row; approved commit febcca0                                                         |
| 2   | Plan 01: Each of 9 segments categorized as broken_emitter / pull_by_design / correlation_lost / working_correctly                                                     | VERIFIED   | SPIKE-MEMO §2 table: 4 broken_emitter / 1 correlation_lost / 4 pull_by_design                                                                                                          |
| 3   | Plan 01: Mobile push-path question has yes/no answer with rationale                                                                                                   | VERIFIED   | SPIKE-MEMO §4a YES (Option B post-capture fire-and-forget for mobile_capture); §4b NO for mobile_ui; rationale present                                                                 |
| 4   | Plan 01: Plan 02 scope defined by memo findings                                                                                                                       | VERIFIED   | SPIKE-MEMO §5 numbered fix list maps 1:1 to Plan 02 commits b6394c7..f9756bc                                                                                                           |
| 5   | Plan 02: Every broken_emitter segment emits workload events in automated tests                                                                                        | VERIFIED   | agent_emitter.py:69 wraps with `IngestEvent(root=event)` — fixes classifier+admin+investigation simultaneously; tests pass (412 backend tests green post-fix)                          |
| 6   | Plan 02: Every correlation_lost segment writes correlation-tagged rows in automated tests                                                                             | VERIFIED   | tools/recipe.py:187 uses `emit_agent_workload(capture_trace_id=...)`; agent_emitter threads kind/id into WorkloadPayload                                                               |
| 7   | Plan 02: Plan does NOT ship a user-visible half-state                                                                                                                 | VERIFIED   | SPIKE-INTEGRATED-RELEASE-VERIFY.md exists with "do not merge Plan 02 alone" banner; Plans 02-05 shipped together                                                                       |
| 8   | Plan 02: Existing emitter tests still pass; new regression tests lock in fixes                                                                                        | VERIFIED   | 412 backend tests passed per 19.2-02-SUMMARY.md; walking regression static source scan in test_spine_capture_correlation_contract.py §5.4; §5.6 integration test added                |
| 9   | Plan 03: API exposes per-segment transaction ledger read path returning correlated workload events (not raw dump)                                                     | VERIFIED   | spine/api.py:249 `/ledger/segment/{segment_id}` registered with auth; delegates to `repo.get_recent_transaction_events` (storage.py:179)                                               |
| 10  | Plan 03: Segment ledger suppresses uncorrelated probe/heartbeat traffic                                                                                               | VERIFIED   | storage.py:179+ `get_recent_transaction_events` WHERE clause filters `IS_DEFINED(c.payload.correlation_kind) AND NOT IS_NULL(...)` — per-plan SQL                                      |
| 11  | Plan 03: API exposes enriched correlation event stream with stage/timestamp/duration/outcome/correlation_id/payload                                                   | VERIFIED   | spine/api.py:301 `/ledger/correlation/{kind}/{correlation_id}` registered; RESEARCH Option A enrichment join against `get_workload_events_for_correlation` (storage.py:215)            |
| 12  | Plan 03: Gap reporting driven by operator-facing ledger policy incorporating approved mobile push decision                                                            | VERIFIED   | spine/ledger_policy.py:46 `LEDGER_EXPECTED_CHAINS` includes mobile_capture per memo §4a YES; api.py:355-362 uses `LEDGER_EXPECTED_CHAINS[kind]` set-math, NOT raw `EXPECTED_CHAINS`    |
| 13  | Plan 03: Segment ledger response tells UI whether segment is transactional or native_only                                                                             | VERIFIED   | models.py:186 `SegmentLedgerResponse.mode`; ledger_policy.py:57 `SEGMENT_LEDGER_METADATA` marks cosmos/container_app as native_only with purposeful empty_state_reason                 |
| 14  | Plan 03: Ledger rows newest-first (DESC); correlation events oldest-first (ASC)                                                                                       | VERIFIED   | storage.py SQL includes `ORDER BY c.timestamp DESC` for ledger; existing `get_correlation_events` already ASC                                                                          |
| 15  | Plan 03: Both read paths honor auth_dependency and time-range limits                                                                                                  | VERIFIED   | api.py:252,304 `dependencies=[Depends(auth_dependency)]`; Query(ge=..., le=...) bounds on both window_seconds and limit                                                                |
| 16  | Plan 04: Segment detail page renders ledger-first section BEFORE native renderer dispatch                                                                             | VERIFIED   | web/app/segment/[id]/page.tsx:114-125 `<LedgerSection>` renders between `<SegmentDetailHeader>` and the "Diagnostics (native telemetry)" h2 + native renderer dispatch                 |
| 17  | Plan 04: Each ledger row shows segment/timestamp/operation/outcome/duration/correlation_id (clickable)                                                                | VERIFIED   | components/LedgerSection.tsx has all 6 columns; correlation_id wrapped in `<Link href="/correlation/{kind}/{encodeURIComponent(id)}">`                                                 |
| 18  | Plan 04: Ledger rows newest-first                                                                                                                                     | VERIFIED   | Backend DESC ordering preserved through to TransactionLedgerRow[] rendering                                                                                                            |
| 19  | Plan 04: Existing SegmentDetailHeader still renders unchanged at top                                                                                                  | VERIFIED   | page.tsx:96-99 `<SegmentDetailHeader>` renders first, unchanged props                                                                                                                  |
| 20  | Plan 04: Existing native renderers render BELOW the ledger                                                                                                            | VERIFIED   | page.tsx:127-138 `<h2>Diagnostics (native telemetry)</h2>` + schema dispatch appears AFTER `<LedgerSection>`                                                                           |
| 21  | Plan 04: Empty ledger renders purposeful message driven by backend `mode`+`empty_state_reason`                                                                        | VERIFIED   | LedgerSection.tsx:36-53 branches on `mode === "native_only"` using `emptyStateReason` from backend; no frontend-hardcoded categorization                                               |
| 22  | Plan 05: Transaction page fetches new `/ledger/correlation/{kind}/{id}` endpoint, not old route                                                                       | VERIFIED   | web/app/correlation/[kind]/[id]/page.tsx:53 `spine.transactionPath(kind, params.id)`; `spine.correlation` does not appear in this file                                                 |
| 23  | Plan 05: Page renders full cross-segment path chronologically (ASC)                                                                                                   | VERIFIED   | Backend `get_correlation_events` preserves ASC; page renders `path.events.map(...)` untransformed                                                                                      |
| 24  | Plan 05: Each event row shows segment/timestamp/operation/outcome/duration/error_class                                                                                | VERIFIED   | page.tsx event row renders segment_id, `<LocalTime>`, operation (monospace), `event.duration_ms`, OUTCOME_COLOR badge, error_class red line                                            |
| 25  | Plan 05: Explicit `missing_required` callout renders when non-empty; `unexpected` yellow; `present_optional` muted                                                    | VERIFIED   | page.tsx:75 conditional red banner for missing_required with red border (`STATUS_COLOR.red`); yellow banner for unexpected; muted present_optional line. Conditionally rendered (no placeholders when empty) |
| 26  | Plan 05: Per-event drill-down link `/segment/{id}?correlation_kind=...&correlation_id=...` preserved                                                                  | VERIFIED   | page.tsx:199 `href={`/segment/${event.segment_id}?correlation_kind=${path.correlation_kind}&correlation_id=${encodeURIComponent(path.correlation_id)}`}`                               |

**Score:** 26/26 truths verified

### Required Artifacts

| Artifact                                                                  | Expected                                                                                                      | Status     | Details                                                                                                                          |
| ------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------- | ---------- | -------------------------------------------------------------------------------------------------------------------------------- |
| `.planning/phases/19.2-.../SPIKE-MEMO.md`                                 | Per-segment categorization table + file/line findings + mobile decision                                       | VERIFIED   | 9-row table present; category column populated; §3-§5 present; approved at commit febcca0                                        |
| `.planning/phases/19.2-.../SPIKE-INTEGRATED-RELEASE-VERIFY.md`            | Integrated release checklist with "do not merge alone" banner + baselines + audit_correlation steps           | VERIFIED   | File exists; Plan 02 summary references banner explicitly                                                                        |
| `backend/src/second_brain/spine/storage.py`                               | `SpineRepository.get_recent_transaction_events(segment_id, window_seconds, limit)` — correlated-only, DESC    | VERIFIED   | Defined at line 179; SQL filters correlation IS_DEFINED/NOT NULL; ORDER BY DESC; respects `limit`                                |
| `backend/src/second_brain/spine/ledger_policy.py`                         | `LEDGER_EXPECTED_CHAINS` + `SEGMENT_LEDGER_METADATA` (with approved memo mobile decision)                      | VERIFIED   | Both exports present (lines 46, 57); encoded at module-level, not read from memo at runtime                                      |
| `backend/src/second_brain/spine/models.py`                                | 4 new models: TransactionLedgerRow, SegmentLedgerResponse (mode + empty_state_reason), TransactionEvent, TransactionPathResponse | VERIFIED   | All 4 present at lines 169/186/196/215; existing CorrelationEvent/CorrelationResponse unchanged                                  |
| `backend/src/second_brain/spine/api.py`                                   | GET `/api/spine/ledger/segment/{id}` + GET `/api/spine/ledger/correlation/{kind}/{id}`                        | VERIFIED   | Routes registered at lines 250, 302; auth_dependency applied; Query(...) validators with ge/le bounds                            |
| `web/lib/types.ts`                                                        | 4 TS interfaces mirroring backend Pydantic models                                                              | VERIFIED   | TransactionLedgerRow, SegmentLedgerResponse, TransactionEvent, TransactionPathResponse all exported (lines 69, 80, 88, 99)       |
| `web/lib/spine.ts`                                                        | `spine.segmentLedger(id, {window_seconds, limit})` + `spine.transactionPath(kind, id, {window_seconds})`       | VERIFIED   | Both methods present (lines 48, 60); `encodeURIComponent` on id params; single merged type import block; server-only guard intact |
| `web/components/LedgerSection.tsx`                                        | Server component rendering TransactionLedgerRow[] table + purposeful empty states                             | VERIFIED   | No "use client"; renders 6-column table OR native-only banner based on `mode`; matches inline-style aesthetic                    |
| `web/app/segment/[id]/page.tsx`                                           | LedgerSection rendered above native renderer dispatch                                                          | VERIFIED   | `<LedgerSection>` appears between `<SegmentDetailHeader>` and "Diagnostics (native telemetry)" h2; fetch wrapped in try/catch for graceful degradation |
| `web/app/correlation/[kind]/[id]/page.tsx`                                | Transaction page consuming TransactionPathResponse with gap reporting + drill-down                            | VERIFIED   | spine.transactionPath consumed; missing_required/unexpected/present_optional callouts all rendered; per-event `/segment/{id}?correlation_kind=...&correlation_id=...` link preserved |

### Key Link Verification

| From                                                    | To                                                                         | Via                                                                 | Status | Details                                                                                          |
| ------------------------------------------------------- | -------------------------------------------------------------------------- | ------------------------------------------------------------------- | ------ | ------------------------------------------------------------------------------------------------ |
| SPIKE-MEMO.md                                           | Plan 02 (emitter fixes)                                                    | memo.broken_emitters list drives plan 02 scope                      | WIRED  | Plan 02 commit map matches memo §5 numbered fixes verbatim                                       |
| Fixed emitter call sites                                | SpineRepository.record_event → spine_correlation upsert                    | `emit_agent_workload(capture_trace_id=...)`                          | WIRED  | agent_emitter.py:69 now wraps with `IngestEvent(root=event)`; repo.record_event receives correct shape |
| GET /api/spine/ledger/segment/{id}                      | SpineRepository.get_recent_transaction_events                              | build_spine_router in api.py                                         | WIRED  | api.py:266 calls repo.get_recent_transaction_events(segment_id, window_seconds, limit)           |
| GET /api/spine/ledger/correlation/{kind}/{id}           | SpineRepository.get_correlation_events + LEDGER_EXPECTED_CHAINS set-math  | build_spine_router + ledger_policy import                           | WIRED  | api.py:325 + api.py:355-362 set-math uses LEDGER_EXPECTED_CHAINS[kind]                            |
| web/app/segment/[id]/page.tsx                           | GET /api/spine/ledger/segment/{id}                                         | spine.segmentLedger from web/lib/spine.ts                           | WIRED  | page.tsx:84 `spine.segmentLedger(params.id, {window_seconds, limit})`                            |
| LedgerSection correlation_id links                      | /correlation/{kind}/{id}                                                   | Next Link href with encodeURIComponent on correlation_id            | WIRED  | LedgerSection.tsx:107-110 `<Link href="/correlation/${kind}/${encodeURIComponent(id)}">`         |
| web/app/correlation/[kind]/[id]/page.tsx                | GET /api/spine/ledger/correlation/{kind}/{id}                              | spine.transactionPath                                                | WIRED  | page.tsx:53 `spine.transactionPath(kind, params.id)`                                             |
| Transaction page event row                              | /segment/{id}?correlation_kind=...&correlation_id=...                      | Next Link                                                            | WIRED  | page.tsx:199 preserves drill-down link shape with encodeURIComponent                             |

### Requirements Coverage

No requirement IDs applicable — this phase's 5 plans all have empty `requirements: []` arrays (inserted phase discovered 2026-04-18; strengthens Phase 19.1 observability posture but carries no numbered REQ-IDs from REQUIREMENTS.md).

### ROADMAP Acceptance Criteria Coverage

The ROADMAP §Phase 19.2 defines 5 acceptance criteria. Verified against live walk-through (user session today):

| #   | Acceptance Criterion                                                                                                               | Status     | Evidence                                                                                                                                                                                             |
| --- | ---------------------------------------------------------------------------------------------------------------------------------- | ---------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | A broken capture can be diagnosed from the web UI without starting in App Insights or Cosmos                                       | VERIFIED   | Live: `/correlation/capture/547941ac-...` red banner surfaces "Missing required segments: mobile_capture" — operator sees the gap without leaving the web UI                                          |
| 2   | From the status board, an operator can identify the segment most likely failing                                                    | VERIFIED   | Preserved from Phase 17/18 status board; unchanged in this phase                                                                                                                                     |
| 3   | From that segment page, the operator can see recent correlated transactions, not just infrastructure-shaped rows                   | VERIFIED   | Live: `/segment/backend_api` renders correlated transaction rows in ledger-first layout ABOVE native renderer; probe/heartbeat noise suppressed by SQL filter                                         |
| 4   | From a transaction page, the operator can see every segment that touched the flow AND every expected segment that did not          | VERIFIED   | Live: `/correlation/capture/547941ac-...` shows events + explicit `missing_required` banner for absent segments                                                                                      |
| 5   | Native telemetry remains available as evidence for root cause analysis                                                             | VERIFIED   | "Diagnostics (native telemetry)" h2 + native renderer dispatch preserved below ledger on segment page                                                                                                |

### Anti-Patterns Found

None. Scan for `TODO`/`FIXME`/`placeholder`/empty implementations / console-only handlers in phase-modified files turned up no blockers or warnings.

### Human Verification Required

None blocking. Live walk-through already completed by the operator during this session:
- Post-deploy trace `547941ac-bdf3-4ea7-85ac-87cd52217f1f` — classifier workload event now landed in spine_events (7185ms) alongside backend_api; pre-deploy baseline `68f84358-...` had only backend_api. Confirms the Plan 02 `agent_emitter.py:69` fix works in production.
- `/segment/backend_api` ledger-first layout renders correctly with correlated rows.
- `/correlation/capture/547941ac-...` Transaction Path renders with red "Missing required segments: mobile_capture" banner.

One residual visual item deferred to a todo, NOT a 19.2 gap:
- `mobile_capture` missing from the live trace — mobile JS shipped in commit `d310f93` (Plan 02 §5.5), but the phone is still running a pre-19.2 EAS bundle. Captured as `.planning/todos/pending/2026-04-19-eas-rebuild-and-verify-mobile-capture-emits.md`. Closes with a routine EAS rebuild; does not require a new phase.

### Known Deferrals (NOT 19.2 gaps)

The following were explicitly out of scope per SPIKE-MEMO §5 / CONTEXT.md — reserved for a future phase (candidate Phase 19.3), documented in the user's global memory (`project_native_foundry_correlation_gap.md` / `project_deferred_19.2_spine_gaps.md`):

1. **Native renderer blindness for the verified trace** — `/segment/classifier` shows "Classifier runs (0) — No recent runs" under the native Diagnostics panel even though the spine ledger directly above shows the classifier ran. This is the native-Foundry-spans-not-tagged-with-`capture_trace_id` gap. Phase 19.2's goal was to stop DEPENDING on native renderers as the first-look view, and the spine ledger now IS the first-look view. Native renderer fix belongs to a Phase 19.3 candidate, not 19.2.
2. `backend_api` native-correlation tagging (AppRequests span not tagged with `capture_trace_id`)
3. Cosmos `activityId_g` → `capture_trace_id` mapping
4. Investigation thread-kind correlation tagging on Foundry custom spans
5. Duplicate Key Vault secret cleanup (`sb-api-key` / `second-brain-api-key`)
6. Mobile offline queue (Option A) — Plan 02 shipped Option B per memo §4a

All five are explicitly enumerated as "Out of scope" in SPIKE-MEMO §5 and 19.2-02-SUMMARY.md "Scope-creep-avoided notes."

### Gaps Summary

None. All 26 plan-level must-have truths verified; all 11 required artifacts pass exists/substantive/wired checks; all 8 key links WIRED; 5/5 ROADMAP acceptance criteria satisfied by live walk-through. The one outstanding operational closure (mobile_capture EAS rebuild) is captured as a todo and does not regress any goal — the phone will start emitting mobile_capture workload events the moment the new EAS bundle loads, at which point `/correlation/capture/<new-trace>` will show a complete chain without the red banner.

---

_Verified: 2026-04-19T21:21:52Z_
_Verifier: Claude (gsd-verifier)_
