---
phase: 17.4-foundry-observability-and-codex-code-review
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/codex-review.yml
  - .github/codex/prompts/review.md
autonomous: true
requirements: [CFIX-03]
user_setup:
  - service: openai
    why: "Codex GitHub Action requires an OpenAI API key (separate from Azure OpenAI)"
    env_vars:
      - name: OPENAI_API_KEY
        source: "OpenAI Dashboard -> API keys -> Create new secret key"
    dashboard_config:
      - task: "Add OPENAI_API_KEY as a GitHub repository secret"
        location: "GitHub repo -> Settings -> Secrets and variables -> Actions -> New repository secret"

must_haves:
  truths:
    - "Codex code review runs automatically on pull request open and synchronize events"
    - "Review results are posted as a PR comment"
    - "Codex runs in read-only sandbox mode with no write access to repository"
  artifacts:
    - path: ".github/workflows/codex-review.yml"
      provides: "GitHub Actions workflow for Codex PR review"
      contains: "openai/codex-action@v1"
    - path: ".github/codex/prompts/review.md"
      provides: "Review prompt tailored to Second Brain codebase"
      contains: "Python"
  key_links:
    - from: ".github/workflows/codex-review.yml"
      to: "openai/codex-action@v1"
      via: "GitHub Action step"
      pattern: "openai/codex-action"
---

<objective>
Add Codex as a CI code review step that runs automatically on pull requests.

Purpose: Every PR gets automated code review before merge, catching type mismatches, test failures, stale docs, and code quality issues like the ones found in the manual Codex review.

Output: GitHub Actions workflow + review prompt for automated Codex PR reviews.
</objective>

<execution_context>
@/Users/willmacdonald/.claude/get-shit-done/workflows/execute-plan.md
@/Users/willmacdonald/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@codex-code-review.md
@.github/workflows/deploy-backend.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Codex CI workflow and review prompt</name>
  <files>
    .github/workflows/codex-review.yml
    .github/codex/prompts/review.md
  </files>
  <action>
  **Create the review prompt** at `.github/codex/prompts/review.md`:

  ```markdown
  # Second Brain Code Review

  You are reviewing a pull request for the Second Brain project -- a personal AI-powered capture and organization system.

  ## Stack
  - Backend: Python 3.12, FastAPI, Azure AI Foundry (Agent Framework), Cosmos DB, App Insights
  - Mobile: React Native / Expo, TypeScript
  - CI/CD: GitHub Actions, Azure Container Apps

  ## Review Focus

  ### High Priority
  1. **Type safety**: TypeScript strict mode compliance (mobile), Python type hints (backend)
  2. **Test health**: Run `backend/.venv/bin/pytest -q` and `cd mobile && npx tsc --noEmit --pretty false`. Report any failures.
  3. **Async correctness**: Unawaited coroutines, missing await, leaked tasks
  4. **Security**: SSRF protection, API key exposure, SQL/NoSQL injection, hardcoded secrets

  ### Medium Priority
  5. **Error handling**: All external operations (API calls, DB, file I/O) wrapped in try/except with logging
  6. **Observability**: New code paths emit appropriate OTel spans or log messages
  7. **Consistency**: New patterns match existing codebase conventions (check middleware.py, tools/*.py patterns)

  ### Low Priority
  8. **Documentation**: Stale references in planning docs, missing docstrings on public functions
  9. **Dead code**: Unused imports, unreachable branches, commented-out code

  ## Output Format
  - Categorize findings as Critical, High, Medium, or Minor
  - Include file path and line number for each finding
  - Provide specific fix recommendations
  - If no issues found, say "No issues found" (don't pad with noise)

  ## What NOT to Flag
  - Style preferences (ruff handles formatting)
  - Import ordering (ruff handles this)
  - Line length (ruff config in pyproject.toml)
  - Planning doc formatting
  ```

  **Create the workflow** at `.github/workflows/codex-review.yml`:

  ```yaml
  name: Codex Code Review

  on:
    pull_request:
      types: [opened, synchronize]

  permissions:
    contents: read
    pull-requests: write

  jobs:
    review:
      runs-on: ubuntu-latest
      steps:
        - uses: actions/checkout@v4
          with:
            fetch-depth: 0

        - uses: openai/codex-action@v1
          id: codex
          with:
            openai-api-key: ${{ secrets.OPENAI_API_KEY }}
            prompt-file: .github/codex/prompts/review.md
            sandbox: read-only
            safety-strategy: drop-sudo

        - uses: actions/github-script@v7
          if: steps.codex.outputs.final-message != ''
          with:
            script: |
              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: context.issue.number,
                body: `## Codex Review\n\n${process.env.REVIEW}`
              });
          env:
            REVIEW: ${{ steps.codex.outputs.final-message }}
  ```

  Create both directories if they don't exist: `.github/codex/prompts/`.
  </action>
  <verify>
    <automated>cd /Users/willmacdonald/Documents/Code/claude/second-brain && cat .github/workflows/codex-review.yml | head -5 && cat .github/codex/prompts/review.md | head -5</automated>
  </verify>
  <done>
  - `.github/workflows/codex-review.yml` exists with PR trigger, read-only sandbox, and comment step
  - `.github/codex/prompts/review.md` exists with project-specific review instructions
  - Workflow uses `openai/codex-action@v1` with `sandbox: read-only` and `safety-strategy: drop-sudo`
  - Review output is posted as a PR comment via `actions/github-script`
  </done>
</task>

</tasks>

<verification>
1. `.github/workflows/codex-review.yml` exists and is valid YAML
2. `.github/codex/prompts/review.md` exists with review instructions
3. Workflow triggers on `pull_request: [opened, synchronize]`
4. Workflow has `contents: read` and `pull-requests: write` permissions
5. Codex step uses `sandbox: read-only`
</verification>

<success_criteria>
- Codex CI workflow file and review prompt are committed
- Workflow will run on next PR (after user adds OPENAI_API_KEY secret)
</success_criteria>

<output>
After completion, create `.planning/phases/17.4-foundry-observability-and-codex-code-review/17.4-03-SUMMARY.md`
</output>
