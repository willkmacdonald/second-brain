---
phase: 21-eval-framework
plan: 05
type: execute
wave: 4
depends_on: [21-04]
files_modified:
  - docs/foundry/investigation-agent-instructions.md
autonomous: false
requirements: [EVAL-05]
must_haves:
  truths:
    - "Investigation agent portal instructions document the three new eval tools"
    - "User can trigger a classifier eval from investigation chat and see results"
  artifacts:
    - path: "docs/foundry/investigation-agent-instructions.md"
      provides: "Updated investigation agent instructions with eval tool documentation"
  key_links:
    - from: "docs/foundry/investigation-agent-instructions.md"
      to: "backend/src/second_brain/tools/investigation.py"
      via: "Documents run_classifier_eval, run_admin_eval, get_eval_results tool usage"
      pattern: "run_classifier_eval|run_admin_eval|get_eval_results"
---

<objective>
Update investigation agent instructions for the Foundry portal to document the three new eval tools, then verify the full eval pipeline end-to-end on the deployed system.

Purpose: The investigation agent needs updated instructions to know about eval tools (D-05). The human-verify checkpoint confirms the pipeline works end-to-end after deployment.
Output: Updated portal instructions, end-to-end verification.
</objective>

<execution_context>
@/Users/willmacdonald/.claude/get-shit-done/workflows/execute-plan.md
@/Users/willmacdonald/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-eval-framework/21-RESEARCH.md
@.planning/phases/21-eval-framework/21-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update investigation agent portal instructions with eval tools</name>
  <files>docs/foundry/investigation-agent-instructions.md</files>
  <read_first>
    - docs/foundry/investigation-agent-instructions.md (current instructions)
    - backend/src/second_brain/tools/investigation.py (new eval tool signatures and docstrings)
  </read_first>
  <action>
    Add a new section to `docs/foundry/investigation-agent-instructions.md` documenting the three new eval tools. Place it after the existing "Feedback and Golden Dataset Tools" section.

    **New section content:**

    ```markdown
    ## Evaluation Tools

    You have three tools for managing evaluation runs:

    ### run_classifier_eval
    Triggers a classifier evaluation run against the golden dataset. The eval sends each golden dataset entry through the Classifier agent and measures accuracy (precision, recall, per-bucket breakdown, confidence calibration).

    - Takes no parameters
    - Returns a run ID for tracking progress
    - Eval runs 3-5 minutes for 50 cases (sequential, one at a time)
    - If an eval is already running, returns the existing run's progress instead

    When user asks "run classifier eval", "evaluate the classifier", or "how accurate is classification":
    1. Call run_classifier_eval to start the eval
    2. Report the run ID and estimated time
    3. Suggest user check back in a few minutes or call get_eval_results

    ### run_admin_eval
    Triggers an Admin Agent evaluation run against admin golden dataset entries. Measures routing accuracy (did items go to the correct destination?).

    - Requires routing_context parameter: the fixed set of destinations and affinity rules for deterministic evaluation
    - Returns a run ID for tracking progress

    When user asks "run admin eval" or "evaluate admin agent routing":
    1. Call run_admin_eval with the appropriate routing context
    2. Report the run ID and estimated time

    ### get_eval_results
    Retrieves the latest evaluation results from storage, plus any currently running eval status.

    - Optional eval_type parameter: "classifier" or "admin_agent" (null for all)
    - Optional limit parameter: how many results to return (default 3)
    - Returns aggregate scores (accuracy, precision, recall) and any running eval status

    When user asks "show eval results", "what's the classifier accuracy", or checks on a running eval:
    1. Call get_eval_results with the relevant eval_type
    2. Format the results as a clear summary:
       - Overall accuracy percentage
       - Per-bucket precision/recall table for classifier
       - Per-destination accuracy for admin
       - Highlight any failures or regressions
       - If an eval is currently running, show progress

    ### Formatting eval results
    Present eval results in this format:
    - **Accuracy**: X% (N/M correct)
    - **Per-bucket breakdown** as a markdown table with Bucket | Precision | Recall columns
    - **Failures** listed with input text snippet and expected vs actual
    - **Calibration** summary if available
    ```

    This matches D-07: results displayed as formatted investigation agent chat response.
  </action>
  <verify>
    <automated>grep -c "run_classifier_eval\|run_admin_eval\|get_eval_results" /Users/willmacdonald/Documents/Code/claude/second-brain/docs/foundry/investigation-agent-instructions.md</automated>
  </verify>
  <acceptance_criteria>
    - docs/foundry/investigation-agent-instructions.md contains "## Evaluation Tools" section
    - File contains documentation for run_classifier_eval, run_admin_eval, and get_eval_results
    - File contains formatting guidance for eval result presentation
    - grep for "run_classifier_eval" returns at least 2 matches (heading + usage)
  </acceptance_criteria>
  <done>Investigation agent instructions updated with eval tool documentation. Ready for portal paste.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Full eval framework: metrics computation, dry-run tools, eval runner, API endpoint, investigation agent tools, golden dataset seed script, and investigation agent instructions. The system can now evaluate Classifier and Admin Agent quality against golden datasets.
  </what-built>
  <how-to-verify>
    **Pre-requisites:**
    1. Deploy to Azure (push to main, CI/CD deploys)
    2. Paste updated investigation agent instructions into Foundry portal
    3. Seed golden dataset: export some inbox items, curate the JSON (mark as approved, verify expectedBucket values), import back

    **Verification steps:**
    1. Open the mobile investigation chat
    2. Say "run classifier eval"
    3. Confirm the agent responds with a run ID and "eval started" message
    4. Wait 3-5 minutes
    5. Say "show eval results"
    6. Confirm the agent displays:
       - Overall accuracy percentage
       - Per-bucket precision/recall table
       - Any failures with input snippet and expected vs actual
    7. Verify GET /api/eval/status/{run_id} returns completed status (via Claude Code MCP or curl)
    8. Verify EvalResults document exists in Cosmos (via Azure portal or investigation agent)

    **Expected outcomes:**
    - Eval runs against golden dataset without errors
    - Results show realistic accuracy (varies based on dataset quality)
    - Status endpoint tracks progress correctly
    - Results persisted in Cosmos EvalResults container
  </how-to-verify>
  <resume-signal>Type "approved" if eval pipeline works end-to-end, or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None new | Portal instructions are documentation only; deployed system security unchanged |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-11 | Information Disclosure | Portal instructions | accept | Instructions are internal documentation for Foundry agent. Not user-facing. No secrets. |
</threat_model>

<verification>
- Updated investigation agent instructions contain all three eval tool sections
- End-to-end eval pipeline verified by human on deployed system
</verification>

<success_criteria>
- Investigation agent understands eval tools and can trigger/display results
- Classifier eval runs successfully against golden dataset on deployed system
- Results visible in investigation chat with accuracy, per-bucket breakdown, and failures
</success_criteria>

<output>
After completion, create `.planning/phases/21-eval-framework/21-05-SUMMARY.md`
</output>
