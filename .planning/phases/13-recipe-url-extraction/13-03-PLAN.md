---
phase: 13-recipe-url-extraction
plan: 03
type: execute
wave: 3
depends_on: [13-01, 13-02]
files_modified: []
autonomous: false
requirements: [RCPE-01, RCPE-02, RCPE-03]

must_haves:
  truths:
    - "Admin Agent instructions in Foundry portal include guidance to call fetch_recipe_url when a URL is in the capture"
    - "Admin Agent instructions tell it to pass sourceName and sourceUrl when calling add_errand_items for recipe ingredients"
    - "End-to-end flow works: paste recipe URL -> classify as Admin -> Admin Agent fetches page -> ingredients appear on Status screen with source attribution"
    - "Error cases handled: fetch failure shows error notification, non-recipe page shows 'no recipe found'"
  artifacts: []
  key_links:
    - from: "Foundry portal Admin Agent instructions"
      to: "backend/src/second_brain/tools/recipe.py"
      via: "Agent calls fetch_recipe_url when capture contains a URL"
      pattern: "fetch_recipe_url"
---

<objective>
Update Admin Agent instructions in the Foundry portal to use the new fetch_recipe_url tool, then validate the full recipe URL extraction flow end-to-end via manual UAT against the deployed backend.

Purpose: The code is complete from Plans 01 and 02, but the Admin Agent won't call the new tool unless its instructions are updated. This plan handles the Foundry portal instruction update (human action) and end-to-end validation (human verify) to confirm all three requirements work together.

Output: Updated Admin Agent instructions, UAT results confirming recipe flow works.
</objective>

<execution_context>
@/Users/willmacdonald/.claude/get-shit-done/workflows/execute-plan.md
@/Users/willmacdonald/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-recipe-url-extraction/13-CONTEXT.md
@.planning/phases/13-recipe-url-extraction/13-01-SUMMARY.md
@.planning/phases/13-recipe-url-extraction/13-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-action">
  <name>Task 1: Update Admin Agent instructions in Foundry portal</name>
  <files>N/A - Foundry portal configuration</files>
  <action>
The Admin Agent instructions live in the Azure AI Foundry portal and are not in code. They need to be updated to tell the Agent about the new `fetch_recipe_url` tool and when/how to use it.

**Add the following section to the Admin Agent instructions in the Foundry portal** (append to the existing instructions):

```
## Recipe URL Extraction

When the user capture contains a URL (starts with http:// or https://):
1. ALWAYS call fetch_recipe_url with the URL to get the page content
2. From the returned content, extract the recipe name and all ingredients
3. Normalize each ingredient to shopper-friendly format (what you'd look for in-store):
   - Include quantities: "2 lbs ground beef", "14 oz can diced tomatoes"
   - Use common names: "diced tomatoes" not "Muir Glen Organic Diced Tomatoes"
4. Include ALL ingredients — do not skip pantry staples (salt, pepper, oil, water)
5. Assign each ingredient to the appropriate destination using routing context and affinity rules
6. Call add_errand_items with ALL ingredients in a single call. For each item include:
   - name: the shopper-friendly ingredient text (lowercase)
   - destination: the destination slug from routing context
   - sourceName: the recipe title (e.g., "Chicken Tikka Masala")
   - sourceUrl: the original URL from the user capture
7. If fetch_recipe_url returns an error, report the error message back
8. If the page loads but no recipe content is found, respond: "No recipe found on this page."
```

User must go to the Azure AI Foundry portal, navigate to the Admin Agent, and append the Recipe URL Extraction section to the existing instructions. Type "instructions updated" when done.
  </action>
  <verify>
    <automated>echo "Manual checkpoint: Admin Agent instructions updated in Foundry portal"</automated>
  </verify>
  <done>Admin Agent instructions in Foundry portal include the Recipe URL Extraction section with guidance on calling fetch_recipe_url and passing sourceName/sourceUrl to add_errand_items</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 2: End-to-end UAT of recipe URL extraction flow</name>
  <files>N/A - manual verification</files>
  <action>
Validate the complete recipe URL extraction pipeline end-to-end against the deployed backend. Plans 01 and 02 must be merged and deployed via CI/CD, and Task 1 (Foundry instructions) must be complete.

**Test 1: Happy path -- recipe URL with ingredients**
1. Open the capture screen in the mobile app
2. Type: `admin https://www.allrecipes.com/recipe/228285/chicken-tikka-masala/` (or any recipe URL)
3. Submit the capture
4. Open the Status screen
5. Wait for processing (processing indicator, then items appear)
6. Verify: Multiple ingredients appear on shopping lists. Each recipe item shows "from: [Recipe Name]" subtitle in muted gray. Tapping subtitle opens URL in browser. Items assigned to appropriate destinations.

**Test 2: Non-recipe URL**
1. Type: `admin https://www.example.com`
2. Submit and open Status screen
3. Verify: Admin notification appears saying "No recipe found on this page" or similar

**Test 3: Invalid/unreachable URL**
1. Type: `admin https://thisdomaindoesnotexist12345.com/recipe`
2. Submit and open Status screen
3. Verify: Admin notification appears with fetch error message

**Test 4: Source attribution display**
1. Look at items added from Test 1
2. Verify: "from: [Recipe Name]" in muted gray (#888) below recipe items. Non-recipe items do NOT show subtitle. Swipe-to-delete works normally on recipe items.

Report results: which tests passed and any issues found.
  </action>
  <verify>
    <automated>echo "Manual checkpoint: UAT results reviewed"</automated>
  </verify>
  <done>All four UAT tests pass: happy path produces ingredients with source attribution, non-recipe URL shows error notification, unreachable URL shows error notification, source attribution display is correct</done>
</task>

</tasks>

<verification>
1. Admin Agent instructions updated in Foundry portal with Recipe URL Extraction section
2. Happy path: recipe URL produces ingredients on shopping lists with source attribution
3. Error path: non-recipe URL shows "no recipe found" notification
4. Error path: unreachable URL shows fetch error notification
5. Source attribution: "from: Recipe Name" visible, tappable URL opens browser
</verification>

<success_criteria>
- Admin Agent correctly calls fetch_recipe_url when capture contains a URL
- Ingredients from recipe pages appear on the Status screen shopping lists
- Source attribution ("from: Recipe Name") is visible on recipe-sourced items
- Tapping source opens URL in device browser
- Error cases produce user-visible notifications (not silently dropped)
- Recipe items are swipe-deletable like regular items
</success_criteria>

<output>
After completion, create `.planning/phases/13-recipe-url-extraction/13-03-SUMMARY.md`
</output>
