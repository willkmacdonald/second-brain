---
phase: 19.4-native-span-correlation-tagging
plan: 04
type: execute
wave: 3
depends_on: ["19.4-02", "19.4-03"]
files_modified: []
autonomous: false
requirements: []
must_haves:
  truths:
    - "Operator drills from spine segment page to native telemetry and sees real data for a fresh capture"
    - "FoundryRunDetail renderer on the web segment page shows >=1 run (was showing 0)"
    - "All 4 emit sites produce filterable native telemetry for the same capture_trace_id"
    - "No regression in spine segment health or status board"
  artifacts: []
  key_links:
    - from: "web segment page (native renderers)"
      to: "AppRequests / AppDependencies / AzureDiagnostics"
      via: "capture_trace_id filter in KQL templates"
      pattern: "Properties.capture_trace_id"
---

> **Native Capability Check** (mandatory header per PRD)
> - Surface 1 / Tracing to App Insights -- verified by Plan 02 checkpoint
> - Surface 6 / Per-operation client-request-id -- verified by Plan 03 checkpoint
> - Surface 2 / Baggage propagation -- resolved by SpanProcessor approach (Plan 01 spike)

<objective>
End-to-end integrated verification of all 4 emit sites. Perform one fresh capture, then drill from the spine status board through the segment page to native telemetry. Confirm the web segment page's native renderers (FoundryRunDetail, AppInsights requests, Cosmos diagnostics) show real data instead of "runs (0)" / empty tables.

Purpose: This is Checkpoint D from the PRD -- the operator verifies the full drill-down path works end-to-end. Plans 02 and 03 verified individual sites; this plan verifies the integrated experience from the operator's perspective.

Output: Operator confirmation that the spine-to-native drill-down shows real data for all sites.
</objective>

<execution_context>
@/Users/willmacdonald/.claude/get-shit-done/workflows/execute-plan.md
@/Users/willmacdonald/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19.4-native-span-correlation-tagging/19.4-CONTEXT.md
@.planning/phases/19.4-native-span-correlation-tagging/19.4-02-SUMMARY.md
@.planning/phases/19.4-native-span-correlation-tagging/19.4-03-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint D: End-to-end spine-to-native drill-down verification</name>
  <files>web/app/segment/[id]/page.tsx</files>
  <action>Plans 02 and 03 deployed SpanProcessor (Sites 1, 2, 4) and extended trace_headers() (Site 3) to production. All native telemetry rows from a capture now carry capture_trace_id. Verify the full operator drill-down shows real data.

Verification steps:
1. Open the mobile app and send a text capture (note the X-Trace-Id if visible, or use curl with a known trace ID)
2. Wait 3-5 minutes for App Insights + Cosmos diagnostic log ingestion
3. Open the web status board at the deployed URL
4. Navigate to the `backend_api` segment page
5. Verify the native telemetry section shows the capture's AppRequests row (not empty)
6. Navigate to the `classifier` segment page
7. Verify the FoundryRunDetail renderer shows >=1 run for the recent capture (was showing 0 before Phase 19.4)
8. If Cosmos diagnostic tab is visible on any segment page, verify it shows Cosmos operations for the capture
9. Confirm: can you trace from spine status board -> segment -> native telemetry and see REAL data for this capture?
10. Confirm: the status board itself still shows correct segment health (no regression from SpanProcessor changes)</action>
  <verify>
    <automated>User types "approved" after confirming end-to-end drill-down shows real native telemetry for a fresh capture.</automated>
  </verify>
  <done>Operator confirms: spine status board -> segment page -> native telemetry drill-down shows real data for a fresh capture. FoundryRunDetail shows >=1 run. No regression in status board health.</done>
</task>

</tasks>

<verification>
- Fresh capture produces filterable native telemetry across all 4 sites
- Web segment page shows real data instead of "runs (0)"
- Status board health is not regressed
- Operator confirms end-to-end drill-down works
</verification>

<success_criteria>
- Checkpoint D approved: operator drills spine -> segment -> native and sees real data
- FoundryRunDetail shows >=1 run for a capture with an active Foundry run
- No regression in spine status board or segment health
</success_criteria>

<output>
After completion, create `.planning/phases/19.4-native-span-correlation-tagging/19.4-04-SUMMARY.md`
</output>
