---
phase: 19.4-native-span-correlation-tagging
plan: 02
type: execute
wave: 2
depends_on: ["19.4-01"]
files_modified:
  - backend/src/second_brain/observability/span_processor.py
  - backend/src/second_brain/main.py
  - backend/src/second_brain/api/capture.py
  - backend/tests/test_span_processor.py
autonomous: false
requirements: []
must_haves:
  truths:
    - "For any capture_trace_id, KQL against AppRequests returns the request span with capture.trace_id as a custom dimension"
    - "Foundry agent spans in AppDependencies carry capture.trace_id for the same capture"
    - "Investigation custom spans carry capture.trace_id when invoked in a capture context"
    - "Non-capture requests (health checks, errand reads) do NOT get spurious capture_trace_id attributes"
  artifacts:
    - path: "backend/src/second_brain/observability/span_processor.py"
      provides: "CaptureTraceSpanProcessor that injects capture_trace_id from ContextVar onto every span"
      exports: ["CaptureTraceSpanProcessor"]
    - path: "backend/tests/test_span_processor.py"
      provides: "Unit tests for SpanProcessor behavior"
      min_lines: 30
  key_links:
    - from: "backend/src/second_brain/observability/span_processor.py"
      to: "backend/src/second_brain/main.py"
      via: "configure_azure_monitor(span_processors=[CaptureTraceSpanProcessor()])"
      pattern: "span_processors.*CaptureTraceSpanProcessor"
    - from: "backend/src/second_brain/api/capture.py"
      to: "backend/src/second_brain/tools/classification.py"
      via: "capture_trace_id_var.set() at handler entry"
      pattern: "capture_trace_id_var\\.set"
---

> **Native Capability Check** (mandatory header per PRD)
> - Surface 2 / Baggage propagation -- spike memo resolved this; SpanProcessor approach used instead
> - Surface 1 / Tracing to App Insights -- gap closed by CaptureTraceSpanProcessor
> - Surface 2 / Cross-process context -- httpx not auto-instrumented -- deferred (low priority)

<objective>
Implement the SPIKE-MEMO section 5 recommendations for Sites 1, 2, and 4 (backend_api AppRequests, Foundry agent spans, investigation custom spans). The CaptureTraceSpanProcessor created in Plan 01 may need refinement based on spike findings; this plan finalizes it with full test coverage and confirms it works in production via KQL.

NOTE: This plan's exact task list is determined by SPIKE-MEMO.md section 5 numbered recommendations. The tasks below are the PREDICTED shape based on RESEARCH.md's strong prediction that the SpanProcessor approach works. If the spike memo reveals a different pattern, the executor must follow the memo's recommendations, not these predicted tasks.

Purpose: After this plan, every App Insights span created during a capture carries `capture.trace_id` as a custom dimension. The web segment page's native renderers can filter by capture_trace_id and show real data instead of "runs (0)".

Output: Production-deployed SpanProcessor with test coverage, confirmed by live KQL query.
</objective>

<execution_context>
@/Users/willmacdonald/.claude/get-shit-done/workflows/execute-plan.md
@/Users/willmacdonald/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19.4-native-span-correlation-tagging/19.4-CONTEXT.md
@.planning/phases/19.4-native-span-correlation-tagging/19.4-RESEARCH.md
@.planning/phases/19.4-native-span-correlation-tagging/SPIKE-MEMO.md
@.planning/phases/19.4-native-span-correlation-tagging/19.4-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts from existing codebase -->

From backend/src/second_brain/tools/classification.py:
```python
capture_trace_id_var: contextvars.ContextVar[str] = contextvars.ContextVar(
    "capture_trace_id_var", default=""
)
```

From backend/src/second_brain/main.py:
```python
configure_azure_monitor(logger_name="second_brain")
# becomes:
configure_azure_monitor(logger_name="second_brain", span_processors=[CaptureTraceSpanProcessor()])
```

From backend/src/second_brain/api/capture.py (4 handlers):
```python
capture_trace_id = request.headers.get("X-Trace-Id", str(uuid4()))
request.state.capture_trace_id = capture_trace_id
# must add: capture_trace_id_var.set(capture_trace_id) HERE
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Finalize SpanProcessor and ContextVar timing per spike memo</name>
  <files>
    backend/src/second_brain/observability/span_processor.py
    backend/src/second_brain/main.py
    backend/src/second_brain/api/capture.py
  </files>
  <action>
Apply SPIKE-MEMO.md section 5 recommendations verbatim. The PREDICTED implementation (if spike confirms SpanProcessor works):

1. **Refine CaptureTraceSpanProcessor** (if spike found issues in Plan 01's minimal version):
   - Ensure `on_start` reads `capture_trace_id_var.get("")` and only sets attribute when non-empty
   - Ensure attribute name is `capture.trace_id` (matching existing KQL templates that filter on `Properties.capture_trace_id` -- verify the attribute name in KQL templates uses dot notation `capture.trace_id` vs underscore)
   - Add `shutdown` and `force_flush` no-op methods for completeness

2. **Verify ContextVar timing fix** from Plan 01:
   - If spike found AppRequests span DID get the tag: Plan 01's handler-level `capture_trace_id_var.set()` worked. Confirm it remains in all 4 handlers.
   - If spike found AppRequests span did NOT get the tag: additional fix needed (the spike memo will specify what)

3. **Confirm main.py registration** is correct:
   - `configure_azure_monitor(logger_name="second_brain", span_processors=[CaptureTraceSpanProcessor()])`
   - Registration MUST happen BEFORE `enable_instrumentation()` (which is the current code order)

CRITICAL: Follow memo section 5 recommendations EXACTLY. If the memo says something different from these predictions, the memo wins.

Verify the attribute name consistency: the KQL templates in `observability/kql_templates.py` reference `Properties.capture_trace_id` (with underscore). OTel span attributes use dots by convention (`capture.trace_id`). Azure Monitor converts dots to underscores in Properties column names. So `span.set_attribute("capture.trace_id", value)` should appear as `Properties.capture_trace_id` in KQL. Confirm this mapping during the spike; if it doesn't match, align the attribute name.
  </action>
  <verify>
    <automated>cd /Users/willmacdonald/Documents/Code/claude/second-brain/backend && python3 -m pytest tests/ -x --timeout=30 -q 2>&1 | tail -5</automated>
  </verify>
  <done>SpanProcessor finalized per spike memo recommendations. All existing tests pass. main.py registration correct. ContextVar timing confirmed.</done>
</task>

<task type="auto">
  <name>Task 2: Add unit tests for CaptureTraceSpanProcessor</name>
  <files>
    backend/tests/test_span_processor.py
  </files>
  <action>
Create `backend/tests/test_span_processor.py` with tests:

1. **test_on_start_sets_attribute_when_contextvar_has_value**: Set `capture_trace_id_var` to a known value, create a mock span, call `on_start`, assert `span.set_attribute` called with `("capture.trace_id", value)`.

2. **test_on_start_skips_when_contextvar_empty**: Ensure `capture_trace_id_var` is empty (default), create a mock span, call `on_start`, assert `span.set_attribute` NOT called.

3. **test_on_start_skips_when_contextvar_default**: Same as above but explicitly reset the ContextVar to default.

4. **test_on_end_is_noop**: Call `on_end` with a mock span, assert no side effects.

5. **test_processor_is_registered_in_main** (static verification): Import `main` and verify `CaptureTraceSpanProcessor` is used. This can be a source scan test: `grep -q "CaptureTraceSpanProcessor" backend/src/second_brain/main.py`.

Use `unittest.mock.MagicMock` for the span object. Use `capture_trace_id_var.set()` / `.reset()` for ContextVar manipulation in tests. Clean up ContextVar state in teardown to prevent test pollution.

Write the entire test file in a single Write to prevent ruff auto-format stripping unused imports (MEMORY.md lesson).
  </action>
  <verify>
    <automated>cd /Users/willmacdonald/Documents/Code/claude/second-brain/backend && python3 -m pytest tests/test_span_processor.py -x -v --timeout=30 2>&1 | tail -15</automated>
  </verify>
  <done>5+ unit tests for CaptureTraceSpanProcessor pass. Tests cover: ContextVar set (attribute injected), ContextVar empty (skipped), on_end noop, registration check.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint B: Verify AppRequests and AppDependencies carry capture_trace_id</name>
  <files>backend/src/second_brain/observability/span_processor.py</files>
  <action>CaptureTraceSpanProcessor finalized with test coverage per spike memo recommendations and deployed to production. Verify that Sites 1, 2, and 4 now tag spans with capture.trace_id by running live KQL queries.

Verification steps:
1. Trigger one fresh capture via the mobile app or curl
2. Wait 3-5 minutes for App Insights ingestion
3. Run KQL: `AppRequests | where TimeGenerated > ago(15m) | where tostring(Properties.capture_trace_id) != "" | take 5`
4. Verify at least one row has your capture's trace ID
5. Run KQL: `AppDependencies | where TimeGenerated > ago(15m) | where tostring(Properties.capture_trace_id) != "" | take 10`
6. Verify Foundry agent spans carry the same trace ID</action>
  <verify>
    <automated>User types "approved" after confirming KQL shows capture_trace_id on both AppRequests and AppDependencies.</automated>
  </verify>
  <done>Operator confirms via live KQL that AppRequests and AppDependencies spans carry capture_trace_id as a custom dimension for a fresh capture.</done>
</task>

</tasks>

<verification>
- CaptureTraceSpanProcessor unit tests pass
- All existing backend tests pass (no regressions)
- Production KQL confirms capture_trace_id appears on AppRequests and AppDependencies spans
</verification>

<success_criteria>
- For any recent capture_trace_id, AppRequests rows carry it as Properties.capture_trace_id
- Foundry agent spans (AppDependencies) carry the same capture_trace_id
- Non-capture requests have empty/absent capture_trace_id (no noise)
- Checkpoint B approved by operator with KQL evidence
</success_criteria>

<output>
After completion, create `.planning/phases/19.4-native-span-correlation-tagging/19.4-02-SUMMARY.md`
</output>
