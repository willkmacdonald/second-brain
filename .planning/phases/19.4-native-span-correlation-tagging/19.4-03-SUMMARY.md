---
phase: 19.4-native-span-correlation-tagging
plan: 03
subsystem: observability
tags: [cosmos, trace-headers, static-analysis, x-ms-client-request-id, app-dependencies]

# Dependency graph
requires:
  - phase: 19.4-native-span-correlation-tagging (Plan 01)
    provides: "SPIKE-MEMO Cosmos audit identifying 3 missing trace_headers() call sites"
  - phase: 19.4-native-span-correlation-tagging (Plan 02)
    provides: "trace_headers() added to all 3 missing sites (adapter.py, capture.py x2); Cosmos KQL path corrected to AppDependencies"
provides:
  - "Static scan test ensuring all capture-correlated Cosmos writes use trace_headers() (CI regression guard)"
  - "Live KQL confirmation that Cosmos operations are queryable by capture.trace_id via AppDependencies"
affects: [19.4-04-PLAN]

# Tech tracking
tech-stack:
  added: []
  patterns: [static source scan tests for instrumentation coverage enforcement]

key-files:
  created:
    - backend/tests/test_cosmos_trace_headers_coverage.py
  modified: []

key-decisions:
  - "Cosmos correlation verified via AppDependencies (SpanProcessor-tagged) not AzureDiagnostics activityId_g -- consistent with spike memo section 6 decision"
  - "Task 1 (add trace_headers to remaining sites) was already completed in Plan 02 commit 89af305 -- SKIPPED to avoid duplication"

patterns-established:
  - "Static source scan pattern: pytest test scans capture-correlated source files for Cosmos write calls and asserts trace_headers() presence -- prevents regression without runtime overhead"

requirements-completed: []

# Metrics
duration: 8min
completed: 2026-04-20
---

# Phase 19.4 Plan 03: Cosmos trace_headers Coverage Summary

**Static scan test guards all capture-correlated Cosmos writes; live KQL confirms AppDependencies pivot by capture.trace_id works for Cosmos operations**

## Performance

- **Duration:** ~8 min (Task 1 skipped -- done in Plan 02; Task 2 committed earlier; Task 3 checkpoint approved)
- **Started:** 2026-04-20T06:33:00Z
- **Completed:** 2026-04-20T13:03:00Z (checkpoint approval wait)
- **Tasks:** 3 (1 skipped, 1 auto, 1 checkpoint)
- **Files modified:** 1

## Accomplishments
- Static source scan test (`test_cosmos_trace_headers_coverage.py`) scans all capture-correlated source files for Cosmos write calls (`create_item`, `upsert_item`, `replace_item`) and asserts `trace_headers` usage -- catches future regressions in CI
- Verified that non-capture-correlated files (api/inbox.py, api/tasks.py, api/errands.py) do NOT use trace_headers -- prevents over-instrumentation of user-initiated operations
- Operator confirmed via live KQL that Cosmos operations are queryable by capture.trace_id through AppDependencies (Plan 02 checkpoint B evidence reused -- same mechanism)

## Task Commits

Each task was committed atomically:

1. **Task 1: Add trace_headers() to remaining capture-correlated Cosmos call sites** - SKIPPED (already done in Plan 02: `89af305`)
2. **Task 2: Add static scan test for trace_headers coverage** - `52edfc9` (test)
3. **Task 3: Checkpoint C: Verify Cosmos activityId_g pivot works** - approved by operator (no commit)

**Plan metadata:** (this commit)

## Files Created/Modified
- `backend/tests/test_cosmos_trace_headers_coverage.py` - Static source scan ensuring all capture-correlated Cosmos writes use trace_headers(); also verifies non-capture files don't use it

## Decisions Made
- **Task 1 skip:** The spike memo's 3 missing trace_headers() sites (adapter.py:131, capture.py:122, capture.py:179) were already fixed in Plan 02 commit 89af305. No additional work needed.
- **Cosmos correlation path:** Checkpoint C approved using AppDependencies query path (not AzureDiagnostics activityId_g), consistent with the spike memo section 6 finding that activityId_g holds server-side UUIDs, not x-ms-client-request-id values.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Task 1 already completed in Plan 02**
- **Found during:** Task 1 (Add trace_headers to remaining sites)
- **Issue:** Plan 02 (commit 89af305) already added trace_headers() to all 3 missing capture-correlated sites identified by the spike memo audit
- **Fix:** Skipped Task 1 entirely -- no code changes needed
- **Files modified:** None
- **Verification:** Existing tests pass; Task 2's static scan confirms all sites are covered
- **Committed in:** N/A (no code change)

---

**Total deviations:** 1 auto-fixed (1 blocking -- Task 1 overlap with Plan 02)
**Impact on plan:** Task 1 was redundant with Plan 02's scope. Static scan (Task 2) and live verification (Task 3) are the actual value of this plan.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- Plan 04 (integrated verification) may be reduced in scope or marked complete -- the SpanProcessor already covers all 4 emit sites (confirmed in Plans 01-03), and the static scan test prevents regression
- All capture-correlated Cosmos writes now use trace_headers() with CI enforcement
- The primary Cosmos correlation path is AppDependencies (not AzureDiagnostics), which is more reliable and requires no mapping table

## Self-Check: PASSED

- [x] backend/tests/test_cosmos_trace_headers_coverage.py exists
- [x] 19.4-03-SUMMARY.md exists
- [x] Commit 52edfc9 exists
- [x] Commit 89af305 exists (Plan 02 overlap)

---
*Phase: 19.4-native-span-correlation-tagging*
*Completed: 2026-04-20*
