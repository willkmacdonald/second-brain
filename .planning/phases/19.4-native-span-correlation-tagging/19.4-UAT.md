---
status: complete
phase: 19.4-native-span-correlation-tagging
source: [19.4-01-SUMMARY.md, 19.4-02-SUMMARY.md, 19.4-03-SUMMARY.md, 19.4-04-SUMMARY.md]
started: 2026-04-20T22:10:00Z
updated: 2026-04-21T02:45:00Z
---

## Current Test

[testing complete]

## Tests

### 1. AppRequests tagged with capture.trace_id
expected: Send a capture. KQL query against AppRequests shows the POST /api/capture row with a non-empty Properties.capture.trace_id custom dimension.
result: pass

### 2. Foundry agent spans tagged with capture.trace_id
expected: For the same capture, KQL query against AppDependencies filtered by capture.trace_id shows Foundry spans (chat unknown, get_agent, process_thread_run, submit_tool_outputs).
result: pass

### 3. Cosmos operations queryable by capture.trace_id
expected: For the same capture, KQL query against AppDependencies filtered by capture.trace_id shows Cosmos SDK spans (ContainerProxy.create_item, ContainerProxy.upsert_item, POST /dbs/.../docs/).
result: pass

### 4. Web backend_api segment shows transaction data
expected: Open the web status board, navigate to backend_api segment page. The native telemetry section shows the recent capture's AppRequests row (not empty / "no recent transactions").
result: pass

### 5. Web classifier segment shows Foundry runs
expected: Navigate to classifier segment page. The FoundryRunDetail renderer shows >=1 run for the recent capture (was showing 0 before Phase 19.4).
result: pass

### 6. Non-capture requests have no spurious tags
expected: KQL query for health check requests should NOT have capture.trace_id: AppRequests where Name == "GET /health" and Properties.capture.trace_id != "" should return 0 rows.
result: pass

### 7. SpanProcessor regression tests pass
expected: Running pytest tests/test_span_processor.py should show all tests passing (ContextVar set/empty/default, on_end noop, registration).
result: pass

### 8. Static trace_headers coverage test passes
expected: Running pytest tests/test_cosmos_trace_headers_coverage.py should show all tests passing (capture-correlated files use trace_headers, non-capture files don't).
result: pass

### 9. Admin agent appears in Transaction Path
expected: Send a capture classified as Admin, open errands screen to trigger processing. Transaction Path shows 4 segments including admin with process_capture operation.
result: pass

## Summary

total: 9
passed: 9
issues: 0
pending: 0
skipped: 0

## Gaps

[none]
