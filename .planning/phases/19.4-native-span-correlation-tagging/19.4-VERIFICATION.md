---
phase: 19.4-native-span-correlation-tagging
verified: 2026-04-20T23:10:00Z
status: passed
score: 5/5 must-haves verified
---

# Phase 19.4: Native Span Correlation Tagging Verification Report

**Phase Goal:** Every native telemetry row produced during a capture is filterable by capture_trace_id. The drill-down from spine to native telemetry stops being blind.
**Verified:** 2026-04-20T23:10:00Z
**Status:** passed
**Re-verification:** No -- initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | For any recent capture_trace_id, a KQL query against AppRequests returns the request span with capture_trace_id as a custom dimension | VERIFIED | `get_current_span().set_attribute("capture.trace_id", capture_trace_id)` in all 4 capture handlers (`capture.py` lines 226-228, 305-307, 372-374, 461-463). Spike memo Site 1 initially failed; Plan 02 commit `89af305` fixed with retroactive tagging. Live KQL evidence in 02-SUMMARY (capture `e4c6297e-...`). |
| 2 | Foundry agent spans in AppDependencies carry capture.trace_id for the same capture | VERIFIED | `CaptureTraceSpanProcessor.on_start()` injects attribute on all spans when ContextVar is set (`span_processor.py` lines 28-30). Spike memo confirmed 20+ Foundry spans tagged (Site 2 PASS). Live KQL in SPIKE-MEMO section 2. |
| 3 | Given a capture_trace_id, Cosmos operations are filterable (intent: via AppDependencies, not AzureDiagnostics activityId_g as originally written) | VERIFIED | Spike empirically proved `activityId_g` holds server-side UUIDs, not `x-ms-client-request-id`. New `COSMOS_BY_CAPTURE_TRACE` KQL template queries `AppDependencies` by `Properties.["capture.trace_id"]` (kql_templates.py lines 463-480). `fetch_cosmos_diagnostics()` routes to this template when `capture_trace_id` provided (queries.py lines 667-673). 9 Cosmos SDK spans confirmed tagged in spike memo Site 3. |
| 4 | The web segment page's native FoundryRunDetail renderer shows >=1 run for a capture with an active Foundry run (was showing 0) | VERIFIED | `FoundryRunDetail` component exists and renders `capture_trace_id` data (`web/components/renderers/FoundryRunDetail.tsx`). Wired into segment page (`web/app/segment/[id]/page.tsx` line 131). `AGENT_RUNS` KQL template includes `capture_trace_id` in projection (kql_templates.py line 386). Plan 04 Summary confirms operator verified both backend_api and classifier segments show transaction data for fresh capture `c439fe76-...`. |
| 5 | Investigation custom spans are filterable by correlation ID identically | VERIFIED | `CaptureTraceSpanProcessor` covers all spans uniformly. Investigation spans within a capture context get `capture.trace_id` automatically. Spike memo Site 4: "PASS by design -- SpanProcessor no-ops when ContextVar is empty." The `capture_text` app-created span confirmed working (same mechanism). |

**Score:** 5/5 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `backend/src/second_brain/observability/span_processor.py` | CaptureTraceSpanProcessor | VERIFIED | 39 lines, exports `CaptureTraceSpanProcessor(SpanProcessor)`, reads ContextVar, sets `capture.trace_id` attribute. Imported and registered in `main.py` line 23. |
| `backend/tests/test_span_processor.py` | Unit tests for SpanProcessor | VERIFIED | 117 lines, 7 tests covering ContextVar set/empty/reset, on_end noop, shutdown, force_flush, registration scan. All pass. |
| `backend/tests/test_cosmos_trace_headers_coverage.py` | Static scan for trace_headers | VERIFIED | 192 lines, 3 tests: capture-correlated files use trace_headers, non-capture files don't, import sanity check. All pass. |
| `backend/src/second_brain/main.py` | SpanProcessor registered | VERIFIED | Line 23: `span_processors=[CaptureTraceSpanProcessor()]` in `configure_azure_monitor()`. Import at line 16. |
| `backend/src/second_brain/api/capture.py` | ContextVar set + retroactive span tag | VERIFIED | All 4 handlers: `capture_trace_id_var.set()` + `get_current_span().set_attribute("capture.trace_id", ...)`. Import of `trace` (line 19), `capture_trace_id_var` (line 29), `trace_headers` (line 22). |
| `backend/src/second_brain/streaming/adapter.py` | trace_headers on safety-net create_item | VERIFIED | Line 133: `**trace_headers(capture_trace_id or None)`. Import at line 21. |
| `backend/src/second_brain/observability/kql_templates.py` | COSMOS_BY_CAPTURE_TRACE template + corrected docs | VERIFIED | Lines 463-480: new AppDependencies-based Cosmos correlation query. Lines 427-430: corrected comment documenting activityId_g is NOT x-ms-client-request-id. |
| `backend/src/second_brain/observability/queries.py` | Cosmos query uses AppDependencies path | VERIFIED | Lines 667-673: `fetch_cosmos_diagnostics()` routes to `COSMOS_BY_CAPTURE_TRACE` when `capture_trace_id` provided. |
| `.planning/phases/19.4-native-span-correlation-tagging/SPIKE-MEMO.md` | Empirical spike results | VERIFIED | 210 lines with per-site verification table, ContextVar timing analysis, 24-site Cosmos audit, 6 numbered recommendations, activityId_g mapping decision, Plan 02 scope recommendation. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `span_processor.py` | `main.py` | `configure_azure_monitor(span_processors=[CaptureTraceSpanProcessor()])` | WIRED | Import line 16, registration line 23 |
| `capture.py` | `classification.py` | `capture_trace_id_var.set()` at handler entry | WIRED | 4 handler sites (lines 222, 303, 370, 459) |
| `capture.py` | `opentelemetry.trace` | `get_current_span().set_attribute()` retroactive tag | WIRED | Import line 19, 4 call sites (lines 226, 305, 372, 461) |
| `kql_templates.py` | `queries.py` | `COSMOS_BY_CAPTURE_TRACE` template used in `fetch_cosmos_diagnostics()` | WIRED | Import line 19 of queries.py, usage line 670 |
| `capture.py` | `cosmos_request_id.py` | `trace_headers()` on Cosmos writes | WIRED | Import line 22, usage lines 126, 185 |
| `adapter.py` | `cosmos_request_id.py` | `trace_headers()` on safety-net create_item | WIRED | Import line 21, usage line 133 |
| Web segment page | `FoundryRunDetail` | Component import and render | WIRED | Import line 4 of `page.tsx`, render line 131 |

### Requirements Coverage

No explicit requirement IDs -- this is an inserted phase from the superpowers observability evolution spec. No requirements mapped to Phase 19.4 in REQUIREMENTS.md.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| (none) | - | - | - | No anti-patterns found in any phase-modified files |

### Human Verification Required

All human verification checkpoints were already completed by the operator during plan execution:

1. **Checkpoint A (Plan 01):** Spike memo approved, Plan 02 scope locked
2. **Checkpoint B (Plan 02):** Live KQL confirmed AppRequests and AppDependencies carry capture_trace_id for capture `e4c6297e-c7e4-4268-bc4a-86ba2c0e9d32`
3. **Checkpoint C (Plan 03):** Cosmos operations queryable by capture.trace_id via AppDependencies
4. **Checkpoint D (Plan 04):** End-to-end spine-to-native drill-down verified with capture `c439fe76-7a7e-4cee-a441-685599734527` -- both backend_api and classifier segment pages show transaction data

### Notes on Success Criteria vs Implementation

**SC3 (Cosmos correlation via AzureDiagnostics activityId_g):** The spike memo empirically disproved the ROADMAP assumption that `activityId_g` holds `x-ms-client-request-id`. The intent (Cosmos operations filterable by capture_trace_id) IS satisfied via AppDependencies where `CaptureTraceSpanProcessor` tags Cosmos SDK spans with `capture.trace_id`. A new `COSMOS_BY_CAPTURE_TRACE` KQL template was created for this path. The original `COSMOS_DIAGNOSTIC_LOGS` template is preserved for non-capture-correlated queries (server-side metrics).

**SC4 (FoundryRunDetail shows >=1 run):** Operator confirmed in Checkpoint D that web segment pages for both backend_api and classifier show transaction data within the 1-hour default time window. The `AGENT_RUNS` KQL template projects `capture_trace_id` from `Properties.capture_trace_id`.

### Gaps Summary

No gaps found. All 5 success criteria are satisfied (SC3 via the corrected AppDependencies path rather than the originally-assumed AzureDiagnostics path). The implementation includes:

- CaptureTraceSpanProcessor covering Sites 2 (Foundry) and 4 (Investigation) automatically
- Retroactive AppRequests tagging via `get_current_span().set_attribute()` for Site 1
- AppDependencies-based Cosmos correlation for Site 3
- 10 regression tests (7 SpanProcessor + 3 static scan)
- Live KQL evidence from 3 operator checkpoints
- 8 commits with atomic task-level granularity

---

_Verified: 2026-04-20T23:10:00Z_
_Verifier: Claude (gsd-verifier)_
