---
phase: 19.4-native-span-correlation-tagging
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/19.4-native-span-correlation-tagging/SPIKE-MEMO.md
autonomous: false
requirements: []
must_haves:
  truths:
    - "Operator has empirical evidence (deployed test, not theory) about whether CaptureTraceSpanProcessor injects capture_trace_id onto all 4 emit sites"
    - "The ContextVar timing question is answered: does capture_trace_id_var have a value when the AppRequests on_start fires?"
    - "Cosmos call site audit identifies which call sites are capture-correlated and which already use trace_headers()"
    - "Plan 02 scope is locked by memo findings -- if SpanProcessor covers Sites 1+2+4, Plan 02 is one task; if not, Plan 02 has per-site tasks"
  artifacts:
    - path: ".planning/phases/19.4-native-span-correlation-tagging/SPIKE-MEMO.md"
      provides: "Empirical SpanProcessor test results, ContextVar timing answer, Cosmos audit, Plan 02 scope recommendation"
      contains: "Per-site verification table"
  key_links:
    - from: "SPIKE-MEMO.md"
      to: "Plan 02 (SpanProcessor implementation)"
      via: "memo findings determine whether SpanProcessor covers Sites 1+2+4 or each needs explicit tagging"
      pattern: "site.*pass|site.*fail"
---

> **Native Capability Check** (mandatory header per PRD)
> - Surface 2 / Baggage propagation -- unverified -- this spike memo tests it empirically
> - Surface 6 / Per-operation client-request-id -- proven -- spike audits remaining call sites
> - Surface 1 / Tracing to App Insights -- gap confirmed -- spike tests SpanProcessor remedy
> - Surface 2 / Cross-process context -- httpx not auto-instrumented -- low priority, noted

<objective>
Deploy a minimal `CaptureTraceSpanProcessor` to the production backend, trigger one fresh capture, query KQL for each of the 4 emit sites, and produce a memo documenting which sites received `capture_trace_id` as a custom dimension and which did not. Also audit all Cosmos SDK call sites in the capture-correlated code path to identify which already use `trace_headers()` and which are missing it.

Purpose: CONTEXT.md makes the spike a non-negotiable first step (locked decision). The RESEARCH.md strongly predicts the SpanProcessor approach will work for Sites 1, 2, and 4, but the ContextVar timing through Foundry SDK async internals needs empirical confirmation. The memo locks Plan 02 scope -- if SpanProcessor works, Plans 02+04 from the PRD merge into one plan.

Output:
- `.planning/phases/19.4-native-span-correlation-tagging/SPIKE-MEMO.md` with:
  - Per-site verification table (4 rows): `site_name`, `kql_query_used`, `rows_returned`, `capture_trace_id_present`, `pass/fail`
  - ContextVar timing answer: Did AppRequests span get capture_trace_id? If not, where must capture_trace_id_var.set() move?
  - Cosmos call site audit: table of all create_item/upsert_item/replace_item calls in capture-correlated code paths with `has_trace_headers` column
  - activityId_g mapping decision: new correlation-map table needed or direct equality filter sufficient?
  - Numbered recommendations (section 5) that become Plan 02 tasks verbatim (pattern from 19.2-01)
- A human checkpoint confirming Plan 02 scope before it executes
</objective>

<execution_context>
@/Users/willmacdonald/.claude/get-shit-done/workflows/execute-plan.md
@/Users/willmacdonald/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19.4-native-span-correlation-tagging/19.4-CONTEXT.md
@.planning/phases/19.4-native-span-correlation-tagging/19.4-RESEARCH.md
@.planning/phases/19.4-native-span-correlation-tagging/PRD.md

<!-- Key source files for the spike -->
<!-- backend/src/second_brain/main.py -- configure_azure_monitor() registration point (line 19) -->
<!-- backend/src/second_brain/tools/classification.py -- capture_trace_id_var ContextVar (lines 38-39) -->
<!-- backend/src/second_brain/api/capture.py -- request.state.capture_trace_id assignment (lines 211, 283, 343, 425) -->
<!-- backend/src/second_brain/streaming/adapter.py -- capture_trace_id_var.set() inside generators (lines 171, 368, 578) -->
<!-- backend/src/second_brain/spine/cosmos_request_id.py -- trace_headers() helper -->
<!-- backend/src/second_brain/processing/admin_handoff.py -- trace_headers usage (lines 123, 187) -->

<!-- MCP tools available for deployed queries -->
<!-- audit_correlation: spine <-> native three-way join -->
<!-- /investigate slash command: ad-hoc KQL against deployed App Insights -->
<!-- az cosmosdb sql query: direct Cosmos queries -->

<!-- Inventory reference -->
@docs/superpowers/specs/native-observability-inventory.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deploy CaptureTraceSpanProcessor and set ContextVar early</name>
  <files>
    backend/src/second_brain/observability/span_processor.py
    backend/src/second_brain/main.py
    backend/src/second_brain/api/capture.py
  </files>
  <action>
Create `backend/src/second_brain/observability/span_processor.py` with `CaptureTraceSpanProcessor(SpanProcessor)`:
- `on_start(self, span, parent_context=None)`: read `capture_trace_id_var.get("")`, if non-empty call `span.set_attribute("capture.trace_id", value)`
- `on_end(self, span)`: no-op
- Import `capture_trace_id_var` from `second_brain.tools.classification`
- Import `SpanProcessor` from `opentelemetry.sdk.trace`
- Import `Span` from `opentelemetry.trace`

Modify `backend/src/second_brain/main.py` line 19:
- Change `configure_azure_monitor(logger_name="second_brain")` to `configure_azure_monitor(logger_name="second_brain", span_processors=[CaptureTraceSpanProcessor()])`
- Add import of `CaptureTraceSpanProcessor` from `second_brain.observability.span_processor` BEFORE the configure call (respecting the noqa E402 pattern)

Modify `backend/src/second_brain/api/capture.py` -- in each of the 4 capture handler functions (lines ~210, ~282, ~342, ~424), add `capture_trace_id_var.set(capture_trace_id)` immediately AFTER `request.state.capture_trace_id = capture_trace_id`. This ensures the ContextVar is set at handler entry time, BEFORE the StreamingResponse generator runs, so the SpanProcessor's on_start for the AppRequests span can read it. Import `capture_trace_id_var` from `second_brain.tools.classification` at the top of the file.

CRITICAL: The adapter.py already sets capture_trace_id_var inside the generator (lines 171, 368, 578). The capture.py handler-level set is ADDITIVE -- it makes the value available earlier (at request time) while the adapter set refreshes it inside the generator context. Do NOT remove the adapter.py sets.

Write import + usage in a single Write per file to prevent ruff auto-format from stripping the import (MEMORY.md Phase 17.1 lesson).
  </action>
  <verify>
    <automated>cd /Users/willmacdonald/Documents/Code/claude/second-brain/backend && python3 -m pytest tests/ -x --timeout=30 -q 2>&1 | tail -5</automated>
  </verify>
  <done>CaptureTraceSpanProcessor exists, registered in main.py, capture_trace_id_var set early in all 4 capture handlers. All existing tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Deploy, trigger capture, query KQL for all 4 sites</name>
  <files>
    .planning/phases/19.4-native-span-correlation-tagging/SPIKE-MEMO.md
  </files>
  <action>
This task deploys the SpanProcessor change, triggers a capture, and queries each emit site.

**Step 1: Commit and push to main.** The deploy pipeline (GitHub Actions) will build and deploy to Azure Container Apps.

**Step 2: Wait for deploy.** Monitor the GitHub Actions workflow run. After deploy completes, verify the health endpoint returns 200.

**Step 3: Trigger one fresh capture.** Use curl to POST a text capture to https://brain.willmacdonald.com/api/capture with a known `X-Trace-Id` header value (e.g., `spike-19.4-{timestamp}`). Record the exact trace ID used.

**Step 4: Wait 3-5 minutes for App Insights ingestion latency.**

**Step 5: Query KQL for each site using the MCP telemetry tools or az CLI:**

Site 1 (backend_api AppRequests):
```sql
AppRequests
| where TimeGenerated > ago(30m)
| where tostring(Properties.capture_trace_id) != ""
| project TimeGenerated, Name, Url, ResultCode, CaptureTraceId=tostring(Properties.capture_trace_id)
| order by TimeGenerated desc
| take 5
```

Site 2 (Foundry agent spans in AppDependencies):
```sql
AppDependencies
| where TimeGenerated > ago(30m)
| where Name endswith "_agent_run" or Name contains "AuditAgent" or Name contains "ToolTiming"
| where tostring(Properties.capture_trace_id) != ""
| project TimeGenerated, Name, DurationMs, CaptureTraceId=tostring(Properties.capture_trace_id)
| order by TimeGenerated desc
| take 10
```

Site 3 (Cosmos activityId_g -- uses existing trace_headers, just verify):
```sql
AzureDiagnostics
| where Category == "DataPlaneRequests"
| where TimeGenerated > ago(30m)
| where activityId_g startswith "spike-19.4"
| project TimeGenerated, OperationType_s, statusCode_s, duration_s, activityId_g, collectionName_s
| take 5
```

Site 4 (Investigation custom spans -- only if an investigate call is made):
Skip if not testing investigation in this spike. Investigation spans are app-created with explicit set_attribute already; the SpanProcessor is additive. Note this in the memo.

**Step 6: Audit Cosmos call sites.** Grep the backend source for all `create_item|upsert_item|replace_item` calls. For each, determine:
- Is it in a capture-correlated code path? (classifier tools, admin handoff, streaming adapter)
- Does it already use `trace_headers()`?
- Produce a table: file, line, operation, capture_correlated, has_trace_headers

**Step 7: Write SPIKE-MEMO.md** with sections:
1. Methodology (what was deployed, trace ID used, timing)
2. Per-site verification table (4 rows: site, KQL query, rows found, capture_trace_id present, pass/fail)
3. ContextVar timing analysis (did AppRequests get the tag? if not, what fix is needed?)
4. Cosmos call site audit table (file, line, operation, capture_correlated, has_trace_headers)
5. Numbered recommendations for Plan 02 (these become Plan 02 tasks verbatim):
   - 5.1: [based on findings]
   - 5.2: [based on findings]
   - etc.
6. activityId_g mapping decision: new table needed or direct equality sufficient?
7. Plan 02 scope recommendation: does SpanProcessor cover Sites 1+2+4? If yes, Plans 02+04 merge.
  </action>
  <verify>
    <automated>test -f /Users/willmacdonald/Documents/Code/claude/second-brain/.planning/phases/19.4-native-span-correlation-tagging/SPIKE-MEMO.md && grep -c "pass\|fail" /Users/willmacdonald/Documents/Code/claude/second-brain/.planning/phases/19.4-native-span-correlation-tagging/SPIKE-MEMO.md</automated>
  </verify>
  <done>SPIKE-MEMO.md exists with per-site pass/fail results, Cosmos audit table, numbered recommendations, and Plan 02 scope recommendation backed by empirical KQL evidence.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint A: Approve spike memo and lock Plan 02 scope</name>
  <files>.planning/phases/19.4-native-span-correlation-tagging/SPIKE-MEMO.md</files>
  <action>Read SPIKE-MEMO.md end to end. For each row in the per-site verification table, confirm the pass/fail determination matches your mental model. Confirm the Cosmos audit table is complete. Confirm section 5 numbered recommendations are actionable Plan 02 tasks. Approve or revise the Plan 02 scope recommendation.

Verification steps:
1. Read `.planning/phases/19.4-native-span-correlation-tagging/SPIKE-MEMO.md`
2. Confirm per-site verification table has KQL evidence (not speculation)
3. Confirm Cosmos audit identifies any missing trace_headers() sites
4. Confirm section 5 numbered recommendations are actionable Plan 02 tasks
5. Confirm Plan 02 scope recommendation (merged or separate plans)</action>
  <verify>
    <automated>User types "approved" or provides corrections; orchestrator pauses until then.</automated>
  </verify>
  <done>User has explicitly approved the spike memo and locked Plan 02 scope, OR the executor has revised the memo per user feedback and re-submitted.</done>
</task>

</tasks>

<verification>
- SPIKE-MEMO.md exists with empirical KQL evidence for all testable sites
- Each site has a pass/fail determination backed by actual query results
- Cosmos audit table covers all capture-correlated Cosmos call sites
- Section 5 recommendations are numbered and specific enough to be Plan 02 tasks
- Plan 02 scope recommendation is explicit: "merge Plans 02+04" or "keep separate"
</verification>

<success_criteria>
- CaptureTraceSpanProcessor is deployed and tested against live traffic
- Per-site results documented with KQL evidence
- Cosmos call site audit complete
- Plan 02 scope locked by operator approval at Checkpoint A
</success_criteria>

<output>
After completion, create `.planning/phases/19.4-native-span-correlation-tagging/19.4-01-SUMMARY.md`
</output>
