---
status: testing
phase: 12.3.1-implement-fixes-for-security-issues-and-from-output-of-dead-code-analysis
source: [12.3.1-01-SUMMARY.md, 12.3.1-02-SUMMARY.md, 12.3.1-03-SUMMARY.md]
started: 2026-03-18T22:45:00Z
updated: 2026-03-18T22:50:00Z
---

## Current Test

number: 8
name: Admin processing still works
expected: |
  Opening the Status screen triggers Admin Agent processing on an inbox item. Shopping list items are created correctly. No regression from routing context consolidation or dead code removal.
awaiting: user response

## Tests

### 1. Voice capture rejects oversized audio
expected: Sending a voice capture with audio larger than 25 MB returns a 413 error. The request does not reach transcription.
result: skipped
reason: 25 MB = ~25 min recording at mobile AAC quality, impractical to trigger from app

### 2. Voice capture rejects non-audio files
expected: Sending a file with a non-audio MIME type (e.g., application/pdf, image/png) to the voice capture endpoint returns a 415 error with "Unsupported audio format".
result: skipped
reason: Mobile app always sends audio from microphone, can't send non-audio MIME types from normal UI

### 3. Text capture rejects very long text
expected: Sending a text capture with more than 10,000 characters returns a 422 validation error from Pydantic. Normal-length text still works.
result: pass

### 4. Error messages don't leak server details
expected: When an internal error occurs during streaming, the SSE error event says "An internal error occurred. Please try again." — not a Python traceback or exception details.
result: skipped
reason: Cannot reliably trigger internal errors against production API for testing

### 5. Normal voice capture still works
expected: Recording a voice note in the app, it transcribes and classifies normally. No regression from the security changes or dead code removal.
result: issue
reported: "follow-up counter shows 'follow up 3 of 2' and keeps going (reached 5 of 2). Also, text follow-up with 'I need oranges and grapes' returned MISUNDERSTOOD — classifier can't resolve clear shopping items in follow-up context"
severity: major
note: pre-existing bugs, not a regression from phase 12.3.1

### 6. Normal text capture still works
expected: Sending a text capture in the app, it classifies and appears in the inbox normally. No regression from input validation or dead code changes.
result: pass

### 7. Inbox list still works
expected: Opening the inbox shows items. Pagination works normally (scrolling loads more items).
result: pass

### 8. Admin processing still works
expected: Opening the Status screen triggers Admin Agent processing on an inbox item. Shopping list items are created correctly. No regression from routing context consolidation or dead code removal.
result: [pending]

## Summary

total: 8
passed: 3
issues: 1
pending: 1
skipped: 3

## Gaps

- truth: "Follow-up counter respects configured maximum and does not allow follow-ups beyond the limit"
  status: failed
  reason: "User reported: follow-up counter reached 5 of 2 — no enforcement of max follow-up limit"
  severity: major
  test: 5
  note: pre-existing bug, not a regression from phase 12.3.1
  artifacts: []
  missing: []

- truth: "Text follow-up with clear intent classifies correctly"
  status: failed
  reason: "User reported: entered 'I need oranges and grapes' as text follow-up, still returned MISUNDERSTOOD despite being a clear shopping/Admin item"
  severity: major
  test: 5
  note: pre-existing bug, not a regression from phase 12.3.1
  artifacts: []
  missing: []
