---
phase: 19.4.1-fix-mobile-ux-issues
verified: 2026-04-21T17:00:00Z
status: human_needed
score: 7/7 must-haves verified (automated); 3 items need human verification
re_verification: false
human_verification:
  - test: "Install from TestFlight and verify first-launch API key prompt"
    expected: "Full-screen modal with 'Second Brain' title, secure text input, Continue button. After entering key, app functions normally."
    why_human: "TestFlight build and on-device runtime behavior cannot be verified from codebase"
  - test: "Verify 4-tab layout and tab functionality on device"
    expected: "4 tabs: Capture | Inbox | Tasks | Status. Tasks shows errands/tasks. Status shows dashboard + spine only."
    why_human: "Tab rendering and navigation behavior requires device testing"
  - test: "Push an OTA update and verify it applies"
    expected: "Run 'eas update --channel production --message test', cold-start app, update applies"
    why_human: "OTA update delivery requires EAS infrastructure and device testing"
---

# Phase 19.4.1: Fix Mobile UX Issues Verification Report

**Phase Goal:** Restructure mobile tab navigation to separate actionable items (Tasks tab) from observability (Status tab), add first-launch API key prompt with SecureStore and settings screen, enhance error card with recency info and time-ranged deep-link, and ship a production EAS build to TestFlight with OTA update support
**Verified:** 2026-04-21T17:00:00Z
**Status:** human_needed
**Re-verification:** No -- initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Tab bar shows 4 tabs: Capture, Inbox, Tasks, Status with correct icons | VERIFIED | `mobile/app/(tabs)/_layout.tsx` has 4 Tabs.Screen entries in order: index (pencil), inbox (folder), tasks (check mark U+2705), status (bar chart U+1F4CA) |
| 2 | Tasks tab displays errands/tasks with collapsible sections and triggers Admin Agent processing on focus | VERIFIED | `mobile/app/(tabs)/tasks.tsx` (587 lines): fetches GET /api/errands + GET /api/tasks, useFocusEffect with FOCUS_POLLS=4, SectionList with StatusSectionRenderer, ErrandRow, TaskRow, RoutingPicker, sections start collapsed, "No tasks" empty state |
| 3 | Status tab shows only dashboard cards + spine tiles + investigate/settings icons (no errands) | VERIFIED | `mobile/app/(tabs)/status.tsx` (141 lines): contains only ScrollView with DashboardCards + 8 SpineStatusTile + gear/magnifying glass icons. Zero matches for SectionList, StatusSectionRenderer, ErrandRow, processingCount, fetchData |
| 4 | Error card displays recency info and deep-link passes time-ranged query | VERIFIED | `mobile/components/DashboardCards.tsx`: DashboardData has lastErrorAge field, shows "last: {data.lastErrorAge}" when hasErrors; `status.tsx`: onErrorPress pushes to /investigate with "last 24 hours" query; formatErrorAge converts ISO timestamp to human-readable age |
| 5 | First-launch API key prompt appears when no key is stored; key persists in SecureStore | VERIFIED | `mobile/components/ApiKeyGate.tsx`: Modal visible={!apiKey && !isLoading}; `mobile/contexts/ApiKeyContext.tsx`: reads SecureStore.getItemAsync("api_key") on mount, dev env var bypass, setRuntimeApiKey for live binding; `mobile/app/_layout.tsx`: wraps in ApiKeyProvider > ApiKeyGate |
| 6 | Settings screen allows viewing (masked) and changing the stored API key | VERIFIED | `mobile/app/settings.tsx` (240 lines): masked display (first 3 + ... + last 4), "Not set" for empty, "Change" button, edit mode with TextInput, Save/Cancel, "API key saved" toast, setApiKey from useApiKey hook |
| 7 | Production EAS build + OTA updates configured | VERIFIED | `mobile/package.json`: expo-updates ~29.0.16 installed; `mobile/app.json`: runtimeVersion policy appVersion, updates URL matching project ID; `mobile/eas.json`: channels on all 3 build profiles; production env safely omits EXPO_PUBLIC_API_KEY |

**Score:** 7/7 truths verified (automated checks)

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `mobile/constants/config.ts` | Live-binding API_KEY + setRuntimeApiKey | VERIFIED | `export let API_KEY` (line 4), `export function setRuntimeApiKey` (line 12), 15 lines |
| `mobile/contexts/ApiKeyContext.tsx` | ApiKeyProvider + useApiKey hook | VERIFIED | Exports ApiKeyProvider and useApiKey, SecureStore read, setRuntimeApiKey sync, dev env var bypass, 57 lines |
| `mobile/components/ApiKeyGate.tsx` | Full-screen first-launch modal | VERIFIED | Modal with "Second Brain" title, "Paste your API key" placeholder, "Continue" button, useApiKey hook, 129 lines |
| `mobile/app/settings.tsx` | Settings screen with masked key + edit | VERIFIED | Masked key display, Change/Save/Cancel, "API key saved" toast, About section, useApiKey hook, 240 lines |
| `mobile/app/_layout.tsx` | Root layout with provider, gate, settings screen | VERIFIED | ApiKeyProvider wraps all, ApiKeyGate rendered, settings Stack.Screen registered, 64 lines |
| `backend/src/second_brain/observability/kql_templates.py` | SYSTEM_HEALTH with last_error_time | VERIFIED | Lines 135-139: toscalar union AppTraces+AppExceptions SeverityLevel >= 3, print includes last_error_time |
| `backend/src/second_brain/observability/models.py` | HealthSummary.last_error_time | VERIFIED | Line 149: `last_error_time: str | None = None` |
| `backend/src/second_brain/api/health.py` | /api/health-summary returns lastErrorTime | VERIFIED | Line 99: `"lastErrorTime": health.last_error_time` |
| `backend/src/second_brain/observability/queries.py` | query_system_health extracts last_error_time | VERIFIED | Lines 185-186: row.get("last_error_time"), str conversion; line 195: passed to constructor |
| `mobile/app/(tabs)/tasks.tsx` | Tasks tab with errands SectionList | VERIFIED | 587 lines, SectionList with focus polling, CRUD handlers, StatusSectionRenderer/ErrandRow/TaskRow/RoutingPicker imports |
| `mobile/app/(tabs)/_layout.tsx` | 4-tab layout | VERIFIED | 4 Tabs.Screen entries: index, inbox, tasks, status with correct icons |
| `mobile/app/(tabs)/status.tsx` | Simplified status (dashboard + spine only) | VERIFIED | ScrollView with DashboardCards + 8 SpineStatusTile, gear + magnifying glass icons, no errand content |
| `mobile/components/DashboardCards.tsx` | Error card with recency | VERIFIED | DashboardData.lastErrorAge field, recencyText style, "last: {data.lastErrorAge}" display |
| `mobile/app.json` | runtimeVersion + updates URL | VERIFIED | runtimeVersion policy appVersion, updates URL with project ID, expo-updates in plugins |
| `mobile/eas.json` | Build profiles with channels | VERIFIED | development/preview/production all have channel field; production omits EXPO_PUBLIC_API_KEY |
| `mobile/package.json` | expo-updates dependency | VERIFIED | expo-updates ~29.0.16 in dependencies |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| ApiKeyContext.tsx | config.ts | setRuntimeApiKey call | WIRED | Line 9: imports setRuntimeApiKey; lines 31, 37, 46: calls it on env read, SecureStore read, and setApiKey |
| _layout.tsx (root) | ApiKeyContext.tsx | ApiKeyProvider wrapper | WIRED | Line 7: imports ApiKeyProvider; line 23: wraps entire app tree |
| ApiKeyGate.tsx | ApiKeyContext.tsx | useApiKey hook | WIRED | Line 11: imports useApiKey; line 19: destructures apiKey, setApiKey, isLoading |
| settings.tsx | ApiKeyContext.tsx | useApiKey hook | WIRED | Line 13: imports useApiKey; line 21: destructures apiKey, setApiKey |
| tasks.tsx | /api/errands | fetch on useFocusEffect | WIRED | Line 84: fetch GET /api/errands with Bearer token; useFocusEffect triggers on tab focus |
| status.tsx | DashboardCards.tsx | renders with dashboardData | WIRED | Line 8-9: imports DashboardCards and DashboardData; line 93-104: renders with data + onErrorPress |
| DashboardCards.tsx | /investigate | onErrorPress deep-link | WIRED | Line 96-103: router.push to /investigate with "last 24 hours" query |
| status.tsx | /settings | gear icon navigation | WIRED | Line 84: router.push("/settings") on Pressable press |
| health.py | queries.py | health.last_error_time | WIRED | Line 87: calls query_system_health; line 99: accesses health.last_error_time |
| app.json | eas.json | runtimeVersion ties to channels | WIRED | app.json runtimeVersion policy appVersion; eas.json production channel "production" |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| UX-01 | Plan 03 | Tab restructuring (4 tabs, Tasks separate from Status) | SATISFIED | _layout.tsx has 4 tabs; tasks.tsx has errands; status.tsx has no errands |
| UX-02 | Plans 02, 03 | Error card recency + time-ranged deep-link | SATISFIED | Backend lastErrorTime field; DashboardCards recency display; "last 24 hours" deep-link |
| UX-03 | Plan 01 | First-launch API key prompt with SecureStore | SATISFIED | ApiKeyGate modal; ApiKeyContext with SecureStore; live-binding config |
| UX-04 | Plan 01 | Settings screen for API key management | SATISFIED | settings.tsx with masked display, edit, save, toast |
| UX-05 | Plan 04 | Production EAS build on TestFlight + OTA updates | SATISFIED (code) | expo-updates installed; channels configured; runtimeVersion set. TestFlight delivery needs human verification |

**Note:** UX-01 through UX-05 are referenced in plans and ROADMAP.md but are not formally defined in REQUIREMENTS.md. This is consistent with project convention for inserted phases (per MEMORY: "Inserted phases from superpowers specs don't go in GSD ROADMAP.md"). The requirement IDs serve as internal tracking only.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| None | - | - | - | No anti-patterns detected |

No TODO/FIXME/PLACEHOLDER comments found in any phase files. No stub implementations detected. No empty handlers or placeholder returns. The single `return null` in tasks.tsx line 465 is functional (collapsed section items).

### Human Verification Required

### 1. TestFlight First-Launch Flow

**Test:** Install Second Brain from TestFlight on a fresh device (or after clearing app data). Verify the API key prompt modal appears with "Second Brain" title, secure text input, and "Continue" button. Enter the API key and confirm the app functions.
**Expected:** Modal blocks interaction until key is entered. After entry, all 4 tabs are accessible and API calls work.
**Why human:** TestFlight build and on-device runtime behavior cannot be verified from codebase.

### 2. Tab Layout and Functionality

**Test:** Open the app and verify the tab bar shows 4 tabs: Capture | Inbox | Tasks | Status. Tap Tasks tab -- errands/tasks should load. Tap Status tab -- only dashboard cards and spine tiles should appear (no errands).
**Expected:** Correct tab order, correct icons (pencil, folder, check mark, bar chart), correct content separation.
**Why human:** Visual layout and navigation behavior requires device testing.

### 3. Error Card Recency and Deep-Link

**Test:** Open Status tab when backend has recent errors. Verify error card shows count + "last: Xh ago" recency line. Tap the error card.
**Expected:** Investigation chat opens with pre-filled query including "last 24 hours". Agent finds and reports the errors.
**Why human:** Requires live backend with error data and investigation agent.

### 4. Settings Screen

**Test:** Tap gear icon on Status screen header. Verify settings screen shows masked API key (first 3 + ... + last 4). Tap "Change", enter new key, tap "Save". Verify toast appears and subsequent API calls use new key.
**Expected:** Settings opens, key displayed masked, edit/save works, toast "API key saved" appears.
**Why human:** Key persistence across app restart requires device testing.

### 5. OTA Update Delivery

**Test:** Run `eas update --channel production --message "Test OTA update"` then cold-start the app.
**Expected:** App picks up the update on next launch.
**Why human:** OTA update delivery requires EAS infrastructure and device testing.

### Gaps Summary

No automated gaps found. All 7 success criteria from the ROADMAP are met at the code level:

1. 4-tab layout with correct icons -- verified in _layout.tsx
2. Tasks tab with errands/tasks + Admin Agent trigger -- verified in tasks.tsx
3. Status tab with dashboard + spine only -- verified in status.tsx (zero errand references)
4. Error card recency + time-ranged deep-link -- verified in DashboardCards.tsx and status.tsx
5. First-launch API key prompt -- verified in ApiKeyGate.tsx, ApiKeyContext.tsx, _layout.tsx
6. Settings screen -- verified in settings.tsx
7. EAS production build + OTA -- verified in app.json, eas.json, package.json (TestFlight delivery is human-verified)

All 7 commits verified in git history. No anti-patterns, no stubs, no missing wiring. The only outstanding items are on-device verification of the TestFlight build, which by nature requires human testing.

---

_Verified: 2026-04-21T17:00:00Z_
_Verifier: Claude (gsd-verifier)_
