---
phase: 19-claude-code-mcp-tool
plan: 02
type: execute
wave: 2
depends_on:
  - 19-01
files_modified:
  - .claude/skills/investigate/SKILL.md
autonomous: true
requirements:
  - MCP-01

must_haves:
  truths:
    - "/investigate skill uses MCP tools directly instead of calling the deployed API endpoint"
    - "Skill triggers on the same vocabulary as before (errors, captures, traces, health, etc.)"
    - "Claude Code can answer 'show me recent errors' using the local MCP tools without hitting brain.willmacdonald.com"
  artifacts:
    - path: ".claude/skills/investigate/SKILL.md"
      provides: "Updated skill routing Claude to MCP tools instead of backend script"
      contains: "second-brain-telemetry"
  key_links:
    - from: ".claude/skills/investigate/SKILL.md"
      to: "MCP tools (second-brain-telemetry)"
      via: "skill instructions reference MCP tool names"
      pattern: "trace_lifecycle|recent_errors|system_health|usage_patterns|admin_audit|run_kql"
---

<objective>
Migrate the `/investigate` skill from calling the deployed Investigation Agent API to using the local MCP tools directly.

Purpose: Eliminates the network hop to brain.willmacdonald.com and the Key Vault API key fetch. Queries run directly against Log Analytics from the local machine via `az login` credentials. Faster, simpler, works even if the deployed backend is down.
Output: Updated SKILL.md that routes Claude to MCP tools instead of the backend script.
</objective>

<execution_context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/19-claude-code-mcp-tool/19-01-SUMMARY.md
</execution_context>

<context>
@.claude/skills/investigate/SKILL.md

<interfaces>
<!-- The 6 MCP tools available after Plan 01 (from mcp/server.py): -->

Tools in the `second-brain-telemetry` MCP server:
- `trace_lifecycle(trace_id?: str)` -- Trace a capture's full lifecycle
- `recent_errors(time_range?: str, component?: str)` -- Query recent errors/failures
- `system_health(time_range?: str)` -- System health metrics with trends
- `usage_patterns(time_range?: str, group_by?: str)` -- Capture usage analysis
- `admin_audit()` -- Admin Agent activity logs (last 24h)
- `run_kql(query: str, time_range?: str)` -- Raw KQL query execution

All tools return structured JSON dicts. Error responses: `{"error": true, "message": "...", "type": "..."}`.
If `type` is `ClientAuthenticationError`, suggest `az login`.
If `type` is `config_error`, the workspace ID env var is missing.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite investigate skill to use MCP tools</name>
  <files>.claude/skills/investigate/SKILL.md</files>
  <action>
    Rewrite `.claude/skills/investigate/SKILL.md` to route Claude Code to the local MCP tools instead of the deployed API script. The skill description and trigger rules (Rule B) stay the same -- only the "How to invoke" section changes.

    **Keep unchanged:**
    - The `---` frontmatter (name, description)
    - "When to invoke (Rule B)" section -- same telemetry vocabulary triggers
    - "When to ASK first" section -- same ambiguity handling
    - "When NOT to invoke" section -- same exclusions

    **Replace the "How to invoke" section** with MCP tool routing guidance:

    The new approach: Claude uses MCP tools from the `second-brain-telemetry` server directly. No Bash commands, no scripts, no network calls to the deployed backend.

    **Tool routing rules** (map user intent to the right MCP tool):

    | User asks about... | Use this MCP tool | Key parameters |
    |---|---|---|
    | "what happened to my capture", specific trace ID, "last capture" | `trace_lifecycle` | `trace_id` (optional -- null for most recent) |
    | "errors", "failures", "exceptions", "crashes" | `recent_errors` | `time_range` (1h/6h/24h/3d/7d), `component` (optional) |
    | "system health", "latency", "success rate", "p95/p99" | `system_health` | `time_range` |
    | "how many captures", "usage", "distribution", "by bucket/destination" | `usage_patterns` | `time_range`, `group_by` (day/hour/bucket/destination) |
    | "admin agent activity", "processing", "admin audit" | `admin_audit` | (no params) |
    | Anything requiring custom KQL, complex or unusual queries | `run_kql` | `query` (KQL string), `time_range` |

    **Error handling:**
    - If a tool returns `{"error": true, "type": "ClientAuthenticationError", ...}`: suggest `az login` to refresh credentials
    - If a tool returns `{"error": true, "type": "config_error", ...}`: explain that `AZURE_LOG_ANALYTICS_WORKSPACE_ID` needs to be set (check MCP server registration)
    - For other errors: show the error message and suggest retrying or simplifying the query

    **Presentation:**
    - Present MCP tool results naturally in conversation -- the raw JSON contains structured data that Claude should format into readable summaries, tables, or bullet points
    - For trace_lifecycle: show events in chronological order with timestamps
    - For recent_errors: show as a table with timestamp, component, message; note if truncated ("showing N of M")
    - For system_health: summarize key metrics (capture count, success rate, latency) with trend comparison
    - For usage_patterns: show as a list or table grouped by the requested dimension

    **Thread management is gone** -- MCP tools are stateless. Each call is independent. Follow-up questions simply call the appropriate tool again with refined parameters. No thread IDs to track.

    **Examples section** should be updated to reflect MCP tool usage:
    - "show me errors from the last 24 hours" → call `recent_errors` with `time_range="24h"`
    - "trace my last capture" → call `trace_lifecycle` with no trace_id
    - "is the system healthy?" → call `system_health` with `time_range="24h"`
    - "how many captures this week by bucket?" → call `usage_patterns` with `time_range="7d"`, `group_by="bucket"`
    - "show me admin agent activity" → call `admin_audit`
  </action>
  <verify>
    <automated>grep -q "second-brain-telemetry" /Users/willmacdonald/Documents/Code/claude/second-brain/.claude/skills/investigate/SKILL.md && grep -q "trace_lifecycle" /Users/willmacdonald/Documents/Code/claude/second-brain/.claude/skills/investigate/SKILL.md && grep -q "run_kql" /Users/willmacdonald/Documents/Code/claude/second-brain/.claude/skills/investigate/SKILL.md && echo "SKILL.md updated with MCP tool references"</automated>
  </verify>
  <done>Investigate skill routes to 6 local MCP tools instead of deployed API script; trigger rules unchanged; thread management removed (stateless tools); error handling covers auth and config failures; examples updated</done>
</task>

</tasks>

<verification>
1. `.claude/skills/investigate/SKILL.md` references `second-brain-telemetry` MCP tools
2. All 6 tool names appear in the skill routing table (trace_lifecycle, recent_errors, system_health, usage_patterns, admin_audit, run_kql)
3. No references to `scripts/investigate.py` or `brain.willmacdonald.com` remain in the skill
4. Skill trigger vocabulary (Rule B) is preserved from the original
</verification>

<success_criteria>
- Investigate skill uses MCP tools directly (no deployed API dependency)
- Same trigger rules detect telemetry questions automatically
- Error handling guides user to fix auth/config issues
- Stateless tool calls replace thread-based conversation management
</success_criteria>

<output>
After completion, create `.planning/phases/19-claude-code-mcp-tool/19-02-SUMMARY.md`
</output>
