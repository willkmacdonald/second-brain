# Phase 23 PLAN-CHECK Report

**Date:** 2026-05-08
**Phase:** 23 — foundry-ga-prep (artifact-only setup)
**Plans verified:** 5 (23-01 through 23-05)
**Locked design:** [docs/superpowers/specs/2026-05-05-foundry-ga-migration-design.md](../../../docs/superpowers/specs/2026-05-05-foundry-ga-migration-design.md) §"Phase 23.0 — Artifact-only setup"

---

## Verdict

**PASS**

Thirteen rounds of code-grounded review against the actual backend surface (`backend/src/second_brain/api/capture.py:217, 271, 366` for X-Trace-Id read on text-capture and follow-up + `file: UploadFile = File(...)` voice multipart contract, `backend/src/second_brain/api/investigate.py:30-50` (round-9 P1-A: InvestigateBody = `{question, thread_id}` NOT `{message}`; round-8 P1-B: investigation endpoint does NOT read X-Trace-Id), `backend/src/second_brain/streaming/sse.py:33-110` (round-8 P1-A: SSE event constructors carry no trace_id), `backend/src/second_brain/streaming/investigation_adapter.py:96-98, 116-117, 187-188` (round-10 P1), `backend/src/second_brain/processing/admin_handoff.py:177, 252` (round-13 P2: `admin_agent_process` custom AppDependencies span + `admin.tool_invoked` attribute), `backend/src/second_brain/api/eval.py:115, 124` (round-13 P1-A: POST returns evalType, status endpoint just merges runs_dict), `backend/src/second_brain/eval/runner.py:200-215, 350-365` (round-13 P1-A: classifier and admin final dicts overwrite runs_dict without preserving evalType), `backend/src/second_brain/main.py:14-31` and `:512-690`, `backend/src/second_brain/observability/span_processor.py`, `backend/src/second_brain/observability/kql_templates.py:44-95` (round-6 KQL canonical pattern), `backend/pyproject.toml:50-51` hatch wheel target) found **41 defects total** (5 in round 1 + 3 in round 2 + 4 in round 3 + 3 in round 4 + 3 in round 5 + 4 in round 6 + 4 in round 7 + 2 in round 8 + 4 in round 9 [2 P1 + 2 P2] + 2 in round 10 [1 P1 + 1 P2] + 4 in round 13 [2 P1 + 1 P2 + 1 P3] + 2 in round 14 [1 P1 + 1 P2] + 1 in round 15 [1 P1, caught by the new gsd-design-coverage-auditor on its first run]). Rounds 11 and 12 produced ledger-only fixes that are not double-counted per the established convention. All 41 have been fixed via targeted Edit-style revisions — no plan was rewritten wholesale; frontmatter, dependencies, must_haves, and unaffected tasks were preserved across all thirteen counted passes. Plans are ready to execute as-is. See "Revision History", "Round 2 Revision History", "Round 3 Revision History", "Round 4 Revision History", "Round 5 Revision History", "Round 6 Revision History", "Round 7 Revision History", "Round 8 Revision History", "Round 9 Revision History", "Round 10 Revision History", "Round 13 Revision History", "Round 14 Revision History", and "Round 15 Revision History" below.

---

## Coverage Matrix — 9 design deliverables × 5 plans

| # | Deliverable | 23-01 | 23-02 | 23-03 | 23-04 | 23-05 | Status |
|---|---|---|---|---|---|---|---|
| 1 | 18 golden-trace fixtures (5 inv + 5 admin + 8 classifier) under `backend/tests/fixtures/<agent>/` | — | — | ✅ Tasks 1-3 (5+5+8) | — | — | COVERED |
| 2 | Eval baseline JSON (`eval-baseline-pre-migration.json`) + `EVAL-INVENTORY.md` | — | — | — | ✅ Tasks 1-2 | — | COVERED |
| 3 | Portal instructions export (`CANDIDATE-instructions/`) + Investigation drift reconciliation (`PORTAL-DRIFT.md`) | — | — | — | ✅ Task 3 | — | COVERED |
| 4 | Local-only dep spike → `CANDIDATE-pyproject.toml` + `CANDIDATE-uv.lock` | ✅ Task 1 | — | — | — | — | COVERED |
| 5 | `backend/scripts/foundry_probe.py` with 5 probe bodies, run against real Foundry | ✅ Task 2 (scaffold) | ✅ Tasks 1-2 (impl + run) | — | — | — | COVERED |
| 6 | `CONFIG-DELTAS.md` | — | — | — | — | ✅ Task 1 | COVERED |
| 7 | `~/.claude/agents/gsd-framework-fidelity-auditor.md` existence verified (`AUDITOR-VERIFICATION.md`) | — | — | — | — | ✅ Task 3 | COVERED |
| 8 | `SPAN-NAME-MAPPING.md` | — | — | — | — | ✅ Task 2 | COVERED |
| 9 | `FOUNDRY-PROBE-FINDINGS.md` | — | ✅ Task 3 | — | — | — | COVERED |

All 9 design deliverables map to a concrete task in a concrete plan. **Coverage: 100%.**

---

## Boundary Verification

Design §"Phase 23.0 — Artifact-only setup" (post round-3 path reconciliation, 2026-05-08) enumerates the allowed write paths:

1. `.planning/phases/23-foundry-ga-prep/` — formerly drafted as `.planning/phases/23.0/` in the design; mass-renamed in round 3 to match the GSD-assigned phase directory. The design's textual "Phase 23.0" labels are retained as synonyms for "Phase 23 (artifact-only prep)" per the design's own placeholder caveat.
2. `backend/tests/fixtures/`
3. `backend/scripts/foundry_probe.py`

Every path in every plan's `files_modified` frontmatter falls inside one of those three buckets:

| Plan | Files declared | All in allow-list? |
|---|---|---|
| 23-01 | 5 paths (3 under `.planning/phases/23-foundry-ga-prep/`, 1 = `backend/scripts/foundry_probe.py`, 1 = `backend/tests/fixtures/foundry-probe/.gitkeep`) | YES |
| 23-02 | 7 paths (1 = `backend/scripts/foundry_probe.py`, 5 fixtures under `backend/tests/fixtures/foundry-probe/`, 1 under `.planning/phases/23-foundry-ga-prep/`) | YES |
| 23-03 | 73 paths (72 fixtures under `backend/tests/fixtures/<agent>/`, 1 = `FIXTURE-CAPTURE-LOG.md` under `backend/tests/fixtures/`) | YES |
| 23-04 | 6 paths (1 = `backend/tests/fixtures/eval-baseline-pre-migration.json`, 5 under `.planning/phases/23-foundry-ga-prep/`) | YES |
| 23-05 | 3 paths (all under `.planning/phases/23-foundry-ga-prep/`) | YES |

**No forbidden paths declared.** Every plan additionally embeds an explicit BOUNDARY GUARD block in each task's `<action>` section reminding the executor not to touch `backend/src/`, `backend/pyproject.toml`, `backend/uv.lock`, or `backend/Dockerfile`. Verify steps in 23-01 Task 1 and 23-05 Task 1 actively grep `backend/pyproject.toml` to assert the RC `agent-framework-azure-ai` line is still present, catching accidental modification.

**Boundary violations: NONE.**

---

## Dependency / Wave Correctness

Declared graph:

```
23-01 (wave 1, depends_on=[])     ← dep spike + probe scaffold
23-03 (wave 1, depends_on=[])     ← 18 golden fixtures
23-04 (wave 1, depends_on=[])     ← eval baseline + portal export

23-02 (wave 2, depends_on=["23-01"])  ← run probes (needs candidate dep set + scaffold)
23-05 (wave 2, depends_on=["23-02"])  ← SPAN-NAME-MAPPING uses GA span names from probes
```

**Required orderings:**
- 23-01 → 23-02: ✅ Correct. 23-02 task 2 sources the candidate dep set from `.planning/phases/23-foundry-ga-prep/CANDIDATE-{pyproject.toml,uv.lock}` (built by 23-01 task 1) and runs against the harness file (scaffolded by 23-01 task 2).
- 23-02 → 23-05: ✅ Correct. SPAN-NAME-MAPPING.md draws GA span names from `FOUNDRY-PROBE-FINDINGS.md`. 23-05 task 2 read_first declares `FOUNDRY-PROBE-FINDINGS.md`.
- 23-03 / 23-04 parallel with 23-01: ✅ Correct. Neither consumes any 23-01 output. 23-03 hits deployed RC; 23-04 hits deployed RC + reads existing `eval/runner.py`. Wave 1 declarations and `depends_on=[]` are consistent.

No cycles. No forward references. No missing references.

---

## CLAUDE.md Compliance

| Rule | Verification |
|---|---|
| Never run backend locally | ✅ 23-03 Task 1 captures against `https://brain.willmacdonald.com`. 23-04 Task 1 triggers eval via deployed `/api/eval/run`. 23-03 frontmatter `must_haves.truths` explicitly states "captured against the deployed RC system at https://brain.willmacdonald.com (NOT against a local backend, per project CLAUDE.md)". |
| Use `uv` for package management | ✅ 23-01 Task 1 uses `mkdir -p /tmp/foundry-ga-spike` + `cd` + `uv sync --locked --python 3.12 --no-install-project` + `uv lock`. 23-02 Task 2 uses the same `mkdir` + `uv sync --locked --python 3.12 --no-install-project` pattern. No `pip install` references. The `--no-install-project` flag is required because the temp project dir does not contain the `src/second_brain/` source tree (round 4 P1 fix). |
| Backend deploys via Container Apps | ✅ Plans never invoke deployment. CONFIG-DELTAS.md (23-05) documents `az containerapp update` as a Phase 24 step, never executed in Phase 23. |

---

## Goal-Backward Verification (must_haves)

Phase goal: "All Phase 24 prerequisites in place. Zero deployed change. No backend/src/ files touched."

Each plan's `must_haves.truths` derives a user-observable subset of that goal:

- 23-01: candidate dep set resolves; harness file exists with valid stubs (Phase 24 prerequisite #4 + #5 substrate)
- 23-02: 5 probe fixtures + findings doc exist with empirical SDK answers (Phase 24 prerequisite #5 + #9)
- 23-03: 72 fixture files exist with golden-trace contract (Phase 24 prerequisite #1)
- 23-04: eval baseline JSON + EVAL-INVENTORY + 4 instruction docs (Phase 24 prerequisite #2 + #3)
- 23-05: CONFIG-DELTAS + SPAN-NAME-MAPPING + AUDITOR-VERIFICATION (Phase 24 prerequisite #6 + #7 + #8)

All five derive backward correctly. The cumulative truth set guarantees the 9 deliverables exist AND boundary is preserved (every plan's `must_haves` includes a boundary guard truth).

---

## Per-Plan Findings

### 23-01 — Dep spike + probe scaffold — **PASS**

- Task 1 has detailed `<read_first>`, `<acceptance_criteria>` is grep-verifiable, `<action>` includes literal `uv lock` / `cp` / file paths.
- Boundary guard explicit (also asserts `backend/pyproject.toml` retains `agent-framework-azure-ai`).
- Two tasks, well-scoped, autonomous-suitable.

### 23-02 — Run probes + FINDINGS — **PASS**

- 4 tasks (3 auto + 1 checkpoint:human-verify). All auto tasks have `<read_first>`, grep/jq-verifiable `<acceptance_criteria>`, concrete code blocks for each probe body.
- Round 1 P2-A replaced the `_tag_current_span` helper with a `ProbeTagSpanProcessor(SpanProcessor)` whose `on_start` hook reads ContextVars at span-creation time.
- Round 1 P2-B added a broad `<[^>]*>` placeholder guard with allow-list to FOUNDRY-PROBE-FINDINGS.md.
- Round 2 P1 made probe telemetry setup explicit (`_setup_probe_telemetry()` at module entry calling `configure_azure_monitor()` + `enable_instrumentation()` + fail-fast on missing `APPLICATIONINSIGHTS_CONNECTION_STRING` or missing `add_span_processor`), tightened `_ensure_probe_processor_installed()` to fail-fast (`sys.exit(2)`) instead of silently no-op'ing, reordered every probe body so ContextVars + processor install precede client/agent construction, and added an AST-walk verify step that confirms the ordering.
- Checkpoint Task 4 documents "approved with provider-dict fallback" / "blocked: session rehydration broken" branches → planner has clear escalation paths.

### 23-03 — Golden-trace fixtures — **PASS**

- Tasks 1-4 + checkpoint. Each `<action>` carries the exact curl + KQL + redaction commands.
- Voice fixture commits no audio file (verify step asserts).
- D-07b decision threaded into `deliberate_misunderstood` and `voice_person` expected-deltas.
- Round 1 P1-A fixed the `/api/capture` endpoint path + `/api/capture/follow-up` (hyphen) + snake_case `inbox_item_id` / `follow_up_text` / `follow_up_round` body fields per the actual `TextCaptureBody` / `FollowUpBody` Pydantic models.
- Round 2 P2 added a broad `<[^>]+>` placeholder guard with allow-list (HTML tags, KQL `Properties.[...]` syntax, URLs, HTML comments) to the FIXTURE-CAPTURE-LOG.md verify step.
- Round 8 P1-A fixed the trace acquisition contract across all three Tasks. SSE event constructors at `streaming/sse.py:33-110` emit no `trace_id` field (only `inboxItemId`, `bucket`, `confidence`, `threadId`, `runId`); the prior plan had operators "extract trace_id from first SSE event" which would have produced empty `EXPECTED_TID` on every fixture and failed verification. Replaced with **client-side UUID generation** before each curl, sent via `X-Trace-Id` header (read by `api/capture.py:217, 366`) and recorded directly into `input.json` — no SSE round-trip required.
- Round 8 P1-B applied a different correlation key for **investigation** fixtures: the `/api/investigate` handler at `api/investigate.py:30-50` does NOT read `X-Trace-Id`, does NOT set `capture_trace_id_var`, and the SSE adapter at `streaming/investigation_adapter.py:108-188` emits `thinking`/`text`/`done` without trace_id. Investigation spans never carry `Properties["capture.trace_id"]`, so the prior strict `all rows match $tid` verifier would have failed every fixture. Initial round-8 resolution (URL + OperationId join over a 15-minute window) was later refined by round 9 (which attempted per-fixture scoping via a synthetic `thread_id`) and then **corrected by round 10** to its current form: the operator parses the server-generated `thread_id` from the SSE `done` event (per `investigation_adapter.py:188`), records it as `thread_id_out` in `input.json`, and the KQL is a two-stage join — find the custom `investigate` AppDependencies span where `Properties["investigate.thread_id_out"] == $server_thread_id` (stamped per `investigation_adapter.py:187`), then OperationId-join all spans (AppRequests + AppDependencies + AppTraces + AppExceptions) at that OperationId. Verifier asserts **exactly one** custom investigate AppDependencies row matching `thread_id_out` AND **exactly one** /api/investigate AppRequests row at the same OperationId with `ResultCode == "200"`. Investigation `.trace_id` is documentary only — Phase 24 task group 23.1 (Investigation migration) owns adding proper trace correlation when the investigation surface is touched. Tasks 2 (admin) and 3 (classifier) keep the `Properties["capture.trace_id"]` correlation contract because those endpoints DO propagate `X-Trace-Id`.

### 23-04 — Eval baseline + EVAL-INVENTORY + portal export — **PASS**

- 3 tasks + checkpoint. Eval runner sample-size sanity is asserted post-trigger using a two-tiered contract: classifier ≥ 50 (EVAL-01 representative-accuracy gate); admin > 0 (runner-level contract per `eval/runner.py:248-263`, with a non-blocking soft warning at <10 cases).
- Investigation reconciliation flow correctly defaults to "canonicalized doc wins."
- Frontmatter comment block in each instruction file records Phase 24 promotion target.
- Round 1 P1-B replaced `eval_type: "admin"` with `eval_type: "admin_agent"`, added required `routing_context` seed (`"Costco: olive oil; Amazon: diapers"`), corrected polling path to `/api/eval/status/{run_id}`, switched JSON extraction to camelCase `.runId` / `.evalType`, and added fail-fast guards if either runId is null. (W3 was resolved here.)
- Round 2 P2 added a broad `<[^>]+>` placeholder guard with allow-list (HTML tags, KQL `Properties.[...]` syntax, URLs, HTML comments, plus `"EvalClassifierTools"` / `"DryRunAdminTools"` Python-string-literal forward-reference type annotations) to the EVAL-INVENTORY.md verify step.
- Round 5 P2 corrected the admin sample-size assertion. Previously the verify block asserted `.admin.total >= 50`, but `eval/runner.py:248-263` shows the admin runner only fails on an empty filtered dataset (`if not test_cases:`) and does NOT enforce a 50-case minimum. The asymmetric `>= 50` assertion would falsely block a legitimate run when the seeded admin dataset has fewer cases. Updated to `.admin.total > 0` (matches runner contract) with a non-blocking soft warning to stderr if `< 10` (signal-to-noise advisory at low N). Classifier `>= 50` retained as-is (EVAL-01 sample-size contract). Acceptance criteria explicitly distinguishes the two contracts.

### 23-05 — CONFIG-DELTAS + SPAN-NAME-MAPPING + AUDITOR-VERIFICATION — **PASS**

- 3 autonomous tasks. CONFIG-DELTAS Task 1 asserts `foundry_model` is NOT in `config.py` (boundary).
- SPAN-NAME-MAPPING Task 2 has the round-1 broad `<[^>]*>` placeholder guard already in place (W4 resolved).
- AUDITOR-VERIFICATION Task 3 confirms the auditor and calibration files exist on disk via `test -f`.
- Round 1 P1-C replaced the unsafe single-`az containerapp update` with a documented three-step staged sequence (Step A pre-deploy add only / Step B GA image push / Step C post-deploy orphan removal), added a NEGATIVE assertion subsection forbidding pre-cutover `--remove-env-vars` against `AZURE_AI_*_AGENT_ID`, and cited the BLANK-portal-agent risk via `agents/classifier.py:40` and `main.py:514`/`:596`/`:687`.
- Round 2 P2 added broad `<[^>]+>` placeholder guards with allow-lists to the CONFIG-DELTAS.md verify step (allowing `<foundry-resource-id>` operator-substituted scope literal) and the AUDITOR-VERIFICATION.md verify step (allowing `<task-group-start-sha>` git-diff argument literal and `FRAMEWORK-FIDELITY-23.{1,2,3}.md` / `FIDELITY-23.{1,2,3}.patch` brace-expansion path patterns).
- W5 (GA span names sourced from design doc claim rather than empirical probe span trees) is mitigated by the Phase 24 framework-fidelity audit row "Tracing" which rechecks against actual GA emissions; accepted risk.

---

## Resolved Warnings (formerly Round 1 Warnings)

- **W1** (23-03, KQL workspace API table choice) — RESOLVED by round-6 P1. The capture KQL block now uses the canonical `union withsource=SourceTable AppRequests, AppDependencies, AppTraces, AppExceptions ... | extend ItemType = case(SourceTable == 'AppRequests', 'Request', ...)` form per `backend/src/second_brain/observability/kql_templates.py:44, 74, 220`. The earlier `ItemType=type()` (which is invalid KQL — `type()` returns column type, not table source) is gone. Phase 24 query updates own the long-term table-name verification anyway.
- **W2** (23-03, side-effect cleanup ownership) — RESOLVED by the broader placeholder discipline. The FIXTURE-CAPTURE-LOG.md "Side effects to clean up" section is now guard-enforced to be filled in (no `<list each side-effect actually created>` token remains), forcing the operator to make an explicit commit-or-leave decision per row.
- **W3** (23-04, eval baseline soft assertion) — RESOLVED by round-1 P1-B's promotion of `.classifier.runId` / `.admin.runId` / `.admin.evalType` assertions into the `<verify>` block plus explicit fail-fast guards in Steps 1, 3, 4. Malformed baselines can no longer be committed silently.
- **W4** (23-05, SPAN-NAME-MAPPING placeholder discipline) — RESOLVED by round-1 P2-B's broad `<[^>]*>` final guard added to the SPAN-NAME-MAPPING.md verify step.
- **W5** (23-05, GA span name source-of-truth fragility) — ACCEPTED. Phase 24 framework-fidelity auditor's "Tracing" checklist row rechecks empirical GA span emissions during the audit gate. If the design doc's `invoke_agent` / `execute_tool` claim is wrong, the audit catches it before merge.

---

## Auto-Mode Compliance

`autonomous: true` on 23-01 and 23-05. `autonomous: false` on 23-02, 23-03, 23-04 (each has a `checkpoint:human-verify` task at the end). Correct: probe runs, fixture captures, and eval baselines need operator review before Phase 24 planning consumes them.

---

## No Git Push Tasks

Searched all 5 plan files: 3 references to `git push` in 23-05 are all contextual (documenting Phase 24's deploy sequence inside CONFIG-DELTAS.md content). **No plan task executes `git push`.** ✅

---

## Final Status

**Plans are ready to execute.** Coverage 9/9, boundary preserved, dependencies acyclic, CLAUDE.md compliant, no scope reduction detected, no forbidden file paths declared, no `git push` actions. **40 defects total (5 round-1 + 3 round-2 + 4 round-3 + 3 round-4 + 3 round-5 + 4 round-6 + 4 round-7 + 2 round-8 + 4 round-9 + 2 round-10 + 4 round-13 + 2 round-14) resolved across twelve counted revision passes.** Rounds 11 and 12 produced ledger-only updates and are not double-counted.

---

## Revision History

A second-pass review on 2026-05-08 (after the original PLAN-CHECK PASS-WITH-WARNINGS verdict above) found 5 defects when the plans were re-grounded against the actual backend code. All 5 were fixed via targeted edits — no plan was rewritten wholesale; frontmatter, dependencies, must_haves, and unaffected tasks were preserved.

| Defect | Plan | One-line resolution |
|---|---|---|
| P1-A | 23-03 | Replaced non-existent `/api/capture/text` with the canonical `/api/capture`, replaced `/api/capture/follow_up` (underscore) with `/api/capture/follow-up` (hyphen), corrected the follow-up body shape to use snake_case `inbox_item_id` + `follow_up_text` + `follow_up_round` per `TextCaptureBody` and `FollowUpBody` Pydantic models, embedded canonical body shapes in the interfaces block, and extended the verify block to assert correct paths and field names in `low_confidence_followup.input.json`. |
| P1-B | 23-04 | Replaced `eval_type: "admin"` with `eval_type: "admin_agent"`, added the required `routing_context` field with a stable seed string `"Costco: olive oil; Amazon: diapers"` (recorded in the baseline as `admin_routing_context_seed` so the Phase 24 GA re-run uses an identical input), changed JSON extraction from `.run_id` to `.runId` (camelCase per `eval.py:115`), changed polling path from `/api/eval/run/{id}` to `/api/eval/status/{id}`, added fail-fast guards if either `.runId` is null, and extended the verify block to assert `.classifier.evalType == "classifier"` / `.admin.evalType == "admin_agent"` / non-empty `admin_routing_context_seed`. |
| P1-C | 23-05 | Removed the unsafe single-`az containerapp update` command that combined `--set-env-vars FOUNDRY_MODEL=...` with `--remove-env-vars AZURE_AI_*_AGENT_ID` against the RC image. Replaced with a documented three-step staged sequence (Step A: pre-deploy env-var add only; Step B: GA image push with old vars still present; Step C: post-deploy orphan removal AFTER successful UAT). Added a NEGATIVE assertion subsection that explicitly forbids removing `AZURE_AI_*_AGENT_ID` env vars before the GA image is deployed and cites the live agent drift risk via `agents/classifier.py:40` (BLANK portal agent creation in `ensure_classifier_agent` if `stored_agent_id` is empty), with line-number citations to `main.py:514`, `main.py:596`, `main.py:687` where each setting is read at lifespan startup. |
| P2-A | 23-02 | Replaced the `_tag_current_span` helper (which tagged only the currently-active span at helper-call time and missed every framework-emitted span created AFTER the call) with a `ProbeTagSpanProcessor(SpanProcessor)` whose `on_start` hook reads `_probe_run_id`/`_probe_name` ContextVars and tags every span at creation time. Mirrors the existing `CaptureTraceSpanProcessor` pattern from `backend/src/second_brain/observability/span_processor.py:27`. Added `_ensure_probe_processor_installed()` (idempotent install on the current TracerProvider) and required every probe body to call it, deleted the per-probe `_tag_current_span` calls, and extended the Task 4 checkpoint with an App Insights `tagged_count` query that asserts non-zero coverage across `AppDependencies`, `AppTraces`, `AppRequests` for each probe's run_id. |
| P2-B | 23-02 + 23-05 | Replaced narrow per-pattern placeholder greps (e.g. `! grep -q "<verify>"`) with a broad `<[^>]*>` final guard in the verify blocks of FOUNDRY-PROBE-FINDINGS.md and SPAN-NAME-MAPPING.md, with explicit `-vE` allow-lists for HTML tags (`<code>`, `<pre>`), KQL bracket syntax (`Properties\.\[`), URL angles (`<https://...>`), and known file path tokens. The pipeline ends with `| grep .` so the verify exits 1 (success) only when no offending placeholders remain. |

---

## Round 2 Revision History

A third-pass code-grounded review on 2026-05-08 (after the round-1 fixes above) found 3 more defects (1 P1 + 1 P2 + 1 P3) when the plans were re-grounded against the actual production telemetry pattern in `backend/src/second_brain/main.py:14-31`. All 3 were fixed via targeted edits — no plan was rewritten wholesale.

| Defect | Plan | One-line resolution |
|---|---|---|
| Round-2 P1 | 23-02 | Made probe telemetry setup explicit. Added `_setup_probe_telemetry()` at module entry that calls `configure_azure_monitor(connection_string=os.environ["APPLICATIONINSIGHTS_CONNECTION_STRING"], ...)` + `enable_instrumentation()` (mirroring production `main.py:14-31`), fails fast with `sys.exit(2)` if the connection string is missing OR if the resulting `TracerProvider` does not expose `add_span_processor` (catches the `ProxyTracerProvider` silent no-op case). Tightened `_ensure_probe_processor_installed()` to fail fast (`sys.exit(2)`) instead of silently returning when `add_span_processor` is missing at install time. Reordered every probe body (`streaming_shape`, `tool_call_extraction`, `tool_choice_required`, `session_rehydration`, `auth_probe`) so ContextVars + processor install happen BEFORE `FoundryChatClient` / `_build_client` construction — previously the SDK created its first spans before tagging was active. Added an AST-walk verify step that fails the task if any probe constructs the client before setting `_probe_name`. |
| Round-2 P2 | 23-03 + 23-04 + 23-05 | Extended the round-1 broad placeholder guard pattern to four more docs whose round-1 verify blocks still used narrow per-token negations: `FIXTURE-CAPTURE-LOG.md` (23-03), `EVAL-INVENTORY.md` (23-04), `CONFIG-DELTAS.md` (23-05), `AUDITOR-VERIFICATION.md` (23-05). Each verify block now uses `! grep -nE '<[^>]+>' <doc> \| grep -vE '<allow-list>' \| grep -vE '^\s*$' \| grep .` to reject any unfilled `<...>` token while permitting documented exceptions (HTML tags, KQL `Properties.[...]` syntax, URLs, HTML comments, plus per-doc legitimate forward references such as `<foundry-resource-id>` operator-substituted scope literals, `<task-group-start-sha>` git-diff argument literals, brace-expansion path patterns like `FRAMEWORK-FIDELITY-23.{1,2,3}.md`, and Python-string-literal type annotations like `"EvalClassifierTools"`). |
| Round-2 P3 | PLAN-CHECK.md | Reconciled this report's per-plan summaries with the Revision History (W1-W5 had been resolved in round 1 but per-plan sections still listed them as PASS-WITH-WARNINGS, and the top verdict was PASS-AFTER-REVISIONS rather than PASS). Updated 23-02/23-03/23-04/23-05 statuses to PASS, moved W1-W5 to a "Resolved Warnings" section, updated the top-level verdict from "PASS-AFTER-REVISIONS" to "PASS", and added this Round 2 Revision History section noting the 3 round-2 fixes (telemetry setup, broader placeholder guards in 4 docs, this PLAN-CHECK reconciliation). Total defects resolved across both passes: 8 (5 round-1 + 3 round-2). |

---

## Round 3 Revision History

A third-pass code-grounded review on 2026-05-08 (after the round-1 and round-2 fixes above) found 4 more defects (2 P1 + 2 P2) when the plans were re-grounded against the canonical `uv` workflow, the design vs. GSD phase-directory naming mismatch, the AST permissiveness in 23-02's verify, and the actual eval runner output keys at `backend/src/second_brain/eval/runner.py:200-360`. All 4 were fixed via targeted edits — no plan was rewritten wholesale.

| Defect | Plan / File | One-line resolution |
|---|---|---|
| Round-3 P1-A | 23-01 + 23-02 | `uv venv PATH --python 3.12` was used to set up the spike + proberun venvs, but `uv venv` creates the venv AT `PATH`, putting the interpreter at `PATH/bin/python` — NOT `PATH/.venv/bin/python` as the plan's later invocations assumed. Replaced with the canonical project-dir + `uv sync` workflow: `mkdir -p PATH; cp pyproject.toml + uv.lock; cd PATH; uv sync --locked --python 3.12`, which creates `PATH/.venv/` automatically. Added `test -x PATH/.venv/bin/python` acceptance criteria to BOTH plans (spike in 23-01, proberun in 23-02) so the interpreter-path assumption is asserted at task-time rather than discovered at probe-run time. Added a "why" callout in each plan explaining the `uv venv` vs. `uv sync` distinction so the next reviewer doesn't re-introduce the bug. |
| Round-3 P1-B | docs/superpowers/specs/2026-05-05-foundry-ga-migration-design.md + this file | Spec used `.planning/phases/23.0/` as the artifact-location placeholder, but GSD created the phase directory as `.planning/phases/23-foundry-ga-prep/` and the calibration report at `.planning/phases/24-foundry-ga-migration/FRAMEWORK-FIDELITY-calibration.md` plus STATE.md round-9 changelog already cite the renamed paths. Reverting to `23.0/` would orphan all that work, so we reconciled the spec to the renamed paths instead: 13 path-reference replacements across the design doc (`.planning/phases/23.0/` → `.planning/phases/23-foundry-ga-prep/`), preserving the textual "Phase 23.0" labels as documented synonyms per the design's own placeholder caveat. Added a header note to the design doc explaining the reconciliation. Updated PLAN-CHECK Boundary Verification opening line to cite the post-reconciliation design as the source of truth. |
| Round-3 P2-A | 23-02 | The previous AST walk in the verify block enforced "Invariant 1" (`_setup_probe_telemetry()` called at module level) too permissively — it accepted ANY module-level call regardless of position relative to probe definitions, so calling setup AFTER a probe def would still pass. It also enforced "Invariant 2" (probe-body ordering) only on `_probe_name.set` vs. `FoundryChatClient`, ignoring `_ensure_probe_processor_installed()` — a probe that set ContextVars but skipped the processor install would still pass. Replaced with two separate stricter AST walks: Invariant 1 asserts `_setup_probe_telemetry()` precedes the FIRST probe function definition in module-body order; Invariant 2 asserts BOTH `_probe_name.set(...)` AND `_ensure_probe_processor_installed()` precede the first `FoundryChatClient(...)` / `_build_client(...)` call inside every probe body, with per-probe failure messages and line numbers. Acceptance criteria updated to name both invariants explicitly. |
| Round-3 P2-B | 23-04 | Eval baseline verify block only checked that `accuracy` appeared SOMEWHERE inside the classifier subtree (`.classifier \| (.. \| objects \| select(has("accuracy"))) // empty`), did not assert `.classifier.accuracy` directly, did not assert `.admin.routing_accuracy` at all, and did not enforce the sample-size minimum (`>=50`) that the acceptance text already mentioned. Replaced the broad shape-check with direct field+type+range assertions: `.classifier.accuracy` AND `.admin.routing_accuracy` are both NUMBERS in `[0, 1]`; `.classifier.total >= 50` AND `.admin.total >= 50` (sample-size contract enforced in jq); `.classifier.correct <= .classifier.total` (sanity check). Acceptance criteria updated to name each strict assertion explicitly. Verified actual runner output keys against `backend/src/second_brain/eval/runner.py:200-210` (classifier: `accuracy`, `total`, `correct`) and `:357` (admin: `routing_accuracy`, `total`, `correct`) — direct properties of the run-status dict, not nested. (Note: the `.admin.total >= 50` assertion was later corrected in round-5 P2 — the admin runner does NOT enforce a 50-case minimum; only the classifier does per EVAL-01.) |

**Total defects resolved across all three passes: 12 (5 round-1 + 3 round-2 + 4 round-3).**


---

## Round 4 Revision History

A fourth-pass code-grounded review on 2026-05-08 (after the round-1, round-2, and round-3 fixes above) found 3 more defects (1 P1 + 1 P2 + 1 P3) when the plans were re-grounded against `backend/pyproject.toml:50-51` (hatch wheel target), the placeholder discipline rolled out in round 2, and STATE.md round-9 path references. All 3 were fixed via targeted edits — no plan was rewritten wholesale.

| Defect | Plan / File | One-line resolution |
|---|---|---|
| Round-4 P1 | 23-01 + 23-02 | The spike (`/tmp/foundry-ga-spike`) and proberun (`/tmp/foundry-ga-proberun`) directories copy ONLY `pyproject.toml` + `uv.lock` from `.planning/phases/23-foundry-ga-prep/` — they deliberately do NOT contain a `src/second_brain/` source tree. But `backend/pyproject.toml:50-51` declares `[tool.hatch.build.targets.wheel] packages = ["src/second_brain"]`, which the candidate `pyproject.toml` inherits. Without `--no-install-project`, `uv sync --locked --python 3.12` would attempt to build the `second_brain` wheel from source and fail before the smoke test / probe runs ever execute. Added `--no-install-project` flag to BOTH `uv sync` invocations (PLAN-01 task 1 step 5; PLAN-02 task 2 step 3), with a "why" callout in each plan explaining the hatch wheel target. Added a positive sanity check after each sync — `python -c "from agent_framework.foundry import FoundryChatClient; from agent_framework import Agent; print(\'OK: GA imports resolve\')"` — that proves the GA framework deps were actually installed (not silently skipped by the flag). Acceptance criteria and verify blocks updated in both plans to assert the flag is present and the secondary import check succeeded. (Note: the import path used in this round-4 sanity check was later corrected in round-5 P1 — the canonical GA path is `agent_framework_foundry` (top-level) not `agent_framework.foundry` (submodule). The blocking check uses the top-level path; the submodule probe is non-blocking.) |
| Round-4 P2 | 23-04 | Task 3 (CANDIDATE-instructions export) introduced placeholder markers in `classifier.md`, `admin.md`, `investigation.md`, and `PORTAL-DRIFT.md` (`<YYYY-MM-DD>`, `<operator>`, `<N>`, `<verbatim>`, `<agent ID>`, `<one of "no drift", ...>`, `<see reconciled file line N>`, `<repeat for each diff hunk>`, `<one sentence>`). The verify block only checked file existence + headings + agent IDs + size — did NOT scan for unfilled placeholders, so a sloppy export could leave `<YYYY-MM-DD>` literally in the committed file (mirroring the original W4 issue from round-1 SPAN-NAME-MAPPING that the round-2 placeholder discipline rollout fixed for 4 docs but missed these 4). Added the broad `<[^>]+>` placeholder guard with the standard `-vE` allow-list (HTML tags, KQL `Properties.[...]`, URLs, HTML comments) to the verify block, iterating over all 4 CANDIDATE-instructions files. Added a "Placeholder substitution rule" table to the action section enumerating every placeholder marker and what to replace it with (today's date, operator name, real counts, real agent IDs, etc.). Verbatim angle-bracket portal text the operator wants to keep must be allow-listed in `PORTAL-DRIFT.md` under a new `## Verbatim angle-bracket allow-list` section AND added to the `-vE` chain — never silently. Acceptance criteria updated to enumerate each placeholder type that must be substituted. |
| Round-4 P3 | STATE.md + PLAN-CHECK.md | STATE.md round-9 changelog at `.planning/STATE.md:213` cited the boundary as `.planning/phases/23/`, but the actual phase directory is `.planning/phases/23-foundry-ga-prep/` (the rename happened in round 3 P1-B and the design + calibration report + this plan-check were all reconciled to it; STATE.md was missed). PLAN-CHECK Boundary Verification claimed "STATE.md cites the renamed paths" — that claim was false until round 4 corrected STATE.md. Edits: (1) replaced the STATE.md path string with `.planning/phases/23-foundry-ga-prep/` directly (one occurrence at L213); (2) the existing PLAN-CHECK boundary section already implies the post-round-3 reconciliation is the source of truth — no further edit needed there since STATE.md is now consistent. (3) Added this Round 4 Revision History entry making the round-4 changes auditable. |

**Total defects resolved across all four passes: 15 (5 round-1 + 3 round-2 + 4 round-3 + 3 round-4).**

---

## Round 5 Revision History

A fifth-pass code-grounded review on 2026-05-08 (after the round-1 through round-4 fixes above) found 3 more defects (1 P1 + 1 P2 + 1 P3) when the plans were re-grounded against the canonical GA SDK import shape (`agent_framework_foundry` top-level package per the PyPI `agent-framework-foundry` distribution), the actual admin runner contract at `backend/src/second_brain/eval/runner.py:248-263`, and a documentation-drift gap in PLAN-CHECK line 87. All 3 were fixed via targeted edits — no plan was rewritten wholesale.

| Defect | Plan / File | One-line resolution |
|---|---|---|
| Round-5 P1 | 23-01 + 23-02 | Round-4 P1 added a positive GA-imports sanity check using `from agent_framework.foundry import FoundryChatClient` (submodule path), but the probe scaffold (23-01 line 298) and the harness imports (23-02 line 91) use `from agent_framework_foundry import FoundryChatClient` (top-level package — the canonical GA SDK shape per the `agent-framework-foundry` PyPI distribution). The plan's verify check made BOTH paths blocking, so if only the top-level path resolved (the actual GA shape) the secondary `agent_framework.foundry` check would fail and block the entire spike before probes could run. Demoted the secondary check to a NON-BLOCKING exploratory probe with a stderr fallback message; promoted the canonical top-level `agent_framework_foundry` check to the SOLE BLOCKING gate. Edits in 23-01: replaced lines 174-180 with two separate steps (BLOCKING top-level check → required `OK: GA imports resolve`; NON-BLOCKING submodule probe → captures either "submodule path also resolves" or "submodule path does not resolve" without failing). Edits in 23-02: same treatment in step 3 of Task 2. Acceptance criteria, verify blocks, and success criteria in both plans updated to clarify which check is blocking and which is informational. The secondary-path outcome is captured into DEP-RESOLUTION-NOTES.md (23-01) and the proberun import-smoke log (23-02) for downstream reference, but neither outcome blocks Phase 23. |
| Round-5 P2 | 23-04 | Verify block asserted `.admin.total >= 50`, but `eval/runner.py:248-263` shows the admin runner filters GoldenDataset to cases with non-null `expectedDestination` and fails the run ONLY if the filtered list is empty (`if not test_cases:`). It does NOT enforce a 50-case minimum on admin. The asymmetric `>= 50` assertion would falsely block a legitimate run when the seeded admin dataset has fewer cases. Replaced `.admin.total >= 50` with `.admin.total > 0` (matches the runner's actual contract). Classifier `>= 50` retained as-is (the seeded classifier dataset is documented at this size in design Section 5.2 / Phase 21 seed file). Added a non-blocking soft warning to stderr: if `.admin.total < 10`, the verify block prints "WARNING: admin eval baseline has only N cases. The ±2pp gate at this sample size has wide variance — consider seeding more admin cases before relying on the migration eval gate." The warning does NOT fail the task; operator can choose to proceed or re-seed. Acceptance criteria updated to enumerate the two-tiered contract explicitly: classifier ≥ 50 (sample-size contract for representative accuracy gate signal); admin > 0 (runner-level contract; filtered dataset must be non-empty). Action section, runner-contract callout, success criteria, and Task 4 checkpoint all updated to surface the asymmetry and the soft-warning behavior. |
| Round-5 P3 | PLAN-CHECK.md | Line 87 read "✅ 23-01 Task 1 uses `uv venv`, `uv lock`, `uv run`. 23-02 Task 2 uses `uv venv` + `uv sync` for the proberun venv." This was stale documentation — the plans actually use `mkdir -p` + `cd` + `uv sync --locked --python 3.12 --no-install-project` (round 3 P1-A switched from `uv venv` to `mkdir + uv sync`; round 4 P1 added `--no-install-project`). A reviewer reading PLAN-CHECK as the source of truth would be misled. Updated line 87 to read: "✅ 23-01 Task 1 uses `mkdir -p /tmp/foundry-ga-spike` + `cd` + `uv sync --locked --python 3.12 --no-install-project` + `uv lock`. 23-02 Task 2 uses the same `mkdir` + `uv sync --locked --python 3.12 --no-install-project` pattern. No `pip install` references. The `--no-install-project` flag is required because the temp project dir does not contain the `src/second_brain/` source tree (round 4 P1 fix)." Updated top-line defect-count from 15 to 18, "four passes" to "five passes", final-status block, and added this Round 5 Revision History entry making the round-5 changes auditable. |

**Total defects resolved across all five passes: 18 (5 round-1 + 3 round-2 + 4 round-3 + 3 round-4 + 3 round-5).**

---

## Round 6 Revision History

A sixth-pass code-grounded review on 2026-05-08 (after the round-1 through round-5 fixes above) found 4 more defects (1 P1 + 2 P2 + 1 P3) when the plans were re-grounded against the canonical KQL `union withsource=SourceTable ... | extend ItemType = case(...)` pattern at `backend/src/second_brain/observability/kql_templates.py:44, 74, 220` and against placeholder discipline gaps in 23-03 fixture-level docs and the 23-02 Probe 3 verdict template. All 4 were fixed via targeted edits — no plan was rewritten wholesale.

| Defect | Plan / File | One-line resolution |
|---|---|---|
| Round-6 P1 | 23-03 | Replaced two non-canonical KQL blocks (interfaces template at line ~180 and the embedded Python `query_workspace` block in Task 1 step 3) that used `ItemType=$table` and `ItemType=type()` — neither is valid KQL (`type()` returns column type not table source; `$table` is not a valid identifier) — with the canonical repo pattern `union withsource=SourceTable AppRequests, AppDependencies, AppTraces, AppExceptions ... | extend ItemType = case(SourceTable == "AppRequests", "Request", SourceTable == "AppDependencies", "Dependency", ...)` per `kql_templates.py:44-95`. Strengthened the spans-file verify in all three per-agent tasks (investigation, admin, classifier): replaced `jq empty <spans>` (which passes for empty array `[]` and malformed-but-syntactically-valid JSON) with a strict pipeline that asserts (a) valid JSON, (b) non-empty array (`type == "array" and length > 0`), (c) every row's `Properties["capture.trace_id"]` (or `Properties.capture_trace_id`) matches the input.json's top-level `.trace_id`. Added a step in each task action ("Record the captured trace_id back into input.json") instructing the operator to use `jq` to merge the trace_id from the first SSE event into the corresponding input.json — the verify block depends on this anchor. Added per-task acceptance criteria asserting "Every `*.spans.json` is a non-empty JSON array AND every row's `Properties['capture.trace_id']` matches the corresponding `*.input.json` `.trace_id`." |
| Round-6 P2-A | 23-03 | Added the same broad `<[^>]+>` placeholder guard pattern (rolled out for FIXTURE-CAPTURE-LOG.md / EVAL-INVENTORY.md / CONFIG-DELTAS.md / AUDITOR-VERIFICATION.md / SPAN-NAME-MAPPING.md / FOUNDRY-PROBE-FINDINGS.md / CANDIDATE-instructions in earlier rounds) to per-fixture validation in Task 1, Task 2, Task 3 verify blocks — scanning every generated `*.input.json` and `*.expected-deltas.md` per agent for unfilled `<...>` tokens. Allow-list permits HTML tags (`<code>`, `<pre>`), KQL bracket syntax (`Properties.[...]`), URLs, HTML comments. Added an explicit substitution rule in Task 3 action for the literal `<from turn-1 SSE>` placeholder in the low_confidence_followup template at line 570: operator must replace it with the actual `inboxItemId` extracted from the turn-1 SSE event (camelCase `value.inboxItemId`) using `jq --arg id "$INBOX_ID" '.turns[1].body.inbox_item_id = $id'`. Added an explicit `! grep -q "<from turn-1 SSE>"` negation in the classifier verify block as belt-and-suspenders. Acceptance criteria updated per task to assert the placeholder guard ran clean. |
| Round-6 P2-B | 23-02 | Replaced the non-canonical KQL probe coverage block at line ~1115 in the Task 4 checkpoint how-to-verify section. Old form `union AppDependencies, AppTraces, AppRequests | summarize tagged_count = count() by SourceTable = $table` is invalid (the `$table` identifier doesn't exist in this context — it's a SQL/Splunk-style construct, not KQL). Replaced with the canonical pattern `union withsource=SourceTable AppDependencies, AppTraces, AppRequests | summarize tagged_count = count() by SourceTable` (the `withsource=SourceTable` clause materializes a `SourceTable` column at union time, which `summarize ... by SourceTable` then aggregates over). Added a one-line callout citing `kql_templates.py:44, 74, 220` as the source-of-truth pattern. Surrounding prose updated to explicitly enumerate which `SourceTable` rows are expected (AppDependencies, AppTraces, AppRequests). |
| Round-6 P3 | 23-02 | Cleaned up Probe 3 verdict template at line ~994 in the Probe 3 findings section. Old form ended with `..."<>"` — a malformed empty `<>` token that would either trip the broad placeholder guard at commit time or pass through silently, neither outcome desirable. Replaced the verdict line with a clean "pick EXACTLY ONE of the three quoted strings; remove the other two before commit" instruction, with each of the three verdict strings (`WORKS`, `DOES NOT WORK`, `BOTH WORK`) presented as its own backticked bullet under a single-line directive. Removed the malformed `<>` token entirely. The operator copies one verdict string and deletes the other two; the broad placeholder guard then has no `<...>` patterns to flag in this section. |

**Total defects resolved across the first six passes: 22 (5 round-1 + 3 round-2 + 4 round-3 + 3 round-4 + 3 round-5 + 4 round-6).**

---

## Round 7 Revision History

A seventh-pass code-grounded review on 2026-05-08 (after the round-1 through round-6 fixes above) found 4 more defects (2 P1 + 1 P2 + 1 P3) when the plans were re-grounded against (a) the `azure-monitor-query` row-coercion shape in 23-03's KQL export snippet — `str(row[i])` stringifies the Properties dict and breaks the `jq -e '.Properties["capture.trace_id"]'` verifier; (b) the actual follow-up endpoint trace_id source at `backend/src/second_brain/api/capture.py:366` which reads `X-Trace-Id` and falls back to `uuid4()` rather than propagating the turn-1 trace_id automatically; (c) the soft "states one of" verdict check in 23-02 that passed even when the operator left all three template strings in place; (d) the stale W1 wording in this PLAN-CHECK report that still cited the pre-round-6 `ItemType=type()` form. All 4 were fixed via targeted edits — no plan was rewritten wholesale.

| Defect | Plan / File | One-line resolution |
|---|---|---|
| Round-7 P1-A | 23-03 | Replaced the canonical Task 1 export snippet's `rows.append({cols[i]: str(row[i]) ...})` row-coercion (which stringifies the Properties dict and breaks the verifier's `.Properties["capture.trace_id"]` indexing — `jq` errors with "Cannot index string with string") with a `_coerce(value)` helper that preserves dict/list/scalar types verbatim AND recovers stringified-JSON dicts via `json.loads` (azure-monitor-query workspace responses sometimes serialize Properties as a JSON string; the helper handles both shapes). Added a note that Tasks 2 and 3 reuse this same canonical pattern. Extended the spans-file verify in all three per-agent verify blocks (investigation, admin, classifier) with `jq -e 'all(.[]; .Properties \| type == "object")'` to fail fast if any row has a stringified Properties (which would mean the `_coerce` helper was bypassed). Added matching acceptance criteria in all three tasks. |
| Round-7 P1-B | 23-03 | Corrected the prose claim at line ~658 ("`.trace_id` is the turn-1 trace_id (it propagates through the follow-up call so all spans across both turns carry the same `capture.trace_id`)") which was wrong — the follow-up handler at `backend/src/second_brain/api/capture.py:366` reads `request.headers.get("X-Trace-Id", str(uuid4()))` and generates a FRESH trace_id when the header is absent. Updated the Task 3 turn-2 curl invocation to explicitly send `-H "X-Trace-Id: $TURN1_TID"` so the backend reuses the turn-1 trace_id. Updated the input.json schema for low_confidence_followup to record both `turn1_trace_id` and `turn2_trace_id` top-level keys (operator extracts each from the first SSE event of each turn; with `X-Trace-Id` propagated correctly they should be equal, but if a header is stripped by an intermediate they differ). Replaced the classifier verify block's single-trace_id check with a dual-trace_id check that allows either turn1 or turn2 trace_id on any spans.json row (`(.Properties["capture.trace_id"] // .Properties.capture_trace_id) as $rowTid \| $rowTid == $tid1 or ($tid2 != "" and $rowTid == $tid2)`). Updated acceptance criteria to require both keys for the follow-up fixture and document the propagation contract. |
| Round-7 P2 | 23-02 | Strengthened the Probe 3 verdict verify block to assert EXACTLY ONE of the three quoted verdict strings appears in FOUNDRY-PROBE-FINDINGS.md (counts grep matches with `grep -qF` against each verbatim verdict; fails if count != 1) AND that the operator instruction line "pick EXACTLY ONE of the three quoted strings" has been deleted from the committed doc (negated grep). The previous "explicitly states one of: WORKS / DOES NOT WORK / BOTH WORK" check was satisfied even if all three remained in place. Acceptance criteria updated to enumerate the three verbatim verdict strings and require the instruction line to be absent. |
| Round-7 P3 | PLAN-CHECK.md | Updated the W1 entry on line ~154 from the stale wording ("the actual capture KQL block uses `ItemType=type()` consistently") to the post-round-6 reality ("uses the canonical `union withsource=SourceTable AppRequests, AppDependencies, AppTraces, AppExceptions ... \| extend ItemType = case(SourceTable == 'AppRequests', 'Request', ...)` form per `kql_templates.py:44, 74, 220`. The earlier `ItemType=type()` (which is invalid KQL) is gone."). Updated the top-line summary from "Six rounds" / "22 defects" to "Seven rounds" / "26 defects" and appended this Round 7 Revision History section. Updated Final Status counts. |

**Total defects resolved across all seven passes: 26 (5 round-1 + 3 round-2 + 4 round-3 + 3 round-4 + 3 round-5 + 4 round-6 + 4 round-7).**
</content>


---

## Round 8 Revision History

An eighth-pass code-grounded review on 2026-05-08 (after the round-1 through round-7 fixes above) found 2 more defects (2 P1) when the plans were re-grounded against (a) the actual SSE event constructors at `backend/src/second_brain/streaming/sse.py:33-110` (no `trace_id` field on any constructor), (b) the `/api/investigate` handler at `backend/src/second_brain/api/investigate.py:30-50` (does not read `X-Trace-Id`, does not set `capture_trace_id_var`), and (c) the investigation SSE adapter at `backend/src/second_brain/streaming/investigation_adapter.py:108-188` (emits `thinking`/`text`/`done` events with no trace_id). Both were fixed via targeted edits to 23-03 — no plan was rewritten wholesale.

| Defect | Plan / File | One-line resolution |
|---|---|---|
| Round-8 P1-A | 23-03 | Replaced the unworkable "extract trace_id from first SSE event" pattern (lines ~281, 506, 684 of the prior plan) with **client-side UUID generation** + `X-Trace-Id` header on every capture curl. Verified that no SSE event constructor in `streaming/sse.py:33-110` carries a `trace_id` field — the prior pattern would have produced empty `EXPECTED_TID` on every fixture. The capture handler at `api/capture.py:217` (text) and `:366` (follow-up) reuses the supplied header value, so a client-generated UUID becomes the canonical `capture.trace_id` for the entire capture (and any in-process admin handoff). For the two-turn `low_confidence_followup` flow, the same `$TURN1_TID` is sent on both turns; explicit propagation guarantees `turn1_trace_id == turn2_trace_id` (no SSE-extraction drift possible). Updated the trace acquisition contract section (line 170) to document the new client-side-generation rule with citations to `streaming/sse.py:33-110` and `api/capture.py:217, 366`. Updated all three Tasks (1: investigation, 2: admin, 3: classifier) to use `TID=$(python3 -c "import uuid; print(uuid.uuid4())")` before each curl. Added an acceptance criterion to each task: "Each `*.input.json`'s `.trace_id` matches the UUID actually sent in the `X-Trace-Id` header for that capture (operator generates client-side, records into input.json, sends on curl — no SSE round-trip)." |
| Round-8 P1-B | 23-03 | Investigation fixtures use a different correlation key from captures because the `/api/investigate` handler at `api/investigate.py:30-50` does NOT read `X-Trace-Id`, the request never sets `capture_trace_id_var`, and the SSE adapter at `investigation_adapter.py:108-188` emits no trace_id. Investigation spans therefore never carry `Properties["capture.trace_id"]`. The prior plan's verifier (`all rows match $EXPECTED_TID`) would have failed on every investigation fixture. Applied **Option B: relaxed verifier + OperationId-based KQL**. Task 1 KQL replaced with a `union withsource=SourceTable AppRequests, AppDependencies, AppTraces, AppExceptions \| where Url endswith '/api/investigate' OR OperationId in (...AppRequests Url endswith '/api/investigate'...)` within a 15-minute window. Task 1 verifier replaced strict `Properties["capture.trace_id"] == $tid` check with structural shape check: (a) `type == "array" and length > 0`, (b) `any(.[]; .Name == "POST /api/investigate" or (.Name // "" \| endswith "/api/investigate"))`. Investigation `.trace_id` becomes documentary only (records the test session's request id, not load-bearing for span correlation). Tasks 2 (admin) and 3 (classifier) keep the existing `capture.trace_id` correlation because `/api/capture` and `/api/capture/follow-up` DO propagate `X-Trace-Id` per `api/capture.py:217, 366`. Added a Phase 24 boundary note: trace correlation for the investigation endpoint is owned by Phase 24 task group 23.1 (Investigation migration), which is already touching the investigation surface — adding instrumentation in Phase 23 would violate the artifact-only boundary. Acceptance criteria updated to make the investigation/capture asymmetry explicit. |

**Total defects resolved across the first eight passes: 28 (5 round-1 + 3 round-2 + 4 round-3 + 3 round-4 + 3 round-5 + 4 round-6 + 4 round-7 + 2 round-8).**

---

## Round 9 Revision History

A ninth-pass code-grounded review on 2026-05-08 (after the round-1 through round-8 fixes above) found 4 more defects (2 P1 + 2 P2) when the plans were re-grounded against (a) the actual `InvestigateBody` Pydantic model at `backend/src/second_brain/api/investigate.py:30` which uses `{question, thread_id}` not `{message}`; (b) the post-round-8 KQL pattern split where Task 1 became investigation-specific (OperationId-join) but Tasks 2-3 still pointed back to it for capture-trace correlation; (c) the 15-minute window on Task 1's KQL allowing concurrent /api/investigate request pollution; and (d) the contradicted expected-deltas template that asserted `capture.trace_id` on every span across all three agent types when only Admin and Classifier carry it. All 4 were fixed via targeted edits to 23-03-PLAN.md and this PLAN-CHECK — no plan was rewritten wholesale.

| Defect | Plan / File | One-line resolution |
|---|---|---|
| Round-9 P1-A | 23-03 | Task 1 step 1 example payloads used the wrong field name (`{"message": "..."}`) — `InvestigateBody` at `api/investigate.py:30-34` defines `{question: str, thread_id: str | None}`. Posting `{"message": ...}` returns HTTP 422 (Pydantic validation error) and the agent never runs; the OperationId-join verifier would have failed on every fixture or, worse, falsely passed by joining to the wrong polluted request. Replaced all 5 example payloads with the correct `{"question": "...", "thread_id": "..."}` shape, wrapped each in a fixture-level input.json schema with separate `trace_id` (documentary) and `thread_id` (load-bearing) keys plus a nested `body` containing the literal InvestigateBody. Updated step 2 curl to post `jq -c '.body'` (NOT `-d @input.json`) so only the InvestigateBody fields hit the server. Added a 200-status verifier: each fixture's spans.json must have at least one AppRequests row with `ResultCode == "200"` matching the fixture's thread_id; a 422 there indicates the body shape was wrong. Acceptance criterion explicitly references `api/investigate.py:30-34` and forbids the legacy `{message: ...}` shape. |
| Round-9 P1-B | 23-03 | Tasks 2 (admin) and 3 (classifier) instructed the operator to "Use the same KQL+Python pattern from Task 1 step 3" — but after round-8 P1-B, Task 1 step 3 became the *investigation-specific* OperationId-join (because /api/investigate doesn't propagate X-Trace-Id). Admin and classifier captures DO propagate X-Trace-Id per `api/capture.py:217, 366`, so they need the canonical capture-trace KQL pattern (`where Properties.['capture.trace_id'] == '$TID'`) — NOT the investigation OperationId-join. Replaced the cross-reference in Task 2 step 4 and Task 3 step 3 with explicit pointers to the canonical pattern at line ~174 of the PLAN, and inlined a complete Python+KQL block in each so the operator doesn't have to backtrack. Added an acceptance criterion to Tasks 2 and 3: "spans.json export uses the canonical capture-trace KQL filter, NOT the investigation-specific OperationId-join from Task 1 step 3." |
| Round-9 P2-A | 23-03 | (See round-10 Revision History below for the full correction — round-9 P2-A's premise turned out to be wrong on two counts and was replaced wholesale.) Task 1's OperationId-join filter used a 15-minute window with no per-fixture scoping — concurrent /api/investigate requests during fixture capture would pollute the export. Round 9 attempted to fix this by generating a UNIQUE synthetic thread_id (`fixture-23-investigation-<probe>-<short_uuid>`) BEFORE the curl, sending it as the `thread_id` field in the InvestigateBody, and adding KQL filters on `Properties["investigate.thread_id"]` against the AppRequests row. **Round 10 found this fix was wrong on two counts: (1) `investigation_adapter.py:116-117` forwards caller `thread_id` to Foundry as `options["conversation_id"]`, asking Foundry to RESUME a conversation — a synthetic id refers to no real conversation, so Foundry would either reject the request or create a dangling resource; (2) `investigation_adapter.py:96-98` stamps `investigate.thread_id` on the custom `investigate` span (started by `tracer.start_as_current_span("investigate")` at line 96, an AppDependencies-kind span), NOT on the FastAPI AppRequests row. The KQL filter against `SourceTable == 'AppRequests'` would have returned zero rows.** Round 10 replaces this with: send no `thread_id` on first-turn captures (let the server create a conversation), parse the SERVER-generated `thread_id` from the SSE `done` event at `investigation_adapter.py:188`, record it as `thread_id_out` in input.json, then KQL-query the custom `investigate` AppDependencies span by `Properties["investigate.thread_id_out"]` (stamped at line 187) and OperationId-join to AppRequests. Verifier asserts both "exactly one custom investigate AppDependencies row matching thread_id_out" AND "exactly one /api/investigate AppRequests row at same OperationId with ResultCode 200". The unique-thread_id pattern guard `^fixture-23-investigation-<probe-slug>-<8-hex-chars>$` was removed as part of the round-10 correction. |
| Round-9 P2-B | 23-03 | Task 1 step 5's expected-deltas.md template asserted "Span attribute names: `capture.trace_id` (or `capture_trace_id`) present on every span" — but this contradicts the round-8 P1-B fix which says investigation fixtures do NOT use capture.trace_id correlation (the `/api/investigate` handler doesn't read `X-Trace-Id`). The template is shared across all three agent types (investigation, admin, classifier), so the per-agent split needed to be made explicit. Replaced the single-line "every span" assertion with a per-agent split: Admin and Classifier fixtures get `capture.trace_id` (these endpoints propagate `X-Trace-Id` per `api/capture.py:217, 366`); Investigation fixtures get the correlation attribute on the **custom `investigate` AppDependencies span** (started at `investigation_adapter.py:96`), with OperationId-inheritance for downstream spans, and `capture.trace_id` is explicitly NOT expected. **Round-10 P1 corrected the attribute name and span identification:** the round-9 entry initially named `Properties["investigate.thread_id"]` on the "request span," but `investigation_adapter.py:96-98` stamps `investigate.thread_id` on the custom AppDependencies span (NOT the AppRequests row), and the load-bearing correlation key for fixture verification is `Properties["investigate.thread_id_out"]` (stamped at line 187 with the server-generated conversation id parsed from the SSE `done` event at line 188). The current expected-deltas template uses `investigate.thread_id_out` on the custom span; Phase 24 task group 23.1 owns adding `capture.trace_id` during the Investigation migration. Cross-references the existing acceptance criterion at line ~441 so future readers see the full picture in either location. |

**Total defects resolved across all nine passes: 32 (5 round-1 + 3 round-2 + 4 round-3 + 3 round-4 + 3 round-5 + 4 round-6 + 4 round-7 + 2 round-8 + 4 round-9). Round 10 adds 2 more for a running total of 34 — see below.**

---

## Round 10 Revision History

A tenth-pass code-grounded review on 2026-05-08 (after round-9's "synthetic thread_id" fix to `23-03-PLAN.md` was inspected against the actual `investigation_adapter.py` source) found 2 more defects (1 P1 + 1 P2). Round-9 P2-A's fix introduced a new bug: the synthetic thread_id pattern was based on two incorrect premises about how the investigation adapter handles thread_id, both verified against the actual code at `backend/src/second_brain/streaming/investigation_adapter.py:96-98, 116-117, 187-188`. Both round-10 defects were fixed via targeted edits to `23-03-PLAN.md` and this `PLAN-CHECK.md` — no plan was rewritten wholesale.

| Defect | Plan / File | One-line resolution |
|---|---|---|
| Round-10 P1 | 23-03 | Round-9 P2-A's "synthetic thread_id" pattern was wrong on two counts. **(1)** Per `investigation_adapter.py:116-117`, when the caller supplies `thread_id`, the adapter forwards it to Foundry as `options["conversation_id"] = thread_id` — asking Foundry to RESUME a conversation with that id. A synthetic fixture-tagged id (`fixture-23-investigation-<probe>-<short_uuid>`) refers to no real Foundry conversation, so Foundry would either reject the request or create a dangling resource — either way the fixture capture would be unreliable. **(2)** `investigation_adapter.py:96-98` stamps `span.set_attribute("investigate.thread_id", thread_id or "")` on the **custom `investigate` span** (started by `tracer.start_as_current_span("investigate")` at line 96, an AppDependencies-kind span), NOT on the FastAPI/ASGI-created AppRequests row. The round-9 KQL filter `where SourceTable == 'AppRequests' and Properties["investigate.thread_id"] == $thread_id` would therefore have returned zero rows. Resolved with a complete rewrite of Task 1's correlation strategy: **(a)** input.json schema — first-turn captures OMIT `thread_id` from the body and reserve a `thread_id_out` slot (initially null) for the server-generated conversation_id; **(b)** step 2 curl posts `{"question": "..."}` (no thread_id), then parses the server-generated `thread_id` from the SSE `done` event at `investigation_adapter.py:188` and records it into `input.json.thread_id_out`; **(c)** step 3 KQL is two-stage — `let target_op_id = AppDependencies | where Name == 'investigate' | where Properties["investigate.thread_id_out"] == '$server_thread_id' | take 1 | project OperationId`, then `union ... | where OperationId in (target_op_id)` to catch the parent AppRequests row plus all sibling/child spans (relying on `investigation_adapter.py:187` which stamps the server thread_id on the custom investigate span as `investigate.thread_id_out`); **(d)** step 5 verifier asserts both "exactly one custom investigate AppDependencies row carrying `Properties["investigate.thread_id_out"] == .thread_id_out`" AND "exactly one /api/investigate AppRequests row at the same OperationId with ResultCode 200"; **(e)** acceptance criteria explicitly call out that round-9's premise was wrong, cite the verified line numbers, and forbid the synthetic-thread_id pattern. The unique-thread_id pattern guard `^fixture-23-investigation-<probe-slug>-<8-hex-chars>$` was removed. The expected-deltas template was also updated to reference `investigate.thread_id_out` on the custom span (not `investigate.thread_id` on the request row). |
| Round-10 P2 | PLAN-CHECK.md | The round-9 P2-A entry recorded the same wrong premise as the plan: "the investigation adapter at `investigation_adapter.py:96` stamps `Properties[\"investigate.thread_id\"]` on the **request span**" — incorrect per `investigation_adapter.py:96-98` which stamps it on the custom `investigate` AppDependencies span. Future readers re-grounding Phase 23 against the source would have followed this misleading entry into the same dead-end KQL filter. Resolved by amending the round-9 P2-A row in this PLAN-CHECK to acknowledge the round-10 correction inline (cross-referencing the new Round 10 Revision History entry above) and updating the top-line PASS paragraph (32 → 34 defects, "Nine passes" → "Ten passes", "ten passes" addition to the revision-history reference list, and source-citation list expanded to include the relevant `investigation_adapter.py` line ranges that round-10 grounded against: `:96-98, 116-117, 187-188`). |

**Total defects resolved across all ten passes: 34 (5 round-1 + 3 round-2 + 4 round-3 + 3 round-4 + 3 round-5 + 4 round-6 + 4 round-7 + 2 round-8 + 4 round-9 + 2 round-10).**

---

## Round 13 Revision History

A thirteenth-pass code-grounded review on 2026-05-08 (after rounds 1-12 — round 11 and 12 were ledger-only fixes per the convention against double-counting non-plan documentation drift) found 4 more defects (2 P1 + 1 P2 + 1 P3) when the plans were re-grounded against (a) the eval status endpoint's actual return shape at `backend/src/second_brain/api/eval.py:115, 124` + the runner's overwriting of `runs_dict` at `backend/src/second_brain/eval/runner.py:200-215, 350-365`; (b) the voice capture endpoint's actual multipart field name at `backend/src/second_brain/api/capture.py:271`; (c) the admin handoff custom span emission at `backend/src/second_brain/processing/admin_handoff.py:177` + `admin.tool_invoked` attribute at `:252`; and (d) the per-agent key_links / truth split that was made explicit at line 514 (round-10 P1) but stale at lines 92 + 105. All 4 were fixed via targeted edits to `23-03-PLAN.md` and `23-04-PLAN.md` — no plan was rewritten wholesale; frontmatter, dependencies, must_haves, and unaffected tasks were preserved.

| Defect | Plan / File | One-line resolution |
|---|---|---|
| Round-13 P1-A | 23-04 | The verify block at lines 265 + 270 asserted `.classifier.evalType == "classifier"` and `.admin.evalType == "admin_agent"` against the FINAL status response, but `runner.py:205-211` (classifier) and `:355-365` (admin) overwrite `runs_dict[run_id]` with `{"status": "completed", "result_id", "accuracy"|"routing_accuracy", "total", "correct"}` and do NOT preserve the `evalType` field; the status endpoint at `eval.py:124` just returns `{"runId": run_id, **run}` — so the final status response also lacks `evalType`. The verifier would have failed on every successful run. Resolved by capturing `CLF_EVAL_TYPE`/`ADM_EVAL_TYPE` from the POST `/api/eval/run` response (which DOES include `evalType` per `eval.py:115`) at submission time in steps 1 and 3, removing the `.evalType` assertion from the step-4 fail-fast on final-status responses, and adding a step-5 fixture-composition layer that splices the POST-time `evalType` shell vars into each per-agent JSON via `jq --arg et "$EVAL_TYPE" '. + {evalType: $et}'` before composing the final fixture. Added a CRITICAL callout in the canonical contract block above step 1 documenting that the status endpoint does NOT preserve `evalType` (with line citations to `runner.py:205-211, 355-365` + `eval.py:124`). Updated acceptance criteria to make the POST-time-merge rule explicit. |
| Round-13 P1-B | 23-03 | The voice multipart curl at line 844 used `-F "audio=@/tmp/voice_person_sample.webm"`, but the real endpoint at `backend/src/second_brain/api/capture.py:271` is `async def capture_voice(request: Request, file: UploadFile = File(...))` — the field name is `file`, NOT `audio`. POSTing with `audio=...` returns HTTP 422 (FastAPI/Pydantic rejects the unknown form field and reports `file` as missing-required); voice_person.sse.raw would have contained an error response and the spans.json export would have had no rows. Resolved by replacing `-F "audio=@..."` with `-F "file=@..."`, adding a 3-line comment block above the curl citing `capture.py:271` as the source-of-truth, and adding an acceptance criterion to Task 3 stating that the voice curl uses `-F "file=@..."` (NOT `-F "audio=@..."`). |
| Round-13 P2 | 23-03 | The admin verifier at line 726 only asserted (a) non-empty spans + (b) every row's `Properties["capture.trace_id"]` matches the input.json's `.trace_id`. A classifier-only capture (no admin handoff — operator forgot to fire Stage B) would pass the verifier even though no admin processing happened, because Stage A's spans alone trivially satisfy both checks. The admin handoff path at `processing/admin_handoff.py:177` opens a custom AppDependencies span named `admin_agent_process`; line 252 stamps `admin.tool_invoked` attribute on it. Resolved by (a) adding a hard verifier assertion that `admin_agent_process` AppDependencies row count >= 1 (proves admin processing actually ran); (b) adding a soft warning if no `admin_agent_process` span carries `admin.tool_invoked == true` (informational — some admin fixtures could legitimately exercise the no-tool path, but routing/extraction/management fixtures normally invoke tools); (c) adding a CRITICAL OPERATOR REMINDER block above Stage B documenting that admin processing is errands-screen-triggered (NOT auto-fired after classification) per MEMORY.md / Phase 12.1 (shipped 2026-03-17), with the explicit 4-step workflow: submit Stage A → fire `GET /api/errands` → wait 60s → export spans; (d) updating the expected-deltas template "Same (RC == GA contract)" section to specify `admin_agent_process` AppDependencies presence + `admin.tool_invoked` soft-warning rule (replacing the prior vague "span where Name matches `admin` or `Admin*`"); (e) updating Task 2 acceptance criteria to enumerate the hard `admin_agent_process` requirement and the soft `admin.tool_invoked` warning. |
| Round-13 P3 | 23-03 | Frontmatter line 92 truth read "Each spans.json contains the App Insights span tree exported via KQL filtered by capture_trace_id" and line 105 `key_links` declared a single entry "via: KQL filter on Properties.capture_trace_id (or Properties.capture.trace_id)". Both contradict the corrected round-10 P1 contract at line 514 which says investigation fixtures use `investigate.thread_id_out` (NOT `capture_trace_id`) because `/api/investigate` doesn't propagate `X-Trace-Id` per `api/investigate.py:30-50`. A reader scanning the frontmatter without scrolling to line 514 would assume one uniform correlation key for all 18 fixtures. Resolved by (a) replacing the line-92 truth with a per-agent split that explicitly says "admin and classifier filter by `Properties.capture_trace_id`; investigation uses two-stage `investigate.thread_id_out` join" with citations to `api/capture.py:217, 366` and `api/investigate.py:30-50` and a forward-pointer to line ~514; (b) restructuring `key_links` into 3 per-agent entries (admin, classifier, investigation) each with the correct `via` description + the canonical `pattern` regex, plus the sse.jsonl→Phase 24 entry preserved unchanged. |

**Total defects resolved across all eleven counted passes: 38 (5 round-1 + 3 round-2 + 4 round-3 + 3 round-4 + 3 round-5 + 4 round-6 + 4 round-7 + 2 round-8 + 4 round-9 + 2 round-10 + 4 round-13).** Rounds 11 and 12 produced ledger-only updates that are not double-counted per the established convention. Round 14 adds 2 more (1 P1 + 1 P2) for a running total of 40 — see Round 14 Revision History below.


---

## Round 14 Revision History

A fourteenth-pass code-grounded review on 2026-05-08 (after rounds 1-13 — round 11 and 12 were ledger-only fixes per the convention against double-counting non-plan documentation drift) found 2 more defects (1 P1 + 1 P2) when the plans were re-grounded against (a) the eval results endpoint shape at `backend/src/second_brain/api/eval.py:127-179` (returns `aggregateScores` per row) + the runner's `aggregateScores` writes at `backend/src/second_brain/eval/runner.py:179, 331` and the migration design's per-class metric requirement at `docs/superpowers/specs/2026-05-05-foundry-ga-migration-design.md` line 568 area; (b) the admin verifier's soft-warning loophole at `23-03-PLAN.md:751-752` which would have silently passed a fixture with no `admin.tool_invoked == true` span despite all 5 admin fixtures being designed to exercise concrete tool flows. Both were fixed via targeted edits to `23-04-PLAN.md` and `23-03-PLAN.md` — no plan was rewritten wholesale; frontmatter, dependencies, must_haves, and unaffected tasks were preserved.

| Defect | Plan / File | One-line resolution |
|---|---|---|
| Round-14 P1 | 23-04 | The verify block at line 277 ("must be in the baseline") asserted that per-class breakdowns were required, but no command in steps 1-7 actually fetched them. The status endpoint at `/api/eval/status/{run_id}` returns ONLY `{runId, status, result_id, accuracy|routing_accuracy, total, correct}` — `aggregateScores` (where the runner persists per-bucket / per-destination precision/recall at `runner.py:179` and `:331`) is exposed via `/api/eval/results?eval_type=...&limit=5` instead. The Phase 24 ±2pp gate operates on overall accuracy AND per-class drops > 5pp per design line 568 area, so per-class metrics MUST be in the baseline. Resolved by splitting old step 5 into three steps: step 5 (decorate with `evalType` from POST), step 6 (fetch `aggregateScores` from `/api/eval/results` for both classifier and admin via the row whose `id` or `resultId` matches the `result_id` from the status response, with defensive `select(.id == $rid or .resultId == $rid)` to handle whichever exact field name Cosmos stores), step 7 (compose final fixture using merged `$CLF_FULL` / `$ADM_FULL`). Renumbered subsequent step numbers (sanity-check 6→8, removed obsolete optional "add per-class breakdowns" 7 and replaced with new mandatory step 9 that documents the defensive key-name matching). Added 4 new jq assertions to the verify block: `.classifier.aggregateScores | type == "object"`, `.admin.aggregateScores | type == "object"`, plus defensive `(has("perBucket") or has("per_bucket") or has("perClass") or has("per_class") or has("byBucket") or has("by_bucket"))` for classifier and the symmetric chain for admin. Updated acceptance_criteria with two new bullets (class-specific metrics required + defensive per-class key matching) and the `<done>` block to reference the rich-metrics endpoint. The defensive `or has(...)` chain handles whichever exact key name `compute_classifier_metrics` / `compute_admin_metrics` produces; if NONE match at run time, the FATAL message instructs the operator to discover the actual keys via a one-liner `curl ... | jq 'keys'` against the live endpoint. |
| Round-14 P2 | 23-03 | The admin verifier at `23-03-PLAN.md:751-752` emitted a soft WARNING (non-blocking) if no `admin_agent_process` span had `admin.tool_invoked == true`. All 5 admin fixtures (errand_routing, task_creation, recipe_extraction, destination_management, affinity_rule_edit) are designed to exercise concrete admin tool flows; a no-tool span would silently produce a fixture that doesn't actually exercise the admin behavior Phase 24 needs to replay. `admin.tool_invoked` is explicitly stamped per `processing/admin_handoff.py:252` — its absence on a real run indicates the agent decided not to call any tool, which is wrong for these fixtures. Resolved by replacing the soft warning with a hard-failure that exits 1 if absent, gated by a `NO_TOOL_FIXTURES=()` allowlist (currently empty since all 5 current fixtures expect tools). The structure makes the no-tool path explicitly opt-in via allowlist rather than implicit via warning; if a future fixture is designed to exercise the no-tool path, add it to `NO_TOOL_FIXTURES` in the verify block. Updated Task 2 acceptance criteria: previous round-13 soft warning is now hard failure; verifier exits 1 if absent; allowlist documented. |

**Total defects resolved across all thirteen counted passes: 41 (5 round-1 + 3 round-2 + 4 round-3 + 3 round-4 + 3 round-5 + 4 round-6 + 4 round-7 + 2 round-8 + 4 round-9 + 2 round-10 + 4 round-13 + 2 round-14 + 1 round-15).** Rounds 11 and 12 produced ledger-only updates that are not double-counted per the established convention.

## Round 15 Revision History

A fifteenth-pass review on 2026-05-08 — the first run of the new **gsd-design-coverage-auditor** subagent — caught 1 defect (1 P1) that 14 prior code-grounded review rounds missed. The auditor's grounding methodology (asserting verifier expectations against the actual `eval/metrics.py` source rather than against naming conventions) surfaced a regression introduced when round 14 added the per-class metric requirement: the verifier's defensive `or has(...)` chain listed `perBucket` / `per_bucket` / `perClass` / `per_class` / `byBucket` / `by_bucket` as candidate key names, none of which the runner actually produces. The fix was applied via targeted edits to `23-04-PLAN.md` — no plan was rewritten wholesale; frontmatter, dependencies, must_haves, and unaffected tasks were preserved.

**Why 14 rounds missed it:** rounds 1-14 grounded against API endpoint shapes, runner persistence sites, and Cosmos document models — but the round-14 P1 fix authored its allow-list of per-bucket key names (`perBucket`/`byBucket`/etc.) from naming-convention prose in the design document instead of from `eval/metrics.py`. The defensive chain looked thorough, so subsequent rounds did not re-check it. The new design-coverage auditor's calibration check explicitly exercises this blind spot by grounding every assertion against the source-of-truth code.

| Defect | Plan / File | One-line resolution |
|---|---|---|
| Round-15 P1 (W-01) | 23-04 | Verifier at line ~339 asserted `.classifier.aggregateScores` exposed at least one of `perBucket` / `per_bucket` / `perClass` / `per_class` / `byBucket` / `by_bucket`. Per `backend/src/second_brain/eval/metrics.py:14-67`, `compute_classifier_metrics` returns `{accuracy, total, correct, precision: <bucket dict>, recall: <bucket dict>}` — none of the candidate keys exist. Per `eval/runner.py:174-176`, the runner injects `metrics["calibration"] = compute_confidence_calibration(...)` so `aggregateScores` also carries `calibration` (array). The naming-guess chain returned false on every clean run, so every clean baseline failed the verifier. **Fix:** replaced the chain with source-grounded assertions — `has("precision") and has("recall")` plus inner-shape assertions that each per-bucket dict is non-empty and contains numeric values in [0, 1] plus a `calibration` array assertion. Tightened the admin verifier the same way: replaced the `(has("perDestination") or has("per_destination") or ...)` chain with the canonical `has("per_destination")` per `compute_admin_metrics` at `eval/metrics.py:137-176`, plus inner-shape numeric-range assertion. **Knock-on cleanup:** removed the dead `or .resultId == $rid` defensive branch from the `select(...)` clauses fetching `aggregateScores` from `/api/eval/results` — per `backend/src/second_brain/api/eval.py:162`, the endpoint only ever projects Cosmos `.id`, never `.resultId`. **Documentation note (P-01):** added an `### Admin metric shape` heading to the EVAL-INVENTORY.md required structure documenting that `per_destination` is flat accuracy, not separate precision/recall — design L568 says the latter but the runner produces the former. This prevents Phase 24 task group 23.2 from silently widening the runner's contract while introducing the EvalAgentInvoker facade. Updated the corresponding verifier grep + acceptance criteria bullet. Updated step 9 prose with the correct canonical key names + a back-reference to `eval/metrics.py` line numbers, and rewrote the acceptance-criteria bullet from "Defensive per-class key matching" to "Source-grounded per-class key assertions" with explicit grounding citations. |

**Total defects resolved across all thirteen counted passes: 41 (5 round-1 + 3 round-2 + 4 round-3 + 3 round-4 + 3 round-5 + 4 round-6 + 4 round-7 + 2 round-8 + 4 round-9 + 2 round-10 + 4 round-13 + 2 round-14 + 1 round-15).** Rounds 11 and 12 produced ledger-only updates that are not double-counted per the established convention.

