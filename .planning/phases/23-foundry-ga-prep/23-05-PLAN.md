---
phase: 23-foundry-ga-prep
plan: 05
type: execute
wave: 2
depends_on:
  - "23-02"
files_modified:
  - .planning/phases/23-foundry-ga-prep/CONFIG-DELTAS.md
  - .planning/phases/23-foundry-ga-prep/SPAN-NAME-MAPPING.md
  - .planning/phases/23-foundry-ga-prep/AUDITOR-VERIFICATION.md
autonomous: true
requirements:
  - PREP-08
  - PREP-09

must_haves:
  truths:
    - "CONFIG-DELTAS.md enumerates every config.py setting Phase 24 must add, every Container App env var Phase 24 must set, and every orphaned setting Phase 24 must remove"
    - "SPAN-NAME-MAPPING.md provides a side-by-side table of RC vs GA span names and lists every KQL query in observability/queries.py + observability/kql_templates.py that needs updating in Phase 24"
    - "AUDITOR-VERIFICATION.md records the framework-fidelity-auditor existence check and a no-op invocation result"
    - "The auditor calibration report at .planning/phases/24-foundry-ga-migration/FRAMEWORK-FIDELITY-calibration.md is referenced and confirmed to exist (per planning context: '19 ❌, no blind spots')"
  artifacts:
    - path: ".planning/phases/23-foundry-ga-prep/CONFIG-DELTAS.md"
      provides: "Authoritative list of config + env-var changes Phase 24 task group 23.1 applies (config.py adds) and 23.3 applies (Container App env vars + orphan cleanup)"
      contains: "FOUNDRY_MODEL"
    - path: ".planning/phases/23-foundry-ga-prep/SPAN-NAME-MAPPING.md"
      provides: "RC -> GA span name table + KQL query consumers needing updates per task group"
      contains: "invoke_agent"
    - path: ".planning/phases/23-foundry-ga-prep/AUDITOR-VERIFICATION.md"
      provides: "Confirmation that the gsd-framework-fidelity-auditor subagent loads cleanly"
      contains: "gsd-framework-fidelity-auditor"
  key_links:
    - from: "CONFIG-DELTAS.md"
      to: "Phase 24 task group 23.1 (adds foundry_model setting)"
      via: "config.py addition spec"
      pattern: "foundry_model"
    - from: "CONFIG-DELTAS.md"
      to: "Phase 24 task group 23.3 (deploy step + orphan cleanup)"
      via: "Container App env-var update + setting removal"
      pattern: "FOUNDRY_MODEL"
    - from: "SPAN-NAME-MAPPING.md"
      to: "Phase 24 task groups 23.1/23.2/23.3 (KQL updates per agent)"
      via: "queries.py + kql_templates.py edits"
      pattern: "queries.py|kql_templates.py"
    - from: "AUDITOR-VERIFICATION.md"
      to: "~/.claude/agents/gsd-framework-fidelity-auditor.md"
      via: "subagent existence check"
      pattern: "gsd-framework-fidelity-auditor"
---

<objective>
Three documentation deliverables that close out Phase 23 and feed Phase 24 planning:

1. **CONFIG-DELTAS.md** — every `config.py` setting and every Container App env var Phase 24 must add/remove. Phase 24 task group 23.1 adds `foundry_model` to `config.py`. Phase 24 deploy step sets `FOUNDRY_MODEL` env var. Phase 24 task group 23.3 final cleanup commit removes orphaned `azure_ai_classifier_agent_id` / `azure_ai_admin_agent_id` / `azure_ai_investigation_agent_id` settings.

2. **SPAN-NAME-MAPPING.md** — RC span Names (e.g. `azure_ai_agent.run`, `azure_ai_agent.tool_call`) vs GA span Names (e.g. `invoke_agent`, `execute_tool`) side-by-side. Plus a per-KQL-query table listing which queries in `backend/src/second_brain/observability/queries.py` and `backend/src/second_brain/observability/kql_templates.py` need updating in Phase 24, broken down by task group (so 23.1 updates investigation queries, 23.2 updates admin queries, 23.3 updates classifier queries).

3. **AUDITOR-VERIFICATION.md** — confirmation that `~/.claude/agents/gsd-framework-fidelity-auditor.md` exists and a no-op invocation loads it cleanly. Per the planning context: the calibration report (19 ❌, no blind spots) is already at `.planning/phases/24-foundry-ga-migration/FRAMEWORK-FIDELITY-calibration.md` from the 2026-05-08 detour. This task only verifies the agent file exists and the spawn surface works — it does NOT redo calibration.

Depends on PLAN-02 because GA span names emerge empirically from the probe runs — the streaming_shape and tool_call_extraction probes capture the actual GA span Names, which feed the RC → GA mapping table.

Output: 3 markdown documents under `.planning/phases/23-foundry-ga-prep/`. **Boundary unchanged: no edits to backend/src/, backend/pyproject.toml, backend/uv.lock, backend/Dockerfile, or anywhere else outside the phase directory.**
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@docs/superpowers/specs/2026-05-05-foundry-ga-migration-design.md
@.planning/phases/23-foundry-ga-prep/FOUNDRY-PROBE-FINDINGS.md
@.planning/phases/24-foundry-ga-migration/FRAMEWORK-FIDELITY-calibration.md
@backend/src/second_brain/config.py
@backend/src/second_brain/observability/queries.py
@backend/src/second_brain/observability/kql_templates.py

<interfaces>
<!-- Existing config + observability surface — read-only for this plan; consumed for the deltas doc and span mapping. -->

Current config.py settings (per inspection):
- `azure_ai_project_endpoint` — REUSED in Phase 24 (per design Section "Phase 23.0 deliverables" item 6)
- `azure_ai_classifier_agent_id` — ORPHANED post-Phase 24 D-02 (instructions in repo, no portal agent ID needed)
- `azure_ai_admin_agent_id` — ORPHANED
- `azure_ai_investigation_agent_id` — ORPHANED

Current Container App env vars affecting Foundry: `AZURE_AI_PROJECT_ENDPOINT`, `AZURE_AI_CLASSIFIER_AGENT_ID`, `AZURE_AI_ADMIN_AGENT_ID`, `AZURE_AI_INVESTIGATION_AGENT_ID`, `API_KEY`, OTel config.

Phase 24 needed config additions (per design):
- `foundry_model: str = "gpt-4o"` — default model deployment (per "Phase 23.0 deliverables" item 6)

Phase 24 needed env vars (per design Section "Deploy sequence"):
- `FOUNDRY_MODEL=<deployment>` — set BEFORE git push origin main, against existing RC revision (the deploy sequence is "env vars FIRST, then image")
- `ENABLE_INSTRUMENTATION=true` — already in current production env per Phase 17.4? Verify.
- `ENABLE_SENSITIVE_DATA=false` — per design Section "Observability — Configure"

RC span Names (from current observability):
- `azure_ai_agent.run` (or similar — verify by reading kql_templates.py)
- `azure_ai_agent.tool_call`
- Other custom spans the codebase emits

GA span Names (per design Section "Observability — Delete" + "Framework-fidelity auditor checklist"):
- `invoke_agent` (framework-emitted)
- `execute_tool` (framework-emitted)

The PLAN-02 fixtures (`streaming_shape.json` and `tool_call_extraction.json`) include span-related data IF the probe captured spans alongside SDK output — but probes primarily capture SDK API output, not OTel span tree. So the GA span names below are sourced from:
1. Design doc Section "Observability — Delete" (says framework emits `invoke_agent` / `execute_tool`)
2. Microsoft Agent Framework GA semantic conventions (research via Context7 / microsoft_docs_search if needed at execution time)
3. Probe output (if the probe runs naturally produced visible span Names in App Insights — operator can check post-PLAN-02)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write CONFIG-DELTAS.md</name>
  <files>
    .planning/phases/23-foundry-ga-prep/CONFIG-DELTAS.md
  </files>
  <read_first>
    backend/src/second_brain/config.py
    docs/superpowers/specs/2026-05-05-foundry-ga-migration-design.md (Section "Phase 23.0 deliverables" item 6 + "Deploy sequence")
    .planning/phases/23-foundry-ga-prep/FOUNDRY-PROBE-FINDINGS.md (auth_probe section — confirms credential class shape)
  </read_first>
  <action>
    Write `.planning/phases/23-foundry-ga-prep/CONFIG-DELTAS.md` enumerating every config + env-var change Phase 24 must apply, broken down by which task group applies it.

    Required structure (verbatim — do not rename headings):

    ```markdown
    # Config + env-var deltas for Phase 24

    **Source of truth:** [docs/superpowers/specs/2026-05-05-foundry-ga-migration-design.md](../../../docs/superpowers/specs/2026-05-05-foundry-ga-migration-design.md) Section "Phase 23.0 deliverables" item 6 + Section "Phase 24 Deploy sequence".
    **Applied by:** Phase 24 task groups 23.1 (config.py additions) and 23.3 (Container App env vars + orphan cleanup) per design.
    **NOT applied in Phase 23:** every change below is documentation-only at the close of Phase 23.

    ## Phase 24 task group 23.1 — config.py additions

    File: `backend/src/second_brain/config.py`

    | Setting | Default | Purpose | Source |
    |---|---|---|---|
    | `foundry_model: str = "gpt-4o"` | `"gpt-4o"` | Model deployment name passed to `FoundryChatClient(model_deployment_name=...)`. Reads from `FOUNDRY_MODEL` env var via pydantic-settings convention. | Design Section "Phase 23.0 deliverables" item 6 |

    Diff (illustrative — Phase 24 task group 23.1 applies):
    ```python
    class Settings(BaseSettings):
        # ... existing ...
        azure_ai_project_endpoint: str = ""        # KEEP — reused by FoundryChatClient
        azure_ai_classifier_agent_id: str = ""     # KEEP for now (Phase 24 task group 23.3 removes)
        azure_ai_admin_agent_id: str = ""          # KEEP for now (Phase 24 task group 23.3 removes)
        azure_ai_investigation_agent_id: str = ""  # KEEP for now (Phase 24 task group 23.3 removes)
        foundry_model: str = "gpt-4o"              # NEW — Phase 24 task group 23.1
    ```

    ## Phase 24 deploy step — Container App env vars

    Per design Section "Deploy sequence (deterministic, env vars before image)", these are set via `az containerapp update --set-env-vars` against the EXISTING RC revision FIRST, then the image is pushed. The RC code ignores GA-only vars; the GA code finds them already in place when it starts.

    Operator-side `az` command (Phase 24 task group 23.3 final pre-push step):
    ```bash
    az containerapp update \
      --name second-brain-api \
      --resource-group shared-services-rg \
      --set-env-vars \
        FOUNDRY_MODEL=gpt-4o \
        ENABLE_INSTRUMENTATION=true \
        ENABLE_SENSITIVE_DATA=false
    ```

    | Env var | Value | Reason | Already present in RC env? |
    |---|---|---|---|
    | `FOUNDRY_MODEL` | `gpt-4o` (or whichever deployment is current — verify with `az cognitiveservices account deployment list`) | GA `FoundryChatClient` requires explicit deployment name | NO — added by deploy step |
    | `ENABLE_INSTRUMENTATION` | `true` | GA framework emits OTel spans + GenAI usage metrics when set | <fill in: check current RC env via `az containerapp show --name second-brain-api --resource-group shared-services-rg --query "properties.template.containers[0].env"`> |
    | `ENABLE_SENSITIVE_DATA` | `false` | Design Section "Observability — Configure" — keeps prompt content out of spans by default | <fill in> |

    Verify before push:
    ```bash
    az containerapp revision list --name second-brain-api --resource-group shared-services-rg \
      --query "[?properties.active].{name:name, env:properties.template.containers[0].env}" -o json \
      | jq '.[0].env | map(select(.name | test("FOUNDRY|ENABLE_INSTRUMENTATION|ENABLE_SENSITIVE_DATA"))) '
    ```
    Output must show all three vars set with the expected values BEFORE `git push origin main`.

    ## Phase 24 task group 23.3 — config.py orphan cleanup

    Final cleanup commit at end of task group 23.3 removes settings made obsolete by D-02 (instructions in repo, no portal agent IDs needed):

    | Setting | Action | Reason |
    |---|---|---|
    | `azure_ai_classifier_agent_id` | DELETE | Classifier agent constructed via `Agent(client=FoundryChatClient(...), instructions=load_instructions("classifier.md"), tools=[file_capture])` — no portal agent ID consumed |
    | `azure_ai_admin_agent_id` | DELETE | Admin agent constructed similarly |
    | `azure_ai_investigation_agent_id` | DELETE | Investigation agent constructed similarly |
    | `azure_ai_project_endpoint` | KEEP | Reused as endpoint URL passed to `FoundryChatClient(endpoint=...)` |

    ### Safe deploy sequence (NON-NEGOTIABLE)

    > **DO NOT remove the `AZURE_AI_*_AGENT_ID` env vars before the GA image is deployed.** The currently-deployed RC image's `backend/src/second_brain/main.py` reads `settings.azure_ai_classifier_agent_id` at lifespan startup (`main.py:514`), `settings.azure_ai_admin_agent_id` at `main.py:596`, and `settings.azure_ai_investigation_agent_id` at `main.py:687`. If any of those settings is empty when an RC revision boots, the corresponding `ensure_*_agent` helper falls through to creating a brand-new BLANK portal agent (see `backend/src/second_brain/agents/classifier.py:40` and the symmetric `agents/admin.py` / `agents/investigation.py` paths), causing live agent drift in the Foundry portal BEFORE the intended cutover. **Removing those env vars against the RC image is a forbidden destructive action.**

    The deploy must be staged in three steps. Each step is its own `az containerapp update` invocation — orphan cleanup is moved to a SEPARATE post-deploy step.

    **Step A — Pre-deploy env-var add (Phase 24 task group 23.3, BEFORE `git push origin main`):**

    Add the GA-only env vars to the existing RC revision. Do NOT remove anything — the RC code still reads `AZURE_AI_*_AGENT_ID` at startup. The new env vars are simply ignored by RC code (they're not referenced anywhere in the RC `main.py`).

    ```bash
    az containerapp update \
      --name second-brain-api \
      --resource-group shared-services-rg \
      --set-env-vars \
        FOUNDRY_MODEL=gpt-4o \
        ENABLE_INSTRUMENTATION=true \
        ENABLE_SENSITIVE_DATA=false
    ```

    Verify the revision boots cleanly with all old vars still present:
    ```bash
    az containerapp revision list -n second-brain-api -g shared-services-rg \
      --query "[?properties.active].{name:name, healthState:properties.healthState}" -o table
    az containerapp show -n second-brain-api -g shared-services-rg \
      --query "properties.template.containers[0].env[?name=='AZURE_AI_CLASSIFIER_AGENT_ID' || name=='FOUNDRY_MODEL']" -o table
    # MUST show BOTH AZURE_AI_CLASSIFIER_AGENT_ID and FOUNDRY_MODEL present.
    ```

    **Step B — GA image push (Phase 24 task group 23.3, the `git push` itself):**

    `git push origin main` triggers GitHub Actions → ACR build → Container Apps deploy of the GA image. The GA image's `main.py` no longer references `settings.azure_ai_classifier_agent_id` / `settings.azure_ai_admin_agent_id` / `settings.azure_ai_investigation_agent_id` (Phase 24 task group 23.3's final cleanup commit removes those reads — the corresponding `azure_ai_*_agent_id` fields are also removed from `config.py` in that same commit). The GA revision starts up with `FOUNDRY_MODEL` / `ENABLE_INSTRUMENTATION` / `ENABLE_SENSITIVE_DATA` already in place from Step A AND with the orphaned `AZURE_AI_*_AGENT_ID` env vars still present — orphaned env vars are harmless because the GA code never references them.

    Confirm the GA revision is healthy and serving traffic before proceeding to Step C:
    ```bash
    curl -s -H "X-API-Key: $API_KEY" https://brain.willmacdonald.com/api/health | jq '.'
    az containerapp logs show -n second-brain-api -g shared-services-rg --tail 50
    ```

    **Step C — Post-deploy orphan removal (Phase 24, AFTER successful UAT):**

    Once the GA image has been observed healthy through the post-deploy UAT (capture flow, errands processing, investigation chat all passing), remove the orphaned env vars in a separate `az containerapp update` call:

    ```bash
    az containerapp update \
      --name second-brain-api \
      --resource-group shared-services-rg \
      --remove-env-vars \
        AZURE_AI_CLASSIFIER_AGENT_ID \
        AZURE_AI_ADMIN_AGENT_ID \
        AZURE_AI_INVESTIGATION_AGENT_ID
    ```

    This causes a new revision to start with the orphan vars absent. The GA code does not read them, so this is safe. The `azure_ai_*_agent_id` fields are also already removed from `backend/src/second_brain/config.py` (Phase 24 task group 23.3 final cleanup commit, in the same commit as the `main.py` lifespan-read removals). The portal-side agent records (`asst_Fnjkq5RVrvdFIOSqbreAwxuq`, the admin agent, `asst_5feSWWTMA8rBSUyQo6aSCsEJ`) become unreferenced; they can be left in the portal as orphaned-but-harmless or deleted manually. Document the choice in the Phase 24 task group 23.3 SUMMARY.

    ### NEGATIVE assertion (do not violate)

    **NEVER** run any variant of the following before Step B (GA image push) is observed healthy:

    ```bash
    # FORBIDDEN before GA image is deployed:
    az containerapp update --name second-brain-api --resource-group shared-services-rg \
      --remove-env-vars AZURE_AI_CLASSIFIER_AGENT_ID AZURE_AI_ADMIN_AGENT_ID AZURE_AI_INVESTIGATION_AGENT_ID

    # ALSO FORBIDDEN: setting any of those agent ID vars to empty string against the RC image
    az containerapp update --name second-brain-api --resource-group shared-services-rg \
      --set-env-vars AZURE_AI_CLASSIFIER_AGENT_ID=
    ```

    Doing so against the RC image will cause the classifier/admin/investigation agents to either fail startup (unlikely — current code falls through gracefully) or, more dangerously, fall through to creating new BLANK portal agents per `agents/classifier.py:40` / `agents/admin.py` / `agents/investigation.py`. That creates live agent drift in the Foundry portal BEFORE the intended cutover and silently changes which agent ID the production system addresses. Recovery requires re-pinning the original agent IDs and possibly deleting the stray blank portal agents — a 15-minute incident at minimum.

    ## Credential class change

    Per design D-07a + auth_probe finding (PLAN-02): the deployed Container App uses `ManagedIdentityCredential`, not `AzureCliCredential`. Phase 24 task group 23.1 wires `FoundryChatClient(credential=ManagedIdentityCredential())` in `main.py` lifespan. The probe validated the credential class shape (FoundryChatClient accepts an azure-credential object) and the RBAC role assignment shape (the role names that work for `az login` are the role names the Container App managed identity needs).

    Required RBAC roles to assign to Container App managed identity (from PLAN-02 `auth_probe.json`): see `.planning/phases/23-foundry-ga-prep/FOUNDRY-PROBE-FINDINGS.md` Probe 5 section. These role assignments may already be in place from the RC era — verify via:
    ```bash
    az role assignment list \
      --assignee $(az containerapp show -n second-brain-api -g shared-services-rg --query identity.principalId -o tsv) \
      --scope <foundry-resource-id> \
      -o table
    ```

    If a role is missing, assign it as part of Phase 24 task group 23.3 deploy step BEFORE the env-var update.

    ## Phase 23 boundary respected

    None of the changes above are applied in Phase 23. Verify:
    ```bash
    grep -q "foundry_model" backend/src/second_brain/config.py   # MUST return non-zero (foundry_model NOT yet added)
    grep -q "azure_ai_classifier_agent_id" backend/src/second_brain/config.py   # MUST return zero (still present)
    ```
    ```

    Fill EVERY angle-bracketed `<...>` placeholder by reading the actual `config.py`, the `auth_probe.json` fixture from PLAN-02, and the current Container App env vars via `az containerapp show`.

    BOUNDARY: This task only writes `.planning/phases/23-foundry-ga-prep/CONFIG-DELTAS.md`. No `az containerapp update` execution — this is documentation only.
  </action>
  <verify>
    <automated>
      test -f .planning/phases/23-foundry-ga-prep/CONFIG-DELTAS.md \
        && grep -q "## Phase 24 task group 23.1 — config.py additions" .planning/phases/23-foundry-ga-prep/CONFIG-DELTAS.md \
        && grep -q "## Phase 24 deploy step — Container App env vars" .planning/phases/23-foundry-ga-prep/CONFIG-DELTAS.md \
        && grep -q "## Phase 24 task group 23.3 — config.py orphan cleanup" .planning/phases/23-foundry-ga-prep/CONFIG-DELTAS.md \
        && grep -q "## Credential class change" .planning/phases/23-foundry-ga-prep/CONFIG-DELTAS.md \
        && grep -q "## Phase 23 boundary respected" .planning/phases/23-foundry-ga-prep/CONFIG-DELTAS.md \
        && grep -q "FOUNDRY_MODEL" .planning/phases/23-foundry-ga-prep/CONFIG-DELTAS.md \
        && grep -q "foundry_model" .planning/phases/23-foundry-ga-prep/CONFIG-DELTAS.md \
        && grep -q "ENABLE_INSTRUMENTATION" .planning/phases/23-foundry-ga-prep/CONFIG-DELTAS.md \
        && grep -q "ENABLE_SENSITIVE_DATA" .planning/phases/23-foundry-ga-prep/CONFIG-DELTAS.md \
        && grep -q "azure_ai_classifier_agent_id" .planning/phases/23-foundry-ga-prep/CONFIG-DELTAS.md \
        && grep -q "azure_ai_project_endpoint" .planning/phases/23-foundry-ga-prep/CONFIG-DELTAS.md \
        && grep -q "ManagedIdentityCredential" .planning/phases/23-foundry-ga-prep/CONFIG-DELTAS.md \
        && grep -q "Safe deploy sequence (NON-NEGOTIABLE)" .planning/phases/23-foundry-ga-prep/CONFIG-DELTAS.md \
        && grep -q "Step A — Pre-deploy env-var add" .planning/phases/23-foundry-ga-prep/CONFIG-DELTAS.md \
        && grep -q "Step B — GA image push" .planning/phases/23-foundry-ga-prep/CONFIG-DELTAS.md \
        && grep -q "Step C — Post-deploy orphan removal" .planning/phases/23-foundry-ga-prep/CONFIG-DELTAS.md \
        && grep -q "NEGATIVE assertion" .planning/phases/23-foundry-ga-prep/CONFIG-DELTAS.md \
        && grep -q "DO NOT remove the .AZURE_AI_\*_AGENT_ID. env vars before the GA image is deployed" .planning/phases/23-foundry-ga-prep/CONFIG-DELTAS.md \
        && ! grep -q "<fill in:" .planning/phases/23-foundry-ga-prep/CONFIG-DELTAS.md \
        && ! grep -q "foundry_model" backend/src/second_brain/config.py \
        && grep -q "azure_ai_classifier_agent_id" backend/src/second_brain/config.py \
        && ( ! grep -nE '<[^>]+>' .planning/phases/23-foundry-ga-prep/CONFIG-DELTAS.md \
              | grep -vE '<code|</?(code|pre|tt)>|<pre|<https?://|Properties\.\[|<!--|foreign-resource-id>|<foundry-resource-id>' \
              | grep -vE '^\s*$' \
              | grep . )
    </automated>
  </verify>
  <acceptance_criteria>
    - File exists at .planning/phases/23-foundry-ga-prep/CONFIG-DELTAS.md
    - All 5 required sections present
    - References both `foundry_model` (the config.py setting) and `FOUNDRY_MODEL` (the env var)
    - References ENABLE_INSTRUMENTATION + ENABLE_SENSITIVE_DATA env vars
    - References all three orphaned settings: azure_ai_classifier_agent_id, azure_ai_admin_agent_id, azure_ai_investigation_agent_id
    - Documents that azure_ai_project_endpoint stays
    - References ManagedIdentityCredential (deployed environment credential class)
    - Contains the "Safe deploy sequence (NON-NEGOTIABLE)" subheading
    - Contains three distinct deploy step subheadings: "Step A — Pre-deploy env-var add" (additive only, BEFORE git push), "Step B — GA image push" (the git push itself, GA image starts up with FOUNDRY_MODEL already in place AND with orphaned AZURE_AI_*_AGENT_ID still present), "Step C — Post-deploy orphan removal" (AFTER successful UAT)
    - Step A's `az containerapp update` does NOT include `--remove-env-vars AZURE_AI_*_AGENT_ID`
    - Contains a "NEGATIVE assertion" subsection that explicitly forbids removing AZURE_AI_*_AGENT_ID env vars before the GA image is deployed and references the live agent drift risk via `agents/classifier.py:40`
    - Cites the specific `main.py` line numbers (514, 596, 687) where each `settings.azure_ai_*_agent_id` is read
    - No `<fill in:` placeholders remain
    - No unfilled `<...>` placeholders remain anywhere (broad guard rejects `<ANY>` tokens with explicit allow-list for HTML tags, KQL bracket syntax `Properties.[...]`, URLs, and the `<foundry-resource-id>` literal documented as an `az role assignment list --scope` argument placeholder operators substitute at runtime)
    - backend/src/second_brain/config.py STILL does NOT contain `foundry_model` (boundary preserved — Phase 23 does not modify config.py)
    - backend/src/second_brain/config.py STILL contains `azure_ai_classifier_agent_id` (boundary preserved — orphan cleanup is Phase 24 work)
  </acceptance_criteria>
  <done>
    CONFIG-DELTAS.md fully populated with the exact additions, env vars, and orphan-cleanup commands Phase 24 task groups 23.1 and 23.3 execute. Boundary preserved: backend/src/second_brain/config.py is unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write SPAN-NAME-MAPPING.md</name>
  <files>
    .planning/phases/23-foundry-ga-prep/SPAN-NAME-MAPPING.md
  </files>
  <read_first>
    backend/src/second_brain/observability/queries.py
    backend/src/second_brain/observability/kql_templates.py
    docs/superpowers/specs/2026-05-05-foundry-ga-migration-design.md (Sections "Observability — Delete", "Framework-fidelity auditor checklist", "Phase breakdown for GSD execution — Phase 24 task group 23.X")
    .planning/phases/23-foundry-ga-prep/FOUNDRY-PROBE-FINDINGS.md (Probe 1 streaming_shape — sometimes shows span info)
  </read_first>
  <action>
    Write `.planning/phases/23-foundry-ga-prep/SPAN-NAME-MAPPING.md` with the RC → GA span Name mapping table and a per-KQL-query consumer table broken down by Phase 24 task group.

    Required structure (verbatim — do not rename headings):

    ```markdown
    # RC → GA span Name mapping

    **Purpose:** Phase 24 task groups 23.1/23.2/23.3 update KQL queries to consume new span Names. This document is the source of truth for that translation.
    **Sources:**
    - Microsoft Agent Framework GA semantic conventions (per design Section "Observability — Delete": framework emits `invoke_agent` and `execute_tool`)
    - Existing RC span Names found in `backend/src/second_brain/observability/queries.py` and `backend/src/second_brain/observability/kql_templates.py`
    - PLAN-02 probe runs (if visible in App Insights — operator can spot-check)

    ## Span Name table

    | RC span Name | GA span Name | Source for the mapping | Notes |
    |---|---|---|---|
    | `azure_ai_agent.run` | `invoke_agent` | Design Section "Observability — Delete" + agent_framework GA conventions | Top-level span emitted per `agent.run()` / `agent.run_stream()` call |
    | `azure_ai_agent.tool_call` | `execute_tool` | Same | Tool-call inner span (child of `invoke_agent`) |
    | `azure_ai_agent.<tool_name>` (if RC named tools individually) | `execute_tool` with `tool.name` attribute | <verify by grepping current RC span names> | If RC used per-tool names, GA collapses to single span Name with `tool.name` attribute |
    | <other RC spans found in queries.py> | <GA equivalent or "no equivalent — preserve as custom span"> | <source> | <reasoning> |

    Custom spans the codebase emits (NOT framework-emitted, NOT changed by RC→GA migration):
    - <list any custom span names found in queries.py — e.g. spine/processing custom spans, eval-related spans, Cosmos write spans>

    Per design Section "Framework-fidelity auditor checklist", spans the auditor expects to see GA-shaped:
    - Agent invocation: `invoke_agent` (was `azure_ai_agent.run`)
    - Tool execution: `execute_tool` (was `azure_ai_agent.tool_call`)
    - Span attributes: `agent.name`, `tool.name`, `gen_ai.usage.input_tokens`, `gen_ai.usage.output_tokens` (GenAI semantic conventions)
    - Capture-trace propagation: `capture.trace_id` attribute on agent + tool spans (per D-07a, set by `AgentMiddleware` + `FunctionMiddleware`)

    ## Span attribute table (preserved across RC → GA)

    | Attribute name | Set by | RC source | GA source |
    |---|---|---|---|
    | `capture.trace_id` (or `capture_trace_id` — both observed) | <ContextVar in api/capture.py + observability/span_processor.py> | RC: CaptureTraceSpanProcessor on every span | GA: AgentMiddleware/FunctionMiddleware on framework spans + CaptureTraceSpanProcessor RETAINED for non-framework spans (D-07a) |
    | `agent.name` | Framework | RC: <verify> | GA: framework auto-emits |
    | `tool.name` | Framework | RC: <verify> | GA: framework auto-emits |
    | `gen_ai.usage.input_tokens` | Framework instrumentation | RC: <verify enable_instrumentation() emits> | GA: framework's `enable_instrumentation()` emits via GenAI semantic conventions |

    Note D-07a: `CaptureTraceSpanProcessor` is RETAINED, not removed. KQL queries union over `AppRequests`, `AppDependencies`, `AppTraces`, `AppExceptions` filtered on `Properties.capture_trace_id` — that filter continues to match in GA because the attribute name is preserved across both sources (framework middleware + retained span processor).

    ## KQL query consumer table

    Phase 24 task groups update KQL queries to use GA span Names. Update is in the SAME commit cluster as the agent that emits the new span Names, so the deployed system never sees a query/code mismatch.

    ### File: `backend/src/second_brain/observability/queries.py`

    | Query function | Current span filter (RC) | New span filter (GA) | Phase 24 owner | Lines (approx) |
    |---|---|---|---|---|
    | <e.g. `query_recent_failures`> | <e.g. `Name == "azure_ai_agent.run"`> | <e.g. `Name == "invoke_agent"`> | task group 23.1 (investigation) | <line range> |
    | <e.g. `query_capture_trace`> | <unioned filter, no specific Name> | <same — Names not filtered, only attribute filter on capture.trace_id> | <task group> | <line range> |
    | <enumerate every function in queries.py that filters by span Name — read the file and list them> | | | | |

    ### File: `backend/src/second_brain/observability/kql_templates.py`

    | Template | Current span filter (RC) | New span filter (GA) | Phase 24 owner |
    |---|---|---|---|
    | <e.g. `RECENT_FAILURES_KQL`> | <verbatim Name filter from template> | <GA-equivalent filter> | <task group> |
    | <enumerate every template constant in kql_templates.py that filters by span Name> | | | |

    ### Update strategy per task group

    - **Task group 23.1 — Investigation:** updates queries that filter on investigation agent Name + investigation tool span Names. Same commit cluster as `agents/investigation.py` rewrite.
    - **Task group 23.2 — Admin:** updates queries that filter on admin agent Name + admin tool span Names.
    - **Task group 23.3 — Classifier:** updates queries that filter on classifier agent Name + classifier tool span Names. ALSO adds new query for `forced_tool_failure` SSE sub-code metric (per D-07b — Phase 24 task group 23.3 introduces this sub-code).

    ## Azure Monitor alert rules to review

    Per design Section "Risks" row 7 ("Span name change breaks existing KQL / dashboards / alerts"), Azure Monitor alert rules in the `SecondBrainAlerts` action group (per MEMORY.md) reference KQL queries that may filter on span Names. Phase 24 task group 23.3 final pre-push step reviews:

    | Alert rule | KQL query | Span Name filter | Action |
    |---|---|---|---|
    | API-Error-Spike | <verify> | <verify> | <"no change needed" or "update Name filter"> |
    | Capture-Processing-Failures | <verify> | <verify> | <action> |
    | API-Health-Check | <verify> | <verify> | <action> |

    Operator command to inspect:
    ```bash
    az monitor scheduled-query list --resource-group shared-services-rg -o json \
      | jq '.[] | {name, criteria: .criteria.allOf[0].query}'
    ```

    If any alert rule references RC-only span Names, Phase 24 task group 23.3 plan must include an alert-rule update step.

    ## Phase 24 verification gate

    Phase 24 cumulative framework-fidelity audit checklist row "Tracing" passes when:
    - All KQL queries that filter by span Name use GA Names (or are updated to be Name-agnostic via attribute filters)
    - No KQL query references `azure_ai_agent.*` Names
    - The auditor's diff scan finds zero such references

    Verify pre-push:
    ```bash
    ! grep -rE 'azure_ai_agent\\.' backend/src/second_brain/observability/   # MUST return zero matches at end of Phase 24
    ```
    ```

    Fill EVERY angle-bracketed `<...>` placeholder by reading queries.py, kql_templates.py, and the alert rule definitions. Be SPECIFIC — vague entries like "update some queries" are rejected. Each table row needs the actual filter string (verbatim from current code) and the actual GA replacement string.

    BOUNDARY: This task only writes `.planning/phases/23-foundry-ga-prep/SPAN-NAME-MAPPING.md`. No edits to queries.py or kql_templates.py.
  </action>
  <verify>
    <automated>
      test -f .planning/phases/23-foundry-ga-prep/SPAN-NAME-MAPPING.md \
        && grep -q "## Span Name table" .planning/phases/23-foundry-ga-prep/SPAN-NAME-MAPPING.md \
        && grep -q "## Span attribute table" .planning/phases/23-foundry-ga-prep/SPAN-NAME-MAPPING.md \
        && grep -q "## KQL query consumer table" .planning/phases/23-foundry-ga-prep/SPAN-NAME-MAPPING.md \
        && grep -q "### File: \`backend/src/second_brain/observability/queries.py\`" .planning/phases/23-foundry-ga-prep/SPAN-NAME-MAPPING.md \
        && grep -q "### File: \`backend/src/second_brain/observability/kql_templates.py\`" .planning/phases/23-foundry-ga-prep/SPAN-NAME-MAPPING.md \
        && grep -q "### Update strategy per task group" .planning/phases/23-foundry-ga-prep/SPAN-NAME-MAPPING.md \
        && grep -q "## Azure Monitor alert rules to review" .planning/phases/23-foundry-ga-prep/SPAN-NAME-MAPPING.md \
        && grep -q "## Phase 24 verification gate" .planning/phases/23-foundry-ga-prep/SPAN-NAME-MAPPING.md \
        && grep -q "invoke_agent" .planning/phases/23-foundry-ga-prep/SPAN-NAME-MAPPING.md \
        && grep -q "execute_tool" .planning/phases/23-foundry-ga-prep/SPAN-NAME-MAPPING.md \
        && grep -q "azure_ai_agent" .planning/phases/23-foundry-ga-prep/SPAN-NAME-MAPPING.md \
        && grep -q "capture.trace_id\|capture_trace_id" .planning/phases/23-foundry-ga-prep/SPAN-NAME-MAPPING.md \
        && grep -q "forced_tool_failure" .planning/phases/23-foundry-ga-prep/SPAN-NAME-MAPPING.md \
        && grep -q "task group 23.1" .planning/phases/23-foundry-ga-prep/SPAN-NAME-MAPPING.md \
        && grep -q "task group 23.2" .planning/phases/23-foundry-ga-prep/SPAN-NAME-MAPPING.md \
        && grep -q "task group 23.3" .planning/phases/23-foundry-ga-prep/SPAN-NAME-MAPPING.md \
        && ! grep -q "<verify>" .planning/phases/23-foundry-ga-prep/SPAN-NAME-MAPPING.md \
        && ! grep -q "<line range>" .planning/phases/23-foundry-ga-prep/SPAN-NAME-MAPPING.md \
        && ! grep -q "<task group>" .planning/phases/23-foundry-ga-prep/SPAN-NAME-MAPPING.md \
        && ( ! grep -nE '<[^>]*>' .planning/phases/23-foundry-ga-prep/SPAN-NAME-MAPPING.md \
              | grep -vE '<code|<pre|</?(code|pre|tt)>|<https?://|Properties\.\[|backend/src/second_brain/observability/queries\.py|backend/src/second_brain/observability/kql_templates\.py|backend/src/second_brain/api/capture\.py|backend/src/second_brain/main\.py|capture\.trace_id|capture_trace_id|tool\.name|agent\.name|gen_ai\.usage' \
              | grep . )
    </automated>
  </verify>
  <acceptance_criteria>
    - File exists
    - All 7 required headings present (Span Name table, Span attribute table, KQL query consumer table, queries.py subsection, kql_templates.py subsection, Update strategy, Azure Monitor alert rules, Phase 24 verification gate)
    - References both `invoke_agent` (GA) and `azure_ai_agent` (RC)
    - References `capture.trace_id` (or `capture_trace_id`) attribute
    - References `forced_tool_failure` (the new SSE sub-code from D-07b that needs a new KQL metric)
    - All three task group references appear (23.1, 23.2, 23.3)
    - No template placeholders remain (`<verify>`, `<line range>`, `<task group>`)
    - File is non-trivial size (> 3000 bytes — sanity check that it's not just headings)
  </acceptance_criteria>
  <done>
    SPAN-NAME-MAPPING.md complete with the RC→GA name mapping, the per-query consumer breakdown by task group, the alert-rule review list, and the Phase 24 verification gate. Phase 24 task groups 23.1/23.2/23.3 can read the relevant section and know exactly which KQL strings to change.
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify framework-fidelity-auditor subagent + write AUDITOR-VERIFICATION.md</name>
  <files>
    .planning/phases/23-foundry-ga-prep/AUDITOR-VERIFICATION.md
  </files>
  <read_first>
    docs/superpowers/specs/2026-05-05-foundry-ga-migration-design.md (Section "D-07 Framework-first principle" + "Framework-fidelity audit workflow")
    .planning/phases/24-foundry-ga-migration/FRAMEWORK-FIDELITY-calibration.md (calibration report — already exists per planning context, 19 ❌)
  </read_first>
  <action>
    The auditor file already exists at `~/.claude/agents/gsd-framework-fidelity-auditor.md` (per planning context). The calibration report already exists at `.planning/phases/24-foundry-ga-migration/FRAMEWORK-FIDELITY-calibration.md` from the 2026-05-08 detour. This task only:

    1. Verifies both files exist on disk.
    2. Confirms the calibration report contains the expected 19 ❌ count and absence of blind spots.
    3. Writes a brief verification doc that Phase 24 planning can reference.

    **Steps:**

    1. **Confirm auditor file exists:**
       ```bash
       test -f ~/.claude/agents/gsd-framework-fidelity-auditor.md && echo "AUDITOR PRESENT"
       wc -l ~/.claude/agents/gsd-framework-fidelity-auditor.md   # expect ~hundreds of lines per planning context (19360 bytes per ls -la from prior listing)
       ```

    2. **Confirm calibration report exists and read it:**
       ```bash
       test -f .planning/phases/24-foundry-ga-migration/FRAMEWORK-FIDELITY-calibration.md && echo "CALIBRATION PRESENT"
       grep -c "❌" .planning/phases/24-foundry-ga-migration/FRAMEWORK-FIDELITY-calibration.md   # expect ~19
       ```

    3. **Spot-check the calibration report categories:**
       Per planning context "19 ❌, no blind spots" — extract the categories of ❌ findings to confirm they cover the surfaces Phase 24 will touch (RC `agent_framework.azure` imports, custom span processor of any sort that would conflict with framework spans, the deleted Python safety net pattern, etc.).

       ```bash
       grep -E "^###" .planning/phases/24-foundry-ga-migration/FRAMEWORK-FIDELITY-calibration.md | head -30
       grep -E "❌" .planning/phases/24-foundry-ga-migration/FRAMEWORK-FIDELITY-calibration.md | head -25
       ```

       Capture the list of ❌ finding categories.

    4. **Write `.planning/phases/23-foundry-ga-prep/AUDITOR-VERIFICATION.md`:**

       ```markdown
       # framework-fidelity-auditor verification

       **Verified:** <YYYY-MM-DD>
       **Auditor file:** `~/.claude/agents/gsd-framework-fidelity-auditor.md`
       **Calibration report:** `.planning/phases/24-foundry-ga-migration/FRAMEWORK-FIDELITY-calibration.md`

       ## Existence checks

       | Check | Result |
       |---|---|
       | `~/.claude/agents/gsd-framework-fidelity-auditor.md` exists | <yes/no> |
       | Auditor file size | <wc -c output> bytes |
       | Auditor file line count | <wc -l output> lines |
       | `.planning/phases/24-foundry-ga-migration/FRAMEWORK-FIDELITY-calibration.md` exists | <yes/no> |
       | Calibration report size | <wc -c> bytes |
       | Number of ❌ findings in calibration | <grep -c output> |

       ## Calibration ❌ category review

       Per the 2026-05-08 calibration detour: the auditor was run against the current RC backend (known-bad case for GA fidelity). It produced ❌ findings in these categories:

       <enumerate the ❌ category headings extracted from the calibration report. Use the section headers (### lines) under which ❌ findings appear.>

       Expected categories (per design D-07 checklist + the auditor's own checklist):
       - Tracing — RC `agent_framework.azure` imports, custom `tracer.start_as_current_span(...)` wrapping
       - Capture-trace propagation — anything that would break with framework-emitted spans
       - Tool registration — RC `@tool(approval_mode=...)` decorators
       - Required-tool semantics — Python loop re-firing tool (the safety net pattern)
       - Conversation continuity — RC-shape thread handling that doesn't use framework session API
       - Token metering — manual token counters (if any)
       - Eval — custom `eval/runner.py` + `metrics.py` (acceptable as the gate during migration; flagged for the follow-up Foundry-native eval phase)
       - App Insights export — anything outside `azure-monitor-opentelemetry` standard wiring

       Match the calibration's actual categories against the expected list. **No-blind-spots claim** holds if every expected category produced at least one ❌. Document discrepancies.

       ## No-op invocation

       Phase 24 will invoke the auditor at end of each task group with: task group ID + scope from design + git diff + the auditor checklist. Phase 23 does NOT invoke it (nothing to audit — Phase 23 is artifact-only and touches zero `backend/src/` code). The auditor is verified to exist and be syntactically valid as a markdown agent definition.

       Validity check (Claude can parse the file at agent-spawn time): file is well-formed markdown, has at least one heading, and contains the auditor checklist or a reference to design Section "Framework-fidelity auditor checklist".

       ```bash
       grep -q "checklist\|Concern.*Pass.*Fail" ~/.claude/agents/gsd-framework-fidelity-auditor.md && echo "AUDITOR CONTAINS CHECKLIST CONTENT"
       ```

       <paste the result of the above grep here>

       ## Phase 24 invocation contract

       Per design Section "Framework-fidelity audit workflow":
       1. `git diff <task-group-start-sha>..HEAD` captured to `.planning/phases/24-foundry-ga-migration/FIDELITY-23.{1,2,3}.patch`
       2. Spawn `gsd-framework-fidelity-auditor` subagent with: task-group ID, scope from design, the diff, the auditor checklist
       3. Auditor produces `.planning/phases/24-foundry-ga-migration/FRAMEWORK-FIDELITY-23.{1,2,3}.md` with sections: **Pass** / **Warnings (⚠️)** / **Failures (❌)**
       4. For each ❌, fix the code OR rebut with explicit-justification template
       5. Moving to the next task group requires zero ❌ on the current one
       6. `git push origin main` requires the cumulative audit (`FRAMEWORK-FIDELITY-cumulative.md`) at zero ❌

       Phase 24 plans MUST include these auditor invocation tasks at the right boundary points.

       ## Status

       <one of: "READY — auditor exists, calibration report has expected ❌ count, no-blind-spots claim holds. Phase 24 unblocked." / "NEEDS WORK — <reason>">
       ```

    Fill EVERY angle-bracketed placeholder. The file is short — ~150 lines. Its purpose is a discoverable confirmation that Phase 24 has the auditor it needs.

    BOUNDARY: This task only writes `.planning/phases/23-foundry-ga-prep/AUDITOR-VERIFICATION.md`. No edits to the auditor file or the calibration report.
  </action>
  <verify>
    <automated>
      test -f .planning/phases/23-foundry-ga-prep/AUDITOR-VERIFICATION.md \
        && grep -q "## Existence checks" .planning/phases/23-foundry-ga-prep/AUDITOR-VERIFICATION.md \
        && grep -q "## Calibration ❌ category review" .planning/phases/23-foundry-ga-prep/AUDITOR-VERIFICATION.md \
        && grep -q "## No-op invocation" .planning/phases/23-foundry-ga-prep/AUDITOR-VERIFICATION.md \
        && grep -q "## Phase 24 invocation contract" .planning/phases/23-foundry-ga-prep/AUDITOR-VERIFICATION.md \
        && grep -q "## Status" .planning/phases/23-foundry-ga-prep/AUDITOR-VERIFICATION.md \
        && grep -q "gsd-framework-fidelity-auditor" .planning/phases/23-foundry-ga-prep/AUDITOR-VERIFICATION.md \
        && grep -q "FRAMEWORK-FIDELITY-calibration.md" .planning/phases/23-foundry-ga-prep/AUDITOR-VERIFICATION.md \
        && grep -q "READY\|NEEDS WORK" .planning/phases/23-foundry-ga-prep/AUDITOR-VERIFICATION.md \
        && test -f ~/.claude/agents/gsd-framework-fidelity-auditor.md \
        && test -f .planning/phases/24-foundry-ga-migration/FRAMEWORK-FIDELITY-calibration.md \
        && ! grep -q "<wc -c" .planning/phases/23-foundry-ga-prep/AUDITOR-VERIFICATION.md \
        && ! grep -q "<grep -c output>" .planning/phases/23-foundry-ga-prep/AUDITOR-VERIFICATION.md \
        && ( ! grep -nE '<[^>]+>' .planning/phases/23-foundry-ga-prep/AUDITOR-VERIFICATION.md \
              | grep -vE '<code|</?(code|pre|tt)>|<pre|<https?://|Properties\.\[|<!--|FRAMEWORK-FIDELITY-23\.|FIDELITY-23\.|<task-group-start-sha>' \
              | grep -vE '^\s*$' \
              | grep . )
    </automated>
  </verify>
  <acceptance_criteria>
    - File exists
    - All 5 required headings present (Existence checks, Calibration ❌ category review, No-op invocation, Phase 24 invocation contract, Status)
    - References `gsd-framework-fidelity-auditor` (the agent file)
    - References `FRAMEWORK-FIDELITY-calibration.md` (the calibration report)
    - Status section explicitly says "READY" or "NEEDS WORK" with reason
    - No `<wc -c>` or `<grep -c output>` placeholders remain
    - No unfilled `<...>` placeholders remain anywhere (broad guard rejects `<ANY>` tokens with explicit allow-list for HTML tags, KQL bracket syntax `Properties.[...]`, URLs, and the `<task-group-start-sha>` literal documented as a `git diff` argument operators substitute at runtime, plus the `FRAMEWORK-FIDELITY-23.{1,2,3}.md` / `FIDELITY-23.{1,2,3}.patch` pathname patterns which contain `{1,2,3}` brace expansions parsed as angle-bracket lookalikes)
    - The actual auditor file at `~/.claude/agents/gsd-framework-fidelity-auditor.md` exists
    - The calibration report at `.planning/phases/24-foundry-ga-migration/FRAMEWORK-FIDELITY-calibration.md` exists
  </acceptance_criteria>
  <done>
    AUDITOR-VERIFICATION.md committed. Phase 24 has confirmation that the auditor subagent loads cleanly and the calibration covers the expected surfaces.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → CONFIG-DELTAS.md execution | Documentation only in Phase 23. Phase 24 task group 23.3 actually runs `az containerapp update`. |
| operator → ~/.claude/agents/ | Auditor file is read-only consumed; never modified by this plan. |
| Phase 23 docs → Phase 24 plans | Documents under `.planning/phases/23-foundry-ga-prep/` are read by the Phase 24 planner; their accuracy directly affects Phase 24 correctness. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-21 | Tampering | CONFIG-DELTAS.md captures wrong env var values | mitigate | Verify step requires literal env var names + values; values cross-reference design doc. Phase 24 verification gate also rechecks via `az containerapp show`. |
| T-23-22 | Information Disclosure | docs reference live endpoint URLs | accept | The endpoint URL `https://brain.willmacdonald.com` is already public-facing; not a secret. |
| T-23-23 | Spoofing | wrong span Name in mapping table breaks Phase 24 KQL | mitigate | SPAN-NAME-MAPPING.md sources the GA Names from design doc + microsoft_docs_search if uncertain; Phase 24 task group 23.1 audit verifies against actual GA emissions. |
| T-23-24 | Repudiation | "auditor exists" claim without verification | mitigate | AUDITOR-VERIFICATION.md verify step requires `test -f` for both auditor file and calibration report, plus grep for actual ❌ count. |
| T-23-25 | Denial of Service | calibration report missing or empty | mitigate | Existence + size checks in AUDITOR-VERIFICATION.md. If missing, Status becomes "NEEDS WORK" and Phase 24 planning is blocked until calibration reruns. |
</threat_model>

<verification>
After all 3 tasks:

```bash
test -f .planning/phases/23-foundry-ga-prep/CONFIG-DELTAS.md
grep -q "FOUNDRY_MODEL" .planning/phases/23-foundry-ga-prep/CONFIG-DELTAS.md
grep -q "ManagedIdentityCredential" .planning/phases/23-foundry-ga-prep/CONFIG-DELTAS.md

test -f .planning/phases/23-foundry-ga-prep/SPAN-NAME-MAPPING.md
grep -q "invoke_agent" .planning/phases/23-foundry-ga-prep/SPAN-NAME-MAPPING.md
grep -q "execute_tool" .planning/phases/23-foundry-ga-prep/SPAN-NAME-MAPPING.md
grep -q "forced_tool_failure" .planning/phases/23-foundry-ga-prep/SPAN-NAME-MAPPING.md

test -f .planning/phases/23-foundry-ga-prep/AUDITOR-VERIFICATION.md
test -f ~/.claude/agents/gsd-framework-fidelity-auditor.md
test -f .planning/phases/24-foundry-ga-migration/FRAMEWORK-FIDELITY-calibration.md

# Boundary
git status backend/src/ backend/pyproject.toml backend/uv.lock backend/Dockerfile docs/foundry/   # all clean
```
</verification>

<success_criteria>
- CONFIG-DELTAS.md complete with config.py additions, env-var sets, orphan removals, credential class change, all bound to specific Phase 24 task groups (no unfilled `<...>` placeholders — broad guard with allow-list)
- SPAN-NAME-MAPPING.md complete with RC→GA name table, attribute table, per-KQL-query consumer breakdown, alert rule review list (no unfilled `<...>` placeholders — broad guard with allow-list)
- AUDITOR-VERIFICATION.md confirms auditor file exists, calibration report exists with expected ❌ count, no-blind-spots claim holds, Phase 24 invocation contract documented (no unfilled `<...>` placeholders — broad guard with allow-list)
- Boundary preserved: nothing under backend/src/, backend/pyproject.toml, backend/uv.lock, backend/Dockerfile, docs/foundry/, ~/.claude/agents/, or .planning/phases/24-foundry-ga-migration/ touched
</success_criteria>

<output>
After completion, create `.planning/phases/23-foundry-ga-prep/23-05-SUMMARY.md` recording:
- CONFIG-DELTAS.md scope (count of additions, env vars, orphan removals)
- SPAN-NAME-MAPPING.md scope (count of RC→GA mappings, count of KQL functions/templates affected per task group)
- AUDITOR-VERIFICATION.md status (READY or NEEDS WORK)
- Phase 23 closeout note: "All 9 design deliverables present. Phase 24 unblocked. Operator ready to run /gsd-plan-phase 24."
</output>
</content>
</invoke>
