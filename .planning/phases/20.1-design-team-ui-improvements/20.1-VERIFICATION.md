---
phase: 20.1-design-team-ui-improvements
verified: 2026-04-23T06:15:00Z
status: human_needed
score: 8/8 roadmap success criteria verified
overrides_applied: 0
human_verification:
  - test: "Verify all screens visually match the design handoff side-by-side"
    expected: "Capture (3 states), Inbox, Tasks, Status, Settings all match docs/ui-design/Second Brain.html"
    why_human: "Visual fidelity and font rendering quality cannot be verified programmatically"
  - test: "Verify custom fonts (Instrument Serif/Sans, JetBrains Mono) render correctly on device"
    expected: "Serif italic headlines visible, mono timestamps crisp, body text readable"
    why_human: "Font loading and rendering varies by device; requires visual confirmation"
  - test: "Verify all existing functionality works after reskin (voice capture, text capture, inbox detail modal, task expand/collapse, settings API key)"
    expected: "No regressions in any user-facing workflow"
    why_human: "Functional regression testing requires interactive device testing"
  - test: "Verify Capture idle state serif headline presence"
    expected: "Design shows 'What's on your mind?' in serif italic with accent question mark; confirm whether this element exists in the build or was intentionally omitted"
    why_human: "Plan must-have claims serif headline exists but code analysis suggests the text element may not be present in voice idle mode; device visual confirms"
---

# Phase 20.1: Design Team UI Improvements Verification Report

**Phase Goal:** Apply the design team's hi-fi visual overhaul to the existing React Native/Expo mobile app -- new color tokens, typography system, refined screen layouts, and polished interaction details across all screens.
**Verified:** 2026-04-23T06:15:00Z
**Status:** human_needed
**Re-verification:** No -- initial verification

## Goal Achievement

### Observable Truths (ROADMAP Success Criteria)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Theme token system (theme.ts) exports all design colors, font families, and bucket colors from tokens.jsx | VERIFIED | `mobile/constants/theme.ts` (66 lines): exports `theme` with all color tokens (bg, surface, surfaceHi, hairline, divider, text hierarchy, accent, recording, ok/warn/err, bucket fg/bg/dot triples for People/Projects/Ideas/Admin), font families (display, body, bodyMedium, bodySemiBold, bodyBold, mono), plus `Theme` and `BucketName` types |
| 2 | All three custom fonts load before app renders via expo-font | VERIFIED | `mobile/app/_layout.tsx` lines 5-16 import all 7 font variants from @expo-google-fonts; `useFonts` hook on line 31; `SplashScreen.preventAutoHideAsync()` on line 26; `if (!fontsLoaded) return null` on line 53; all 5 packages confirmed in package.json |
| 3 | Tab bar uses Lucide icons with design-matched active/inactive stroke weights | VERIFIED | `mobile/app/(tabs)/_layout.tsx` imports Mic/Inbox/CheckSquare/Sparkles from lucide-react-native (line 3); each tabBarIcon uses `strokeWidth={focused ? 1.8 : 1.4}` ternary; dark translucent bg (rgba(10,10,18,0.85)), hairline border, theme.colors.text/textMuted for active/inactive |
| 4 | All screens use theme tokens exclusively -- zero hardcoded colors from old palette | VERIFIED | grep for "#0f0f23", "#1a1a2e", "#4a90d9", "#4a4a6e", "#16162b" across all 16 modified files returns zero matches. All 14 screen/component files contain theme.colors references (min 5 refs per file). Root _layout.tsx uses correct token VALUES (#0a0a12, #f0f0f5) but as hardcoded strings (see Anti-Patterns) |
| 5 | Screen titles use Instrument Serif italic at 36px | VERIFIED | `theme.fonts.display` + `fontStyle: "italic"` + `fontSize: 36` confirmed in inbox.tsx (L609-612), tasks.tsx (L523-526), status.tsx (L137-140), settings.tsx (L161-164). Capture screen uses serif for transcript (L1429) but voice idle mode has no traditional headline (see note below) |
| 6 | Per-bucket colors visible in inbox rows, HITL picker, and detail modal | VERIFIED | InboxItem.tsx L104: `theme.colors.buckets[key]` via getBucketColors helper; index.tsx L1283: `theme.colors.buckets[bucket]` for HITL; inbox.tsx L530: `theme.colors.buckets[bucket]` for modal bucket buttons |
| 7 | HITL Clarification flow uses design's bubble + 2x2 bucket grid styling | VERIFIED | index.tsx L1555: `theme.colors.accentDim` for agent bubble; L1292-1306: per-bucket dynamic inline styles with `bucketColors.bg`, `bucketColors.dot + "66"` border, `bucketColors.fg` text on primary buttons; secondary buttons use `theme.colors.hairline` border |
| 8 | Settings uses grouped iOS-style cards with CapsLabel section headers | VERIFIED | settings.tsx: imports CapsLabel (L17), uses `<CapsLabel>Account</CapsLabel>` (L76) and `<CapsLabel>About</CapsLabel>` (L122); grouped card with `borderRadius: 14` (L180), `theme.colors.surface` bg, `StyleSheet.hairlineWidth` border; ChevronRight Lucide icons (L84, 90, 128, 136) |

**Score:** 8/8 roadmap success criteria verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `mobile/constants/theme.ts` | Design token system | VERIFIED | 66 lines, exports theme object with colors, fonts, types; imported by 14 files |
| `mobile/hooks/useTheme.ts` | Theme access hook | ORPHANED | 5 lines, exists and exports `useTheme()`, but zero consumers import it. All files import `theme` directly from `constants/theme.ts` |
| `mobile/components/CapsLabel.tsx` | Reusable uppercase mono label | VERIFIED | 37 lines, exports CapsLabel with bodySemiBold font, uppercase, dynamic letterSpacing; imported by inbox.tsx and settings.tsx |
| `mobile/app/_layout.tsx` | Root layout with font loading | VERIFIED | 96 lines, loads 7 font variants via useFonts, SplashScreen gate, existing providers preserved |
| `mobile/app/(tabs)/_layout.tsx` | Tab bar with Lucide icons | VERIFIED | 83 lines, 4 Lucide icons, theme colors, hairline border, stroke weight ternary |
| `mobile/app/(tabs)/index.tsx` | Reskinned capture screen | VERIFIED | 1694 lines, 67 theme refs, 0 old colors, serif transcript, 86px mic, per-bucket HITL |
| `mobile/app/(tabs)/inbox.tsx` | Reskinned inbox screen | VERIFIED | 789 lines, 37 theme refs, serif heading, CapsLabel usage, per-bucket modal, borderRadius 20 |
| `mobile/components/InboxItem.tsx` | Reskinned inbox row | VERIFIED | 257 lines, 18 theme refs, getBucketColors helper, hairline separators, mono metadata |
| `mobile/app/(tabs)/tasks.tsx` | Reskinned tasks screen | VERIFIED | 592 lines, 17 theme refs, serif heading at 36px, surface/accent/bg tokens |
| `mobile/components/ErrandRow.tsx` | Reskinned errand row | VERIFIED | 111 lines, 10 theme refs, surface bg, hairline borders, body/mono fonts |
| `mobile/components/TaskRow.tsx` | Reskinned task row | VERIFIED | 83 lines, 8 theme refs, surface bg, hairline borders, body font |
| `mobile/app/(tabs)/status.tsx` | Reskinned status screen | VERIFIED | 151 lines, 6 theme refs, serif heading, Lucide Settings/Search icons |
| `mobile/app/settings.tsx` | Reskinned settings screen | VERIFIED | 269 lines, 32 theme refs, grouped iOS cards, CapsLabel, ChevronRight, borderRadius 14 |
| `mobile/components/DashboardCards.tsx` | Reskinned dashboard cards | VERIFIED | 129 lines, 12 theme refs, surface bg, hairline borders, mono labels |
| `mobile/components/SpineStatusTile.tsx` | Reskinned spine tiles | VERIFIED | 146 lines, 18 theme refs, COLOR map uses theme.colors.ok/warn/err/textMuted, green glow |
| `mobile/components/StatusSectionRenderer.tsx` | Reskinned section headers | VERIFIED | 105 lines, 13 theme refs, Lucide ChevronDown/ChevronRight, bodySemiBold |
| `mobile/components/RoutingPicker.tsx` | Reskinned destination picker | VERIFIED | 96 lines, 9 theme refs, borderRadius 999 pill, Admin bucket colors as default |
| `mobile/components/ApiKeyGate.tsx` | Reskinned API key modal | VERIFIED | 139 lines, 18 theme refs, serif title, accent button, surfaceHi input |
| `mobile/components/ErrorFallback.tsx` | Reskinned error fallback | VERIFIED | 60 lines, 9 theme refs, serif title, accent button, body text |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `_layout.tsx` | `expo-font` | useFonts hook | WIRED | Line 5: `import { useFonts }`, Line 31: `const [fontsLoaded] = useFonts({...})` |
| `useTheme.ts` | `theme.ts` | import | WIRED | Line 1: `import { theme } from "../constants/theme"` |
| `(tabs)/_layout.tsx` | `lucide-react-native` | Lucide icon imports | WIRED | Line 3: `import { Mic, Inbox, CheckSquare, Sparkles } from "lucide-react-native"` |
| `CapsLabel.tsx` | `theme.ts` | theme token import | WIRED | Line 2: `import { theme } from '../constants/theme'` |
| `(tabs)/index.tsx` | `theme.ts` | theme token import | WIRED | Contains `theme.colors` (46 refs) and `theme.fonts` (20 refs) |
| `(tabs)/inbox.tsx` | `theme.ts` | theme token import | WIRED | 37 theme refs including `theme.colors.buckets` for per-bucket colors |
| `InboxItem.tsx` | `theme.ts` | bucket colors | WIRED | `theme.colors.buckets[key]` via getBucketColors helper (L104) |
| `SpineStatusTile.tsx` | `theme.ts` | status colors | WIRED | COLOR map uses `theme.colors.ok`, `theme.colors.warn`, `theme.colors.err` (L13-15) |
| `status.tsx` | `lucide-react-native` | Settings/Search icons | WIRED | L5: `import { Settings, Search }`, L88-91: rendered with theme colors |
| `settings.tsx` | `CapsLabel.tsx` | section headers | WIRED | L17: `import { CapsLabel }`, L76/L122: `<CapsLabel>` rendered |
| `settings.tsx` | `lucide-react-native` | ChevronRight | WIRED | L12: `import { ChevronRight }`, L84/90/128/136: rendered |
| `StatusSectionRenderer.tsx` | `lucide-react-native` | ChevronDown/Right | WIRED | L2: import, L52/54: rendered |

### Data-Flow Trace (Level 4)

Not applicable -- this phase is a visual reskin only. All data flows (API fetches, state management, event handlers) were explicitly preserved unchanged per plan instructions. No new data rendering paths were introduced.

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| TypeScript compiles | `npx tsc --noEmit` | Clean exit, zero errors | PASS |
| All 5 new packages present | `node -e` package.json check | expo-font, 3 @expo-google-fonts/*, lucide-react-native present | PASS |
| Zero old palette colors | grep for 5 old hex values across 16 files | Zero matches | PASS |
| All 14 screen/component files import theme | grep for "theme" in each file | All files have theme references (min 5) | PASS |
| Serif heading in all main screens | grep for `theme.fonts.display` + `fontSize: 36` | Found in inbox.tsx, tasks.tsx, status.tsx, settings.tsx | PASS |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-----------|-------------|--------|----------|
| D-01 | 01 | Foundation first -- build tokens before screens | SATISFIED | Plan 01 (wave 1) creates foundation; Plans 02-06 (wave 2) depend on it |
| D-02 | 07 | All existing screens in scope | SATISFIED | All 5 screens reskinned: Capture (03), Inbox (04), Tasks (05), Status (06), Settings (06) |
| D-03 | N/A | Daily Digest NOT in scope | SATISFIED | No Daily Digest screen created -- correctly excluded |
| D-04 | 03 | HITL Clarification gets design treatment | SATISFIED | Per-bucket colors on HITL buttons, accentDim agent bubble (index.tsx L1283-1306, L1555) |
| D-05 | 01 | Constants file + useTheme hook, StyleSheet.create stays | SATISFIED | theme.ts created, useTheme.ts created, all styling via StyleSheet.create |
| D-06 | 01 | Three custom fonts via expo-font + @expo-google-fonts | SATISFIED | All 3 families installed and loaded (Instrument Serif, Instrument Sans, JetBrains Mono) |
| D-07 | 01 | Design's bucket colors exactly | SATISFIED | theme.ts L31-51: People=warm sand, Projects=dusty blue, Ideas=muted violet, Admin=sage with fg/bg/dot triples |
| D-08 | 01 | Color tokens from tokens.jsx canonical | SATISFIED | theme.ts colors match tokens.jsx SB object exactly (surfaces, text hierarchy, accent, status) |
| D-09 | 03 | Capture screen: match all 3 design states | SATISFIED | Idle: 86px white mic, surfaceHi toggle. Listening: recording red, mono timer, serif transcript. Filed: bucket colors. (Note: idle serif headline may be missing -- human verify) |
| D-10 | 06 | Status screen: keep real data, apply design card anatomy | SATISFIED | DashboardCards: surface bg, hairline borders, mono labels. SpineStatusTile: theme status colors, 12px radius. Real data preserved. |
| D-11 | 05 | Tasks screen: restyle with design treatment | SATISFIED | Serif heading, surface/hairline card rows, theme tokens throughout, no new features added |
| D-12 | 04 | Inbox screen: full reskin including detail modal | SATISFIED | Serif heading, per-bucket rows, borderRadius 20 modal, per-bucket move-to buttons, surfaceHi feedback buttons |
| D-13 | 06 | Settings screen: grouped iOS-style list for real settings only | SATISFIED | Account + About sections with CapsLabel headers, borderRadius 14 cards, ChevronRight, mono values |
| D-14 | 02 | Lucide icons for tab bar and UI elements | SATISFIED | Tab bar: Mic/Inbox/CheckSquare/Sparkles. Status: Settings/Search. Sections: ChevronDown/ChevronRight |
| D-15 | 02 | Tab bar: 4 tabs, design visual treatment | SATISFIED | rgba(10,10,18,0.85) bg, hairline border, height 70, active/inactive stroke weights |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `app/_layout.tsx` | 67, 68, 76, 77, 85, 86 | Hardcoded hex values `#0a0a12` and `#f0f0f5` instead of `theme.colors.bg` / `theme.colors.text` | INFO | Values are correct design token values but not using the token system. If tokens change, these 3 Stack.Screen options need manual updates. Low risk since dark theme is permanent. |
| `hooks/useTheme.ts` | 1-5 | Orphaned artifact -- exists but zero consumers import it | INFO | All files import `theme` directly from `constants/theme.ts`. The hook works correctly but is unused. Not a blocker since direct import is equally valid. |

### Human Verification Required

### 1. Visual Fidelity Check

**Test:** Open app on device alongside `docs/ui-design/Second Brain.html` and compare each screen
**Expected:** All screens visually match the design handoff -- correct colors, fonts, spacing, card anatomy
**Why human:** Visual fidelity and font rendering quality cannot be verified programmatically

### 2. Custom Font Rendering

**Test:** Check that serif italic headlines, mono timestamps, and body text render with correct fonts
**Expected:** Instrument Serif italic visible on screen titles, JetBrains Mono on timestamps/labels, Instrument Sans on body text
**Why human:** Font loading and rendering varies by device; requires visual confirmation

### 3. Functional Regression Check

**Test:** Exercise all user flows: voice capture, text capture, inbox detail modal, task expand/collapse, settings API key, investigate navigation
**Expected:** No regressions -- all existing functionality works identically after reskin
**Why human:** Interactive testing requires device/simulator

### 4. Capture Idle State Serif Headline

**Test:** Check whether the Capture idle state (voice mode) shows a serif italic headline "What's on your mind?" with accent-colored question mark per CaptureC_Serif design
**Expected:** Either the headline text is visible, or its absence is confirmed as intentional (the existing capture UX centers on mic button + "Tap to record" hint without a headline)
**Why human:** Code analysis shows no headline text element in voice idle mode, but the design spec and Plan 03 must-have reference this element. Visual confirmation needed to determine if this is a gap or a deliberate UX choice.

### Gaps Summary

No blocking gaps identified. All 8 roadmap success criteria are met based on code analysis.

Two INFO-level findings:
1. **Root layout hardcoded values** -- `_layout.tsx` uses correct design token hex values but as hardcoded strings rather than `theme.colors.*` imports. Functionally correct but not using the token system.
2. **Orphaned useTheme hook** -- Created but never imported by any consumer. All files use direct `theme` import, which is equally valid.

One item requiring human confirmation:
- **Capture idle serif headline** -- Plan 03 must-have claims "serif italic headline with accent-colored question mark" but code analysis shows no such text element in voice idle mode. The design reference (CaptureC_Serif) shows this element, but it may be a new UI element beyond the "style-only reskin" scope. Human visual verification will confirm.

---

_Verified: 2026-04-23T06:15:00Z_
_Verifier: Claude (gsd-verifier)_
