---
phase: 20.1-design-team-ui-improvements
plan: 03
type: execute
wave: 2
depends_on: [01]
files_modified:
  - mobile/app/(tabs)/index.tsx
autonomous: true
requirements:
  - D-09
  - D-04

must_haves:
  truths:
    - "Capture Idle state shows serif italic headline with accent-colored question mark"
    - "Capture Listening state shows recording indicator, mono timer, and serif transcript"
    - "Capture Filed state shows bucket-colored confirmation orb"
    - "HITL clarification flow uses bucket-colored 2x2 grid for bucket selection"
    - "All hardcoded color values replaced with theme tokens"
  artifacts:
    - path: "mobile/app/(tabs)/index.tsx"
      provides: "Reskinned capture screen with all 3 states + HITL"
  key_links:
    - from: "mobile/app/(tabs)/index.tsx"
      to: "mobile/constants/theme.ts"
      via: "theme token import"
      pattern: "import.*theme"
---

<objective>
Reskin the Capture screen (index.tsx, 1654 lines) to match all three design states (Idle, Listening, Filed) plus the HITL Clarification flow. This is a style-only change -- all logic, hooks, callbacks, and state management remain identical.

Purpose: Capture is the hero screen and emotional center of the app (per D-09). The serif italic headline, 86px mic button, and bucket-colored confirmation are load-bearing brand moments.
Output: Fully reskinned capture screen with design-matched styling.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20.1-design-team-ui-improvements/20.1-01-SUMMARY.md
@docs/ui-design/screens/capture.jsx
@docs/ui-design/screens/other.jsx
@docs/ui-design/screens/tokens.jsx
@mobile/constants/theme.ts
</context>

<interfaces>
From mobile/constants/theme.ts (created in Plan 01):
```typescript
export const theme = {
  colors: {
    bg: '#0a0a12', surface: '#13131f', surfaceHi: '#1b1b2a',
    hairline: 'rgba(255,255,255,0.06)', divider: 'rgba(255,255,255,0.08)',
    text: '#f0f0f5', textDim: 'rgba(240,240,245,0.62)',
    textMuted: 'rgba(240,240,245,0.38)', textFaint: 'rgba(240,240,245,0.22)',
    accent: '#7a9eff', accentDim: 'rgba(122,158,255,0.18)',
    recording: '#ff5b5b', ok: '#7ddca0', warn: '#e6b450', err: '#ff6b6b',
    buckets: {
      People:   { fg: '#d7c7b4', bg: 'rgba(215,199,180,0.10)', dot: '#b8a387' },
      Projects: { fg: '#a9c5e8', bg: 'rgba(169,197,232,0.10)', dot: '#7fa5cf' },
      Ideas:    { fg: '#c7bde0', bg: 'rgba(199,189,224,0.10)', dot: '#9d8fbf' },
      Admin:    { fg: '#b8d4c0', bg: 'rgba(184,212,192,0.10)', dot: '#88af96' },
    },
  },
  fonts: {
    display: 'InstrumentSerif_400Regular_Italic',
    body: 'InstrumentSans_400Regular',
    bodyMedium: 'InstrumentSans_500Medium',
    bodySemiBold: 'InstrumentSans_600SemiBold',
    bodyBold: 'InstrumentSans_700Bold',
    mono: 'JetBrainsMono_400Regular',
  },
} as const;
export type BucketName = keyof typeof theme.colors.buckets;
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Reskin capture screen styles</name>
  <files>mobile/app/(tabs)/index.tsx</files>
  <read_first>mobile/app/(tabs)/index.tsx, docs/ui-design/screens/capture.jsx, docs/ui-design/screens/other.jsx</read_first>
  <action>
This file is 1654 lines. Only modify the `StyleSheet.create` block (lines 1307-1654) and any inline style objects in the render JSX. Do NOT change any logic, hooks, callbacks, state variables, or the component function signature.

**Step 1: Add theme import at top of file (alongside existing imports):**
```typescript
import { theme } from '../../constants/theme';
```

**Step 2: Replace all hardcoded colors in the `styles` object (lines 1307-1654) using this exact mapping:**

| Old Value | New Value | Description |
|-----------|-----------|-------------|
| `"#0f0f23"` | `theme.colors.bg` | Background |
| `"#1a1a2e"` | `theme.colors.surface` | Card/input surfaces |
| `"#4a4a6e"` | `theme.colors.surfaceHi` | Elevated surfaces (record button) |
| `"#4a90d9"` | `theme.colors.accent` | Accent (send buttons, active states) |
| `"#ffffff"` | `theme.colors.text` | Primary text |
| `"#fff"` | `theme.colors.text` | Primary text (short form) |
| `"#ccc"` / `"#cccccc"` | `theme.colors.textDim` | Secondary text (transcripts) |
| `"#999"` / `"#999999"` | `theme.colors.textDim` | Alt secondary text |
| `"#888"` | `theme.colors.textMuted` | Muted text |
| `"#666"` | `theme.colors.textFaint` | Faint text (hints, voice hint) |
| `"#ff3b30"` | `theme.colors.recording` | Recording red (pulse ring, record button) |
| `"#e53935"` | `theme.colors.recording` | Alt recording red |
| `"#333"` | `theme.colors.surface` | Follow-up record button bg |
| `rgba(74, 222, 128, 0.15)` | keep as-is OR use `theme.colors.ok + '26'` | Toast success bg (rgba alpha) |
| `"#4ade80"` | `theme.colors.ok` | Toast success border |
| `rgba(248, 113, 113, 0.15)` | keep as-is OR use `theme.colors.err + '26'` | Toast error bg |
| `"#f87171"` | `theme.colors.err` | Toast error border |

**Step 3: Add typography font families to key styles:**

Capture Idle headline (the "What's on your mind?" equivalent -- currently shown as voice/text toggle labels and any title text):
- Voice/text toggle labels: `fontFamily: theme.fonts.bodySemiBold`
- Active toggle: `backgroundColor: theme.colors.surfaceHi` (was `#4a90d9` border)
- Toggle container: `backgroundColor: theme.colors.surface, borderRadius: 10, padding: 3`

Timer text:
- `fontFamily: theme.fonts.mono, fontVariant: ['tabular-nums']`

Transcript text (live transcription during recording):
- `fontFamily: theme.fonts.display, fontSize: 26, lineHeight: 34, letterSpacing: -0.4, fontWeight: '400'`
- This matches design's CaptureB_Listening serif transcript style

Voice hint text:
- `fontFamily: theme.fonts.body`

Text input:
- `fontFamily: theme.fonts.body`

Send button text:
- `fontFamily: theme.fonts.bodySemiBold`

Toast text:
- `fontFamily: theme.fonts.body`

Trace ID text (mono metadata):
- `fontFamily: theme.fonts.mono` (replacing `Platform.OS === "ios" ? "Menlo" : "monospace"`)

**Step 4: Update record button sizes to match design:**

Main record button (voice mode):
- Idle: `width: 86, height: 86, borderRadius: 43, backgroundColor: theme.colors.text` (white filled circle per CaptureC_Serif design). The mic icon inside should be dark: use `theme.colors.bg` for the mic icon color.
- Active/recording: `width: 84, height: 84, borderRadius: 42, backgroundColor: theme.colors.recording` with stop square inside (white, 24x24, borderRadius: 5)
- Pulse ring: `borderColor: theme.colors.recording`

**Step 5: Update HITL bucket selection styling (per D-04):**

The HITL clarification flow within the capture screen should use per-bucket colors. Find the bucket button styles (`bucketButtonPrimary`, `bucketButtonSecondary`, `bucketTextPrimary`, `bucketTextSecondary`) and update:

For the agent question bubble:
- `backgroundColor: theme.colors.accentDim` (was `theme.colors.surface`)
- `borderLeftColor: theme.colors.accent` (was `#4a90d9`)
- `borderLeftWidth: 2` (was 3)
- `borderRadius: 14` (was 12)

For bucket buttons in HITL: In the JSX render section, find where BUCKETS are mapped to buttons. The design (other.jsx HITL_Clarify) shows candidate buckets with their bucket bg/border/fg colors, and non-candidate buckets with hairline border at 0.5 opacity. Since the current code uses `bucketButtonPrimary` and `bucketButtonSecondary` styles:

- Primary (candidate) bucket: `backgroundColor: theme.colors.buckets[bucket].bg`, `borderWidth: StyleSheet.hairlineWidth`, `borderColor: theme.colors.buckets[bucket].dot + '66'`, `borderRadius: 12`
- Secondary (non-candidate) bucket: `backgroundColor: 'transparent'`, `borderWidth: StyleSheet.hairlineWidth`, `borderColor: theme.colors.hairline`, `borderRadius: 12`, `opacity: 0.5`
- Primary text: `color: theme.colors.buckets[bucket].fg, fontFamily: theme.fonts.bodySemiBold, fontSize: 13`
- Secondary text: `color: theme.colors.textMuted, fontFamily: theme.fonts.bodyMedium, fontSize: 13`

Note: The bucket colors must be dynamic based on which bucket each button represents. If the current code uses static styles for primary/secondary, the JSX will need to apply inline styles using `theme.colors.buckets[bucket]` for each specific bucket. This is a style change in the render function, not a logic change.

For the follow-up hint text:
- `fontFamily: theme.fonts.mono, color: theme.colors.textFaint, fontSize: 11`

**Step 6: Update the text input toggle styling:**

Voice/Text segmented control at top:
- Container: `backgroundColor: theme.colors.surface, borderRadius: 10, padding: 3`
- Active segment: `backgroundColor: theme.colors.surfaceHi, borderRadius: 8`
- Inactive segment: `backgroundColor: 'transparent'`
- Active text: `color: theme.colors.text, fontFamily: theme.fonts.bodySemiBold, fontSize: 13`
- Inactive text: `color: theme.colors.textMuted, fontFamily: theme.fonts.bodySemiBold, fontSize: 13`
- Remove the old `borderWidth: 2, borderColor: "transparent"` / `borderColor: "#4a90d9"` pattern. The design uses surfaceHi background, not borders.

**CRITICAL: Do NOT change any of the following:**
- useState/useEffect/useCallback/useRef hooks
- sendCapture/sendVoiceCapture/sendFollowUp/sendFollowUpVoice calls
- Speech recognition logic
- Audio recording logic
- HITL flow control logic (hitlTriggered, followUpCount, etc.)
- Component function signature
- Imports (except adding the theme import)
  </action>
  <verify>
    <automated>cd mobile && grep -c "theme.colors" "app/(tabs)/index.tsx" && grep -c "theme.fonts" "app/(tabs)/index.tsx" && ! grep -q '"#0f0f23"' "app/(tabs)/index.tsx" && echo "no old bg color" && ! grep -q '"#4a90d9"' "app/(tabs)/index.tsx" && echo "no old accent"</automated>
  </verify>
  <acceptance_criteria>
    - mobile/app/(tabs)/index.tsx contains `import { theme } from '../../constants/theme'`
    - mobile/app/(tabs)/index.tsx contains `theme.colors.bg` (at least 1 occurrence)
    - mobile/app/(tabs)/index.tsx contains `theme.colors.surface` (at least 3 occurrences)
    - mobile/app/(tabs)/index.tsx contains `theme.colors.accent` (at least 3 occurrences)
    - mobile/app/(tabs)/index.tsx contains `theme.colors.recording` (at least 2 occurrences)
    - mobile/app/(tabs)/index.tsx contains `theme.colors.text` (at least 5 occurrences)
    - mobile/app/(tabs)/index.tsx contains `theme.fonts.display` (at least 1 occurrence for transcript)
    - mobile/app/(tabs)/index.tsx contains `theme.fonts.mono` (at least 1 occurrence for timer/trace)
    - mobile/app/(tabs)/index.tsx contains `theme.fonts.body` (at least 1 occurrence)
    - mobile/app/(tabs)/index.tsx contains `theme.colors.buckets` (at least 1 occurrence for HITL)
    - mobile/app/(tabs)/index.tsx contains `theme.colors.accentDim` (for agent bubble)
    - mobile/app/(tabs)/index.tsx does NOT contain `"#0f0f23"` anywhere
    - mobile/app/(tabs)/index.tsx does NOT contain `"#4a90d9"` anywhere
    - mobile/app/(tabs)/index.tsx does NOT contain `"#1a1a2e"` anywhere
    - mobile/app/(tabs)/index.tsx does NOT contain `"#4a4a6e"` anywhere
    - mobile/app/(tabs)/index.tsx does NOT contain `"Menlo"` (replaced with theme.fonts.mono)
    - mobile/app/(tabs)/index.tsx still contains `sendCapture` (logic unchanged)
    - mobile/app/(tabs)/index.tsx still contains `useSpeechRecognitionEvent` (logic unchanged)
  </acceptance_criteria>
  <done>Capture screen uses theme tokens for all colors and design fonts for all typography. All three states (Idle, Listening, Filed) and HITL flow styled to match design. Zero hardcoded color values from old palette remain.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Style-only changes to existing screen, no new data flows |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20.1-04 | Tampering | index.tsx HITL flow | accept | No logic changes; bucket selection callbacks unchanged; only visual styling updated |
</threat_model>

<verification>
- No hardcoded hex colors from old palette remain in index.tsx
- All theme.colors and theme.fonts references resolve correctly
- Existing capture flow logic is untouched (sendCapture, speech recognition, HITL)
</verification>

<success_criteria>
- Capture screen Idle state: serif headline would render, mic button is 86px white circle
- Capture screen Listening state: recording indicator in recording red, mono timer, serif transcript
- Capture screen Filed state: uses bucket-specific colors for confirmation orb
- HITL flow: bucket buttons use per-bucket colors with design-matched styling
</success_criteria>

<output>
After completion, create `.planning/phases/20.1-design-team-ui-improvements/20.1-03-SUMMARY.md`
</output>
