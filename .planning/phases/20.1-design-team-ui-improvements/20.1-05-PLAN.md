---
phase: 20.1-design-team-ui-improvements
plan: 05
type: execute
wave: 2
depends_on: [01]
files_modified:
  - mobile/app/(tabs)/tasks.tsx
  - mobile/components/ErrandRow.tsx
  - mobile/components/TaskRow.tsx
autonomous: true
requirements:
  - D-11

must_haves:
  truths:
    - "Tasks heading uses serif italic at 36px"
    - "Task rows use theme tokens for all colors"
    - "Errand rows use theme tokens for all colors"
    - "Processing/notification banners use theme tokens"
  artifacts:
    - path: "mobile/app/(tabs)/tasks.tsx"
      provides: "Reskinned tasks screen"
    - path: "mobile/components/ErrandRow.tsx"
      provides: "Reskinned errand row component"
    - path: "mobile/components/TaskRow.tsx"
      provides: "Reskinned task row component"
  key_links:
    - from: "mobile/app/(tabs)/tasks.tsx"
      to: "mobile/constants/theme.ts"
      via: "theme token import"
      pattern: "import.*theme"
---

<objective>
Reskin the Tasks screen and its ErrandRow/TaskRow components to match the design's visual treatment. Replace all hardcoded colors with theme tokens and update typography to use custom fonts.

Purpose: Tasks screen gets the same design treatment as all other screens (per D-11). No new functionality -- just visual consistency.
Output: Reskinned tasks.tsx, ErrandRow.tsx, TaskRow.tsx with theme tokens.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20.1-design-team-ui-improvements/20.1-01-SUMMARY.md
@docs/ui-design/screens/tasks-unified.jsx
@docs/ui-design/screens/tokens.jsx
@mobile/constants/theme.ts
</context>

<interfaces>
From mobile/constants/theme.ts (created in Plan 01):
```typescript
export const theme = {
  colors: {
    bg: '#0a0a12', surface: '#13131f', surfaceHi: '#1b1b2a',
    hairline: 'rgba(255,255,255,0.06)',
    text: '#f0f0f5', textDim: 'rgba(240,240,245,0.62)',
    textMuted: 'rgba(240,240,245,0.38)', textFaint: 'rgba(240,240,245,0.22)',
    accent: '#7a9eff', accentDim: 'rgba(122,158,255,0.18)',
    ok: '#7ddca0', warn: '#e6b450', err: '#ff6b6b',
    buckets: { People: {...}, Projects: {...}, Ideas: {...}, Admin: {...} },
  },
  fonts: {
    display: 'InstrumentSerif_400Regular_Italic',
    body: 'InstrumentSans_400Regular',
    bodyMedium: 'InstrumentSans_500Medium',
    bodySemiBold: 'InstrumentSans_600SemiBold',
    mono: 'JetBrainsMono_400Regular',
  },
} as const;
```

From mobile/components/CapsLabel.tsx (created in Plan 02):
```typescript
export function CapsLabel({ children, color, size, style }: CapsLabelProps): JSX.Element;
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Reskin ErrandRow and TaskRow components</name>
  <files>mobile/components/ErrandRow.tsx, mobile/components/TaskRow.tsx</files>
  <read_first>mobile/components/ErrandRow.tsx, mobile/components/TaskRow.tsx</read_first>
  <action>
**ErrandRow.tsx** (104 lines):

Import theme: `import { theme } from '../constants/theme';`

Replace all hardcoded colors in the styles object (lines 72-104):

| Old Value | New Value |
|-----------|-----------|
| `backgroundColor: "#1a1a2e"` | `backgroundColor: theme.colors.surface` |
| `color: "#ffffff"` | `color: theme.colors.text` |
| `color: "#aaa"` or `"#999"` | `color: theme.colors.textDim` |
| `color: "#888"` or `"#666"` | `color: theme.colors.textMuted` |
| `color: "#4a90d9"` | `color: theme.colors.accent` |
| Any red for delete | `backgroundColor: theme.colors.err` |

Add typography:
- Item name text: `fontFamily: theme.fonts.body, fontSize: 15, lineHeight: 20, color: theme.colors.text, letterSpacing: -0.15`
- Destination/meta text: `fontFamily: theme.fonts.mono, fontSize: 10.5, color: theme.colors.textMuted`
- Row card: `backgroundColor: theme.colors.surface, borderRadius: 12, borderWidth: StyleSheet.hairlineWidth, borderColor: theme.colors.hairline`

Swipeable delete action: `backgroundColor: theme.colors.err, color: theme.colors.text`

Keep all existing logic (onPress, swipeable, data binding).

**TaskRow.tsx** (77 lines):

Import theme: `import { theme } from '../constants/theme';`

Apply identical color replacement pattern as ErrandRow. Same typography updates:
- Task text: `fontFamily: theme.fonts.body, fontSize: 13.5, lineHeight: 19, color: theme.colors.text, letterSpacing: -0.15`
- Meta text: `fontFamily: theme.fonts.mono`
- Card: `backgroundColor: theme.colors.surface, borderWidth: StyleSheet.hairlineWidth, borderColor: theme.colors.hairline`

Keep all existing logic.
  </action>
  <verify>
    <automated>cd mobile && grep -c "theme.colors" components/ErrandRow.tsx && grep -c "theme.colors" components/TaskRow.tsx && ! grep -q '"#1a1a2e"' components/ErrandRow.tsx && echo "ErrandRow clean" && ! grep -q '"#1a1a2e"' components/TaskRow.tsx && echo "TaskRow clean"</automated>
  </verify>
  <acceptance_criteria>
    - mobile/components/ErrandRow.tsx contains `import { theme }` from theme.ts
    - mobile/components/ErrandRow.tsx contains `theme.colors.surface`
    - mobile/components/ErrandRow.tsx contains `theme.fonts.body`
    - mobile/components/ErrandRow.tsx contains `theme.colors.hairline`
    - mobile/components/ErrandRow.tsx does NOT contain `"#1a1a2e"`
    - mobile/components/ErrandRow.tsx does NOT contain `"#ffffff"`
    - mobile/components/TaskRow.tsx contains `import { theme }` from theme.ts
    - mobile/components/TaskRow.tsx contains `theme.colors.surface`
    - mobile/components/TaskRow.tsx contains `theme.fonts.body`
    - mobile/components/TaskRow.tsx does NOT contain `"#1a1a2e"`
  </acceptance_criteria>
  <done>ErrandRow and TaskRow use theme tokens for all colors and design fonts for typography. Zero hardcoded values remain.</done>
</task>

<task type="auto">
  <name>Task 2: Reskin Tasks screen</name>
  <files>mobile/app/(tabs)/tasks.tsx</files>
  <read_first>mobile/app/(tabs)/tasks.tsx</read_first>
  <action>
Import theme: `import { theme } from '../../constants/theme';`
Import CapsLabel: `import { CapsLabel } from '../../components/CapsLabel';`

Replace all hardcoded colors in the styles object (lines ~521-586):

**Screen title** (lines 521-525):
- Replace: `fontSize: 28, fontWeight: "bold", color: "#ffffff"`
- With: `fontFamily: theme.fonts.display, fontSize: 36, fontWeight: '400', fontStyle: 'italic', letterSpacing: -0.8, color: theme.colors.text`

**Container/background:**
- `backgroundColor: theme.colors.bg` (was `"#0f0f23"`)

**Processing banner** (if present):
- Background: `backgroundColor: theme.colors.accentDim`
- Text: `color: theme.colors.accent, fontFamily: theme.fonts.body`
- Activity indicator: `color: theme.colors.accent`

**Notification banner:**
- Background: `backgroundColor: theme.colors.surface, borderWidth: StyleSheet.hairlineWidth, borderColor: theme.colors.hairline`
- Text: `color: theme.colors.text, fontFamily: theme.fonts.body`

**Section headers:**
- Title: `color: theme.colors.text, fontFamily: theme.fonts.bodySemiBold, fontSize: 16`
- Count: `color: theme.colors.textMuted, fontFamily: theme.fonts.mono, fontSize: 10.5`
- Chevron: `color: theme.colors.textMuted` (if using text chevrons, keep as-is for now -- Lucide icons on headers are a nice-to-have handled by Plan 02's StatusSectionRenderer if pattern applies here)

**Empty state text:**
- `color: theme.colors.textMuted, fontFamily: theme.fonts.body`

**All remaining standard replacements:**
- `"#ffffff"` -> `theme.colors.text`
- `"#0f0f23"` -> `theme.colors.bg`
- `"#1a1a2e"` -> `theme.colors.surface`
- `"#4a90d9"` -> `theme.colors.accent`
- `"#888"` / `"#888888"` -> `theme.colors.textMuted`
- `"#666"` -> `theme.colors.textFaint`
- `"#999"` -> `theme.colors.textDim`
- `"#ccc"` -> `theme.colors.textDim`

Keep all existing logic (useFocusEffect triggering Admin Agent, section list, collapsible sections). Only change styles.
  </action>
  <verify>
    <automated>cd mobile && grep -c "theme.colors" "app/(tabs)/tasks.tsx" && grep -c "theme.fonts" "app/(tabs)/tasks.tsx" && ! grep -q '"#0f0f23"' "app/(tabs)/tasks.tsx" && echo "no old bg" && ! grep -q '"#ffffff"' "app/(tabs)/tasks.tsx" && echo "no old white"</automated>
  </verify>
  <acceptance_criteria>
    - mobile/app/(tabs)/tasks.tsx contains `import { theme }` from theme.ts
    - mobile/app/(tabs)/tasks.tsx contains `theme.fonts.display` (for serif heading)
    - mobile/app/(tabs)/tasks.tsx contains `fontStyle: 'italic'`
    - mobile/app/(tabs)/tasks.tsx contains `fontSize: 36`
    - mobile/app/(tabs)/tasks.tsx contains `theme.colors.bg`
    - mobile/app/(tabs)/tasks.tsx contains `theme.colors.surface`
    - mobile/app/(tabs)/tasks.tsx contains `theme.fonts.body`
    - mobile/app/(tabs)/tasks.tsx does NOT contain `"#0f0f23"`
    - mobile/app/(tabs)/tasks.tsx does NOT contain `"#ffffff"`
    - mobile/app/(tabs)/tasks.tsx does NOT contain `"#4a90d9"`
    - mobile/app/(tabs)/tasks.tsx does NOT contain `fontWeight: "bold"` on the screen title
  </acceptance_criteria>
  <done>Tasks screen has serif italic heading and all hardcoded colors replaced with theme tokens.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Style-only changes to existing screen |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20.1-06 | Tampering | tasks.tsx | accept | No logic changes; Admin Agent processing trigger (useFocusEffect) unchanged |
</threat_model>

<verification>
- No hardcoded hex colors from old palette remain in tasks.tsx, ErrandRow.tsx, or TaskRow.tsx
- Screen heading is serif italic at 36px
- Row components use hairline borders and surface backgrounds
</verification>

<success_criteria>
- Tasks heading matches design spec (serif italic, 36px)
- ErrandRow and TaskRow components use theme tokens consistently
- Processing and notification banners use theme colors
</success_criteria>

<output>
After completion, create `.planning/phases/20.1-design-team-ui-improvements/20.1-05-SUMMARY.md`
</output>
