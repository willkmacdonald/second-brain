---
phase: 20.1-design-team-ui-improvements
plan: 02
subsystem: ui
tags: [lucide-react-native, tab-bar, caps-label, theme-tokens, react-native, design-system]

# Dependency graph
requires:
  - phase: 20.1-01
    provides: "Design token system (theme.ts) with color, typography, and bucket tokens"
provides:
  - "Tab bar with Lucide icons and design-matched active/inactive states"
  - "CapsLabel reusable component for uppercase mono labels"
  - "ErrorFallback, ApiKeyGate, RoutingPicker reskinned with theme tokens"
affects: [20.1-03, 20.1-04, 20.1-05, 20.1-06, 20.1-07]

# Tech tracking
tech-stack:
  added: []
  patterns: [lucide-icon-tab-bar, caps-label-primitive, theme-token-reskin]

key-files:
  created:
    - mobile/components/CapsLabel.tsx
  modified:
    - mobile/app/(tabs)/_layout.tsx
    - mobile/components/ErrorFallback.tsx
    - mobile/components/ApiKeyGate.tsx
    - mobile/components/RoutingPicker.tsx

key-decisions:
  - "RoutingPicker uses Admin bucket colors as default since destinations are Admin-domain items"
  - "ApiKeyGate label styled as CapsLabel-like (SemiBold, uppercase, letterSpacing) without importing CapsLabel component to keep minimal dependencies"
  - "ErrorFallback message uses textDim (0.62 opacity) instead of original #999999 for better hierarchy with text token system"

patterns-established:
  - "Tab bar icons: Lucide imports with strokeWidth ternary on focused state (1.8 active, 1.4 inactive)"
  - "CapsLabel: import from '../components/CapsLabel' for any uppercase mono label in the app"
  - "Component reskin pattern: replace hardcoded hex -> theme.colors.*, add fontFamily from theme.fonts.*"

requirements-completed: [D-14, D-15]

# Metrics
duration: 2min
completed: 2026-04-23
---

# Phase 20.1 Plan 02: Tab Bar + Shared Primitives Summary

**Lucide icon tab bar with dark translucent styling, CapsLabel primitive, and ErrorFallback/ApiKeyGate/RoutingPicker reskinned to theme tokens**

## Performance

- **Duration:** 2 min
- **Started:** 2026-04-23T04:46:48Z
- **Completed:** 2026-04-23T04:48:47Z
- **Tasks:** 2
- **Files modified:** 5

## Accomplishments
- Replaced unicode emoji tab icons with Lucide Mic/Inbox/CheckSquare/Sparkles icons with design-matched stroke weights
- Tab bar styled with dark translucent background, hairline border, and proper font sizing
- Created reusable CapsLabel component for uppercase mono labels used across subsequent screen reskins
- Reskinned ErrorFallback with serif display title, accent button, and body font
- Reskinned ApiKeyGate with full theme token coverage (bg, surfaceHi input, accent button, themed typography)
- Reskinned RoutingPicker with per-bucket colors, pill radius (999), and hairline borders

## Task Commits

Each task was committed atomically:

1. **Task 1: Create CapsLabel component and reskin tab bar** - `d80f11f` (feat)
2. **Task 2: Reskin ErrorFallback, ApiKeyGate, and RoutingPicker** - `fdfde21` (feat)

## Files Created/Modified
- `mobile/components/CapsLabel.tsx` - Reusable uppercase mono label with bodySemiBold font, dynamic letterSpacing
- `mobile/app/(tabs)/_layout.tsx` - Tab bar with Lucide icons, translucent bg, hairline border, theme colors
- `mobile/components/ErrorFallback.tsx` - Error screen with serif title, accent button, body font
- `mobile/components/ApiKeyGate.tsx` - API key modal with serif title, surfaceHi input, hairline border, accent button
- `mobile/components/RoutingPicker.tsx` - Destination chips with bucket colors, pill shape, themed fonts

## Decisions Made
- RoutingPicker uses Admin bucket colors as default chip style since destinations are Admin-domain items (errands routing)
- ApiKeyGate label styled inline as CapsLabel-like without component import to avoid unnecessary dependency for a single use
- ErrorFallback message uses textDim (0.62 opacity) for better visual hierarchy compared to original #999999

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- CapsLabel component ready for import by Capture screen (Plan 03), Inbox (Plan 04), Tasks (Plan 05), Status (Plan 06)
- Tab bar design-complete -- all subsequent plans only modify individual screen content
- All three shared components (ErrorFallback, ApiKeyGate, RoutingPicker) fully themed and ready

## Self-Check: PASSED

All 5 files verified present. Both task commits (d80f11f, fdfde21) verified in git log.

---
*Phase: 20.1-design-team-ui-improvements*
*Completed: 2026-04-23*
