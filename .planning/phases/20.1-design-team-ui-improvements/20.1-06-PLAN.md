---
phase: 20.1-design-team-ui-improvements
plan: 06
type: execute
wave: 2
depends_on: [01]
files_modified:
  - mobile/app/(tabs)/status.tsx
  - mobile/app/settings.tsx
  - mobile/components/DashboardCards.tsx
  - mobile/components/SpineStatusTile.tsx
  - mobile/components/StatusSectionRenderer.tsx
autonomous: true
requirements:
  - D-10
  - D-13

must_haves:
  truths:
    - "Status heading uses serif italic at 36px"
    - "Dashboard cards use surface bg with hairline borders and CapsLabel for labels"
    - "Spine tiles use theme status colors (ok/warn/err) instead of hardcoded values"
    - "Settings heading uses serif italic at 36px"
    - "Settings sections use grouped iOS-style card treatment with CapsLabel headers"
  artifacts:
    - path: "mobile/app/(tabs)/status.tsx"
      provides: "Reskinned status screen"
    - path: "mobile/app/settings.tsx"
      provides: "Reskinned settings screen"
    - path: "mobile/components/DashboardCards.tsx"
      provides: "Reskinned dashboard cards"
    - path: "mobile/components/SpineStatusTile.tsx"
      provides: "Reskinned spine tiles with theme status colors"
    - path: "mobile/components/StatusSectionRenderer.tsx"
      provides: "Reskinned section headers"
  key_links:
    - from: "mobile/app/(tabs)/status.tsx"
      to: "mobile/constants/theme.ts"
      via: "theme token import"
      pattern: "import.*theme"
    - from: "mobile/components/SpineStatusTile.tsx"
      to: "mobile/constants/theme.ts"
      via: "theme status colors replace hardcoded COLOR map"
      pattern: "theme.colors.ok"
---

<objective>
Reskin the Status screen (dashboard cards + spine tiles), Settings screen, and their shared components (DashboardCards, SpineStatusTile, StatusSectionRenderer). These are grouped in one plan since they are small files with minimal modification scope.

Purpose: Status shows real health data (per D-10 -- keep real data, apply design card anatomy). Settings gets grouped iOS-style treatment (per D-13 -- only show real settings that exist).
Output: Reskinned status screen, settings screen, and 3 shared components.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20.1-design-team-ui-improvements/20.1-01-SUMMARY.md
@docs/ui-design/screens/other.jsx
@docs/ui-design/screens/tokens.jsx
@mobile/constants/theme.ts
</context>

<interfaces>
From mobile/constants/theme.ts (created in Plan 01):
```typescript
export const theme = {
  colors: {
    bg: '#0a0a12', surface: '#13131f', surfaceHi: '#1b1b2a',
    hairline: 'rgba(255,255,255,0.06)',
    text: '#f0f0f5', textDim: 'rgba(240,240,245,0.62)',
    textMuted: 'rgba(240,240,245,0.38)', textFaint: 'rgba(240,240,245,0.22)',
    accent: '#7a9eff',
    ok: '#7ddca0', warn: '#e6b450', err: '#ff6b6b',
    buckets: { People: {...}, Projects: {...}, Ideas: {...}, Admin: {...} },
  },
  fonts: {
    display: 'InstrumentSerif_400Regular_Italic',
    body: 'InstrumentSans_400Regular',
    bodyMedium: 'InstrumentSans_500Medium',
    bodySemiBold: 'InstrumentSans_600SemiBold',
    mono: 'JetBrainsMono_400Regular',
  },
} as const;
```

From mobile/components/CapsLabel.tsx (created in Plan 02):
```typescript
export function CapsLabel({ children, color, size, style }: CapsLabelProps): JSX.Element;
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Reskin DashboardCards, SpineStatusTile, and StatusSectionRenderer</name>
  <files>mobile/components/DashboardCards.tsx, mobile/components/SpineStatusTile.tsx, mobile/components/StatusSectionRenderer.tsx</files>
  <read_first>mobile/components/DashboardCards.tsx, mobile/components/SpineStatusTile.tsx, mobile/components/StatusSectionRenderer.tsx, docs/ui-design/screens/other.jsx</read_first>
  <action>
**DashboardCards.tsx** (120 lines):

Import theme: `import { theme } from '../constants/theme';`

Replace all hardcoded colors in styles (lines 78-120):

Card anatomy per design StatusA (other.jsx lines 43-65):
- Card: `backgroundColor: theme.colors.surface, borderRadius: 12, padding: 12, borderWidth: StyleSheet.hairlineWidth, borderColor: theme.colors.hairline`
  - For error card specifically: `borderColor: theme.colors.err + '33'` (error tint like design's `SB.err + '44'`)
- Label (the metric name): `color: theme.colors.textMuted, fontSize: 10, fontFamily: theme.fonts.mono, textTransform: 'uppercase', letterSpacing: 1.4, fontWeight: '600'` (CapsLabel style inline, or import and use CapsLabel component)
- Value: `color: theme.colors.text, fontSize: 18, fontWeight: '600', fontFamily: theme.fonts.body`
- Sub-label/detail: `color: theme.colors.textDim, fontSize: 10.5, fontFamily: theme.fonts.mono`

Replace old colors:
- `"#1a1a2e"` -> `theme.colors.surface`
- `"#888888"` -> `theme.colors.textMuted`
- `"#ffffff"` -> `theme.colors.text`
- Any error card border red -> `theme.colors.err + '33'`

**SpineStatusTile.tsx** (96 lines):

Import theme: `import { theme } from '../constants/theme';`

Replace the hardcoded COLOR map (lines 10-15):
```typescript
// OLD:
const COLOR: Record<SegmentStatus, string> = {
  green: "#3a7d3a",
  yellow: "#c89010",
  red: "#b33b3b",
  stale: "#555",
};

// NEW:
const COLOR: Record<SegmentStatus, string> = {
  green: theme.colors.ok,      // '#7ddca0'
  yellow: theme.colors.warn,   // '#e6b450'
  red: theme.colors.err,       // '#ff6b6b'
  stale: theme.colors.textMuted,
};
```

Replace tile styles:
- Tile background: `backgroundColor: theme.colors.surface` (was some dark bg)
- Tile border: `borderWidth: StyleSheet.hairlineWidth, borderColor: theme.colors.hairline`
- Tile border for warning: `borderColor: theme.colors.warn + '33'`
- Tile border for error: `borderColor: theme.colors.err + '44'`
- Title text: `color: theme.colors.text, fontSize: 12.5, fontWeight: '500', fontFamily: theme.fonts.bodyMedium, letterSpacing: -0.15`
- Role/description: `color: theme.colors.textMuted, fontSize: 10.5, fontFamily: theme.fonts.body`
- Status line: `fontFamily: theme.fonts.mono, fontSize: 10.5, color: theme.colors.textDim`
- Status dot: 10x10 circle, color from COLOR map. Green dot gets glow: `shadowColor: theme.colors.ok, shadowRadius: 3, shadowOpacity: 0.22`
- Add `borderRadius: 12` to match design card anatomy

**StatusSectionRenderer.tsx** (99 lines):

Import theme: `import { theme } from '../constants/theme';`
Import Lucide chevrons: `import { ChevronDown, ChevronRight } from 'lucide-react-native';`

Replace all hardcoded colors in styles (lines 53-99):
- Section header background: `backgroundColor: theme.colors.surface` (was `"#16162b"`)
- Header title: `color: theme.colors.text, fontSize: 16, fontWeight: '600', fontFamily: theme.fonts.bodySemiBold`
- Section count: `color: theme.colors.textMuted, fontFamily: theme.fonts.mono, fontSize: 10.5`

Replace unicode chevron characters in the JSX with Lucide icons:
- Expanded: `<ChevronDown size={14} color={theme.colors.textMuted} strokeWidth={1.8} />`
- Collapsed: `<ChevronRight size={14} color={theme.colors.textMuted} strokeWidth={1.8} />`

Find the render section where chevrons are displayed (likely `"▼"` or `"▶"` or `"›"` text) and replace with the Lucide component.

Keep all existing expand/collapse logic.
  </action>
  <verify>
    <automated>cd mobile && grep -c "theme.colors" components/DashboardCards.tsx && grep -c "theme.colors.ok" components/SpineStatusTile.tsx && grep -c "theme.colors" components/StatusSectionRenderer.tsx && ! grep -q '"#3a7d3a"' components/SpineStatusTile.tsx && echo "SpineTile clean"</automated>
  </verify>
  <acceptance_criteria>
    - mobile/components/DashboardCards.tsx contains `theme.colors.surface`
    - mobile/components/DashboardCards.tsx contains `theme.fonts.mono`
    - mobile/components/DashboardCards.tsx contains `theme.colors.hairline`
    - mobile/components/DashboardCards.tsx does NOT contain `"#1a1a2e"`
    - mobile/components/DashboardCards.tsx does NOT contain `"#888888"`
    - mobile/components/SpineStatusTile.tsx contains `theme.colors.ok` in the COLOR map
    - mobile/components/SpineStatusTile.tsx contains `theme.colors.warn` in the COLOR map
    - mobile/components/SpineStatusTile.tsx contains `theme.colors.err` in the COLOR map
    - mobile/components/SpineStatusTile.tsx does NOT contain `"#3a7d3a"`
    - mobile/components/SpineStatusTile.tsx does NOT contain `"#c89010"`
    - mobile/components/SpineStatusTile.tsx does NOT contain `"#b33b3b"`
    - mobile/components/StatusSectionRenderer.tsx contains `theme.colors.surface`
    - mobile/components/StatusSectionRenderer.tsx contains `theme.fonts.bodySemiBold`
    - mobile/components/StatusSectionRenderer.tsx contains `ChevronDown` or `ChevronRight` import
    - mobile/components/StatusSectionRenderer.tsx does NOT contain `"#16162b"`
  </acceptance_criteria>
  <done>DashboardCards, SpineStatusTile, and StatusSectionRenderer use theme tokens. Spine tiles use semantic status colors. Section headers use Lucide chevrons.</done>
</task>

<task type="auto">
  <name>Task 2: Reskin Status screen and Settings screen</name>
  <files>mobile/app/(tabs)/status.tsx, mobile/app/settings.tsx</files>
  <read_first>mobile/app/(tabs)/status.tsx, mobile/app/settings.tsx, docs/ui-design/screens/other.jsx</read_first>
  <action>
**Status screen** (`mobile/app/(tabs)/status.tsx`, 140 lines):

Import theme: `import { theme } from '../../constants/theme';`
Import CapsLabel: `import { CapsLabel } from '../../components/CapsLabel';`
Import Lucide icons: `import { Settings, Search } from 'lucide-react-native';`

Replace all hardcoded colors:
- Container: `backgroundColor: theme.colors.bg` (was `"#0f0f23"`)
- Heading: `fontFamily: theme.fonts.display, fontSize: 36, fontWeight: '400', fontStyle: 'italic', letterSpacing: -0.8, color: theme.colors.text` (was `fontSize: 28, fontWeight: "bold", color: "#ffffff"`)

Replace emoji icons in header (lines 83-90):
- Settings emoji `"⚙️"` -> `<Settings size={22} color={theme.colors.textDim} strokeWidth={1.6} />`
- Search/investigate emoji `"🔍"` -> `<Search size={22} color={theme.colors.textDim} strokeWidth={1.6} />`

Replace `headerIcon` style (was `fontSize: 22`) -> remove it (icons are sized via props now).

Replace any remaining hardcoded colors:
- `"#ffffff"` -> `theme.colors.text`
- `"#888"` -> `theme.colors.textMuted`
- `"#0f0f23"` -> `theme.colors.bg`

Keep all existing logic (dashboard data fetching, spine tiles, navigation to investigate/settings).

**Settings screen** (`mobile/app/settings.tsx`, 239 lines):

Import theme: `import { theme } from '../constants/theme';`
Import CapsLabel: `import { CapsLabel } from '../components/CapsLabel';`
Import Lucide: `import { ChevronRight } from 'lucide-react-native';`

Replace all hardcoded colors in styles (lines 126-239) per D-13:

**Header** (Stack.Screen options, lines 54-60):
- `headerStyle: { backgroundColor: theme.colors.bg }` (was `"#0f0f23"`)
- `headerTintColor: theme.colors.text` (was `"#ffffff"`)

**Title** (if separate from Stack.Screen header):
- `fontFamily: theme.fonts.display, fontSize: 36, fontWeight: '400', fontStyle: 'italic', letterSpacing: -0.8, color: theme.colors.text`

**Section headers** per design SettingsA (other.jsx lines 96-97):
- Use CapsLabel component: `<CapsLabel>{sectionTitle}</CapsLabel>`
- Padding: `paddingHorizontal: 20, paddingBottom: 6`
- Section spacing: `marginTop: 18` between sections (0 for first)

**Section cards** per design SettingsA (other.jsx lines 98-99):
- Card: `marginHorizontal: 16, backgroundColor: theme.colors.surface, borderRadius: 14, overflow: 'hidden', borderWidth: StyleSheet.hairlineWidth, borderColor: theme.colors.hairline`
- Row: `paddingVertical: 13, paddingHorizontal: 14, flexDirection: 'row', alignItems: 'center'`
- Row separator: `borderTopWidth: StyleSheet.hairlineWidth, borderTopColor: theme.colors.hairline` (skip for first row: `borderTop: i === 0 ? 'none'`)
- Row title: `flex: 1, fontSize: 14, color: theme.colors.text, letterSpacing: -0.15, fontFamily: theme.fonts.body`
- Row value: `fontSize: 13, color: theme.colors.textDim, marginRight: 8, fontFamily: theme.fonts.mono`
- Chevron: replace any text chevron with `<ChevronRight size={11} color={theme.colors.textFaint} strokeWidth={1.8} />`

**Other replacements:**
- Container: `backgroundColor: theme.colors.bg`
- Button backgrounds: `backgroundColor: theme.colors.accent` (was `"#4a90d9"`)
- Input backgrounds: `backgroundColor: theme.colors.surfaceHi` (was `"#2a2a4e"`)
- Muted text: `color: theme.colors.textMuted` (was `"#888"`)
- Input border: `borderColor: theme.colors.hairline`

Keep all existing logic (API key display/update, navigation, SecureStore).
  </action>
  <verify>
    <automated>cd mobile && grep -c "theme.colors" "app/(tabs)/status.tsx" && grep -c "theme.colors" app/settings.tsx && ! grep -q '"⚙️"' "app/(tabs)/status.tsx" && echo "no emoji settings" && ! grep -q '"🔍"' "app/(tabs)/status.tsx" && echo "no emoji search"</automated>
  </verify>
  <acceptance_criteria>
    - mobile/app/(tabs)/status.tsx contains `import { theme }` from theme.ts
    - mobile/app/(tabs)/status.tsx contains `theme.fonts.display`
    - mobile/app/(tabs)/status.tsx contains `fontStyle: 'italic'`
    - mobile/app/(tabs)/status.tsx contains `Settings` and `Search` imports from lucide-react-native
    - mobile/app/(tabs)/status.tsx does NOT contain emoji `"⚙️"` or `"🔍"`
    - mobile/app/(tabs)/status.tsx does NOT contain `"#0f0f23"`
    - mobile/app/settings.tsx contains `import { theme }` from theme.ts
    - mobile/app/settings.tsx contains `import { CapsLabel }` from CapsLabel
    - mobile/app/settings.tsx contains `theme.fonts.display`
    - mobile/app/settings.tsx contains `borderRadius: 14` (grouped card style)
    - mobile/app/settings.tsx contains `theme.colors.surface`
    - mobile/app/settings.tsx contains `theme.fonts.mono`
    - mobile/app/settings.tsx contains `ChevronRight` import from lucide-react-native
    - mobile/app/settings.tsx does NOT contain `"#0f0f23"`
    - mobile/app/settings.tsx does NOT contain `"#4a90d9"`
    - mobile/app/settings.tsx does NOT contain `"#2a2a4e"`
  </acceptance_criteria>
  <done>Status screen uses serif heading, Lucide icons for settings/search, and theme tokens. Settings screen has grouped iOS-style cards with CapsLabel headers, mono values, and Lucide chevrons.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Style-only changes to existing screens |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20.1-07 | Spoofing | settings.tsx | accept | No logic changes; API key masking and SecureStore operations unchanged |
</threat_model>

<verification>
- No hardcoded hex colors from old palette remain in any of the 5 files
- Status uses Lucide Settings/Search icons instead of emoji
- Settings has grouped card layout with CapsLabel section headers
- Spine tiles use theme.colors.ok/warn/err for traffic light colors
</verification>

<success_criteria>
- Status heading is serif italic at 36px with Lucide icons
- Dashboard cards use theme tokens with mono labels
- Spine tiles use semantic status colors from theme
- Settings has grouped iOS-style cards with CapsLabel headers and Lucide chevrons
</success_criteria>

<output>
After completion, create `.planning/phases/20.1-design-team-ui-improvements/20.1-06-SUMMARY.md`
</output>
