---
phase: 20.1-design-team-ui-improvements
plan: 04
type: execute
wave: 2
depends_on: [01]
files_modified:
  - mobile/app/(tabs)/inbox.tsx
  - mobile/components/InboxItem.tsx
autonomous: true
requirements:
  - D-12

must_haves:
  truths:
    - "Inbox heading uses serif italic at 36px with item count CapsLabel"
    - "Inbox rows use per-bucket fg color for bucket label and hairline separators"
    - "Detail modal has surface bg, borderRadius 20, hairline border, and per-bucket colored move-to-bucket buttons"
    - "Thumbs up/down buttons use surfaceHi background"
  artifacts:
    - path: "mobile/app/(tabs)/inbox.tsx"
      provides: "Reskinned inbox screen with list and detail modal"
    - path: "mobile/components/InboxItem.tsx"
      provides: "Reskinned inbox list row with per-bucket colors"
  key_links:
    - from: "mobile/app/(tabs)/inbox.tsx"
      to: "mobile/constants/theme.ts"
      via: "theme token import"
      pattern: "import.*theme"
    - from: "mobile/components/InboxItem.tsx"
      to: "mobile/constants/theme.ts"
      via: "theme token import for bucket colors"
      pattern: "theme.colors.buckets"
---

<objective>
Reskin the Inbox screen (list view + detail/recategorize modal) and the InboxItem component to match the design handoff. Replaces all hardcoded colors with theme tokens, adds serif italic heading, per-bucket colored labels, and design-matched detail modal.

Purpose: Inbox is a primary user surface (per D-12). The detail modal with bucket recategorization and thumbs up/down feedback is a key interaction point.
Output: Fully reskinned inbox screen and InboxItem component.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20.1-design-team-ui-improvements/20.1-01-SUMMARY.md
@docs/ui-design/screens/inbox.jsx
@docs/ui-design/screens/tokens.jsx
@mobile/constants/theme.ts
</context>

<interfaces>
From mobile/constants/theme.ts (created in Plan 01):
```typescript
export const theme = {
  colors: {
    bg: '#0a0a12', surface: '#13131f', surfaceHi: '#1b1b2a',
    hairline: 'rgba(255,255,255,0.06)',
    text: '#f0f0f5', textDim: 'rgba(240,240,245,0.62)',
    textMuted: 'rgba(240,240,245,0.38)', textFaint: 'rgba(240,240,245,0.22)',
    accent: '#7a9eff', accentDim: 'rgba(122,158,255,0.18)',
    ok: '#7ddca0', err: '#ff6b6b',
    buckets: {
      People:   { fg: '#d7c7b4', bg: 'rgba(215,199,180,0.10)', dot: '#b8a387' },
      Projects: { fg: '#a9c5e8', bg: 'rgba(169,197,232,0.10)', dot: '#7fa5cf' },
      Ideas:    { fg: '#c7bde0', bg: 'rgba(199,189,224,0.10)', dot: '#9d8fbf' },
      Admin:    { fg: '#b8d4c0', bg: 'rgba(184,212,192,0.10)', dot: '#88af96' },
    },
  },
  fonts: {
    display: 'InstrumentSerif_400Regular_Italic',
    body: 'InstrumentSans_400Regular',
    bodyMedium: 'InstrumentSans_500Medium',
    bodySemiBold: 'InstrumentSans_600SemiBold',
    mono: 'JetBrainsMono_400Regular',
  },
} as const;
export type BucketName = keyof typeof theme.colors.buckets;
```

From mobile/components/CapsLabel.tsx (created in Plan 02):
```typescript
export function CapsLabel({ children, color, size, style }: CapsLabelProps): JSX.Element;
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Reskin InboxItem component</name>
  <files>mobile/components/InboxItem.tsx</files>
  <read_first>mobile/components/InboxItem.tsx, docs/ui-design/screens/inbox.jsx</read_first>
  <action>
Import theme: `import { theme, BucketName } from '../constants/theme';`

Replace all hardcoded colors in the styles object (lines 148-220):

**Row layout** -- switch from card style to list style per design (InboxA_List lines 40-66):
- Remove `backgroundColor: "#1a1a2e"` and `borderRadius: 10` from row style
- Remove `marginHorizontal: 16` and `marginVertical: 4`
- Add `borderBottomWidth: StyleSheet.hairlineWidth, borderBottomColor: theme.colors.hairline`
- Set `padding: '12px 20px'` equivalent: `paddingVertical: 12, paddingHorizontal: 20`

**Text styles:**
- Body text: `fontSize: 13.5, lineHeight: 19, color: theme.colors.text, letterSpacing: -0.15, fontFamily: theme.fonts.body`
- Bucket label: use per-bucket color dynamically. The bucket name prop should map to `theme.colors.buckets[bucket].fg`. Style: `fontSize: 10.5, fontFamily: theme.fonts.mono, letterSpacing: 0.2, color: theme.colors.buckets[bucketName].fg`. Show bucket name in lowercase per design.
- Timestamp: `fontSize: 10.5, fontFamily: theme.fonts.mono, color: theme.colors.textMuted, fontVariantNumeric: 'tabular-nums'` (per InboxA_List line 61)
- Separator dot between bucket and time: `fontSize: 10.5, color: theme.colors.textFaint`

**Status indicator colors:**
- Replace any `"#4ade80"` / green with `theme.colors.ok`
- Replace any `"#f97316"` / orange with `theme.colors.warn`
- Replace any `"#ef4444"` / red with `theme.colors.err`

**Swipeable delete action:**
- Delete background: `backgroundColor: theme.colors.err`
- Delete text: `color: theme.colors.text`

Keep all existing logic (onPress, swipeable gestures, data binding). Only change styles.
  </action>
  <verify>
    <automated>cd mobile && grep -c "theme.colors" components/InboxItem.tsx && ! grep -q '"#1a1a2e"' components/InboxItem.tsx && echo "no old surface" && ! grep -q '"#4a90d9"' components/InboxItem.tsx && echo "no old accent"</automated>
  </verify>
  <acceptance_criteria>
    - mobile/components/InboxItem.tsx contains `import { theme` from theme.ts
    - mobile/components/InboxItem.tsx contains `theme.colors.buckets` (for per-bucket colors)
    - mobile/components/InboxItem.tsx contains `theme.fonts.mono` (for timestamp/bucket label)
    - mobile/components/InboxItem.tsx contains `theme.colors.hairline` (for row separator)
    - mobile/components/InboxItem.tsx contains `StyleSheet.hairlineWidth`
    - mobile/components/InboxItem.tsx does NOT contain `"#1a1a2e"`
    - mobile/components/InboxItem.tsx does NOT contain `"#4a90d9"`
    - mobile/components/InboxItem.tsx does NOT contain `borderRadius: 10` on the row
  </acceptance_criteria>
  <done>InboxItem uses per-bucket colored labels, mono typography for metadata, hairline row separators, and zero hardcoded colors.</done>
</task>

<task type="auto">
  <name>Task 2: Reskin Inbox screen</name>
  <files>mobile/app/(tabs)/inbox.tsx</files>
  <read_first>mobile/app/(tabs)/inbox.tsx, docs/ui-design/screens/inbox.jsx</read_first>
  <action>
Import theme: `import { theme, BucketName } from '../../constants/theme';`
Import CapsLabel: `import { CapsLabel } from '../../components/CapsLabel';`

Replace all hardcoded colors in the styles object (lines ~508-659):

**Screen heading** (lines 508-514, per InboxA_List lines 19-21):
- Replace: `fontSize: 28, fontWeight: "700", color: "#ffffff"`
- With: `fontFamily: theme.fonts.display, fontSize: 36, fontWeight: '400', fontStyle: 'italic', letterSpacing: -0.8, color: theme.colors.text`
- Add a CapsLabel next to the heading showing item count: `<CapsLabel>{items.length} items</CapsLabel>`
  This is a JSX change in the render section -- find the heading text and add the CapsLabel beside it in a row layout.

**Filter pills** (if present -- per InboxA_List lines 25-37):
- Active filter: `backgroundColor: theme.colors.surfaceHi, color: theme.colors.text, fontSize: 12, fontFamily: theme.fonts.bodyMedium, borderRadius: 8, paddingVertical: 5, paddingHorizontal: 10`
- Inactive filter: `backgroundColor: 'transparent', borderWidth: StyleSheet.hairlineWidth, borderColor: theme.colors.hairline, color: theme.colors.textMuted, fontSize: 12, fontFamily: theme.fonts.bodyMedium, borderRadius: 8, paddingVertical: 5, paddingHorizontal: 10`

**Detail modal** (per InboxC_Detail lines 133-188):
- Modal card: `backgroundColor: theme.colors.surface, borderRadius: 20, borderWidth: StyleSheet.hairlineWidth, borderColor: theme.colors.hairline, padding: 18`
- Shadow: add `shadowColor: '#000', shadowOffset: { width: 0, height: 20 }, shadowOpacity: 0.5, shadowRadius: 40, elevation: 20`
- "Captured" label: use `<CapsLabel>Captured</CapsLabel>`
- Captured text: `fontSize: 15, color: theme.colors.text, lineHeight: 22, letterSpacing: -0.2, fontFamily: theme.fonts.body`
- "Bucket" label: use `<CapsLabel>Bucket</CapsLabel>`
- "Confidence" label: use `<CapsLabel>Confidence</CapsLabel>`
- Confidence value: `fontFamily: theme.fonts.mono, fontSize: 14, color: theme.colors.text`

**Move-to-bucket buttons** in the detail modal (per InboxC_Detail lines 156-170):
- Layout: 4 buttons in a row with `gap: 6`
- Current/selected bucket: `backgroundColor: theme.colors.buckets[bucket].bg, borderWidth: StyleSheet.hairlineWidth, borderColor: theme.colors.buckets[bucket].dot + '66', color: theme.colors.buckets[bucket].fg, borderRadius: 9, fontSize: 11.5, fontWeight: '500', fontFamily: theme.fonts.bodyMedium`
- Other buckets: `backgroundColor: 'transparent', borderWidth: StyleSheet.hairlineWidth, borderColor: theme.colors.hairline, color: theme.colors.textDim, borderRadius: 9, fontSize: 11.5, fontWeight: '500', fontFamily: theme.fonts.bodyMedium`
- These bucket buttons need dynamic styling based on which bucket they represent. Apply inline styles in the JSX using `theme.colors.buckets[bucketName]`.

**Feedback buttons** (thumbs up/down, per InboxC_Detail lines 173-184):
- Button: `width: 36, height: 32, borderRadius: 9, backgroundColor: theme.colors.surfaceHi, alignItems: 'center', justifyContent: 'center'`
- Close link: `fontSize: 12, color: theme.colors.accent, fontWeight: '600', fontFamily: theme.fonts.bodySemiBold`

**Container/background:**
- `backgroundColor: theme.colors.bg` (was `"#0f0f23"`)

**All remaining colors:** Apply the standard replacement map:
- `"#ffffff"` -> `theme.colors.text`
- `"#888"` / `"#888888"` -> `theme.colors.textMuted`
- `"#666"` -> `theme.colors.textFaint`
- `"#4a90d9"` -> `theme.colors.accent`

Keep all existing logic (data fetching, modal open/close, recategorize, feedback submission). Only change styles and add CapsLabel usage.
  </action>
  <verify>
    <automated>cd mobile && grep -c "theme.colors" "app/(tabs)/inbox.tsx" && grep -c "theme.fonts" "app/(tabs)/inbox.tsx" && ! grep -q '"#0f0f23"' "app/(tabs)/inbox.tsx" && echo "no old bg" && ! grep -q '"#4a90d9"' "app/(tabs)/inbox.tsx" && echo "no old accent"</automated>
  </verify>
  <acceptance_criteria>
    - mobile/app/(tabs)/inbox.tsx contains `import { theme` from theme.ts
    - mobile/app/(tabs)/inbox.tsx contains `import { CapsLabel }` from CapsLabel component
    - mobile/app/(tabs)/inbox.tsx contains `theme.fonts.display` (for serif heading)
    - mobile/app/(tabs)/inbox.tsx contains `fontStyle: 'italic'` (for serif heading)
    - mobile/app/(tabs)/inbox.tsx contains `fontSize: 36` (heading size)
    - mobile/app/(tabs)/inbox.tsx contains `theme.colors.buckets` (for per-bucket modal buttons)
    - mobile/app/(tabs)/inbox.tsx contains `borderRadius: 20` (modal card)
    - mobile/app/(tabs)/inbox.tsx contains `theme.colors.surfaceHi` (for feedback buttons)
    - mobile/app/(tabs)/inbox.tsx contains `theme.fonts.mono` (for confidence display)
    - mobile/app/(tabs)/inbox.tsx does NOT contain `"#0f0f23"`
    - mobile/app/(tabs)/inbox.tsx does NOT contain `"#4a90d9"`
    - mobile/app/(tabs)/inbox.tsx does NOT contain `"#1a1a2e"`
    - mobile/app/(tabs)/inbox.tsx does NOT contain `fontWeight: "700"` on the heading
  </acceptance_criteria>
  <done>Inbox screen has serif italic heading with item count, design-matched detail modal with per-bucket move-to-bucket buttons, and all hardcoded colors replaced with theme tokens.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Style-only changes; no new data flows or user input handling |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20.1-05 | Information Disclosure | InboxItem bucket color | accept | Bucket names are already displayed to user; adding color is not new information disclosure |
</threat_model>

<verification>
- No hardcoded hex colors from old palette remain in inbox.tsx or InboxItem.tsx
- Detail modal renders with borderRadius 20 and hairline border
- Bucket buttons in detail modal show per-bucket colors
</verification>

<success_criteria>
- Inbox heading is serif italic at 36px with item count label
- Inbox rows have per-bucket colored labels and hairline separators
- Detail modal matches design spec with per-bucket recategorize buttons
- Feedback buttons use surfaceHi background
</success_criteria>

<output>
After completion, create `.planning/phases/20.1-design-team-ui-improvements/20.1-04-SUMMARY.md`
</output>
