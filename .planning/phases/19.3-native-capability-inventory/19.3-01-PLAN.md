---
phase: 19.3-native-capability-inventory
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/superpowers/specs/native-observability-inventory.md
autonomous: false
requirements: []
must_haves:
  truths:
    - "Inventory cites actual behavior of the deployed system, not docs-only speculation"
    - "Three live-evidence checkpoints (Foundry trace depth, Foundry evals, Sentry RN) have documented findings with links"
    - "Every inventory row names at least one downstream phase it affects, or it is deleted"
    - "Phase impact addendum contains concrete scope deltas for 19.4 / 19.5 / 20 / 21 / 22"
    - "Two housekeeping todos (mobile EAS rebuild; Key Vault dup cleanup) are captured via /gsd:add-todo"
  artifacts:
    - path: "docs/superpowers/specs/native-observability-inventory.md"
      provides: "The native capability inventory table + phase impact addendum + checkpoint findings"
      contains: "≥15 rows covering all 9 surfaces; addendum for 5 downstream phases"
  key_links:
    - from: "native-observability-inventory.md"
      to: "Phase 19.4 / 19.5 / 20 / 21 / 22 plans"
      via: "inventory rows cited in each downstream PLAN.md Native Capability Check header"
      pattern: "cite rows by surface + capability, not by row number"
---

<objective>
Produce a single deliverable — `docs/superpowers/specs/native-observability-inventory.md` — that audits the nine native surfaces the Second Brain depends on and states, for each relevant capability, (a) what the platform gives us natively, (b) what we still have to build, and (c) which downstream phase is affected. Run three live-evidence checkpoints against the deployed system to ground the most load-bearing rows in observed behavior. Finish with a phase-impact addendum that declares concrete scope deltas for Phases 19.4 / 19.5 / 20 / 21 / 22, and capture two housekeeping todos.

Zero production code changes. This phase is investigation + documentation only. The inventory is the feedstock for every plan from 19.4 onward, so its quality directly determines how much over/underbuilding we do in the rest of v3.1.
</objective>

<execution_context>
@/Users/willmacdonald/.claude/get-shit-done/workflows/execute-plan.md
@/Users/willmacdonald/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19.3-native-capability-inventory/19.3-CONTEXT.md
@docs/superpowers/specs/2026-04-19-observability-evolution-design.md

<!-- Tools to use (don't hand-roll these): -->
<!-- microsoft-learn MCP: official Microsoft/Foundry/Azure docs -->
<!-- ref MCP: third-party library docs (opentelemetry, sentry-sdk) -->
<!-- /investigate slash command: deployed App Insights lookups -->
<!-- audit_correlation / trace_lifecycle / recent_errors MCP tools: deployed telemetry -->
<!-- az CLI: Foundry, Monitor, Cosmos, Container Apps, Key Vault -->

<!-- Deploy-only discipline (from CLAUDE.md): -->
<!-- Do NOT run the backend locally. All live-evidence checkpoints run against https://brain.willmacdonald.com -->
<!-- Mobile checkpoints run against the installed EAS build on Will's phone -->
</context>

<tasks>

<task type="auto">
  <name>Task 1: Seed the inventory document skeleton and scratch pad</name>
  <files>
    docs/superpowers/specs/native-observability-inventory.md
    .planning/phases/19.3-native-capability-inventory/INVENTORY-SCRATCH.md
  </files>
  <action>
Create two files in one commit:

1. `docs/superpowers/specs/native-observability-inventory.md` — the final deliverable, skeleton only at this step. Use this exact structure:

```markdown
# Native Observability Capability Inventory

**Date:** 2026-04-19
**Status:** Draft (in progress)
**Parent spec:** docs/superpowers/specs/2026-04-19-observability-evolution-design.md

## Scope

Audits 9 native surfaces the Second Brain depends on, grouped by surface. Each row
captures what the platform gives us natively and what we would still need to build.
Every row names at least one downstream phase it affects.

## Surfaces audited

1. Azure AI Foundry
2. OpenTelemetry
3. Application Insights / Log Analytics
4. Azure Monitor
5. Sentry.IO
6. Azure Cosmos DB
7. Azure Container Apps
8. GitHub Actions
9. Expo / EAS

## Inventory

### 1. Azure AI Foundry

_Rows TBD (Task 2)._

### 2. OpenTelemetry

_Rows TBD (Task 3)._

### 3. Application Insights / Log Analytics

_Rows TBD (Task 4)._

### 4. Azure Monitor

_Rows TBD (Task 5)._

### 5. Sentry.IO

_Rows TBD (Task 6)._

### 6. Azure Cosmos DB

_Rows TBD (Task 7)._

### 7. Azure Container Apps

_Rows TBD (Task 8)._

### 8. GitHub Actions

_Rows TBD (Task 9)._

### 9. Expo / EAS

_Rows TBD (Task 10)._

## Live-Evidence Checkpoints

### Checkpoint A — Foundry trace depth
_TBD (Task 11)._

### Checkpoint B — Foundry evals applicability
_TBD (Task 12)._

### Checkpoint C — Sentry RN status on deployed phone
_TBD (Task 13)._

## Phase Impact Addendum

### Phase 19.4 — Native Span Correlation Tagging
_TBD (Task 14)._

### Phase 19.5 — Agent Decision Record & RCA View
_TBD (Task 14)._

### Phase 20 — Labels: Feedback Signals
_TBD (Task 14)._

### Phase 21 — Eval: Automated Scoring
_TBD (Task 14)._

### Phase 22 — Self-Monitoring
_TBD (Task 14)._

## Housekeeping Captured
_TBD (Task 15)._
```

2. `.planning/phases/19.3-native-capability-inventory/INVENTORY-SCRATCH.md` — scratch pad for raw findings (query outputs, docs excerpts, screenshots links). Header only:

```markdown
# Inventory Investigation Scratch

Raw findings per surface. Gets pruned into `native-observability-inventory.md` as tasks progress. Not a final artifact — committed only so the evidence trail survives.
```

Commit both files:

```
git add docs/superpowers/specs/native-observability-inventory.md .planning/phases/19.3-native-capability-inventory/INVENTORY-SCRATCH.md
git commit -m "docs(19.3): seed inventory skeleton and investigation scratch"
```
  </action>
  <verify>
    <automated>test -f docs/superpowers/specs/native-observability-inventory.md &amp;&amp; test -f .planning/phases/19.3-native-capability-inventory/INVENTORY-SCRATCH.md &amp;&amp; grep -q "Surfaces audited" docs/superpowers/specs/native-observability-inventory.md</automated>
  </verify>
  <done>Skeleton inventory exists with placeholder sections for all 9 surfaces + 3 checkpoints + 5 phase impacts. Scratch file exists.</done>
</task>

<task type="auto">
  <name>Task 2: Audit Surface 1 — Azure AI Foundry</name>
  <files>
    docs/superpowers/specs/native-observability-inventory.md (Section "1. Azure AI Foundry")
    .planning/phases/19.3-native-capability-inventory/INVENTORY-SCRATCH.md (Foundry findings section)
  </files>
  <action>
Investigate four capability areas for Azure AI Foundry, backed by docs + deployed evidence:

**Capabilities to audit:**

1. **Tracing depth and Application Insights connection**
   - Question: What does Foundry tracing capture when connected to App Insights? What fields land in which tables?
   - Evidence sources:
     - `microsoft_docs_search` for "Foundry tracing Application Insights spans" and `microsoft_docs_fetch` https://learn.microsoft.com/en-us/azure/foundry/observability/how-to/trace-agent-setup
     - `microsoft_docs_fetch` https://learn.microsoft.com/en-us/azure/foundry/observability/how-to/trace-agent-framework
     - Direct check: `az rest --method get --uri "https://management.azure.com/subscriptions/<sub>/resourceGroups/shared-services-rg/providers/Microsoft.CognitiveServices/accounts/second-brain-foundry-resource?api-version=2024-10-01"` OR Foundry portal URL to confirm Application Insights is linked.
     - /investigate or direct KQL: `AppDependencies | where Type contains "Foundry" or Name contains "agent" | take 5` — look at the actual span shape for one recent agent run.
   - Record in scratch: the 1-2 most recent dependency rows' full schema (field names + representative values).

2. **Conversation UI and Agent Service thread/run/message persistence**
   - Question: Can an operator open the Foundry portal, find a specific `thread_id` or `run_id`, and inspect the prompt + output + tool calls? What is the retention window?
   - Evidence sources:
     - `microsoft_docs_fetch` https://learn.microsoft.com/en-us/azure/foundry-classic/agents/concepts/threads-runs-messages
     - `microsoft_docs_search` for "Foundry conversation results retention"
     - Live check: take a `thread_id` from a recent investigation run (grep App Insights for `thread_<...>`) and attempt to open it in the portal. Screenshot the view. If the portal shows prompt/output, the 19.5 plan can lean on "click through to Foundry" as primary UX.
   - Record in scratch: portal URL template, retention period if documented, screenshot path/description.

3. **Evaluations SDK — what evaluators ship natively**
   - Question: Which evaluators does `azure-ai-evaluation` / the Foundry Evals SDK provide built-in (groundedness, relevance, fluency, etc.)? Can custom scorers be added? Is there a way to score classification accuracy (label match) natively?
   - Evidence sources:
     - `microsoft_docs_search` for "Foundry evaluation SDK evaluators" and "azure-ai-evaluation built-in evaluators"
     - `ref_search_documentation` for "azure-ai-evaluation" package
     - Do NOT live-test here — Checkpoint B covers that. This task documents what the SDK claims to support.
   - Record in scratch: list of built-in evaluators with 1-line descriptions; note any label-match or classification-accuracy evaluator.

4. **Prompt-agent instruction versioning**
   - Question: Does Foundry retain a version history of prompt-agent instructions when they change in the portal? Is that history accessible via SDK (i.e., "which version was active at time T")?
   - Evidence sources:
     - `microsoft_docs_search` for "Foundry prompt agent versioning history"
     - Portal: open Classifier agent; look for a "history" / "versions" UI element. Screenshot.
     - If no native versioning, the 19.5 plan needs the `AgentReleaseManifest` (repo-versioned JSON snapshot) component.
   - Record in scratch: yes/no on native versioning, with evidence.

**After gathering evidence, write rows into `native-observability-inventory.md` Section "1. Azure AI Foundry"** using this table format:

```markdown
| Capability | Native today | What's missing | Downstream phase(s) |
|---|---|---|---|
| Tracing → App Insights | <what lands, which table> | <gaps> | 19.4, 19.5 |
| Conversation / thread persistence | <what's visible in portal> | <gaps> | 19.5 |
| Evaluations SDK — built-ins | <evaluators that match our needs> | <gaps> | 21 |
| Prompt-agent versioning | <yes/no + mechanism> | <gaps> | 19.5 |
```

Add 1-2 sentences of prose below the table if any row needs nuance.

Commit both files:

```
git add docs/superpowers/specs/native-observability-inventory.md .planning/phases/19.3-native-capability-inventory/INVENTORY-SCRATCH.md
git commit -m "docs(19.3): audit Azure AI Foundry surface"
```
  </action>
  <verify>
    <automated>grep -A 30 "^### 1. Azure AI Foundry" docs/superpowers/specs/native-observability-inventory.md | grep -q "| Tracing" &amp;&amp; grep -A 30 "^### 1. Azure AI Foundry" docs/superpowers/specs/native-observability-inventory.md | grep -q "| Evaluations" &amp;&amp; grep -A 30 "^### 1. Azure AI Foundry" docs/superpowers/specs/native-observability-inventory.md | grep -q "| Prompt-agent versioning"</automated>
  </verify>
  <done>Foundry section has ≥4 rows covering tracing, conversation, evals, versioning. Each row cites downstream phase. Scratch file has supporting evidence (doc excerpts + screenshot paths or KQL results).</done>
</task>

<task type="auto">
  <name>Task 3: Audit Surface 2 — OpenTelemetry</name>
  <files>
    docs/superpowers/specs/native-observability-inventory.md (Section "2. OpenTelemetry")
    .planning/phases/19.3-native-capability-inventory/INVENTORY-SCRATCH.md
  </files>
  <action>
Investigate three capability areas for OpenTelemetry as we configure it today:

1. **Span attributes vs. logs — how `capture_trace_id` could flow through native spans**
   - Question: Can we attach `capture_trace_id` as a span attribute via `configure_azure_monitor` + explicit `span.set_attribute(...)`? Does baggage propagate through the Foundry SDK's internal HTTP client without additional code?
   - Evidence sources:
     - `ref_search_documentation` for "opentelemetry-python baggage propagation"
     - `ref_search_documentation` for "azure-monitor-opentelemetry configure_azure_monitor"
     - Existing code: grep `backend/src/second_brain` for `configure_azure_monitor`, `set_attribute`, `baggage` — establish what's already wired.
   - Record in scratch: answer to "can baggage carry capture_trace_id through Foundry SDK calls without instrumentation at every site?" with evidence — yes / no / unknown.

2. **Log correlation (linking log records to spans)**
   - Question: When we call `logger.info(...)` inside a span, do App Insights' `AppTraces` rows automatically carry `operation_Id` matching the span? Does `capture_trace_id` as span attribute propagate to the log record as a custom dimension?
   - Evidence source: `microsoft_docs_search` for "opentelemetry python log correlation operation_id"; existing KQL-confirmed behavior from Phase 17 memory notes.
   - Record: yes/no + how. If no, that becomes "missing" in this row.

3. **Context propagation across process boundaries**
   - Question: Does the Foundry SDK extract/inject W3C trace-context headers on outbound agent calls? What about our Cosmos SDK calls? Our httpx calls in `tools/recipe.py`?
   - Evidence: `ref_search_documentation` for each SDK's OTel support.
   - Record: per-SDK support matrix (Foundry, Cosmos, httpx).

Write Section "2. OpenTelemetry" with a table:

```markdown
| Capability | Native today | What's missing | Downstream phase(s) |
|---|---|---|---|
| Span attributes (explicit) | <what works> | <gaps> | 19.4 |
| Baggage propagation | <what works through which SDKs> | <gaps — e.g., "Foundry SDK baggage propagation unverified"> | 19.4 |
| Log ↔ span correlation | <auto-correlation via App Insights> | <gaps> | 19.4, 19.5 |
| Cross-process context propagation | <per-SDK support> | <gaps> | 19.4 |
```

Commit:

```
git add docs/superpowers/specs/native-observability-inventory.md .planning/phases/19.3-native-capability-inventory/INVENTORY-SCRATCH.md
git commit -m "docs(19.3): audit OpenTelemetry surface"
```
  </action>
  <verify>
    <automated>grep -A 30 "^### 2. OpenTelemetry" docs/superpowers/specs/native-observability-inventory.md | grep -q "| Span attributes" &amp;&amp; grep -A 30 "^### 2. OpenTelemetry" docs/superpowers/specs/native-observability-inventory.md | grep -q "| Baggage" &amp;&amp; grep -A 30 "^### 2. OpenTelemetry" docs/superpowers/specs/native-observability-inventory.md | grep -q "| Log"</automated>
  </verify>
  <done>OTel section has ≥4 rows with explicit yes/no on baggage propagation through Foundry SDK. If unknown, the row flags that as ambiguity for 19.4's spike memo.</done>
</task>

<task type="auto">
  <name>Task 4: Audit Surface 3 — Application Insights / Log Analytics</name>
  <files>
    docs/superpowers/specs/native-observability-inventory.md (Section "3. Application Insights / Log Analytics")
    .planning/phases/19.3-native-capability-inventory/INVENTORY-SCRATCH.md
  </files>
  <action>
Investigate three capability areas:

1. **Auto-instrumentation surface for our stack**
   - Question: With `configure_azure_monitor` enabled, what auto-instruments and into which tables? Confirm for: FastAPI routes, httpx, Cosmos SDK (`azure-cosmos`), Foundry SDK (`azure-ai-projects` / `azure-ai-agents`), Azure OpenAI SDK.
   - Evidence:
     - `microsoft_docs_search` for "azure-monitor-opentelemetry auto-instrumentation supported libraries"
     - Live check: KQL `AppDependencies | where TimeGenerated > ago(1h) | summarize count() by Type, Name | order by count_ desc | take 20` — what does Second Brain actually emit.
   - Record: per-SDK auto-instrumentation status + which table the span lands in.

2. **Table schema we rely on (for downstream phases)**
   - Question: Exact column names on `AppRequests`, `AppDependencies`, `AppExceptions`, `AppTraces` that Phase 19.4 will need to filter by. Specifically: where does a custom dimension like `capture_trace_id` land — is it under a top-level column or inside a dynamic `Properties` column?
   - Evidence:
     - Live check: `AppRequests | take 1 | getschema`, same for other three tables.
   - Record: per-table schema snapshot.

3. **Retention and cost policy**
   - Question: Log Analytics workspace retention is 90 days (per memory). Is there any table-level retention override, archive tier, or sampling we should know about for content-rich tables (prompts/outputs)?
   - Evidence: `az monitor log-analytics workspace show -g shared-services-rg -n shared-services-logs` and `az monitor log-analytics workspace table list -g shared-services-rg --workspace-name shared-services-logs`.
   - Record: default retention + any overrides.

Section "3. Application Insights / Log Analytics" table:

```markdown
| Capability | Native today | What's missing | Downstream phase(s) |
|---|---|---|---|
| Auto-instrumentation (FastAPI, httpx, Cosmos, Foundry, AOAI) | <per-SDK yes/no + table> | <gaps> | 19.4, 19.5 |
| Custom dimension shape (Properties dynamic column) | <how to filter> | <gaps> | 19.4 |
| Retention / archive / sampling | <policy today> | <gaps> | 19.5 (retention for decision records is Cosmos, not AppInsights, but content-rich traces rely on this) |
```

Commit:

```
git add docs/superpowers/specs/native-observability-inventory.md .planning/phases/19.3-native-capability-inventory/INVENTORY-SCRATCH.md
git commit -m "docs(19.3): audit App Insights / Log Analytics surface"
```
  </action>
  <verify>
    <automated>grep -A 30 "^### 3. Application Insights" docs/superpowers/specs/native-observability-inventory.md | grep -q "| Auto-instrumentation" &amp;&amp; grep -A 30 "^### 3. Application Insights" docs/superpowers/specs/native-observability-inventory.md | grep -q "| Custom dimension"</automated>
  </verify>
  <done>App Insights section has ≥3 rows with per-SDK instrumentation status + retention policy documented.</done>
</task>

<task type="auto">
  <name>Task 5: Audit Surface 4 — Azure Monitor (Workbooks + Alerts + Action Groups)</name>
  <files>
    docs/superpowers/specs/native-observability-inventory.md (Section "4. Azure Monitor")
    .planning/phases/19.3-native-capability-inventory/INVENTORY-SCRATCH.md
  </files>
  <action>
Investigate three capability areas:

1. **Azure Monitor Workbooks — could any replace Phase 19.5's web RCA page?**
   - Question: What Workbook templates ship for App Insights + Log Analytics? Are any tailored to agent/LLM observability? Can a workbook URL be parameterized by trace_id?
   - Evidence:
     - `microsoft_docs_search` for "Azure Monitor Workbooks template gallery LLM"
     - Live: Azure portal → Application Insights `second-brain-insights` → Workbooks → review template gallery. Screenshot or list the relevant templates.
   - Record: list of templates that plausibly cover 19.5 needs, plus yes/no on "can be linked from our web UI with trace_id as a parameter." If YES → Phase 19.5 Plan 07 may shrink.

2. **Existing action group `SecondBrainAlerts`**
   - Question: Confirm the 3 rules (API-Error-Spike, Capture-Processing-Failures, API-Health-Check) exist and the action group delivers to Will's phone. Can we add eval-degradation rules without restructuring?
   - Evidence: `az monitor action-group show -g shared-services-rg -n SecondBrainAlerts` and `az monitor metrics alert list -g shared-services-rg`.
   - Record: action group members (email, push), existing rule names, whether adding rules requires portal work or is CLI-able.

3. **Alert rules targeting App Insights custom metrics**
   - Question: Phase 22 needs alerts on metrics like `eval.classifier.accuracy < threshold`. Does Azure Monitor support log-based alerts on custom metric names written via `logger.info`? Or do we need to emit as OTel metrics (different path)?
   - Evidence: `microsoft_docs_search` for "Azure Monitor log alert custom metric Application Insights"
   - Record: yes/no + which emit path Phase 22 must use.

Section "4. Azure Monitor" table:

```markdown
| Capability | Native today | What's missing | Downstream phase(s) |
|---|---|---|---|
| Workbooks (template gallery) | <what's relevant> | <gaps> | 19.5 |
| Workbook parameterized URLs | <yes/no> | <gaps> | 19.5 |
| `SecondBrainAlerts` action group | <delivery path, current rules> | <gaps> | 22 |
| Alerts on custom App Insights metrics | <which emit path works> | <gaps> | 22 |
```

Commit:

```
git add docs/superpowers/specs/native-observability-inventory.md .planning/phases/19.3-native-capability-inventory/INVENTORY-SCRATCH.md
git commit -m "docs(19.3): audit Azure Monitor surface"
```
  </action>
  <verify>
    <automated>grep -A 30 "^### 4. Azure Monitor" docs/superpowers/specs/native-observability-inventory.md | grep -q "| Workbooks" &amp;&amp; grep -A 30 "^### 4. Azure Monitor" docs/superpowers/specs/native-observability-inventory.md | grep -q "SecondBrainAlerts"</automated>
  </verify>
  <done>Azure Monitor section has ≥4 rows covering Workbooks (both static and parameterized), existing action group, and custom-metric alerting.</done>
</task>

<task type="auto">
  <name>Task 6: Audit Surface 5 — Sentry.IO</name>
  <files>
    docs/superpowers/specs/native-observability-inventory.md (Section "5. Sentry.IO")
    .planning/phases/19.3-native-capability-inventory/INVENTORY-SCRATCH.md
  </files>
  <action>
Investigate three capability areas:

1. **Python SDK (backend)**
   - Question: Is `sentry-sdk` attached to the backend today? What integrations (FastAPI, httpx, Azure) are enabled? Can we tag events with `capture_trace_id`?
   - Evidence: grep `backend/src/second_brain` for `sentry_sdk`, `sentry-sdk` in `pyproject.toml`, deployed env vars via `az containerapp show -g shared-services-rg -n second-brain-api` looking for `SENTRY_DSN`.
   - Record: yes/no + integrations in use + tagging capability.

2. **React Native SDK (mobile, post-18-03)**
   - Question: Post-18-03 regression fix, what's captured by mobile Sentry today? Breadcrumbs? Release tracking? User context?
   - Evidence: grep `mobile/` for `@sentry/react-native`, inspect existing init config, check Sentry project dashboard for recent events.
   - Record: what's actually arriving in Sentry today (a deferred live test happens in Checkpoint C).

3. **Correlation — can Sentry events carry our `capture_trace_id`?**
   - Question: Does `sentry_sdk.set_tag("capture_trace_id", ...)` enable filtering/search in Sentry UI? Does it propagate to Sentry's own Span/Trace feature if we enable performance monitoring?
   - Evidence: `ref_search_documentation` for "sentry-sdk set_tag" and "@sentry/react-native setTag"; Sentry docs on custom tags vs. trace context.
   - Record: yes/no + any caveats.

Section "5. Sentry.IO" table:

```markdown
| Capability | Native today | What's missing | Downstream phase(s) |
|---|---|---|---|
| Python SDK integrations | <what's wired> | <gaps> | 19.4 |
| React Native SDK post-18-03 | <what's captured> | <gaps — deferred live test in Checkpoint C> | 19.5 |
| Custom tags for correlation | <set_tag behavior> | <gaps> | 19.4, 19.5 |
```

Commit:

```
git add docs/superpowers/specs/native-observability-inventory.md .planning/phases/19.3-native-capability-inventory/INVENTORY-SCRATCH.md
git commit -m "docs(19.3): audit Sentry.IO surface"
```
  </action>
  <verify>
    <automated>grep -A 30 "^### 5. Sentry.IO" docs/superpowers/specs/native-observability-inventory.md | grep -q "| Python SDK" &amp;&amp; grep -A 30 "^### 5. Sentry.IO" docs/superpowers/specs/native-observability-inventory.md | grep -q "| React Native"</automated>
  </verify>
  <done>Sentry section has ≥3 rows. RN row explicitly defers live validation to Checkpoint C.</done>
</task>

<task type="auto">
  <name>Task 7: Audit Surface 6 — Azure Cosmos DB</name>
  <files>
    docs/superpowers/specs/native-observability-inventory.md (Section "6. Azure Cosmos DB")
    .planning/phases/19.3-native-capability-inventory/INVENTORY-SCRATCH.md
  </files>
  <action>
Investigate two capability areas:

1. **Diagnostic logs — `AzureDiagnostics` table + `activityId_g` pivot for 19.4**
   - Question: The gap memo states `AzureDiagnostics.activityId_g` holds `x-ms-client-request-id`. Can the Python `azure-cosmos` SDK either (a) expose the per-operation client-request-id on the response, or (b) accept a caller-supplied client-request-id that we set to `capture_trace_id`? Either mechanism lets Phase 19.4 record `(capture_trace_id, activityId_g)` pairs without bespoke SDK forks.
   - Evidence:
     - `microsoft_docs_search` for "azure-cosmos python client request id"
     - `ref_search_documentation` for "azure-cosmos CosmosClient" + client_request_id
     - Direct code inspection: look at how `backend/src/second_brain` constructs the Cosmos client and whether the `headers` kwarg or response-header access pattern is already used.
   - Record: yes/no on each mechanism + code path for whichever works. This is the biggest 19.4 scoping question after OTel baggage.

2. **Cosmos Change Feed — substrate for Phase 20 implicit signals**
   - Question: Can a Change Feed listener detect (a) inbox item recategorizations, (b) errand re-routes, (c) HITL bucket changes as distinct document mutations? What infrastructure does the listener run on (Functions? Container Apps? in-process?)
   - Evidence:
     - `microsoft_docs_search` for "Cosmos DB Change Feed Python SDK" and "Change Feed processor"
     - Confirm which containers would need listeners: `Inbox`, whichever holds errands/tasks.
     - Record: latency expectation, hosting options, whether it can run in-process with our FastAPI app or needs separate deployment. If in-process is viable, Phase 20 shrinks.

Section "6. Azure Cosmos DB" table:

```markdown
| Capability | Native today | What's missing | Downstream phase(s) |
|---|---|---|---|
| Diagnostic logs + activityId_g | <what's queryable today> | <gaps> | 19.4 |
| Per-operation client-request-id (write path) | <SDK mechanism> | <gaps> | 19.4 |
| Change Feed — detect recategorization | <latency, hosting> | <gaps> | 20 |
```

Commit:

```
git add docs/superpowers/specs/native-observability-inventory.md .planning/phases/19.3-native-capability-inventory/INVENTORY-SCRATCH.md
git commit -m "docs(19.3): audit Azure Cosmos DB surface"
```
  </action>
  <verify>
    <automated>grep -A 30 "^### 6. Azure Cosmos DB" docs/superpowers/specs/native-observability-inventory.md | grep -q "activityId" &amp;&amp; grep -A 30 "^### 6. Azure Cosmos DB" docs/superpowers/specs/native-observability-inventory.md | grep -q "Change Feed"</automated>
  </verify>
  <done>Cosmos section has ≥3 rows. activityId_g pivot mechanism is documented with a yes/no on whether bespoke instrumentation is avoidable. Change Feed viability for Phase 20 is documented with latency + hosting specifics.</done>
</task>

<task type="auto">
  <name>Task 8: Audit Surface 7 — Azure Container Apps</name>
  <files>
    docs/superpowers/specs/native-observability-inventory.md (Section "7. Azure Container Apps")
    .planning/phases/19.3-native-capability-inventory/INVENTORY-SCRATCH.md
  </files>
  <action>
Investigate two capability areas (narrow — Container Apps is mostly already solved for us):

1. **Revision metadata / release correlation**
   - Question: Phase 19.5's `AgentReleaseManifest` needs `agent_release_id`. Container Apps revisions already embed the git SHA in revision names (per Phase 16.1). Can Phase 19.5 read the active revision name at request time and use `revision:<name>` as the release reference when Foundry doesn't version prompts natively?
   - Evidence:
     - `az containerapp revision list -g shared-services-rg -n second-brain-api -o table`
     - Check whether the revision name is available in-process (env var, metadata endpoint).
   - Record: revision-name access mechanism + how it maps to the manifest strategy.

2. **Container Apps built-in logs vs. App Insights**
   - Question: Memory notes console logs only show uvicorn access. Is there any Container Apps telemetry we should reference in the inventory that isn't App Insights (e.g., scale events, revision transitions)? Likely low-value but confirm.
   - Evidence: `az monitor log-analytics query -w <workspace> --analytics-query "ContainerAppConsoleLogs_CL | take 5"` and `ContainerAppSystemLogs_CL | take 5`.
   - Record: one-liner summary.

Section "7. Azure Container Apps" table:

```markdown
| Capability | Native today | What's missing | Downstream phase(s) |
|---|---|---|---|
| Revision metadata + git SHA in name | <access path in-process> | <gaps> | 19.5 |
| System logs (scale, revisions) | <what's there, mostly noise> | <gaps or N/A> | - |
```

Commit:

```
git add docs/superpowers/specs/native-observability-inventory.md .planning/phases/19.3-native-capability-inventory/INVENTORY-SCRATCH.md
git commit -m "docs(19.3): audit Azure Container Apps surface"
```
  </action>
  <verify>
    <automated>grep -A 15 "^### 7. Azure Container Apps" docs/superpowers/specs/native-observability-inventory.md | grep -q "Revision metadata"</automated>
  </verify>
  <done>Container Apps section has ≥2 rows. Revision-name access mechanism is documented with a concrete answer on whether 19.5 can avoid a release manifest.</done>
</task>

<task type="auto">
  <name>Task 9: Audit Surface 8 — GitHub Actions</name>
  <files>
    docs/superpowers/specs/native-observability-inventory.md (Section "8. GitHub Actions")
    .planning/phases/19.3-native-capability-inventory/INVENTORY-SCRATCH.md
  </files>
  <action>
Investigate three capability areas — Phase 22 depends entirely on this.

1. **Cron schedule triggers**
   - Question: Is `schedule: cron` available on our GitHub plan? Any constraints on minimum interval or total scheduled-workflow count?
   - Evidence: `ref_search_documentation` for "GitHub Actions schedule cron"; confirm no existing scheduled workflows conflict.
   - Record: cron support + any quirks (e.g., skipped runs on inactive repos).

2. **Artifact storage + retention**
   - Question: Phase 22 eval runs produce result payloads. Do we need a separate storage artifact strategy or does `actions/upload-artifact` + default retention suffice?
   - Evidence: `ref_search_documentation` for "actions upload-artifact retention"; existing workflow artifact usage in `.github/workflows/`.
   - Record: retention defaults + per-artifact limits.

3. **Workflow summaries + annotations**
   - Question: Phase 22 wants eval scores visible at a glance on the workflow run page. Does `$GITHUB_STEP_SUMMARY` work (it's used by Phase 16.1 deploy summary)? Can we emit annotations tied to failures for threshold breach?
   - Evidence: existing `.github/workflows/deploy-backend.yml` (uses summary per memory); `ref_search_documentation` for "GitHub Actions workflow commands annotations".
   - Record: yes/no + any caveats.

Section "8. GitHub Actions" table:

```markdown
| Capability | Native today | What's missing | Downstream phase(s) |
|---|---|---|---|
| `schedule: cron` triggers | <our plan, any limits> | <gaps> | 22 |
| `actions/upload-artifact` + retention | <default days> | <gaps> | 22 |
| `$GITHUB_STEP_SUMMARY` + annotations | <usage today, pattern> | <gaps> | 22 |
```

Commit:

```
git add docs/superpowers/specs/native-observability-inventory.md .planning/phases/19.3-native-capability-inventory/INVENTORY-SCRATCH.md
git commit -m "docs(19.3): audit GitHub Actions surface"
```
  </action>
  <verify>
    <automated>grep -A 20 "^### 8. GitHub Actions" docs/superpowers/specs/native-observability-inventory.md | grep -q "cron" &amp;&amp; grep -A 20 "^### 8. GitHub Actions" docs/superpowers/specs/native-observability-inventory.md | grep -q "upload-artifact"</automated>
  </verify>
  <done>GitHub Actions section has ≥3 rows all citing Phase 22.</done>
</task>

<task type="auto">
  <name>Task 10: Audit Surface 9 — Expo / EAS</name>
  <files>
    docs/superpowers/specs/native-observability-inventory.md (Section "9. Expo / EAS")
    .planning/phases/19.3-native-capability-inventory/INVENTORY-SCRATCH.md
  </files>
  <action>
Investigate two capability areas — narrow scope, mobile-side only.

1. **Crash reports / runtime errors (vs. Sentry RN)**
   - Question: What does EAS provide natively for runtime diagnostics (crashes, update rollbacks)? Does this duplicate Sentry RN or complement it?
   - Evidence: `microsoft_docs_search` or `web_search_exa` for "EAS Update crash diagnostics" — but actually prefer `ref_search_documentation` for "expo eas diagnostics"; check the Expo dashboard for the Second Brain project.
   - Record: what EAS adds beyond Sentry + whether Phase 19.5 mobile Segment View should link to EAS as well.

2. **OTA update metrics**
   - Question: Does EAS track adoption of a given update (which phones are on which bundle)? This matters for mobile EAS rebuild follow-up (housekeeping) — we need a way to confirm Will's phone is running the latest.
   - Evidence: Expo dashboard; `ref_search_documentation` for "eas update adoption metrics".
   - Record: yes/no on adoption tracking + how operator can verify phone has correct bundle.

Section "9. Expo / EAS" table:

```markdown
| Capability | Native today | What's missing | Downstream phase(s) |
|---|---|---|---|
| Crash reports | <EAS coverage vs Sentry> | <gaps or "Sentry is the primary path"> | - (noted but low downstream impact) |
| OTA update adoption | <yes/no, mechanism> | <gaps> | housekeeping (mobile EAS rebuild verification) |
```

Commit:

```
git add docs/superpowers/specs/native-observability-inventory.md .planning/phases/19.3-native-capability-inventory/INVENTORY-SCRATCH.md
git commit -m "docs(19.3): audit Expo/EAS surface"
```
  </action>
  <verify>
    <automated>grep -A 15 "^### 9. Expo / EAS" docs/superpowers/specs/native-observability-inventory.md | grep -qE "Crash|OTA"</automated>
  </verify>
  <done>Expo/EAS section has ≥2 rows. OTA update adoption capability is documented so the housekeeping todo references it.</done>
</task>

<task type="auto">
  <name>Task 11: Checkpoint A — Foundry trace depth (live evidence)</name>
  <files>
    docs/superpowers/specs/native-observability-inventory.md (Section "Checkpoint A")
    .planning/phases/19.3-native-capability-inventory/INVENTORY-SCRATCH.md
  </files>
  <action>
Live test against deployed system. Do NOT run the backend locally.

**Procedure:**

1. Trigger one real capture against https://brain.willmacdonald.com (via mobile app or curl).
2. Note the `capture_trace_id` (get it from the SSE stream, or from the /correlation endpoint after a few seconds).
3. For each of the three agents (Classifier, Admin if the capture routes that way, Investigation via a separate /api/investigate call), do the following:
   a. Open the Foundry portal → Agents → `<agent>` → Traces (or Conversation results if that's the view that shows prompts+outputs).
   b. Locate the run corresponding to the capture (may require matching on timestamp + thread_id if capture_trace_id isn't natively searchable — this itself is a finding).
   c. Inspect and screenshot (save to `.planning/phases/19.3-native-capability-inventory/evidence/foundry-<agent>-trace.png` — or a markdown description of what's visible if screenshots aren't feasible):
      - Is the prompt text visible in full?
      - Is the model output visible?
      - Are tool calls visible with arguments + results?
      - Is there a confidence / score value visible (classifier only)?
      - Is the conversation/thread/run ID visible and copyable?

4. Cross-check: query `AppDependencies | where TimeGenerated > ago(30m) and cloud_RoleName contains "foundry" or Type contains "agent" | project TimeGenerated, Name, Properties | take 20` and confirm at least one row corresponds to the capture you just made.

**Write Checkpoint A findings into the inventory:**

```markdown
### Checkpoint A — Foundry trace depth

**Tested:** 2026-04-19 (or actual date)
**Trace ID tested:** <capture_trace_id>
**Agents tested:** Classifier, Admin (if applicable), Investigation

| Field | Classifier | Admin | Investigation |
|---|---|---|---|
| Prompt visible in portal | yes/no | yes/no | yes/no |
| Output visible in portal | yes/no | yes/no | yes/no |
| Tool calls visible | yes/no + detail | yes/no + detail | yes/no + detail |
| Confidence visible | yes/no | N/A | N/A |
| Run/thread ID copyable | yes/no | yes/no | yes/no |
| Searchable by capture_trace_id directly | yes/no | yes/no | yes/no |

**Implication for Phase 19.5:** <one paragraph — if all "yes", decision record stays thin. If critical gaps exist, call them out and flag ambiguity for 19.5 spike memo.>

**Evidence:** <screenshot paths or markdown descriptions in `evidence/` subdir>
```

If Foundry does NOT show prompts/outputs for our agents (e.g., tracing misconfigured), this is a blocker for Phase 19.5 — stop and escalate before continuing to Checkpoint B.

Commit:

```
git add docs/superpowers/specs/native-observability-inventory.md .planning/phases/19.3-native-capability-inventory/
git commit -m "docs(19.3): checkpoint A — Foundry trace depth findings"
```
  </action>
  <verify>
    <automated>grep -A 30 "^### Checkpoint A" docs/superpowers/specs/native-observability-inventory.md | grep -qE "Trace ID tested" &amp;&amp; grep -A 30 "^### Checkpoint A" docs/superpowers/specs/native-observability-inventory.md | grep -q "Implication for Phase 19.5"</automated>
  </verify>
  <done>Checkpoint A section has a live trace ID, per-agent findings table, and an implication statement for Phase 19.5.</done>
</task>

<task type="auto">
  <name>Task 12: Checkpoint B — Foundry Evaluations SDK live test</name>
  <files>
    docs/superpowers/specs/native-observability-inventory.md (Section "Checkpoint B")
    .planning/phases/19.3-native-capability-inventory/INVENTORY-SCRATCH.md
    .planning/phases/19.3-native-capability-inventory/eval-spike/ (optional, gitignored from main repo if code is sensitive — otherwise keep)
  </files>
  <action>
**Goal:** Determine whether `azure-ai-evaluation` / Foundry Evaluations SDK can score classifier output against a known label with minimal bespoke code. The answer determines whether Phase 21 Plan 02 is "configure an evaluator" or "write an evaluator."

**Procedure:**

1. Create a throwaway script `.planning/phases/19.3-native-capability-inventory/eval-spike/score_one_capture.py` that:
   - Imports `azure.ai.evaluation` (install if not present — `uv pip install azure-ai-evaluation`).
   - Uses DefaultAzureCredential against the `second-brain-foundry-resource`.
   - Takes 3 known classifier outcomes from recent captures (query Cosmos `Inbox` for 3 items with their assigned bucket, pick a representative mix — e.g., 1 Admin, 1 Meal, 1 Note).
   - Constructs a minimal eval dataset: `[{"query": "<capture text>", "response": "<bucket>", "ground_truth": "<bucket>"}]` — for this spike, response == ground_truth so the accuracy evaluator should report 1.0.
   - Tries to run a built-in evaluator that scores label match (e.g., exact-match, classification-accuracy) — pick the closest available from the SDK.
   - If no built-in fits, try `azure.ai.evaluation.evaluate` with a custom callable scorer — note how invasive that is.
   - Print the result.

2. Run the script locally (this IS acceptable — it's a spike, not production).

3. Record in Checkpoint B:
   - SDK version tested (pinned version for 19.3 row cite).
   - Which evaluator worked (if any).
   - Whether a custom scorer was needed for label match.
   - Error messages if it failed (so Phase 21 spike memo has ground truth to work from).

**Write Checkpoint B findings:**

```markdown
### Checkpoint B — Foundry evals applicability

**Tested:** 2026-04-19 (or actual date)
**SDK version pinned:** azure-ai-evaluation==X.Y.Z
**Outcome:** works / partial / doesn't work

**What worked:**
- <evaluator name>: <1-line result>

**What didn't work:**
- <gap>

**Implication for Phase 21:**
- If works → Phase 21 Plan 02 replaced by "configure Foundry evaluator, upload golden dataset." Shrinks from ~4 plans to ~2.
- If partial → Phase 21 retains custom scorer for <what>; Foundry covers <what>.
- If doesn't work → Phase 21 stays as currently spec'd (bespoke scorers).

**Script:** `.planning/phases/19.3-native-capability-inventory/eval-spike/score_one_capture.py`
```

Commit:

```
git add docs/superpowers/specs/native-observability-inventory.md .planning/phases/19.3-native-capability-inventory/
git commit -m "docs(19.3): checkpoint B — Foundry evals applicability live test"
```
  </action>
  <verify>
    <automated>grep -A 30 "^### Checkpoint B" docs/superpowers/specs/native-observability-inventory.md | grep -qE "SDK version pinned" &amp;&amp; grep -A 30 "^### Checkpoint B" docs/superpowers/specs/native-observability-inventory.md | grep -q "Implication for Phase 21"</automated>
  </verify>
  <done>Checkpoint B has a pinned SDK version, live test result, and a concrete implication for Phase 21.</done>
</task>

<task type="auto">
  <name>Task 13: Checkpoint C — Sentry RN status on deployed phone</name>
  <files>
    docs/superpowers/specs/native-observability-inventory.md (Section "Checkpoint C")
    .planning/phases/19.3-native-capability-inventory/INVENTORY-SCRATCH.md
  </files>
  <action>
**Goal:** Confirm mobile Sentry is capturing post-18-03 regression fix. Documents what mobile-side error visibility we actually have before 19.5 builds mobile Segment View on top of it.

**Procedure:**

1. On the installed EAS dev/preview build on Will's phone, trigger a deliberate error:
   - Option A: Force a network error by toggling airplane mode mid-capture and observing the error toast.
   - Option B: If there's a dev-only "throw test error" button from 17.3 work, use it. Otherwise pick Option A.
2. Open the Sentry dashboard for the mobile project. Confirm the event arrived.
3. Record for the event:
   - Event fingerprint / title.
   - Breadcrumb trail — how many breadcrumbs, what types (navigation, http, custom)?
   - Tags present — is `capture_trace_id` or any correlation key present?
   - Release tag — is the EAS build identifier captured?
   - User context — is any user identifier attached (dev-mode OK)?

**Write Checkpoint C findings:**

```markdown
### Checkpoint C — Sentry RN status on deployed phone

**Tested:** 2026-04-19 (or actual date)
**EAS build tested:** <build ID or version>
**Trigger used:** <airplane mode mid-capture | dev throw button | …>
**Arrived in Sentry:** yes/no

| Field | Captured? | Detail |
|---|---|---|
| Event title / fingerprint | yes/no | <description> |
| Breadcrumbs | yes/no | <count + types> |
| capture_trace_id tag | yes/no | <value or "absent"> |
| Release tag | yes/no | <value or "absent"> |
| User context | yes/no | <value or "absent"> |

**Implication for Phase 19.5:** <one paragraph — does mobile Segment View's error count source from Sentry, Cosmos decision records, or both? If capture_trace_id is absent from Sentry, mobile Segment View must source errors from AgentDecisionRecords with error terminal states, not Sentry directly — flag this for 19.5 design.>
```

If Sentry RN is NOT capturing (post-18-03 regression returned), this is a blocker for 19.5 — flag it and create a new todo to re-fix before 19.5 starts.

Commit:

```
git add docs/superpowers/specs/native-observability-inventory.md .planning/phases/19.3-native-capability-inventory/INVENTORY-SCRATCH.md
git commit -m "docs(19.3): checkpoint C — Sentry RN status on deployed phone"
```
  </action>
  <verify>
    <automated>grep -A 30 "^### Checkpoint C" docs/superpowers/specs/native-observability-inventory.md | grep -qE "Tested:" &amp;&amp; grep -A 30 "^### Checkpoint C" docs/superpowers/specs/native-observability-inventory.md | grep -q "Implication for Phase 19.5"</automated>
  </verify>
  <done>Checkpoint C has a live test result + per-field capture table + implication for Phase 19.5 mobile Segment View data source decision.</done>
</task>

<task type="auto">
  <name>Task 14: Write the phase impact addendum</name>
  <files>
    docs/superpowers/specs/native-observability-inventory.md (Section "Phase Impact Addendum")
  </files>
  <action>
For each of the five downstream phases, populate a subsection using this exact template. Cite inventory rows by surface + capability (e.g., "Surface 1 / Evaluations SDK — built-ins"), not by row number.

```markdown
### Phase 19.4 — Native Span Correlation Tagging

**Original scope (from spec):** Tag backend_api AppRequests, Foundry agent spans, Cosmos activityId_g, and Investigation custom spans with capture_trace_id.

**Inventory findings that affect this phase:**
- <cite Surface 2 / Baggage propagation finding>
- <cite Surface 6 / Per-operation client-request-id finding>
- <cite Surface 1 / Tracing → App Insights finding>
- <cite other relevant rows>

**Scope delta:** <one of: `no change` | `smaller scope — OTel baggage covers sites #1/#2/#4 together; Plans 02+04 merge to one plan` | `larger scope — Cosmos SDK requires bespoke wrapper for activityId_g capture` | `add spike memo — <specific ambiguity>`>

**Revised plan count estimate:** <e.g., "3–5 → 3" if baggage collapses plans, or "3–5 → 4" if no change>
```

Repeat for:
- Phase 19.5 — Agent Decision Record & RCA View (cite Checkpoint A, Surface 1 / Prompt-agent versioning, Surface 7 / Revision metadata, Surface 4 / Workbooks)
- Phase 20 — Labels (cite Surface 6 / Change Feed)
- Phase 21 — Eval (cite Checkpoint B, Surface 1 / Evaluations SDK)
- Phase 22 — Self-Monitoring (cite Surface 4 / action group + custom-metric alerts, Surface 8 / cron + artifacts + summary)

**Guardrail:** every "Inventory findings" bullet must point to a real row you wrote in Tasks 2-10 or a Checkpoint in Tasks 11-13. If you can't cite, the inventory is missing a row — go back and add it.

Commit:

```
git add docs/superpowers/specs/native-observability-inventory.md
git commit -m "docs(19.3): phase impact addendum — scope deltas for 19.4/19.5/20/21/22"
```
  </action>
  <verify>
    <automated>grep -A 500 "^## Phase Impact Addendum" docs/superpowers/specs/native-observability-inventory.md | grep -qE "Phase 19.4" &amp;&amp; grep -A 500 "^## Phase Impact Addendum" docs/superpowers/specs/native-observability-inventory.md | grep -qE "Phase 19.5" &amp;&amp; grep -A 500 "^## Phase Impact Addendum" docs/superpowers/specs/native-observability-inventory.md | grep -qE "Phase 20" &amp;&amp; grep -A 500 "^## Phase Impact Addendum" docs/superpowers/specs/native-observability-inventory.md | grep -qE "Phase 21" &amp;&amp; grep -A 500 "^## Phase Impact Addendum" docs/superpowers/specs/native-observability-inventory.md | grep -qE "Phase 22" &amp;&amp; [ "$(grep -A 500 '^## Phase Impact Addendum' docs/superpowers/specs/native-observability-inventory.md | grep -c 'Scope delta' | tr -d ' ')" -eq 5 ]</automated>
  </verify>
  <done>Addendum has 5 subsections (one per downstream phase), each with original scope, 2+ findings citations, explicit scope delta, and revised plan count estimate.</done>
</task>

<task type="auto">
  <name>Task 15: Capture housekeeping todos</name>
  <files>
    .planning/todos/ (via /gsd:add-todo)
    docs/superpowers/specs/native-observability-inventory.md (Section "Housekeeping Captured")
  </files>
  <action>
Capture the two housekeeping items from the spec as formal todos so they don't rot in memory files.

1. **Mobile EAS rebuild + 19.2 closure verification:**
   - Invoke `/gsd:add-todo` with title `"Mobile EAS rebuild to close 19.2 mobile_capture gap"` and description:
     ```
     After Phase 19.2 the mobile JS was centralised to emit mobile_capture via Option B, but the phone runs a pre-19.2 EAS bundle. Need to: run `eas build --profile development`, reinstall on phone, perform one fresh capture, confirm the web UI at `/correlation/capture/<trace>` shows mobile_capture + backend_api + classifier with no "Missing required segments" warning. Trigger: before Phase 19.3 executes (close 19.2 baseline first) OR immediately after 19.3 completes if it slipped.

     Reference: `memory/project_deferred_19.2_spine_gaps.md`.
     Adoption check mechanism: see inventory Surface 9 / OTA update adoption row.
     ```
   - If `/gsd:add-todo` is not available or errors, write the todo manually to `.planning/todos/YYYY-MM-DD-mobile-eas-rebuild-close-19.2.md` with a frontmatter matching existing todos (check `.planning/todos/` for format).

2. **Duplicate Key Vault secret cleanup:**
   - Invoke `/gsd:add-todo` with title `"Delete duplicate Key Vault secret for API key"` and description:
     ```
     Two Key Vault secrets in wkm-shared-kv point at the same API key (noted in `memory/project_followup_duplicate_api_key_secrets.md`). Identify the duplicate, confirm neither is actively referenced twice (check Container App secret bindings via `az containerapp show`), delete the unused one. Zero downstream blocker — purely housekeeping.
     ```

3. **Update the inventory's "Housekeeping Captured" section:**

```markdown
## Housekeeping Captured

The spec defined two housekeeping items that do not warrant their own phase. Both are captured as todos via `/gsd:add-todo` on 2026-04-19:

- **Mobile EAS rebuild + 19.2 closure verification** — todo `<todo-id-or-filename>`. Trigger: before 19.3 execution if possible, else after. Reference: `memory/project_deferred_19.2_spine_gaps.md`.
- **Duplicate Key Vault secret cleanup** — todo `<todo-id-or-filename>`. Trigger: whenever; zero blocker. Reference: `memory/project_followup_duplicate_api_key_secrets.md`.
```

Commit:

```
git add docs/superpowers/specs/native-observability-inventory.md .planning/todos/
git commit -m "docs(19.3): capture housekeeping todos (EAS rebuild, Key Vault dup)"
```
  </action>
  <verify>
    <automated>grep -A 10 "^## Housekeeping Captured" docs/superpowers/specs/native-observability-inventory.md | grep -qE "Mobile EAS rebuild" &amp;&amp; grep -A 10 "^## Housekeeping Captured" docs/superpowers/specs/native-observability-inventory.md | grep -qE "Key Vault"</automated>
  </verify>
  <done>Both housekeeping todos are captured (via /gsd:add-todo or manual file), and the inventory's Housekeeping Captured section references them.</done>
</task>

<task type="auto">
  <name>Task 16: Flip the inventory status to "ready for review" and sanity-check</name>
  <files>
    docs/superpowers/specs/native-observability-inventory.md (header + final pass)
  </files>
  <action>
Final sanity-check pass before the human checkpoint:

1. Change the header `Status: Draft (in progress)` to `Status: Ready for review`.

2. Run these self-checks and fix inline:
   - **Row count ≥ 15.** Count table rows across Sections 1-9. If below 15, either add missing rows or the inventory is under-scoped — flag it.
   - **Every row cites a downstream phase.** Grep each section's table — every row must have a phase in the `Downstream phase(s)` column, or be explicitly marked `-` with a rationale.
   - **No TBD / TODO / "in progress" markers left.** Any remaining is a bug.
   - **Three checkpoint sections populated** (A/B/C).
   - **Phase impact addendum has all 5 subsections** with explicit scope deltas (not blank "no change" unless that's truly the finding).
   - **Housekeeping Captured section references real todo IDs**, not placeholders.

3. Add a short "Open questions for human review" section at the very end (below Housekeeping Captured) listing any rows where the investigator couldn't reach a confident answer — these are candidates for the downstream phase spike memos, not for re-investigation within 19.3.

Commit:

```
git add docs/superpowers/specs/native-observability-inventory.md
git commit -m "docs(19.3): flip inventory to ready-for-review after sanity check"
```
  </action>
  <verify>
    <automated>grep -q "Status: Ready for review" docs/superpowers/specs/native-observability-inventory.md &amp;&amp; ! grep -qE "TBD|TODO|in progress" docs/superpowers/specs/native-observability-inventory.md &amp;&amp; [ "$(grep -E '^\| [A-Z]' docs/superpowers/specs/native-observability-inventory.md | grep -vE 'Capability|Field' | wc -l | tr -d ' ')" -ge 15 ]</automated>
  </verify>
  <done>Inventory is flipped to "Ready for review", has ≥15 table rows, no TBD/TODO markers, all three checkpoints populated, five-phase addendum complete, and an "Open questions for human review" section.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 17: Human review of inventory (gate for Phase 19.4 planning)</name>
  <files>docs/superpowers/specs/native-observability-inventory.md</files>
  <action>Read `docs/superpowers/specs/native-observability-inventory.md` end to end. For each section (1-9), confirm the rows match your mental model of what the platform does. For each checkpoint (A/B/C), confirm the live-evidence finding is acceptable. For each phase impact addendum subsection, confirm the scope delta is the delta you want to apply to that phase. See <how-to-verify> for the full checklist.</action>
  <verify>
    <automated>User types "inventory approved" or provides corrections; orchestrator pauses until then.</automated>
  </verify>
  <done>User has explicitly approved the inventory, OR the planner has revised it per user feedback and re-submitted.</done>
  <what-built>
The native capability inventory document plus three live-evidence checkpoints plus a phase impact addendum with concrete scope deltas for Phases 19.4, 19.5, 20, 21, 22, plus two captured housekeeping todos.
  </what-built>
  <how-to-verify>
1. Read the document end to end.
2. For each of the 9 surface sections, confirm the rows capture how the platform actually works in your mental model. Any row that's wrong means downstream phases will be miscopsed — flag it.
3. For Checkpoint A: confirm the Foundry trace depth finding is acceptable. If prompts/outputs are visible in the portal for all three agents, the "Foundry First" design rule holds for Phase 19.5. If gaps, Phase 19.5's spike memo needs to address them.
4. For Checkpoint B: confirm the Foundry evals SDK finding. If a built-in evaluator scores classifier labels, Phase 21 shrinks substantially. If not, Phase 21 stays bespoke.
5. For Checkpoint C: confirm Sentry RN is capturing events post-18-03 fix. If no, create a new todo to re-fix before 19.5 starts.
6. For each phase impact addendum subsection: confirm the scope delta is what you want to apply. This becomes the scope fence for that phase's PLAN.md Native Capability Check header.
7. Review "Open questions for human review" — decide which to fold into downstream spike memos vs. which to resolve now.
  </how-to-verify>
  <resume-signal>Type "inventory approved" to unblock Phase 19.4 planning, or describe corrections and the planner will revise the inventory before 19.4 starts.</resume-signal>
</task>

</tasks>

<verification>
- `docs/superpowers/specs/native-observability-inventory.md` exists with ≥15 rows across 9 surfaces
- Every row cites at least one downstream phase
- Three checkpoint sections have live-evidence findings (trace IDs, SDK versions, screenshot paths)
- Phase impact addendum has 5 subsections with explicit scope deltas
- Two housekeeping todos are captured
- Human checkpoint approves the inventory before Phase 19.4 planning begins
</verification>

<success_criteria>
- Inventory document has ≥15 rows across all 9 native surfaces
- Every inventory row cites ≥1 downstream phase
- Checkpoint A finding determines whether Phase 19.5 decision record is cheap-reference or needs-to-copy-fields
- Checkpoint B finding determines Phase 21's Foundry-native vs. bespoke split with a pinned SDK version
- Checkpoint C finding determines Phase 19.5 mobile Segment View data source (Sentry vs. AgentDecisionRecords)
- Phase impact addendum has concrete scope deltas for 19.4/19.5/20/21/22 — not "TBD" or "no change" unless that's genuinely the finding
- Two housekeeping todos are captured via /gsd:add-todo
- Human approves the inventory before Phase 19.4 is planned
</success_criteria>

<output>
After completion, create `.planning/phases/19.3-native-capability-inventory/19.3-01-SUMMARY.md` with:
- Summary (1-2 sentences)
- Per-surface row count (e.g., "Foundry 4, OTel 4, AppInsights 3, ...")
- Checkpoint outcomes (one line each: "A: Foundry portal shows full prompts+outputs for all 3 agents", "B: Foundry evals work partial — label-match requires custom scorer", "C: Sentry RN captures events but capture_trace_id tag absent")
- Phase impact summary: for each downstream phase, one-line scope delta
- Link to `docs/superpowers/specs/native-observability-inventory.md`
- Housekeeping todo IDs
</output>
