---
phase: 12.3-destination-affinity-and-knowledge-system-for-errand-routing
plan: 03
subsystem: api
tags: [fastapi, cosmos, errands, routing, admin-agent, affinity-rules]

requires:
  - phase: 12.3-01
    provides: "Destinations and AffinityRules containers, DestinationDocument/AffinityRuleDocument models, seed data"
  - phase: 12.3-02
    provides: "Admin Agent tools for manage_destination, manage_affinity_rule, query_rules, get_routing_context"
provides:
  - "Dynamic destination queries in GET /api/errands (replaces hardcoded KNOWN_DESTINATIONS)"
  - "HITL route endpoint POST /api/errands/{item_id}/route with auto-rule-save"
  - "Admin Agent routing context injection (_build_routing_context)"
  - "Admin Agent response storage and delivery via adminNotifications"
  - "Dismiss notification endpoint POST /api/errands/notifications/{id}/dismiss"
affects: [12.3-04-cleanup, mobile-status-screen]

tech-stack:
  added: []
  patterns:
    - "Dynamic container queries replace hardcoded destination lists"
    - "Routing context prepended to agent messages for knowledge injection"
    - "Response delivery heuristic (_response_needs_delivery) for user-facing messages"

key-files:
  created: []
  modified:
    - "backend/src/second_brain/api/errands.py"
    - "backend/src/second_brain/processing/admin_handoff.py"
    - "backend/tests/test_errands_api.py"
    - "backend/tests/test_admin_handoff.py"

key-decisions:
  - "Response delivery heuristic uses keyword indicators (?, conflict, rule, created, deleted, destination, etc.) to classify agent responses"
  - "Unrouted items appear as special 'Needs Routing' section with type='unrouted'"
  - "Routing context injection wrapped in try/except to gracefully fall back to raw text on failure"
  - "DELETE /api/errands/{id} accepts any destination string (no validation) since destinations are dynamic"
  - "Notification dismiss endpoint deletes the inbox item (response has been delivered)"

patterns-established:
  - "Admin notifications: completed inbox items with adminAgentResponse field surfaced via GET /api/errands"
  - "HITL routing: POST route endpoint moves item + auto-saves affinity rule"

requirements-completed: [DEST-03, DEST-04, DEST-06]

duration: 6min
completed: 2026-03-18
---

# Phase 12.3 Plan 03: Dynamic Errands Routing Summary

**Dynamic destination queries from Cosmos replace hardcoded routing, HITL route endpoint with auto-rule-save, and Admin Agent response delivery via notifications**

## Performance

- **Duration:** 6 min
- **Started:** 2026-03-18T04:36:53Z
- **Completed:** 2026-03-18T04:43:20Z
- **Tasks:** 2
- **Files modified:** 4

## Accomplishments

- GET /api/errands queries Destinations container dynamically with type field (physical/online/unrouted)
- POST /api/errands/{item_id}/route handles HITL routing with destination validation and auto-rule-save
- Admin Agent receives routing context (destinations + affinity rules) prepended to every capture
- Agent responses that need user attention (rule queries, conflicts, confirmations) are stored and delivered via adminNotifications
- All 143 tests pass (18 errands API tests including 11 new ones)

## Task Commits

Each task was committed atomically:

1. **Task 1: Replace hardcoded destinations with dynamic queries, add HITL route endpoint** - `4c0c32a` (feat)
2. **Task 2: Inject routing context, store Admin Agent responses, update tests** - `4411699` (feat)

## Files Created/Modified

- `backend/src/second_brain/api/errands.py` - Dynamic destination queries, HITL route endpoint, admin notifications, dismiss endpoint
- `backend/src/second_brain/processing/admin_handoff.py` - _build_routing_context(), _response_needs_delivery(), response storage on inbox items
- `backend/tests/test_errands_api.py` - Complete rewrite with dynamic destination mocks, 18 tests total
- `backend/tests/test_admin_handoff.py` - Updated test_calls_admin_agent_with_enriched_text to expect routing context

## Decisions Made

- Response delivery heuristic uses keyword matching (?, conflict, rule, created, deleted, destination, etc.) -- simple and effective for classifying whether a response needs user attention
- Routing context injection is wrapped in try/except so context loading failure doesn't break processing -- falls back to raw text only
- DELETE endpoint no longer validates destination against a hardcoded list -- any destination string is accepted since destinations are now dynamic
- Unrouted items are queried separately (partition_key="unrouted") and displayed as "Needs Routing" with type="unrouted"

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Updated existing admin_handoff test for routing context**
- **Found during:** Task 2 (running full test suite)
- **Issue:** test_calls_admin_agent_with_raw_text expected exact raw text match, but now text is enriched with routing context
- **Fix:** Renamed to test_calls_admin_agent_with_enriched_text, checks for AVAILABLE DESTINATIONS header and raw text presence
- **Files modified:** backend/tests/test_admin_handoff.py
- **Verification:** Full test suite passes (143 tests)
- **Committed in:** 4411699 (Task 2 commit)

---

**Total deviations:** 1 auto-fixed (1 bug fix)
**Impact on plan:** Necessary fix for test correctness after routing context injection. No scope creep.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Dynamic routing is fully operational -- Plan 04 can now remove KNOWN_DESTINATIONS from documents.py
- Admin notifications are ready for mobile UI consumption
- HITL routing endpoint ready for mobile Status screen integration
- Plan 05 (mobile UI) can consume the new DestinationSection.type field and adminNotifications

## Self-Check: PASSED

All files exist. All commit hashes verified.

---
*Phase: 12.3-destination-affinity-and-knowledge-system-for-errand-routing*
*Completed: 2026-03-18*
