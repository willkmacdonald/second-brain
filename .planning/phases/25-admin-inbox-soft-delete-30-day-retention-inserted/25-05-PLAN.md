---
phase: 25-admin-inbox-soft-delete-30-day-retention-inserted
plan: 05
type: execute
wave: 4
depends_on: [25-01, 25-02, 25-03, 25-04]
files_modified:
  - .planning/ROADMAP.md
autonomous: false
requirements:
  - REQ-SD-10

must_haves:
  truths:
    - "ROADMAP Phase 25 success criterion #4 reads 'fully out of sight (no opt-in include_filed=true clause)' — old wording about 'with opt-in' is removed"
    - "Codebase is deployed to Azure Container Apps via CI/CD push-to-main pipeline"
    - "Post-deploy UAT confirms a fresh admin capture from the mobile app results in a Cosmos Inbox doc with status='filed' + ttl=2592000 + adminProcessingStatus='completed'"
    - "Mobile inbox UI shows non-filed items only (visual regression check passes)"
    - "Phase 24 backlog 'Admin Retry Bound' orthogonality preserved in production: failed items DO NOT get status='filed' (live spot-check or KQL)"
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Phase 25 success criterion #4 amended"
      contains: "Filed items are fully hidden"
  key_links:
    - from: "mobile admin capture"
      to: "Cosmos Inbox doc with status=filed + ttl"
      via: "admin_handoff Branch B (Plan 01) + Settings TTL (Plan 02 + container TTL=-1)"
      pattern: "status.*filed.*ttl.*2592000"
    - from: "ROADMAP success criterion #4"
      to: "Plan 03 implementation"
      via: "no include_filed parameter exists in code"
      pattern: "no include_filed"
---

<objective>
Land Phase 25 atomically: amend ROADMAP wording (CONTEXT.md Decision 3 — drop "with opt-in include_filed=true"), commit the consolidated Phase 25 code changes from Plans 01-04 to main, trigger the CI/CD deploy, then run the post-deploy UAT documented in 25-VALIDATION.md against the live Azure Container Apps endpoint.

This plan does NOT make any code changes — Plans 01-04 already shipped all code. Plan 05's responsibilities:
1. Amend ROADMAP.md (single doc edit)
2. Trigger deploy via git push to main (CI/CD handles ACR + Container Apps update)
3. Verify post-deploy health
4. Run UAT (operator captures an admin item via mobile + verifies the resulting Cosmos doc shape + verifies phone inbox visual regression + spot-checks orthogonality)

Purpose: Close Phase 25 by validating end-to-end behavior in production and aligning documentation with the locked decision set.
Output: Amended ROADMAP, deployed Phase 25 code, UAT sign-off captured in 25-05-SUMMARY.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-admin-inbox-soft-delete-30-day-retention-inserted/25-CONTEXT.md
@.planning/phases/25-admin-inbox-soft-delete-30-day-retention-inserted/25-RESEARCH.md
@.planning/phases/25-admin-inbox-soft-delete-30-day-retention-inserted/25-VALIDATION.md
@.planning/phases/25-admin-inbox-soft-delete-30-day-retention-inserted/25-01-SUMMARY.md
@.planning/phases/25-admin-inbox-soft-delete-30-day-retention-inserted/25-02-SUMMARY.md
@.planning/phases/25-admin-inbox-soft-delete-30-day-retention-inserted/25-03-SUMMARY.md
@.planning/phases/25-admin-inbox-soft-delete-30-day-retention-inserted/25-04-SUMMARY.md
@./CLAUDE.md
@./.claude/CLAUDE.md

<interfaces>
From .planning/ROADMAP.md Phase 25 block (lines 370-393 of current ROADMAP):

```markdown
### Phase 25: Admin Inbox Soft-Delete + 30-day Retention (INSERTED)

**Context:** ...

**Goal:** ...

**Depends on:** Phase 24 ...

**Requirements:** TBD ...

**Success Criteria** (what must be TRUE):
  1. Admin agent success path sets `status="filed"` on the Inbox doc (instead of `delete_item`)
  2. Cosmos TTL on filed docs = 30 days (configurable via `INBOX_FILED_RETENTION_DAYS` env var)
  3. Phone inbox view filters out `filed` status (no UI regression)
  4. `/investigate` Inbox queries default to excluding `filed` (with opt-in `include_filed=true`)   ← AMEND THIS LINE
  5. Scope is limited to **Admin-bucket** Inbox items processed by the Admin agent — People/Ideas/Projects inbox docs are unchanged
  6. Recipe URL captures (which generate N Errand items) follow the same path: source Inbox → filed once, ingredients persist normally in Errands
```

Target line 4 reword (CONTEXT.md Decision 3 — Filed visibility: Fully out of sight, NO opt-in):

```markdown
  4. Filed items are fully hidden from user surfaces — phone inbox excludes them via server-side SQL filter, and the investigation agent has no direct Inbox Cosmos query path (verified by CI guard). No `include_filed=true` opt-in parameter exists anywhere — the 30-day window is purely internal for eval/debugging via direct Cosmos query if needed.
```

Plus update **Requirements:** line from "TBD" to the actual REQ-IDs:

```markdown
**Requirements:** REQ-SD-01, REQ-SD-02, REQ-SD-03, REQ-SD-04, REQ-SD-05, REQ-SD-06, REQ-SD-07, REQ-SD-08, REQ-SD-09, REQ-SD-10, REQ-SD-11, REQ-BL-01, REQ-BL-02, REQ-BL-03, REQ-BL-04, REQ-BL-05
```

Plus update **Plans:** count from "TBD (likely 1-3 plans...)" to the actual 5:

```markdown
**Plans:** 5 plans

Plans:
- [ ] 25-01-PLAN.md — admin_handoff Branch B soft-delete (status='filed' + ttl + adminProcessingStatus='completed') + ContextVar shell in tools/admin.py
- [ ] 25-02-PLAN.md — Settings.inbox_filed_retention_days + operator-authorized Cosmos container TTL=-1
- [ ] 25-03-PLAN.md — api/inbox.py listing filter + api/errands.py dismiss soft-delete + unprocessed-orthogonality test + investigation guard
- [ ] 25-04-PLAN.md — ErrandItem/TaskItem backlink fields (sourceInboxItemId, sourceCaptureTraceId) + add_errand_items/add_task_items ContextVar reads
- [ ] 25-05-PLAN.md — ROADMAP amendment + deploy + UAT
```

Deployment pipeline (per MEMORY.md):
- Push to main triggers GitHub Actions (OIDC/Workload Identity)
- CI builds Docker image, pushes to ACR (`wkmsharedservicesacr`), tags with git SHA
- Container Apps creates new revision via `az containerapp update`
- Health endpoint poll: 12s × 15 attempts (3 min) before promoting to 100% traffic
- Revision name suffix: `sha-<git_sha_prefix>`

UAT procedure (per 25-VALIDATION.md Manual-Only Verifications):
- Operator captures an admin-bucket item via mobile (e.g., "add eggs to shopping list")
- Wait ~10 sec for Admin agent processing
- Azure Portal → Cosmos Data Explorer → Inbox container → find most recent admin-bucket doc
- Verify fields: `status: "filed"`, `ttl: 2592000`, `adminProcessingStatus: "completed"`
- Verify phone inbox no longer shows the item (matches pre-Phase-25 UX of "agent processes it away")

Endpoint Grounding: NONE for code changes — Plan 05 doesn't touch code. UAT exercises live endpoints (mobile capture → backend) end-to-end.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Amend ROADMAP.md Phase 25 — fix success criterion #4 + populate Requirements + Plans</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md (current state — full Phase 25 section, lines 370-393)
    - .planning/phases/25-admin-inbox-soft-delete-30-day-retention-inserted/25-CONTEXT.md (Decision 3 — Filed visibility lock)
    - .planning/phases/25-admin-inbox-soft-delete-30-day-retention-inserted/25-VALIDATION.md (canonical REQ-IDs list)
  </read_first>
  <action>
    Read the current .planning/ROADMAP.md file. Locate the Phase 25 block (search for `### Phase 25: Admin Inbox Soft-Delete + 30-day Retention (INSERTED)`).

    Apply THREE atomic edits to the Phase 25 block:

    **Edit 1 — Replace the Success Criterion #4 line.**

    Find this line (exact text):

    ```
      4. `/investigate` Inbox queries default to excluding `filed` (with opt-in `include_filed=true`)
    ```

    Replace with:

    ```
      4. Filed items are fully hidden from user surfaces — phone inbox excludes them via server-side SQL filter, and the investigation agent has no direct Inbox Cosmos query path (verified by CI guard `test_investigation_has_no_direct_inbox_query`). No `include_filed=true` opt-in parameter exists anywhere — the 30-day window is purely internal for eval/debugging via direct Cosmos query if needed.
    ```

    **Edit 2 — Replace the Requirements line.**

    Find this line (exact text):

    ```
    **Requirements:** TBD (likely OBS-XX, EVAL-XX series — to be assigned at plan-phase time)
    ```

    Replace with:

    ```
    **Requirements:** REQ-SD-01, REQ-SD-02, REQ-SD-03, REQ-SD-04, REQ-SD-05, REQ-SD-06, REQ-SD-07, REQ-SD-08, REQ-SD-09, REQ-SD-10, REQ-SD-11, REQ-BL-01, REQ-BL-02, REQ-BL-03, REQ-BL-04, REQ-BL-05
    ```

    **Edit 3 — Replace the Plans line.**

    Find this line (exact text):

    ```
    **Plans:** TBD (likely 1-3 plans depending on bundled scope — minimum is the soft-delete + TTL flip in admin_handoff.py)
    ```

    Replace with:

    ```
    **Plans:** 5 plans

    Plans:
    - [x] 25-01-PLAN.md — admin_handoff Branch B soft-delete (status='filed' + ttl + adminProcessingStatus='completed') + ContextVar shell in tools/admin.py
    - [x] 25-02-PLAN.md — Settings.inbox_filed_retention_days + operator-authorized Cosmos container TTL=-1
    - [x] 25-03-PLAN.md — api/inbox.py listing filter + api/errands.py dismiss soft-delete + unprocessed-orthogonality + investigation guard
    - [x] 25-04-PLAN.md — ErrandItem/TaskItem backlink fields + add_errand_items/add_task_items ContextVar reads
    - [x] 25-05-PLAN.md — ROADMAP amendment + deploy + UAT
    ```

    **Also update the Progress table row for Phase 25** if it exists (lines 490-491 area in ROADMAP):

    ```
    | 25. Admin Inbox Soft-Delete + 30-day Retention (INSERTED) | v3.1 | 5/5 | Complete    | 2026-05-XX |
    ```

    (Use today's date in YYYY-MM-DD format; if the date column for other phases uses a different convention, match that convention.)

    DO NOT modify any other Phase 25 content (Context, Goal, Depends on, Optional bundled scope, etc.) — only the three lines listed above and the Progress table row.

    DO NOT touch any other phase's content in ROADMAP.md.
  </action>
  <verify>
    <automated>grep -c "Filed items are fully hidden" .planning/ROADMAP.md && grep -c "REQ-SD-01" .planning/ROADMAP.md && grep -c "25-01-PLAN.md" .planning/ROADMAP.md && ! grep -c "with opt-in.*include_filed" .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - ROADMAP.md no longer contains "with opt-in `include_filed=true`" anywhere (verify: `grep -c "with opt-in.*include_filed" .planning/ROADMAP.md` returns 0)
    - ROADMAP.md Phase 25 Success Criterion #4 contains the new wording "Filed items are fully hidden" (verify: `grep -c "Filed items are fully hidden" .planning/ROADMAP.md` returns 1)
    - ROADMAP.md Phase 25 Requirements line contains all 16 REQ-IDs (verify: `grep -c "REQ-SD-01.*REQ-BL-05" .planning/ROADMAP.md` returns 1 or each ID found via separate greps)
    - ROADMAP.md Phase 25 Plans line shows "5 plans" (verify: `grep -c "Plans:.* 5 plans" .planning/ROADMAP.md` returns 1)
    - ROADMAP.md lists all 5 plan filenames: 25-01-PLAN.md through 25-05-PLAN.md (verify: `grep -c "25-0[1-5]-PLAN.md" .planning/ROADMAP.md` returns ≥ 5)
    - No other Phase content modified: `git diff .planning/ROADMAP.md` shows changes only within the Phase 25 block + Progress table row
  </acceptance_criteria>
  <done>
    ROADMAP amended per CONTEXT.md Decision 3. Requirements + Plans count populated. Progress table updated. No other phases touched.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Operator-driven git commit + push + deploy verification</name>
  <files>(no files modified by Claude in this task — operator runs git commit / git push / gh CLI commands manually; all source/test/docs file changes from Plans 01-04 + Task 1 ROADMAP edit are staged in this checkpoint task by the operator)</files>
  <read_first>
    - .planning/STATE.md (current Phase 24 deploy posture — Phase 25 is the next deploy on top of revision --0000089)
    - MEMORY.md (deployment pipeline notes: OIDC, Container App revision conventions, health endpoint expectations)
  </read_first>
  <action>
    **OPERATOR-AUTHORIZED MANUAL DEPLOY — Claude does NOT execute git push or any az CLI deploy commands. The operator (Will) runs the full Step 1-5 sequence in `<how-to-verify>` below.**

    Action overview: stage all Phase 25 working-tree changes (13+ files) into a single commit, push to main, verify CI/CD deploys the new revision, confirm the new Container App revision is healthy at 100% traffic. The commit message must follow the template in Step 3 of `<how-to-verify>` (which references all 16 REQ-IDs and lists the per-file changes).

    Pre-flight gate: full backend test suite MUST be green locally before pushing. If any test is RED, STOP and resolve before deploying — never push broken code.
  </action>
  <verify>
    Operator verifies via `<how-to-verify>` Steps 4-5: new Container App revision `sha-<short-sha>` is `Healthy` at trafficWeight=100; `curl https://brain.willmacdonald.com/health` returns 200 with valid JSON body.
  </verify>
  <acceptance_criteria>
    - Local pre-deploy: `cd backend && pytest tests/ -x --tb=short` exits 0
    - Single commit created with all Phase 25 files (13+ files including 2 new test files + ROADMAP + 4 SUMMARYs)
    - `git push origin main` succeeds
    - GitHub Actions `deploy-backend` workflow run completes with green check (all steps including `Verify health endpoint`)
    - `az containerapp revision list --name second-brain-api --resource-group shared-services-rg --query "[?properties.active]"` shows a revision `sha-<short_git_sha>` at trafficWeight=100 and healthState=Healthy
    - `curl -fsS https://brain.willmacdonald.com/health` returns HTTP 200 with valid JSON body (e.g., `{"status": "ok", ...}`)
  </acceptance_criteria>
  <done>
    Phase 25 code is live in production. New revision active at 100% traffic. Health endpoint green. Plan 02 already established the infra precondition (defaultTtl=-1) so per-doc ttl writes from Plan 01 take effect immediately.
  </done>
  <what-built>
    Plans 01-04 already produced all code changes. Plan 05 amended ROADMAP. The working tree should contain:

    - backend/src/second_brain/processing/admin_handoff.py (Branch B soft-delete + ContextVar set)
    - backend/src/second_brain/tools/admin.py (ContextVar definition + read sites in add_errand_items + add_task_items)
    - backend/src/second_brain/api/inbox.py (listing query filter)
    - backend/src/second_brain/api/errands.py (dismiss endpoint soft-delete)
    - backend/src/second_brain/config.py (new Settings field)
    - backend/src/second_brain/models/documents.py (backlink fields on ErrandItem + TaskItem)
    - backend/tests/test_admin_handoff.py (renamed + extended tests)
    - backend/tests/test_admin_tools.py (new ContextVar tests)
    - backend/tests/test_inbox_api.py (filed-filter test)
    - backend/tests/test_errands_api.py (dismiss + orthogonality tests)
    - backend/tests/test_investigation_queries.py (guard test)
    - backend/tests/test_documents_models.py (new file — Pydantic model tests)
    - backend/tests/test_config.py (new file — Settings validation tests)
    - .planning/ROADMAP.md (Phase 25 success criterion + Requirements + Plans amended)
    - .planning/phases/25-admin-inbox-soft-delete-30-day-retention-inserted/25-*-SUMMARY.md (4 plan summaries)

    Plan 02 Task 2 already enabled the Cosmos Inbox container's `defaultTtl=-1` under operator authorization. The code is now safe to deploy (per Landmine #8 ordering: infra → code).
  </what-built>
  <how-to-verify>
    **Step 1 — Confirm full backend test suite is green locally before deploy:**

    ```bash
    cd backend && pytest tests/ -x --tb=short
    ```

    Expected: exit code 0. If anything is RED, STOP and resolve before deploying.

    **Step 2 — Review the diff before commit:**

    ```bash
    git status
    git diff --stat
    ```

    Expected: ~13 files changed (10 source/test + 1 ROADMAP + 2 new test files). No unrelated changes.

    **Step 3 — Stage + commit Phase 25 atomically:**

    ```bash
    git add backend/src/second_brain/processing/admin_handoff.py \
            backend/src/second_brain/tools/admin.py \
            backend/src/second_brain/api/inbox.py \
            backend/src/second_brain/api/errands.py \
            backend/src/second_brain/config.py \
            backend/src/second_brain/models/documents.py \
            backend/tests/test_admin_handoff.py \
            backend/tests/test_admin_tools.py \
            backend/tests/test_inbox_api.py \
            backend/tests/test_errands_api.py \
            backend/tests/test_investigation_queries.py \
            backend/tests/test_documents_models.py \
            backend/tests/test_config.py \
            .planning/ROADMAP.md \
            .planning/phases/25-admin-inbox-soft-delete-30-day-retention-inserted/

    git commit -m "$(cat <<'EOF'
    feat(25): Admin Inbox soft-delete + 30-day retention

    Phase 25 ships the lifecycle change from hard-delete-on-admin-success to
    soft-delete with status="filed" + per-doc Cosmos TTL. Preserves audit trail
    for evals/observability without unbounded Cosmos growth.

    Code changes:
    - admin_handoff.py Branch B: read→mutate→upsert with status/adminProcessingStatus/ttl atomically (replaces delete_item). Landmine #4 mitigated: all three fields land in same upsert body.
    - api/errands.py dismiss_admin_notification: same soft-delete pattern (lifecycle symmetry with Branch B per RESEARCH.md Open Question #1).
    - api/inbox.py listing query: AND (NOT IS_DEFINED(c.status) OR c.status != 'filed').
    - config.py: inbox_filed_retention_days Field(default=30, ge=1) prevents misconfig immediate-purge.
    - models/documents.py: ErrandItem + TaskItem gain optional sourceInboxItemId + sourceCaptureTraceId backlinks.
    - tools/admin.py: add_errand_items + add_task_items stamp backlinks from admin_inbox_item_id_var + capture_trace_id_var ContextVars.
    - tests: 15+ new/renamed test cases across 6 test files. Investigation guard test prevents future direct Inbox queries.

    Infra precondition (already applied):
    - Cosmos Inbox container defaultTtl=-1 enabled via az CLI (Plan 02 Task 2, operator-authorized).

    Orthogonality preserved:
    - Phase 24 backlog "Admin Retry Bound": failed items DO NOT get status="filed" (test_agent_error_does_not_file_inbox_item enforces).
    - api/errands.py:174 unprocessed query continues to filter by adminProcessingStatus (test_unprocessed_admin_query_skips_filed enforces).
    - tools/investigation.py untouched — RESEARCH.md confirmed CONTEXT.md line refs were wrong.

    Closes REQ-SD-01..11 + REQ-BL-01..05 (16 requirements).
    EOF
    )"
    ```

    Expected: commit succeeds (pre-commit hook may run ruff + pytest — both should be green).

    **Step 4 — Push to main:**

    ```bash
    git push origin main
    ```

    Expected: push succeeds. GitHub Actions workflow `deploy-backend` triggers automatically. Watch in browser or via gh CLI:

    ```bash
    gh run watch
    ```

    Expected output: workflow steps succeed (uv lock check, Docker build, ACR push, container app update). Final step `Verify health endpoint` polls 12s × 15 attempts; expect green check within 3 min.

    **Step 5 — Verify new revision is live and healthy:**

    ```bash
    az containerapp revision list \
      --name second-brain-api \
      --resource-group shared-services-rg \
      --query "[?properties.active].{name:name, traffic:properties.trafficWeight, healthy:properties.healthState}" \
      -o table
    ```

    Expected: the newest revision (with `sha-<short_git_sha>` matching this commit's SHA) has trafficWeight=100 and healthState=Healthy.

    ```bash
    curl -fsS https://brain.willmacdonald.com/health | python3 -m json.tool
    ```

    Expected output: JSON body with `"status": "ok"` (or equivalent). HTTP 200.
  </how-to-verify>
  <resume-signal>Type "deployed" after Steps 1-5 succeed with new revision healthy at 100% traffic. Type "blocked" with the error output if any step fails.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Post-deploy UAT — fresh admin capture + Cosmos doc verification + mobile inbox visual regression + orthogonality spot-check</name>
  <files>(no source files modified by Claude — operator interacts with the live mobile app + Azure Portal Data Explorer + optional KQL query; only Phase 25 SUMMARY is updated post-UAT in Plan 05's `<output>` section)</files>
  <read_first>
    - .planning/phases/25-admin-inbox-soft-delete-30-day-retention-inserted/25-VALIDATION.md "Manual-Only Verifications" section (canonical UAT procedure)
    - .planning/phases/25-admin-inbox-soft-delete-30-day-retention-inserted/25-CONTEXT.md (success criteria + landmine awareness for the post-deploy check)
  </read_first>
  <action>
    **OPERATOR-DRIVEN END-TO-END UAT — Claude does NOT capture admin items, browse Azure Portal, or open the mobile app. The operator (Will) runs Steps 1-7 in `<how-to-verify>` below against the live production environment.**

    Action overview: prove that Phase 25 soft-delete + 30-day TTL + backlinks work end-to-end against live Cosmos, not just in unit-test mocks. UAT exercises the actual mobile → backend → Cosmos chain with a fresh capture, then inspects the resulting Cosmos doc in Data Explorer to verify fields. Includes a mobile visual-regression check (Plan 03 filter) and a defensive orthogonality spot-check (Phase 24 backlog "Admin Retry Bound" — failed items must NOT get status="filed").

    If any field is wrong or any regression is observed, STOP and roll back via the Phase 24-established revision-promote-back pattern; capture the failure details in the resume signal for gap-closure planning.
  </action>
  <verify>
    Operator pastes Step 7 documented findings (capture text + Cosmos doc field values + backlink field values + mobile visual result + orthogonality spot-check result) into the resume signal. UAT passes iff every expected field matches AND mobile shows no regression AND failed-path orthogonality holds.
  </verify>
  <acceptance_criteria>
    - Fresh admin capture (Step 1) results in a Cosmos Inbox doc with: `status="filed"`, `adminProcessingStatus="completed"`, `ttl=2592000`, `classificationMeta.bucket="Admin"`, non-empty `captureTraceId`
    - Resulting Errand or Task doc (Step 4) has both `sourceInboxItemId` (matching the Inbox doc id from Step 3) and `sourceCaptureTraceId` (matching the captureTraceId from Step 3) — proves the ContextVar → tool propagation chain works end-to-end
    - Mobile inbox tab (Step 5) does NOT show the filed item; pre-existing non-filed items render identically to pre-deploy (no visual regression)
    - Orthogonality spot-check (Step 6 — optional): a failed-capture spot-check (or KQL query) confirms `adminProcessingStatus="failed"` items do NOT have `status="filed"` (Phase 24 backlog "Admin Retry Bound" semantics preserved)
    - All findings documented in resume signal Step 7
  </acceptance_criteria>
  <done>
    End-to-end Phase 25 behavior verified in production. Soft-delete + TTL + backlinks work against real Cosmos. Mobile UI parity preserved. Phase 24 backlog orthogonality preserved. Phase 25 ready for SHIPPED status.
  </done>
  <what-built>
    Phase 25 code is now live in production. UAT exercises the end-to-end flow against the live Azure Container Apps endpoint to prove the soft-delete + TTL + backlink machinery works in the real Cosmos environment, not just in mocks.
  </what-built>
  <how-to-verify>
    **UAT Step 1 — Capture a fresh admin item via mobile.**

    Open the mobile app on iPhone. Capture an admin-bucket item using voice or text. Example: "add eggs to shopping list" or "remind me to book eye appointment". Wait ~10 sec for the Admin agent to process (status icon settles, capture flows out of inbox).

    Record:
    - The exact capture text used (paste below in resume signal)
    - The approximate timestamp of capture (HH:MM)

    **UAT Step 2 — Find the resulting Cosmos doc in Azure Portal.**

    Open Azure Portal → Cosmos DB account `shared-services-cosmosdb` (RG: shared-services-rg) → Data Explorer → second-brain database → Inbox container → Items.

    Sort by `createdAt` descending (or `_ts` descending). Find the most recent doc matching the capture text. It should have `classificationMeta.bucket == "Admin"`.

    **UAT Step 3 — Verify the doc shape matches Phase 25 expectations.**

    Open the doc JSON. Verify ALL of these fields are present and correct:

    | Field | Expected Value | Notes |
    |-------|----------------|-------|
    | `status` | `"filed"` | Phase 25 soft-delete sentinel |
    | `adminProcessingStatus` | `"completed"` | Both fields MUST be set atomically (Landmine #4) |
    | `ttl` | `2592000` | 30 * 86400 — default `inbox_filed_retention_days` |
    | `classificationMeta.bucket` | `"Admin"` | Confirms admin-bucket scope |
    | `captureTraceId` | `<some-trace-id>` | Should be a non-empty string |
    | `_ts` | `<timestamp from Step 1>` | Cosmos last-modified timestamp |

    If ANY field is missing or wrong, STOP and investigate. Document the doc JSON in the resume signal.

    **UAT Step 4 — Verify backlinks landed on the resulting Errand/Task doc.**

    Open the Errands container (or Tasks container, depending on whether the capture text triggered add_errand_items or add_task_items). Sort by `_ts` descending. Find the most recent doc.

    Verify these fields are present:

    | Field | Expected Value | Notes |
    |-------|----------------|-------|
    | `sourceInboxItemId` | The `id` of the Inbox doc from Step 3 | REQ-BL-03 / REQ-BL-04 |
    | `sourceCaptureTraceId` | The `captureTraceId` from Step 3 | Durable forever |

    Record the values for the UAT summary.

    **UAT Step 5 — Mobile inbox visual regression check.**

    Open the mobile app inbox tab. Verify:
    - The capture from Step 1 is NOT visible (it was filed)
    - Other pre-existing non-filed items render identically to before deploy (no visual regression — fonts, layout, badges)

    If filed item appears in the inbox, STOP — Plan 03's filter regressed. Investigate.

    **UAT Step 6 — Orthogonality spot-check (optional, defensive).**

    Trigger a failure case to verify Phase 24 backlog orthogonality holds in production. Easiest path: capture an admin item that the agent will fail on (e.g., a URL that the recipe fetcher can't process, or an intentionally garbled instruction).

    Wait ~15 sec, then check the Cosmos Inbox doc for that capture. Verify:
    - `adminProcessingStatus` is `"failed"`
    - `status` is NOT `"filed"` (should be the pre-existing classification status like `"classified"`)

    If failed items get marked `filed`, STOP — Landmine #1 regressed. Investigate.

    Alternative (less invasive): run a KQL query in Log Analytics:

    ```kql
    AppDependencies
    | where TimeGenerated > ago(30m)
    | where Name == "invoke_agent"
    | where Properties.["admin.outcome"] == "failed"
    | take 5
    | project TimeGenerated, Properties
    ```

    Then cross-reference the corresponding Inbox docs via the capture_trace_id custom dimension.

    **UAT Step 7 — Document findings in resume signal.**

    Paste the following:
    - Step 1 capture text + timestamp
    - Step 3 Inbox doc field values (status / adminProcessingStatus / ttl / captureTraceId)
    - Step 4 Errand/Task backlink field values (sourceInboxItemId / sourceCaptureTraceId)
    - Step 5 result: "passed" or "regression observed: ..."
    - Step 6 result (if attempted): "passed" or "regression observed: ..."
  </how-to-verify>
  <resume-signal>Type "uat passed" with the documented findings from Step 7. Type "uat failed" with the failing step + observed behavior + Cosmos doc JSON for investigation.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local main branch → GitHub Actions OIDC → ACR → Azure Container Apps | CI/CD deploy chain; SHA-tagged image; revision auto-promote on healthy health probe |
| Mobile client → live backend → Cosmos | End-to-end UAT path; exercises Plans 01-04 in production |
| ROADMAP.md amendment | Documentation alignment with locked CONTEXT.md Decision 3 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-05-01 | Tampering | Deploy lands code without infra TTL=-1 | mitigate | Plan 02 Task 2 already enabled `defaultTtl=-1` BEFORE Plan 05 deploys (Landmine #8 — infra → code ordering enforced by wave structure). Plan 05 Task 2 Step 2 includes az CLI re-check as defensive verification |
| T-25-05-02 | Denial of Service | New revision crashes on startup due to Settings ValidationError | mitigate | Plan 02 validated `Field(ge=1)` locally. Health probe (12s × 15 attempts) catches startup failures; CI fails the deploy gate. Rollback path: revision-promote-back primary, git revert durable |
| T-25-05-03 | Information Disclosure | UAT operator pastes Cosmos doc with sensitive content into resume signal | accept | Operator (Will) is sole user; no PII concerns for solo developer workflow. If content is sensitive in some future capture, operator redacts before pasting |
| T-25-05-04 | Repudiation | Deploy lacks audit trail | mitigate | Azure Activity Log records `az containerapp update` events with caller identity. GitHub Actions run page captures full deploy log with revision name (sha-prefixed) for commit correlation |
| T-25-05-05 | Tampering | UAT misses a real regression because mock matches mock | mitigate | UAT runs against LIVE Cosmos (not mocks). Plan 05 Task 3 Step 3 reads the actual Cosmos doc shape from Azure Portal Data Explorer. End-to-end test cannot be tricked by test fixtures |
</threat_model>

<verification>
**Plan 05 completion verification:**

1. ROADMAP.md amended (verify: `grep -c "Filed items are fully hidden" .planning/ROADMAP.md` returns 1; `grep -c "with opt-in" .planning/ROADMAP.md` returns 0 for the Phase 25 block)
2. Git commit created with all Phase 25 files staged
3. Push to main succeeds
4. GitHub Actions deploy workflow succeeds (health probe green within 3 min)
5. New Container App revision is active at 100% traffic with health=Healthy
6. UAT Step 3: Cosmos Inbox doc for a fresh admin capture has status="filed" + adminProcessingStatus="completed" + ttl=2592000
7. UAT Step 4: Corresponding Errand/Task doc has sourceInboxItemId + sourceCaptureTraceId backlinks
8. UAT Step 5: Mobile inbox shows no visual regression; filed items invisible
9. UAT Step 6 (defensive): failed items do NOT get status="filed" — Phase 24 orthogonality preserved
</verification>

<success_criteria>
- ROADMAP success criterion #4 reworded per CONTEXT.md Decision 3 (no opt-in include_filed)
- ROADMAP Requirements + Plans populated with actual IDs and 5-plan count
- Phase 25 code deployed to Azure Container Apps via push-to-main pipeline
- Live UAT confirms end-to-end soft-delete + TTL + backlinks work in production
- No visual regression on mobile inbox
- Phase 24 backlog "Admin Retry Bound" orthogonality preserved in production
- Phase 25 closed — all 16 REQ-SD-* + REQ-BL-* requirements satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/25-admin-inbox-soft-delete-30-day-retention-inserted/25-05-SUMMARY.md` capturing:
- The git SHA + revision name of the Phase 25 deploy
- The UAT findings from Task 3 Step 7 (Cosmos doc field values, backlink values, mobile + orthogonality results)
- The ROADMAP amendment diff (showing the success criterion #4 reword)
- Confirmation that Phase 25 is shipped + soft-delete is live in production
- Any follow-ups discovered during UAT (e.g., if backlinks weren't populated for some reason, capture as a gap-closure item)

Also update .planning/STATE.md (or note in the SUMMARY that the orchestrator should update STATE.md) to reflect:
- Phase 25 status: SHIPPED
- progress.completed_phases: incremented
- progress.completed_plans: incremented by 5
- last_activity: today's date + brief description
</output>
</content>
</invoke>