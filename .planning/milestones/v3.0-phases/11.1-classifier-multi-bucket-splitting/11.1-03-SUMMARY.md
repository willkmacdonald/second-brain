---
phase: 11.1-classifier-multi-bucket-splitting
plan: 03
subsystem: streaming
tags: [agent-framework, call_id, batched-tool-calls, sse, adapter]

# Dependency graph
requires:
  - phase: 11.1-01
    provides: "Multi-result tracking (file_capture_results list) and classified_event with buckets/itemIds"
  - phase: 11.1-02
    provides: "Mobile multi-bucket toast and classifier instructions"
provides:
  - "pending_calls dict keyed by call_id replacing single-variable tracking in adapter"
  - "Correct pairing of batched function_call/function_result content objects"
  - "4 unit tests verifying SDK call_id contract and pairing logic"
affects: [11.1-04]

# Tech tracking
tech-stack:
  added: []
  patterns: ["pending_calls dict keyed by call_id for batched tool call pairing"]

key-files:
  created: []
  modified:
    - backend/src/second_brain/streaming/adapter.py
    - backend/tests/test_streaming_adapter.py

key-decisions:
  - "Store all function_calls (not just file_capture) in pending_calls -- future-proof for new tools"
  - "Pop from pending_calls on function_result -- cleanup prevents stale entries"
  - "stream_follow_up_capture intentionally unchanged -- single-tool pattern correct for reclassification"

patterns-established:
  - "pending_calls dict: keyed by call_id, stores {name, args} per function_call, popped on matching function_result"

requirements-completed: [SPLIT-01, SPLIT-02, SPLIT-05]

# Metrics
duration: 2min
completed: 2026-03-02
---

# Phase 11.1 Plan 03: Batched Tool Call Handling Summary

**pending_calls dict keyed by call_id replaces single-variable tracking to correctly pair batched function_call/function_result SDK updates**

## Performance

- **Duration:** 2 min
- **Started:** 2026-03-02T05:57:23Z
- **Completed:** 2026-03-02T05:59:46Z
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments
- Replaced current_fc_name/current_fc_args single-variable tracking with pending_calls dict in both stream_text_capture and stream_voice_capture
- Fixed batched tool call handling where SDK delivers all function_calls in one update (second call's args were overwriting first) and all function_results in another (second result was dropped because current_fc_name reset to None)
- Added 4 unit tests verifying SDK call_id contract and full pending_calls pairing simulation
- Preserved stream_follow_up_capture unchanged (uses detected_tool pattern, single-result by design)

## Task Commits

Each task was committed atomically:

1. **Task 1: Replace single-variable tracking with pending_calls dict** - `991883b` (fix)
2. **Task 2: Add unit tests for call_id-based pairing** - `8a44523` (test)

## Files Created/Modified
- `backend/src/second_brain/streaming/adapter.py` - Replaced current_fc_name/current_fc_args with pending_calls dict in stream_text_capture and stream_voice_capture
- `backend/tests/test_streaming_adapter.py` - Added TestCallIdPairing class with 4 tests for SDK contract and pairing logic

## Decisions Made
- Store all function_calls (not just file_capture) in pending_calls -- future-proof for new tools
- Pop from pending_calls on function_result -- cleanup prevents stale entries accumulating
- stream_follow_up_capture intentionally unchanged -- single-tool pattern is correct for reclassification use case

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- Adapter now correctly handles batched SDK updates with multiple function_calls and function_results
- Ready for 11.1-04 (UAT verification of multi-bucket flow end-to-end)
- 86 tests pass (82 existing + 4 new), 5 skipped

---
*Phase: 11.1-classifier-multi-bucket-splitting*
*Completed: 2026-03-02*
