---
phase: 11.1-classifier-multi-bucket-splitting
plan: 04
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
gap_closure: true
requirements: [SPLIT-04]

must_haves:
  truths:
    - "Classifier files 'I need to buy dog food' directly as Admin with status=classified, no clarification"
    - "Classifier files 'pick up milk' directly as Admin with status=classified (regression check)"
    - "Classifier still splits 'I need milk and also remind me to call the vet' into Admin + People (multi-intent preserved)"
    - "Classifier keeps 'buy cake, candles, and a card for the party' as a single Admin item (same-bucket check)"
  artifacts: []
  key_links: []
---

<objective>
Revise the Classifier Foundry agent instructions to stop over-analyzing simple single-intent captures with conversational phrasing.

Purpose: UAT gap 1 -- the instruction "Before classifying, check if the capture contains MULTIPLE distinct intents" causes the Classifier to second-guess conversational phrasing like "I need to buy dog food", treating it as ambiguous and calling file_capture with status="misunderstood" instead of status="classified". Imperative phrasing ("Buy dog food") works fine. This is purely an instruction issue, not a code bug.

Output: Revised Classifier instructions in the AI Foundry portal that classify conversational phrasing correctly while preserving multi-intent splitting for captures with explicit conjunction markers.
</objective>

<execution_context>
@/Users/willmacdonald/.claude/get-shit-done/workflows/execute-plan.md
@/Users/willmacdonald/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/debug/classifier-clarification-on-simple-captures.md
@.planning/phases/11.1-classifier-multi-bucket-splitting/11.1-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Update Classifier Foundry instructions for conversational phrasing tolerance</name>
  <files>AI Foundry portal (no code files)</files>
  <action>
Update the Classifier agent instructions in the AI Foundry portal. Replace the current multi-intent detection section with the revised version below.

**Find** the current multi-intent section that starts with "Before classifying, check if the capture contains MULTIPLE distinct intents that belong to DIFFERENT buckets."

**Replace it with:**

```
## Multi-Intent Detection

After determining the primary bucket for the capture, check if it also contains
a SECOND distinct intent targeting a DIFFERENT bucket.

Rules:
1. MOST captures are single-intent. A capture like "I need to buy dog food" or
   "pick up milk" or "remind me about the dentist" is a single intent -- classify
   it immediately. Do NOT ask for clarification on simple captures.

2. Multi-intent captures contain explicit conjunction markers connecting different
   types of tasks. Look for: "and also", "plus", "oh and", "and remind me",
   "also need to", or similar bridging phrases.
   Example: "need milk AND ALSO remind me to call the vet"

3. If all items belong to the SAME bucket, call file_capture ONCE with the full
   text. Do NOT split within a single bucket.
   Example: "buy milk, eggs, and bread" -> single file_capture, bucket="Admin"

4. If intents span DIFFERENT buckets (identified by conjunction markers), call
   file_capture ONCE PER BUCKET with only the text segment for that intent.
   Preserve the user's exact words. Each segment gets its own title.

5. When in doubt, classify as a single item. Never ask for clarification just
   because a capture uses conversational phrasing like "I need to" or "I want to".

6. Conversational phrasing ("I need to buy dog food", "I should call the vet",
   "thinking about a garden shed") is NORMAL for captures. Treat these the same
   as imperative phrasing ("buy dog food", "call the vet", "garden shed idea").
```

**Key changes from the current instructions:**
- "After determining the primary bucket" instead of "Before classifying" -- analysis happens after initial classification, not before
- Rule 1 explicitly says most captures are single-intent with examples of conversational phrasing
- Rule 2 lists explicit conjunction markers as the trigger for multi-intent splitting
- Rule 5 explicitly prohibits clarification due to conversational phrasing
- Rule 6 provides explicit examples of conversational vs imperative equivalence

Navigate to the AI Foundry portal, Classifier agent, Instructions tab. Verify the multi-intent section now starts with "After determining the primary bucket" (not "Before classifying"). Type "done" when the update is saved.
  </action>
  <verify>Classifier instructions in Foundry portal updated and saved. Multi-intent section starts with "After determining the primary bucket".</verify>
  <done>Classifier Foundry instructions revised to tolerate conversational phrasing while preserving multi-intent detection via conjunction markers.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify Classifier behavior with conversational and multi-intent captures</name>
  <files>No code files -- live testing via mobile app</files>
  <action>
Test all four scenarios via the mobile app against the deployed backend to verify the instruction changes work correctly:

1. Send "I need to buy dog food"
   Expected: Files directly to Admin with high confidence. Toast shows "Filed -> Admin (0.xx)". NO clarification prompt.

2. Send "pick up milk"
   Expected: Files directly to Admin. Toast shows "Filed -> Admin (0.xx)". (Regression check -- this worked before)

3. Send "I need milk and also remind me to call the vet"
   Expected: Splits into two items (Admin + People). Toast shows multi-bucket format "Filed to Admin, People".
   NOTE: This test also validates Plan 03 (adapter fix). If Plan 03 has not been deployed yet, this will show single-bucket toast even if splitting works. In that case, check Cosmos DB for two items and skip the toast format check.

4. Send "buy cake, candles, and a card for the party"
   Expected: Files as a single Admin item. Toast shows "Filed -> Admin (0.xx)". NOT split.

Report results for each test. Type "approved" or describe any issues.
  </action>
  <verify>All four test scenarios produce expected behavior as described above.</verify>
  <done>Conversational phrasing ("I need to buy dog food") classifies immediately. Multi-intent splitting with conjunction markers still works. Same-bucket multi-item stays single.</done>
</task>

</tasks>

<verification>
All four test scenarios pass as described in Task 2's action section.
</verification>

<success_criteria>
- "I need to buy dog food" classifies immediately as Admin without clarification
- "pick up milk" still works (regression)
- Multi-intent splitting with conjunction markers still works
- Same-bucket multi-item stays as single item
</success_criteria>

<output>
After completion, create `.planning/phases/11.1-classifier-multi-bucket-splitting/11.1-04-SUMMARY.md`
</output>
