---
phase: 14-app-insights-operational-audit
plan: 03
type: execute
wave: 2
depends_on:
  - 14-01
files_modified:
  - backend/queries/capture-trace.kql
  - backend/queries/recent-failures.kql
  - backend/queries/system-health.kql
  - backend/queries/admin-agent-audit.kql
  - backend/queries/README.md
autonomous: false
requirements:
  - OBS-07
  - OBS-08

must_haves:
  truths:
    - "Four KQL query files exist in the repo covering all four user-requested queries"
    - "capture-trace.kql filters AppTraces by capture_trace_id custom dimension and shows chronological lifecycle"
    - "recent-failures.kql shows ERROR+ logs and exceptions from the last 24 hours"
    - "system-health.kql shows capture volume, success rate, and error rate trends"
    - "admin-agent-audit.kql shows Admin Agent processing decisions, errand items created, and failures"
    - "Azure Monitor alert rules are configured for API down, capture failures, and error spikes"
    - "Alert action group sends push notifications via Azure mobile app"
  artifacts:
    - path: "backend/queries/capture-trace.kql"
      provides: "What happened to capture X? timeline query"
      contains: "capture_trace_id"
    - path: "backend/queries/recent-failures.kql"
      provides: "What failed recently? error-first query"
      contains: "SeverityLevel"
    - path: "backend/queries/system-health.kql"
      provides: "System health overview query"
      contains: "summarize"
    - path: "backend/queries/admin-agent-audit.kql"
      provides: "Admin Agent audit query"
      contains: "admin"
    - path: "backend/queries/README.md"
      provides: "Instructions for using queries in App Insights portal"
  key_links:
    - from: "backend/queries/capture-trace.kql"
      to: "App Insights AppTraces"
      via: "customDimensions.capture_trace_id filter"
      pattern: "customDimensions.capture_trace_id"
---

<objective>
Create version-controlled KQL query files for the four operational queries Will needs, plus a README explaining how to use them. Then configure Azure Monitor alert rules for API down, capture failures, and error spikes with push notification delivery.

Purpose: Will said "I looked in App Insights once and it was cryptic." These queries provide pre-built, copy-paste investigative tools. Alerts ensure he learns about problems without manually checking dashboards.

Output: 4 KQL files in backend/queries/, a README, and 3 Azure Monitor alert rules with an action group.
</objective>

<execution_context>
@/Users/willmacdonald/.claude/get-shit-done/workflows/execute-plan.md
@/Users/willmacdonald/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-app-insights-operational-audit/14-RESEARCH.md
@.planning/phases/14-app-insights-operational-audit/14-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create KQL query files and README</name>
  <files>
    backend/queries/capture-trace.kql
    backend/queries/recent-failures.kql
    backend/queries/system-health.kql
    backend/queries/admin-agent-audit.kql
    backend/queries/README.md
  </files>
  <action>
    Create the `backend/queries/` directory and 4 KQL query files plus a README.

    **capture-trace.kql** -- "What happened to capture X?"
    Query that takes a capture_trace_id and returns a chronological timeline of all events for that capture across AppTraces, AppDependencies, AppRequests, and AppExceptions tables. Uses `union` to combine all telemetry types. Projects TimeGenerated, ItemType (Log/Dependency/Request/Exception), Message, SeverityLevel, Component, and full customDimensions. Ordered ascending by time.

    Use the query structure from RESEARCH.md as a starting point but refine:
    - Add both bracket notation and dot notation for customDimensions.capture_trace_id (for robustness)
    - Include the `let trace_id = "PASTE_TRACE_ID_HERE";` variable at the top for easy editing
    - Include helpful comments explaining each section

    **recent-failures.kql** -- "What failed recently?"
    Query showing ERROR+ logs and unhandled exceptions from the last 24 hours. Includes capture_trace_id (when available) so Will can drill into specific captures. Shows message, component, trace ID, and full details. Ordered by time descending, limited to 50 results. Use `SeverityLevel >= 3` for ERROR.

    **system-health.kql** -- "How is the system doing overall?"
    Query with multiple sections (use `let` bindings):
    1. Capture volume by hour for the last 24h (count of AppRequests to /api/capture*)
    2. Success rate (2xx vs 4xx/5xx for capture endpoints)
    3. Error rate trend by hour
    4. Average response time for capture endpoints
    5. Admin Agent processing count (traces with component=admin_agent)

    Use `summarize` with `bin(TimeGenerated, 1h)` for time-series data. Include a `render timechart` hint in comments.

    **admin-agent-audit.kql** -- "What did the Admin Agent do?"
    Query showing Admin Agent processing activity from the last 24 hours:
    1. Items processed (component=admin_agent traces)
    2. Errand items created (look for add_errand_items-related logs)
    3. Failures (ERROR level in admin processing)
    4. Processing duration (from OTel spans for admin_agent_process)

    Group by capture_trace_id to show per-capture outcomes. Include destination and item counts where available in customDimensions.

    **README.md** -- How to use these queries
    Short README explaining:
    1. Navigate to App Insights > Logs in Azure portal
    2. Copy-paste the .kql file contents
    3. For capture-trace.kql, replace PASTE_TRACE_ID_HERE with the actual trace ID from the mobile app
    4. Resource name: `second-brain-insights` in `shared-services-rg`
    5. Retention: 30 days (data older than 30 days is purged)
  </action>
  <verify>
    <automated>cd /Users/willmacdonald/Documents/Code/claude/second-brain/backend && python3 -c "
import pathlib

queries_dir = pathlib.Path('queries')
assert queries_dir.is_dir(), 'queries/ directory missing'

expected_files = [
    'capture-trace.kql',
    'recent-failures.kql',
    'system-health.kql',
    'admin-agent-audit.kql',
    'README.md',
]

for f in expected_files:
    p = queries_dir / f
    assert p.exists(), f'{f} missing'
    content = p.read_text()
    assert len(content) > 50, f'{f} too short'

# Verify key content in each file
ct = (queries_dir / 'capture-trace.kql').read_text()
assert 'capture_trace_id' in ct, 'capture-trace.kql missing trace_id filter'
assert 'PASTE_TRACE_ID_HERE' in ct, 'capture-trace.kql missing placeholder'

rf = (queries_dir / 'recent-failures.kql').read_text()
assert 'SeverityLevel' in rf, 'recent-failures.kql missing SeverityLevel'

sh = (queries_dir / 'system-health.kql').read_text()
assert 'summarize' in sh, 'system-health.kql missing summarize'

aa = (queries_dir / 'admin-agent-audit.kql').read_text()
assert 'admin' in aa.lower(), 'admin-agent-audit.kql missing admin reference'

print('All KQL query file checks passed')
"</automated>
  </verify>
  <done>
    - backend/queries/ directory created with 4 KQL files and README.md
    - capture-trace.kql: full lifecycle trace by capture_trace_id with chronological ordering
    - recent-failures.kql: ERROR+ logs and exceptions from last 24h
    - system-health.kql: capture volume, success rate, error trends, processing counts
    - admin-agent-audit.kql: Admin Agent processing audit with per-capture outcomes
    - README.md: step-by-step instructions for using queries in App Insights portal
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Configure Azure Monitor alert rules</name>
  <files>N/A -- Azure CLI infrastructure commands, no repo files modified</files>
  <action>
    Configure Azure Monitor alert rules via Azure CLI. This requires Will's Azure account email for push notification delivery.

    **What will be created:**
    1. Action Group "SecondBrainAlerts" with Azure mobile app push notification target
    2. Three scheduled query alert rules:
       - **API-Error-Spike**: Fires when >3 ERROR+ traces in a 5-minute window (sustained problems only)
       - **Capture-Processing-Failures**: Fires when Admin Agent processing failures exceed 2 in a 15-minute window
       - **API-Health-Check**: Fires when >5 failed requests (5xx) in a 10-minute window
    3. All alerts use auto-mitigate (auto-resolve when condition clears)

    **Azure CLI commands (after Will provides email):**
    ```bash
    az monitor app-insights component show --app second-brain-insights --resource-group shared-services-rg --query id -o tsv
    az monitor action-group create --resource-group shared-services-rg --name "SecondBrainAlerts" --short-name "SBAlerts" --action azureapppush will YOUR_EMAIL_HERE
    # Then create 3 scheduled query alert rules using the resource ID
    ```

    If Will says "skip alerts", defer to a future phase.
  </action>
  <verify>
    Will verifies:
    1. Alerts appear in Azure Portal > Monitor > Alerts
    2. Push notification test works on Azure mobile app
    3. Provide Azure account email, or type "skip alerts" to defer
  </verify>
  <done>
    - Action Group created for push notifications
    - 3 alert rules configured: API errors, capture failures, health check
    - Auto-resolve enabled on all rules
    - OR: alerts explicitly deferred by Will
  </done>
</task>

</tasks>

<verification>
1. 4 KQL files exist in backend/queries/ with correct content
2. README.md provides clear instructions for using queries
3. Alert rules configured (or explicitly deferred by user)
4. All files committed to git
</verification>

<success_criteria>
- Will can copy-paste any of the 4 KQL queries into App Insights Logs and get actionable results
- capture-trace.kql shows the full lifecycle of a capture when given a trace ID
- recent-failures.kql surfaces errors without requiring KQL knowledge
- Alert rules fire on sustained problems and auto-resolve (if configured)
</success_criteria>

<output>
After completion, create `.planning/phases/14-app-insights-operational-audit/14-03-SUMMARY.md`
</output>
