---
phase: 12.3-destination-affinity-and-knowledge-system-for-errand-routing
plan: 01
subsystem: database
tags: [cosmos-db, pydantic, destinations, affinity-rules, migration]

# Dependency graph
requires:
  - phase: 12.2-rename-admin-to-errands
    provides: Errands container with destination-based partition key
provides:
  - DestinationDocument and AffinityRuleDocument Pydantic models
  - Destinations and AffinityRules registered in CONTAINER_NAMES
  - Seed migration script for initial destinations
affects: [12.3-02, 12.3-03, 12.3-04, 12.3-05]

# Tech tracking
tech-stack:
  added: []
  patterns: [Azure CLI management plane for container creation, slug-as-id for idempotent upserts]

key-files:
  created:
    - backend/scripts/seed_destinations.py
  modified:
    - backend/src/second_brain/models/documents.py
    - backend/src/second_brain/db/cosmos.py

key-decisions:
  - "Use Azure CLI management plane for container creation (data plane gets 403 with AAD tokens)"
  - "Use slug as document id for idempotent destination upserts"
  - "Keep KNOWN_DESTINATIONS for backward compatibility until Plan 03 replaces it"

patterns-established:
  - "Azure CLI management plane for Cosmos container creation: subprocess az cosmosdb sql container create"

requirements-completed: [DEST-01, DEST-02]

# Metrics
duration: 3min
completed: 2026-03-18
---

# Phase 12.3 Plan 01: Data Foundation Summary

**DestinationDocument and AffinityRuleDocument models with Cosmos containers and 8-destination seed script using Azure CLI management plane**

## Performance

- **Duration:** 3 min
- **Started:** 2026-03-18T04:22:43Z
- **Completed:** 2026-03-18T04:25:55Z
- **Tasks:** 2
- **Files modified:** 3

## Accomplishments
- Added DestinationDocument (slug, displayName, type) and AffinityRuleDocument (naturalLanguage, itemPattern, destinationSlug, ruleType, exceptions) Pydantic models
- Registered Destinations and AffinityRules in CONTAINER_NAMES (mock cosmos manager auto-picks them up)
- Created seed_destinations.py that creates containers via Azure CLI and upserts 8 initial destinations

## Task Commits

Each task was committed atomically:

1. **Task 1: Add DestinationDocument and AffinityRuleDocument models, update Cosmos containers** - `e940118` (feat)
2. **Task 2: Create seed destinations migration script** - `5c8df9c` (feat)

## Files Created/Modified
- `backend/src/second_brain/models/documents.py` - Added DestinationDocument and AffinityRuleDocument models
- `backend/src/second_brain/db/cosmos.py` - Added Destinations and AffinityRules to CONTAINER_NAMES
- `backend/scripts/seed_destinations.py` - Migration script seeding 8 initial destinations via Azure CLI + data plane upsert

## Decisions Made
- Used Azure CLI management plane (`az cosmosdb sql container create`) for container creation instead of data plane API which gets 403 Forbidden with AAD tokens
- Used destination slug as document id for idempotent upserts (safe to re-run script)
- Kept existing KNOWN_DESTINATIONS constant for backward compatibility (removed in Plan 03)
- Removed unused `json` import from seed script (Rule 1 auto-fix, trivial)

## Deviations from Plan

None - plan executed exactly as written. The Azure CLI approach for container creation was specified in the execution context.

## Issues Encountered
None

## User Setup Required
**Run the seed script after deploying.** The seed script must be run manually against production Cosmos DB:
```bash
COSMOS_ENDPOINT=https://... \
COSMOS_ACCOUNT_NAME=second-brain-cosmos \
COSMOS_RESOURCE_GROUP=shared-services-rg \
python3 backend/scripts/seed_destinations.py
```

## Next Phase Readiness
- DestinationDocument and AffinityRuleDocument models ready for Plan 02 (Admin Agent tools)
- Destinations and AffinityRules containers ready for data
- Seed script ready to run against production
- All 112 existing tests pass with no regressions

---
*Phase: 12.3-destination-affinity-and-knowledge-system-for-errand-routing*
*Completed: 2026-03-18*
